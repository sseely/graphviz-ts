// graphviz-ts reference input: 6-node graph with two cluster subgraphs
// engine: osage
// tolerance: deterministic
graph G {
    subgraph cluster_0 {
        A; B; C;
    }
    subgraph cluster_1 {
        D; E; F;
    }
}
