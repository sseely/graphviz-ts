# Decision Journal — aux-back-edge-curl

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).
One writer per row.

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| scope | 2026-06-20 | 6th/final #241_0 mission, scoped from `group-adjacent-flats`'s honest stop. Targets the SECOND divergence: the aux back-edge clone (regular adjacent-rank back edge) routes straight via `routeFaithfulAdjacentBack`, ignoring its corner ports, where C `make_regular_edge` curls it. Builds ON branch `fix/group-adjacent-flats` (grouping is the prerequisite, AD-3). Batch 1 = diagnose-and-prove (AD-1: run the actual config, the lesson from the prior stop); Batch 2 = gated fix + full-corpus regression (AD-4, the crux — back-edge routing is global). The xfail tripwire on the grouping branch is the red test. | The fix is core back-edge routing (high golden-risk) → diagnosis-first + full-corpus gate, narrowest possible gate (hasSidePort). | no |
| T1 | 2026-06-20 | Mechanism PROVEN = **ports**. Root divergence is `makeFwdEdge` (`edge-route-chain.ts:286-287`): C `makefwdedge` (dotsplines.c:58-59) SWAPS tail_port↔head_port (preserves the sw/se corner ports → curl size 7); the TS port RESETS both to empty `makePort()` (→ port-less straight, size 4). Confirmed gate = swap-not-strip in `makeFwdEdge`, naturally gated by port presence (port-less edges swap empty Center ports = no change), so NO extra `hasSidePort` branch needed — *narrower* than AD-2's anticipated dispatch gate and more C-faithful. Throwaway run (applied+reverted): reversed back edge 4→7 (matches C); full vitest 1992 pass, xfail flips, ZERO out-of-family golden churn. `gateConfirmed=true`. | The same `routeRegularEdgeFaithful` curls the forward corner edge (ports intact) but straightens the back clone (ports stripped) — the only difference is `makeFwdEdge` dropping ports. Fixing it to match C is the faithful translation, not a new gate. | no |
| T2 | 2026-06-20 | Implemented the proven fix in `src/layout/dot/edge-route-chain.ts:makeFwdEdge` (swap ports: `tail_port = head_port ?? makePort()`, vice versa), NOT the task file's guessed `edge-route.ts:routeOneEdge`. Flipped the xfail tripwire to a normal passing `it(...)`. Gates: tsc exit 0; vitest 147 files / 1993 pass (tripwire green, zero out-of-family churn); lizard clean (makeFwdEdge CCN 8/len 17). | T2 explicitly mandates "implement whatever T1 proved, not the guess if they differ." T1 proved the divergence is `makeFwdEdge` stripping ports, so the fix lives there. The dispatch-gate guess (routeOneEdge/hasSidePort) would be less faithful and unnecessary. Write-set deviation authorized by the task. | no |
| T1 | 2026-06-20 | PUSH-FORWARD: skipped the ephemeral `gvplugin_dot_layout` rebuild that T1 nominally requested. Mechanism is proven from the C **source** (`makefwdedge` swaps ports — unambiguous) plus running the **actual fixed port config** (the precise AD-1 step that the prior mission skipped), and C's size=7 is established ground truth (prior mission + harness doc + parity baseline diverged 126). A rebuild would only re-confirm the source. C tree untouched (AD-5). | Re-instrumenting C to read a value the source states literally adds no information; the actual-config run is the AD-1 requirement and it passed. | no |
