# Decision journal

Appended during execution. Log every non-trivial judgment call, the T2
order-pin, and the T4 before/after parity metrics.

| Date | Task | Decision / Finding | Rationale |
|------|------|--------------------|-----------|
| 2026-06-22 | plan | Mission opened as follow-up to fix-xcoord-pivots (e39072d). Root cause pre-pinned: C `id.c:62` `2*counter+1` shared anon counter; port `builder.ts:182` counts anon subgraphs only. Arithmetic on 2475_2 confirms keyless edges advance the counter. | Carries forward verified evidence from the keepout mission so Batch 1 only needs to confirm creation-order, not re-discover the model. |
| 2026-06-22 | T0 | **Impact-confirm: payoff ≈ 0 → STOP before Batch 2.** Only 8/796 applicable inputs have any anonymous subgraph; only 3 have `cluster=true`. Native `%N` `<title>` emission count per input: **2475_2=805, all other 7 = 0** (their anon subgraphs are `{rank=same}`/non-cluster or named clusters → no `%N` surfaces in SVG). So the ONLY input where naming matters is 2475_2, which is currently `timeout` (27s > 20s survey wall, per fix-xcoord-pivots residual). A naming fix cannot flip a `timeout` verdict. The other 7 diverge/err for unrelated reasons. **Inputs whose verdict this mission would flip now: 0.** | T0 gate (README): payoff trivially small → stop and confirm scope with human before investing in Batch 2. Faithfulness value remains (correct titles), but it only pays off once 2475_2 renders <20s — a separate, out-of-scope perf problem. |
| 2026-06-22 | T0 | **Human decision: implement anyway (faithfulness).** Success bar reframed: port `%N` == native `%N` on oracle inputs + 0 survey regressions; 2475_2 title-equality proven by direct render-compare (bypassing the 20s wall). No verdict flips expected now. | User chose faithfulness over deferral; fix is correct-toward-C and pre-positions 2475_2. |
| 2026-06-22 | T1 | Oracle model confirmed via cluster=true title renders (probes/oracle-model.md): id=2·counter+1; advances = anon root + anon subgraph(at open) + keyless edge(parse order); strict-dup edges merge (no advance); named objects don't advance. | Ground truth for the port; matches the 2475_2 arithmetic from planning. |
| 2026-06-22 | T2 | Port order pin: builder.ts `processSubgraph` allocates name BEFORE body (matches cgraph), statements dispatch in parse order (matches). Gaps: (1) edges don't advance, (2) anon root not consumed, (3) sequential formula. 2 out-of-scope edge cases noted (strict-dup, subgraph-edge-endpoint) — absent from 2475_2 + verdict-relevant corpus. | No traversal-order divergence; fix is formula + edge/root advances, all in builder.ts. |
| 2026-06-22 | T3 | Implemented in builder.ts (1 file): `anonCounter` + `nextAnonId()` (=2·c+1) replace `anonSeq`; subgraph name uses it; each keyless edge calls `advanceAnonId()`; unnamed root advances once in buildFromAst. 6 oracle inputs + 2475_2 verified port %N == native %N. | Faithful port; within 1-file write-set (no model file needed). |
| 2026-06-22 | T4 | **Verify:** tsc clean; vitest 2272 (+6 builder.test.ts %N cases); survey **0 regressions, 0 improvements** (expected — only 2475_2 surfaces %N and it's timeout); 2475_2 direct compare: **all 805 %N titles == native**. parity.json verdicts conformant (reverted timestamp-only churn). Documented limitations: strict-duplicate edges + subgraph-as-edge-endpoint (absent from corpus). | All gates pass; faithfulness achieved with 0 verdict movement now (pays off once 2475_2 renders <20s). |
