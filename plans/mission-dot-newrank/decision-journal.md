# Decision Journal — DOT-newrank

Appended during execution (per ~/.claude/rules/autonomous-execution.md).

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| Batch1 | 2026-06-17 | Run T1/T2/T5 as 3 parallel opus agents; disjoint write-sets (cgraph-ops.ts / fastgr.ts / position.ts+leafset.test). | parallelism.md: independent, no shared writes. Baseline tsc clean. | no |
| T2 | 2026-06-17 | `remove_from_rank` lives in `dotinit.c:234`, not `mincross.c` as the brief's read-set states. Ported from dotinit.c. | Brief read-set pointer corrected against actual C source. | no |
| T5 | 2026-06-17 | Port `expand_leaves` reproducing the verbatim upstream bug `d = ND_rank(aghead(e)) - ND_rank(aghead(e))` (position.c:1025) — head minus head, ALWAYS 0, so the loop body (zapinlist/fast_edge) is unreachable; net effect = `make_leafslots` only. | "C source is sacred": the oracle binary is built from this exact tree (dormant bug since 2004 initial revision), so faithful d==0 matches oracle. The brief's prose ("head-rank delta") predates noticing the bug; sacred-C overrides. | **yes — review** |
| T5 | 2026-06-17 | `ND_ranktype = LEAFSET` is NEVER assigned anywhere in upstream graphviz (only read at position.c:985, rank.c:486) — the leaf-collapse path is absent in this version. Kept faithful (A) impl (`expandLeaves` = `makeLeafslots`); pinned oracle parity on a 13-node leaf-heavy graph (max delta 0.0pt) instead of faking LEAFSET. No `comparisons/leafset.md` — parity proven by passing test. | LEAFSET unreachable is a spec property, not a porting gap; AD-4 comparison-page requirement met by the oracle-pinned test. | yes — note |
| Batch1 | 2026-06-17 | Gates GREEN: tsc 0; vitest 1831 pass / 0 fail; golden suite 122 byte-identical; lizard 0 warns. Commits 5537a0a (T1), 5f91a2d (T2), 87333d5 (T5), each within write-set. Brief says "115 goldens" but suite has 122 (brief undercount; invariant "no golden moves" holds). | Autonomous protocol quality gates between batches. | no |
| T3 | 2026-06-17 | `GD_clust(g)[c]` (C 1-based, c=1..n_cluster) maps to TS `clust[c-1]` — this repo stores clusters 0-indexed (rank.ts:211 writes `clust[nc-1]`). Literal `clust[c]` would skip cluster 0 and read past end. | Faithful to the cluster the C reads, adapted to repo's 0-based array. Documented inline + commit body. | no |
| T3 | 2026-06-17 | T1's `agnode(sg,...)` registers in root.nodes ONLY (not sg membership, unlike C). makeFillNode therefore calls `agsubnode(sg,n,true)` explicitly so the fill node is a `_new_rank` member (removeFill/T4 finds nodes by iterating that subgraph). Names `__fill_<rank>_<seq>` via module `fillSeq` reset per fillRanks call. | Bridges the T1 agnode/C-agnode semantic gap; guarantees removeFill can find + delete fill nodes. | no |
| Batch2 | 2026-06-17 | Gates GREEN: tsc 0; vitest 1834 pass / 0 fail; goldens byte-identical; lizard 0 warns (mincross-build.ts 420<500). Commit 4e03a14 (T3) within write-set. Stale-LSP diagnostics flood (e.g. fastgr.test.ts "no removeFromRank") disproved by authoritative `tsc --noEmit` exit 0. | Autonomous protocol gates; do not trust lagging IDE diagnostics over the compiler. | no |
| T4 | 2026-06-17 | `removeFill` ported faithfully (commit 7705284, init.ts): `agsubg(g.root,'_new_rank',false)`→snapshot members→`deleteFastNode`/`removeFromRank`/`agdelnode` each→`agdelsubg`; skips `dot_cleanup_node` (no TS equivalent, GC reclaims). Gates green; 122 goldens byte-identical. | Faithful dotinit.c:removeFill port; iteration-safe snapshot mirrors C `nxt=agnxtnode`. | no |
| T4 | 2026-06-17 | **RESCOPED (AD-4 STOP).** End-to-end newrank parity is UNREACHABLE within the declared write-sets. ORCHESTRATOR-VERIFIED two blockers, both outside all task write-sets: (1) `dotRank` (rank.ts:462) tests `flags & NEW_RANK` — a bit nothing sets — instead of `mapbool(agget(g,"newrank"))` per rank.c:523, so dot2Rank/fillRanks never run; (2) patching the dispatch to read the attr makes `renderSvg` HANG (independently reproduced: `timeout 15` → exit 124) in `furthestNode` (mincross-utils.ts:161) — fill-node order indices make the neighbor walk non-terminating, in dotMincross BEFORE removeFill. | Brief premise ("only the 4 stubs were missing") was incomplete. STOP triggers hit: "fix needs changes outside declared write-set (ranking phase)" + "parity unreachable even with faithful port → re-scope w/ comparison page, do NOT regress." The two fixes are COUPLED — landing only the dispatch fix would convert wrong-but-terminating output into a HANG (a regression), so neither lands. Kept current behaviour; comparisons/newrank.md documents the residual. | **yes — review/decide** |
| Batch3 | 2026-06-17 | Final gates GREEN on HEAD: tsc 0; vitest 1839 pass / 0 fail (123 files); 122 goldens byte-identical; lizard init.ts 0 warns. +25 mission tests. NOT merged to main — objective (parity) unmet; merge deferred to human decision. | Autonomous protocol; merge-to-main of a parity-unmet branch is an outward milestone action for the human to authorize. | yes |
