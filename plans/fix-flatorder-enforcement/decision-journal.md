# Decision journal

| When | Task | Decision / observation | Rationale |
|------|------|------------------------|-----------|
| 2026-06-29 | startup | Branch `feature/fix-flatorder-enforcement` created off main. Read README, decisions.md, batch-0 overview, prior-mission residual analysis (`fix-ordering-mincross/batch-2`), memory `ordering-agseq-inherit-done`. Plan: B0 diagnostic spike (debugger agent, instrument C+port build_ranks/enqueue_neighbors/flat_reorder on b58, pin first divergence of `6->8` enforcement) → B1 fix at pinned site + unit tests → B2 survey/baseline. | Autonomous protocol startup; AD-1 requires instrumenting C before any fix. |
| 2026-06-29 | T0 | Batch-0 debugger agent finished; trace in `test/diagnostic/flatorder-enforce-trace.md`. **README weight-0 premise OVERTURNED**: C `new_virtual_edge(orig=NULL)` sets `ED_weight=1` (fastgr.c:161-166); both C+port build `6->8` weight=1, both constraining. Independently verified C `left2right`/`flatindex` (mincross.c:115,557-578) and port build sites (mincross-flat.ts:52-63,99). **Pinned divergence:** `left2right` (mincross-cross.ts:90-94) reads flat matrix by `order-vStart`; matrix is built by `low`; C reads by `flatindex=ND_low`. `order-vStart==low` only right after flatBreakcycles, drifts after reorder → b58 pass-1 swaps 6,8 wrongly. | AD-1 satisfied; C instrumented + reverted clean (verified `git status` empty for C source). |
| 2026-06-29 | T0→B1 | Both `left2right` consumers (transposeStep mincross-cross.ts:220, reorderFindRp mincross-order.ts:216) test only `!== 0`; sign unused. Port's fused both-direction check (`M[v][w] OR M[w][v]`) makes C's `GD_flip` v/w swap behaviorally INERT in the port (only flips unread sign). **Decision: REJECT agent's proposed flip swap** (would be speculative no-op dead code, violates code-principles). B1 fix = index basis `order-vStart`→`low` ONLY; keep established both-direction idiom (passes 493). Minimal blast radius per AD-2; bisectable. | Faithful to C's matrix-index semantics; minimal change targets pinned root cause. |
