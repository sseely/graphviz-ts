# Decision Journal — fix-pack-dot-2458

Append one row per non-trivial judgment call during execution.

| When | Task | Decision | Rationale |
|------|------|----------|-----------|
| 2026-06-27 | T1 | initSubg IS needed (kept per ADR-2) | Empty `sg.attrs` after `buildSubgraph` → `dotGraphInit` reads defaults not root's `rankdir/nodesep/ranksep`; probe showed root rankdir=5 but component=0 without seed. Ordering: `dotGraphInit(sg)` then `initSubg(sg,root)` to override. |
| 2026-06-27 | T1 | ratio guard = direct attr read `ratioIsNone(g)`, not `g.info.drawing` | `g.info.drawing` only set for `ratio=compress`; fill/expand/value/auto leave it undefined and must NOT activate pack. Read `ratio` attr directly per C `setRatio`. |
| 2026-06-27 | T1 | Per-component layout excludes `gvPostprocess`; run it ONCE on root after `packSubgraphs` | C's `dot_layout` calls `dotneato_postprocess(g)` once on root; per-component `gvPostprocess` would double-rotate rankdir coords. Root bb set by `packSubgraphs→computeSubgraphBB`. |
| 2026-06-27 | T1 | Cluster oracle = corpus 2592 (real, diverged) | pack=100 packmode=array_3, 2 components, cluster_b1+cluster_b2 in comp-1. No synthetic graph needed for T3. |
| 2026-06-27 | T1 | Write-set assumption HOLDS — proceed | No `src/layout/pack/**` or `twopi/**` change needed; pack module works in points already. `writeSetAssumptionBroken: false`. |
| 2026-06-27 | T2 | Component layout sets `sg.info.dotroot = sg` | `buildSubgraph` (pack module) set dotroot=true-root, so `dotRoot(sg)≠sg` → mincross treated the component as a CLUSTER (backward nlist walk) → empty rank.v → null crash. C's `dotLayout(sg)` calls `dot_init_subg(sg,sg)` setting GD_dotroot=sg. One-line dot-local override (not a pack change). |
| 2026-06-27 | T2 | Clear `sg.info.has_labels` on each component; do NOT propagate to root | C sets GD_has_labels on `agraphof(e)`=ROOT (common_init_edge), never the component, so packed components get no edge-label rank and drop the label (native 2458 omits "connected", q1–q2=72pt). Port set it on the component → wrong rank-doubling. Setting root's flag instead makes the port's pre-computed-arrowhead label path mis-park the arrow; leaving root clear reproduces C's output (byte-match). Faithful-locus is `common_init_edge` (out of write-set). |
| 2026-06-27 | T2 | Port-only arrow-op shift compensation in `pack-components.ts` (not pack module) | Port pre-computes arrowhead draw-ops during routing (C regenerates at render time); `shiftEdge` shifts splines but not these ops, so after packing arrows lag by the pack delta. ADR-3 forbids touching pack/**; compensate dot-locally via a per-component witness-node delta. twopi is immune (routes after packing). NOT a forced STOP — fix stays in write-set. |
| 2026-06-27 | T2 | Gate pack branch to cluster-free graphs (`graphHasCluster`) | Survey showed 2683 (packmode + 2 clusters) regress structural-match→diverged: T2's `copyClusterInfo` is a stub (cluster carry is T3). T2 is explicitly the cluster-free path, so clustered multi-component graphs fall back to whole-graph layout (baseline behavior preserved). T3 removes this guard. 2592 (T3 oracle) unaffected (stays diverged, pre-existing). |
| 2026-06-27 | T2 | 2458 reached BYTE-match (bar was structural-match); 2682 bonus improvement | Pushed-forward per ADR-5 (byte-match if free). Gate: stable=672, improvements=2 (2458,2682), regressions=0. |
| 2026-06-27 | T3 | **STOP — T3 mis-scoped; halt for human input** | T3 was framed as "extend T2 dot-locally," but the oracle 2592 proves cluster nodes SPAN the port's connected components (cluster_b1={b11,b12}: b11∈comp1, b12∈comp0; port ccomps→3 comps, C→2). Matching C requires porting C's **cluster-aware** `cccomps`: `deriveGraph` (collapse each cluster to a derived node so cluster-mates stay together), `projectG` (clone each cluster into its component subgraph with an ORIG back-pointer), `subgInduce`, plus recursive `copyCluster`/`copyClusterInfo`/`mapClust`. This is a substantial subsystem port, not a T2 extension. Risks: (a) the cluster-ranking subsystem couples to graph-vs-component (`findClusters` reads `g.root.subgraphs` on the GLOBAL clType path; mark_clusters has documented defects A/C/D and RC1–RC4 in memory) → may force out-of-write-set edits to rank.ts/cluster.ts (STOP condition); (b) clusters are the most regression-prone area and the gate is currently GREEN — a long unattended reimplementation could regress it (hard STOP gate). Per autonomous protocol ("task mis-scoped / needs subtasks", potential out-of-write-set fix), halt. **Primary objective is met+exceeded** (2458 diverged→byte-match, 0 regressions). Recommendation: bank the T1+T2 win and run T3's cluster-aware-cccomps port as a dedicated derisk mission (cf. existing `plans/cluster-membership-derisk`), or explicitly authorize the larger port here. T4 (baseline refresh) also pends that decision. |
| 2026-06-27 | T3 | User authorized proceeding with the cluster-aware cccomps port | Plan: dot-local `cccompsWithClusters` (union-find: union edge endpoints + cluster-mates → components; reuse pack `buildSubgraph` for the base, then `projectG`-clone ALL subgraphs — clusters + rank=same sets — into each component with an ORIG back-pointer on clusters); `dotLayoutPipeline(comp)` discovers clones via `collapseSets`(clType LOCAL reads comp.subgraphs); port `copyCluster`/`copyClusterInfo`/`mapClust` to copy bb/label/border back to root clusters; remove the T2 `graphHasCluster` guard; golden=2592 + nested-cluster test. STOP again if rank.ts/cluster.ts edits prove necessary or the gate regresses. |
| 2026-06-27 | T3 | **STOP (2nd time) — cluster mincross core needs a multi-site rework; reverted to green T2** | Implemented the full dot-local cluster-carry in pack-components.ts (cccompsWithClusters + projectG + copyCluster/copyClusterInfo/mapClust), tsc-clean, and 2458 stayed byte-match. But laying out a clustered component crashes in the **cluster mincross core**, a cascade of component-vs-root assumptions: (1) `rank.ts:expandRanksets` gates the cluster `set_minmax` on `g === g.root` but C uses `g == dot_root(g)` → component clusters get no minmax → undersized `rankleader` → null in `buildSkeleton`. Fixing that (1-line, C-faithful, behavior-preserving) exposed (2) `cluster.ts:mergeRanks`→`cluster-path.ts:makeSlots` indexing the component's root rank array at a rank it doesn't span (`root.info.rank[r]` undefined), and there are likely more (`install_cluster`, etc.). These are out-of-write-set (rank.ts/cluster.ts/cluster-path.ts) edits in the most regression-prone subsystem (memory: cluster defects A/C/D, RC1–RC4) — a dedicated derisk effort, not safe to finish unattended. Per the agreed boundary, **reverted index.ts/pack-components.ts/rank.ts to the committed T2 state**; gate GREEN again (stable=672, improvements=2, regressions=0); clustered graphs fall back via the T2 `graphHasCluster` guard (no crash, no regression). The cluster-carry code + cascade map are captured here for a follow-up derisk mission. Oracle goldens kept: `test/golden/{inputs,refs}/pack-clusters-2592.*`. |
| 2026-06-27 | T4 | Baseline refreshed for the T2 scope; comparison page written | `comparisons/T4-survey-verify.md`. All 7 verdict moves are improvements (2458+2682 diverged→byte verified direct vs headless; 1724 + 4 `*pack_neato*` structural→byte, not from this dot-only change — pre-existing tightens). 0 regressions. Refreshed `parity.json`←`parity-rules.json` + regenerated `PARITY.md` (byte 442→449, diverged 107→105). T4's clustered-case acceptance is scoped to T2 (T3 deferred). |
