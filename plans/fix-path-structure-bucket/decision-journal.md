# Decision journal

| when | task | decision | why | alternatives rejected |
|---|---|---|---|---|
| 2026-07-02 | exec | Batch 1 execution plan: T1–T4 launched as 4 parallel `debugger` agents (sonnet, effort high); write-sets = 4 distinct note files; instrumentation ownership per overview (T1 rank*, T2 emit/bbox, T3 mincross*, T4 ns*/position). No conflicts. Branch fix/path-structure-bucket cut from 9032e52. | parallelism.md autonomous exception: log plan instead of user review | serializing diagnosis (slower, no conflict benefit) |
| 2026-07-03 | exec | Batch-1 agents' first Bash calls auto-denied (cd-prefixed / node -e not on allowlist); resumed all with allowlist guidance. No work lost. | background session auto-denies non-matching Bash | — |
| 2026-07-03 | T2/T4 | T4 corrected the T4 brief's assumption: graphs-b51 blok_16 diverges in PRIMARY rank NS (balance=0, no TB_balance), not x-coord NS. Same shared feasibleTree core. 2475_2 = mix of both sub-classes. | paired instrumentation + C `-v2` 0-iter evidence | — |
| 2026-07-03 | exec | F3 authored as BOUNDED feasibleTree-order attempt despite T4's tracked-deep classification, overriding batch-2 overview rule 4. User-approved scope explicitly included bounded x-NS attempts; T4's 7-node minimal repro makes it tractable. Hard stops + D2 gating in F3 spec. | user scope choice beats the plan's default deferral rule; repro is small enough to hand-trace C | pure deferral (leaves 4+ ids untouched despite approved scope) |
| 2026-07-03 | exec | D5 diagnosis task added for T2's secondary finding (36 ltail pre-clip spline divergences in 1879) — 1879 cannot reach conformant on F2 alone. F5 placeholder gated on D5's classification. | 1879 is in the bucket; "fix the issues" needs the second mechanism | folding into F2 (violates one-mechanism-per-fix) |
| 2026-07-03 | exec | F1/F2/D5 launched while T1 still diagnosing (batch overlap). Write-sets disjoint (ortho / render+gvc / edge-route notes vs T1's rank*). F3 held to run last, sequential per D2. F4 waits on T1. | parallelism.md default: no shared writes + no dependency → parallelize | strict batch barrier (idles 3 independent tasks) |
| 2026-07-03 | T1 | T1 verdicts: 2521 = NS pivot/tie-break on CONTRADICTORY constraint pair (cluster/classify/rank proven faithful via C -v5 counts) → joins F3's NS-core watch list; 1718 = distinct, candidate virtualNode id=0 defect (fastgr.ts:348) → F4 bounded fix; 2239 = distinct, nested-cluster rank-axis compression under LR → D6 diagnosis task. No shallow rank-extent fix exists. | T1 note evidence | authoring a speculative 2521 fix in rank/cluster files (proven faithful — would violate diagnosis.md) |
| 2026-07-03 | F2 | DONE (dd56749): pad attr ported (parseGraphPad + job.pad threading). Bonus: 2082 + 2592 now exact-match C. 1879 bbox residual re-pinned to unported graph `margin` attr → F6 authored + launched (same territory, F2 committed so no conflict). F2 also reported files "externally reverted" mid-task (infra hiccup w/ concurrent agents) — it re-applied and verified; watch for recurrence. | F2 report + git show dd56749 | folding margin into F2 (separate mechanism, separate commit) |
| 2026-07-03 | D5 | tracked-deep: ltail edge is ADJACENT-RANK via compound polyline route (chain-router hypothesis disproven); clip faithful; divergence is raw pre-clip head-side control points; root needs C-side dumps → deferred w/ follow-up recipe. F5 cancelled. D5 ALSO self-reported a boundary violation (edited dotsplines.c for dumps) — caught pre-oracle-contamination, reverted, rebuilt, oracle verified byte-identical to baseline. | D5 report + note | orchestrator-level C instrumentation now (would invalidate oracle cache + disrupt F1/F6 mid-flight) |
| 2026-07-03 | D6 | tracked-deep, SAME NS-core class: 2239's compression = port picks minimal feasible width; C's LR_balance spreads slack over a DIFFERENT feasible_tree state. Constraint graph verified structurally identical (28x-diverging cluster hand-verified). Now 5/8 bucket ids (2521, 1447, graphs-b51, 2475_2, 2239) + 2371 converge on feasibleTree construction order → F3 is the pivotal task; its watch list updated. | D6 report + note | — |
| 2026-07-03 | F1 | D3 stop condition fired cleanly: partition/maze construction order fixed to C's exact preorder (real bug, 9933511, 0 regressions, kept per CLAUDE.md fidelity) but A/B PROVED it is not the 2361/2620 tie-break driver. 2620 → deferred tracked-deep; open candidates: routing-order×updateWts interplay or cost divergence. No further ortho attempts this mission (would exceed the ordering bound). | F1 A/B evidence on live 2361 maze geometry | continuing to chase 2620 beyond the D3 bound (locked decision) |
| 2026-07-03 | F4 | Premise false (C virtual_node also id=0; port faithful). REAL defect found: ufUnion compared AGID where C's ND_id is never written under dot (tie-break always false → first-arg root wins). Fix improved 2521 to 7pt (supersedes T1's ns.ts attribution). Agent reverted on survey regression 2646→timeout per protocol (74339f6→6b22cd3). | F4 report | — |
| 2026-07-03 | exec | RE-LANDED F4 as 5cc7c6b after orchestrator re-verification: 2646 output BYTE-IDENTICAL with/without fix; timing within noise (306s fix vs 327s base; native 72s; ~4.3x native either way = pre-existing NS perf debt at the budget edge). The survey "regression" was a flaky timeout under CPU contention from concurrent F6. Gate evidence = F4's survey (762 stable, 2521 improved, no genuine regressions) + targeted 2646 verification; Batch-3 final survey re-measures all. 1718 mechanism now fully ruled out as ufUnion (never executes there) — stays tracked-deep/undiagnosed. | byte-identical diff + paired timings | full immediate survey re-run (hours; sole disputed id already verified; final survey covers it) |
| 2026-07-03 | F3 | Stop condition hit correctly (fix outside ns write-set) but ROOT CAUSE PINNED for the whole NS-selection class: port iterates g.nodes.values() (subgraph-local first-insertion) where C's agfstnode iterates AGSEQ (global creation seq; cgraph node.c:283-290). class1 + decompose both affected; patching BOTH → graphs-b51 BYTE-EXACT (Δ0). NS functions verified faithful (isolated repro byte-identical). Also corrected T4's "0 iter" misattribution (6 similar NS calls in b51; C does 1 pivot for cluster_if_40). | F3 controlled experiment | — |
| 2026-07-03 | exec | F7 authored + launched: apply the AGSEQ-order iteration fix at the C-mirroring sites (class1, decompose, + audited siblings in cluster.ts etc.), verify port's id field truly mirrors AGSEQ, full-survey gated, alone in tree. Targeted-sites fix chosen over a global Graph-substrate ordering change (correct long-term candidate, but corpus-wide risk too high to land autonomously; logged as follow-up). | mechanism pinned + byte-exact experiment; mission goal is to fix | substrate-level fix (defer); leaving pinned+proven mechanism unfixed (violates mission objective) |
| 2026-07-03 | F7 | LANDED 264484f: nodesInSeq helper at 44 agfstnode-mirroring sites (15 files) + splines-clone fresh-id fallout fix. graphs-b51 BYTE-EXACT; 2521 improved; share/windows-b51 stable; 5 timeout "regressions" re-verified standalone as flaky (F4 precedent); 2606 tests green. | F7 report + gate output | — |
| 2026-07-03 | exec | 1581 crash (clustHt, position-ycoords.ts:126) bisected by orchestrator: renders at 9032e52 AND 0ce0a54; crashes from 5cc7c6b (F4 ufUnion re-land) — GENUINE mission regression F7 mislabeled pre-existing (it only checked one commit back). Decision: FIX at origin (F8) rather than re-revert F4 — F4's gains are confirmed (2521; F3's byte-exact experiment ran atop it), C renders 1581 without crashing so the port has a real defect the faithful UF topology exposes (suspect: stale cluster rank window in init_rank recovery, cf. 2471 rec_reset_vlists family). If F8 cannot pin+fix cleanly, revisit revert-vs-keep and stop for human input. | bisect evidence | immediate re-revert of F4 (sacrifices confirmed gains; crash is a port defect, not the fix's fault) |
| 2026-07-03 | exec | Batch-2 gates: tsc ✓, 2606 tests ✓, write-set audit ✓ (all changed files map to declared sets). First Batch-3 survey: improvements 1308→conformant, 2521→structural-match(7pt), graphs-b51→CONFORMANT; two timeout flags (2343, 2371). Root-caused the flags: F8's C-plugin rebuild invalidated the oracle cache → all 789 native renders ran interleaved with port renders → giant-tail pool starvation past even the 817s floor. Verified 2343 standalone: output Δ0 vs oracle, 102s (vs 97s at mission start, byte-identical output base-vs-HEAD) — no code regression. Re-running survey with the now-warm cache (= baseline conditions) rather than accepting timeout verdicts into the refreshed baseline. | budget floor 817795ms in log + cold cache sig + standalone A/B | accepting environment-artifact verdicts into the baseline; raising the floor via env override (masks the real conditions) |
| 2026-07-03 | T9 | Final survey (concurrency 3, warm cache): GATE PASS, 0 regressions. 602/163/12/0 errored/1 timeout/11 oracle-error. Deltas vs mission start: graphs-b51→conformant, 2521→structural-match(7), 1308→conformant, 1652 oracle-error→timeout (newly scored; native 208s ≈ oracle cap; port standalone >15min = genuine slow-tail perf debt, follow-up #5). Baseline+dashboard committed f6f5866; mission summary written to README. Branch left UNMERGED for user review (merge-commit policy when merging). | gate output + per-id diff + 1652 standalone probe | auto-merging to main (outward-facing; user gates merges) |
| 2026-07-03 | exec | F4 sequenced AFTER F1/F2/D5 (its mandatory full survey needs a clean committed tree; running it now would measure siblings' in-progress edits). D6 launched immediately (diagnosis-only, no survey, disjoint instrumentation: cluster*/position-cluster*). | survey integrity | immediate F4 launch (polluted survey) |
