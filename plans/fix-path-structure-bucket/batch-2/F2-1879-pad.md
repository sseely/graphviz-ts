# F2 — 1879: graph `pad` attribute never read (SVG_PAD hardcoded)

## Context (from .agent-notes/path-structure-1879.md — read it fully)
The graph attribute `pad=2` (2in → 144pt) is never read anywhere in the port.
`SVG_PAD` (`src/render/svg-helpers.ts:52`, `= 4`) is used unconditionally in
`src/gvc/viewport.ts:108-117` (`initJobViewportZoom`) and throughout
`src/render/svg-graph.ts` for bbox/viewBox/polygon/translate emission, where
C's `init_job_pad` (`lib/common/emit.c:3241-3251, 3290-3304`) resolves
`pad="x[,y]"` via sscanf `%lf,%lf` × POINTS_PER_INCH(72), falling back to
DEFAULT_GRAPH_PAD=4 (`lib/common/const.h:96`). Verified: raw pre-scale
background-polygon extents differ by exactly 280.00pt = 2*(144-4) on both
axes; downstream `size=150,150` scale-fit turns that into the observed
width/height/translate deltas.

## Task
Port C's `init_job_pad` faithfully: read `pad` from the graph attrs, parse
`"x[,y]"` (y defaults to x; units are inches × 72), fall back to 4pt when
unset/unparseable (match C's sscanf semantics: sscanf returns count parsed —
1 → y=x; 0/absent → default). Thread the resolved {x,y} pad through
`initJobViewportZoom` and `svg-graph.ts` in place of the bare `SVG_PAD`
constant (the constant remains as the default value). JSDoc `@see
common/emit.c:init_job_pad`. Check how C treats negative/zero pad and
preserve it exactly.

## Write-set
- `src/render/svg-helpers.ts`, `src/gvc/viewport.ts`, `src/render/svg-graph.ts`
- the job/render-construction file where per-job pad must be read (agent
  identifies it; note suggests `src/gvc/job.ts` or equivalent)
- colocated `.test.ts` for the parse function + a golden/regression test
- `.agent-notes/path-structure-1879.md` (append fix record)

## Read-set
- `.agent-notes/path-structure-1879.md`
- C: `~/git/graphviz/lib/common/emit.c:3241-3310` (init_job_pad,
  init_job_viewport neighborhood), `lib/common/const.h:96`
- Port: `src/render/svg-helpers.ts`, `src/gvc/viewport.ts`,
  `src/render/svg-graph.ts`

## Acceptance criteria
- Given 1879.dot, when rendered, then the top-level translate/width/height
  match C's (translate x 278.69, height 1563-class values) — verify with the
  repro in the note; the remaining diffs are the separate ltail-spline
  residual (D5), NOT bbox/translate
- Given a minimal `pad=2` digraph golden, when rendered, then padding is
  144pt per side (test asserts the emitted viewBox/translate)
- Given graphs WITHOUT pad, when the full test suite runs, then zero changes
  (default path identical) — `npm run test` exit 0
- Given `npx tsc --noEmit`, then exit 0

## Tests (TDD)
Failing test first: unit test for the pad parse (absent/single/pair/garbage)
+ a small golden asserting translate with pad=2.

## Observability: N/A. Rollback: Reversible — single commit
`fix(render): read graph pad attribute for viewport/translate (F2)`.

## Boundaries
- Never: touch layout code (src/layout/**), parity baselines, other render
  behavior.
- One commit; body notes the mechanism.
