# Decision Journal — dot-flat-labels (G4)

Appended during execution (per ~/.claude/rules/autonomous-execution.md).

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| T1 | 2026-06-16 | Single-agent inline (no parallel fan-out) | T1 is one logical unit on 2 files (flat.ts, position.ts); per parallelism.md, no bottleneck justifies multi-agent. | No |
| T1 | 2026-06-16 | Updated `flat.test.ts` abomination block (AC4) to AD-2 contract | The 2 abomination unit tests asserted the old negative-index behavior (`minrank=-1`) that AD-2 deliberately replaces. Test co-locates with the rewritten fn; TDD requires it track the new contract. Not a source file outside write-set; not an oracle/golden. | No |
| T1 | 2026-06-16 | Faithfully ported full `flat_node` (ypos, dims, ports, ht1/ht2, coord, ND_alg→posAlg) | Prior TS `flatNode` was missing `ND_alg(vn)=e` (the T2 interface contract) and set lw/rw/ht wrong (used nodesep instead of dimen.x/2). Ported flat.c:flat_node 137-184 exactly; split into helpers (flatLabelYpos/flatNodeDims/flatNodeEdges) to satisfy the 30-line/CCN-10 hook. | No |
| T1 | 2026-06-16 | Kept simplified `flatLimits` (not full C bounds[4]) | T1 scope = vnode creation; the simplified placement returns a valid in-range slot and AC passes. Full `flat_limits` (setbounds/HLB-SRB) deferred to T2 if oracle pins need exact x-placement. | Review in T2 |
| T1 | 2026-06-16 | Lizard "length" counts inter-fn comments toward the preceding fn | The TS parser attributes comments between fn N and N+1 to fn N's length. Moved AD-2 rationale into `abomination`'s body as inline comments + trimmed JSDocs so `flatNode` measures ≤30. | No |
| T2 | 2026-06-16 | **Write-set extension (FLAGGED):** add `splines-flat-labeled.ts` (new) + minimal `edge-route.ts` diversion, beyond the declared `splines-flat.ts`+test | (1) `splines-flat.ts` is 456/500 lines — the hook's 500-line file cap forbids adding ~70-line `makeFlatLabeledEdge` there; the test is already named `splines-flat-labeled.test.ts`, implying a `splines-flat-labeled.ts` module was anticipated. (2) The live flat dispatch is `edge-route.ts:routeForwardEdge` (the `hasSidePort` gate sends label-less flats to the simplified fitter); the task text explicitly mandates "wire into the live router … mirror how flat side-port edges reach routeFlatEdgeFaithful". `edge-route.ts` is in no task's write-set, so per the stop-rule this is logged. Mitigation: the `edge-route.ts` change is a single guarded branch firing ONLY for non-adjacent labeled same-rank edges (zero golden coverage); 115-golden byte-identity is the hard gate — if any golden changes, STOP. Not mincross-build.ts (the README's emphasized no-go). | Yes — review |
| T2 | 2026-06-16 | Spline case is the primary pin; result is **byte-exact** to dot 15.0.0 | TS spline path + label `x` (117,-57.2) match the oracle byte-for-byte (reused makeFlatEndBox + the C box channel + routeSplines). Pinned at tol 0.5 in `splines-flat-labeled.test.ts`. | No |
| T2 | 2026-06-16 | **Quarantine (AD-5):** `splines=line` full-render line case | `splines=line` is unported globally — `dotPhaseInit` hardcodes `setEdgeType(EDGETYPE_SPLINE)` and `edgeType` never reads the `splines` attr; wiring it is a separate graph-init feature touching all engines (out of flat-label scope, golden-risk). Not a `make_flat_labeled_edge` geometry failure: the 7-point branch (`flatLabeledLinePoints`) is ported faithfully and unit-tested directly. Comparison page: `comparisons/dot-flat-label-line.md`. | Yes — review |
| T2 | 2026-06-16 | Skip adding the labeled dispatch to `makeFlatEdge`'s body | `makeFlatEdge` has no live callers in this port (flats route per-edge via `routeForwardEdge`); adding the call would only create a runtime-safe-but-ugly `splines-flat ↔ splines-flat-labeled` import cycle for dead code. Documented the live-dispatch location in a comment instead. | No |
| T2 | 2026-06-16 | Gate result: tsc 0, lizard clean, **1796 passed / 0 failed, 115 goldens byte-identical** | Batch 2 gates pass. The `edge-route.ts` diversion did not perturb any golden (declines for all non-labeled-flat edges). | No |
