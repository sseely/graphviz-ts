# Decision Journal

Append one row per non-trivial judgment call made during execution.
"Non-trivial" means a reasonable developer might have chosen differently.

| Date | Task | Decision | Rationale | Outcome |
|------|------|----------|-----------|---------|
| 2026-05-27 | T19 | AC3 spec says "color not modified on unknown" but C source (colxlate.c:369) sets black/opaque on COLOR_UNKNOWN. Test written to match C source behavior. | C source is canonical spec per mission brief; T19 spec doc was incorrect. | Test asserts color IS set to black/opaque on unknown input — matches C behavior. |
| 2026-05-28 | T14-fix | vispath.ts: added `if (min === -1) break` in dijkstra and undefined/-1 guards in countPath/extractPath. Dijkstra on disconnected visibility graphs returns dad[V]===-1; JS `dad[-1]` is `undefined` (unlike C's allocated memory), causing infinite loop. Fix matches C intent. | AD-13: never change test assertions; fix implementation. Disconnected path returns direct [p0,p1]. | 51/51 pathplan tests pass. |
| 2026-05-28 | T14-fix | triang.ts: added signedArea helper; `triangulate` reverses polygon when area>0 (Y-up CCW). `isdiagNeighborhood` requires Y-up CW winding; graphviz always pre-normalizes in production, but test polygons were Y-up CCW. Normalization added at `triangulate` entry. | C `Ptriangulate` does not normalize — graphviz callers do. TypeScript port adds it inside `triangulate` to be defensive and match caller contracts. | CCW square→2 tris and CCW pentagon→3 tris both pass. |
| 2026-05-28 | T20 | HTML label parser split into 3 files (lex/types/parse) plus entry point htmltable.ts to stay under the 500-line file limit enforced by check-complexity.py. | Single file would exceed 500 lines. Split at natural layer boundaries. | 4/4 acceptance tests pass; complexity hook clean. |
| 2026-05-28 | T23 | splines-clip.ts: clipAndInstall omits bb update (Edge has no graph ref). Deferred — callers that need bb pass it separately. Noted in source comment. | Edge model does not carry a graph pointer per the data model design. The C uses agraphof(agtail(e)); no safe TS equivalent without changing the model. | 8/8 splines tests pass. Will revisit in Batch 6 if layout accuracy requires bb. |
| 2026-05-28 | T24 | emit.c layer/pagination/viewport machinery stubbed (single-layer, single-page per AD-2). Core rendering dispatch (node/edge/cluster/xdot) fully ported. parseStyle: C style_token stops identifiers at whitespace (isspace check in C) — initial TS port missed this, causing all-whitespace-separated tokens to merge. Fixed by extracting isIdentStop() with the whitespace check. | AD-2 authorizes the stub. parseStyle bug was an implementation error vs. C spec. | 21/21 emit tests pass; 407/407 total. |
| 2026-05-28 | T25 | Created minimal job.ts scaffold (export interface RenderJob) as part of T25, outside the declared write-set. Needed so tsc --noEmit passes after T25 commits — context.ts imports RenderJob from ./job.js as a type-only forward reference. T26 replaces the scaffold with the full class. | Without the scaffold, tsc fails on the missing module even for type-only imports. Deviating from declared write-set is the lesser evil vs. breaking the quality gate. | tsc exit 0; 6/6 context tests pass; 413/413 total. |
| 2026-05-29 | T33 | rank.c (1107 lines) split into rank.ts (dot1_rank) and rank-dot2.ts (dot2_rank) plus ns-core/ns-range/ns-subtree/ns.ts for the NS solver. XgState struct replaces C statics Last_node and id. Iterative DFS used in breakCycles/dfsccNode to avoid call-stack overflow. ND_comp overloaded as n.info.hops for connected component index (matches C rank.c:connect_components). | check-complexity.py enforces 500-line file limit and Lizard -L 30 per-function physical length. A single rank.c port would exceed both. Split at natural algorithmic boundaries. | tsc exit 0; 542/542 tests pass; all 10 rank tests green. |
| 2026-06-01 | T35 | Cluster expansion (cluster.c) committed separately from position phase. fastNode/deleteFastNode/virtualNode added to fastgr.ts (needed by cluster). mergeChain stub in cluster.ts to be replaced in T36. | T35 position phase was committed first; cluster files were untracked and completed in a follow-up commit. | 602/602 tests pass after T35 cluster commit. |
| 2026-06-01 | T36 | class2 uses n.info.out (fast graph) for outEdges instead of cgraph agfstout. In production, class1 populates n.info.out before class2 runs; in tests, addTestEdge populates both g.edges and n.info.out so tests pass either way. Real divergence will only manifest if class2 is called without class1 first. | The C agfstout iterates cgraph edges; TS outEdges helper reads the fast graph. Since class1 runs before class2 in the pipeline, n.info.out holds the class1-built fast graph when class2 runs in production. | Noted for T11 golden-file validation. |
| 2026-06-01 | T36 | newVirtualEdge bug fix: missing `if (orig.info.to_virt === undefined) orig.info.to_virt = e` line. C new_virtual_edge sets ED_to_virt(orig)=e; TS port was missing this. Backward edge test revealed it. | AD-13: fix implementation, not tests. Added the line to fastgr.ts:newVirtualEdge. | 637/637 tests pass after fix. |
| 2026-06-01 | T36 | checkLabelOrder and recResetVlists stubbed in flat.ts. C flat_edges calls these from position.c context with no MincrossContext available. Real integration happens in T39 dotLayout. | flatEdges called from position phase which has no ctx; stubs are safe since T36 AC don't test label ordering. Will revisit in T11. | 637/637 tests pass. |
| 2026-06-01 | T37 | makeRegularEdge stubbed in splines-route.ts pending full pathplan integration. dotsplines.c is 2309 lines; the entry point and main dispatch were ported; the full per-edge routing (routesplines, beginpath, endpath) deferred to T11 integration work. | Acceptance criteria focus on flat-adj recursive pipeline, sameports, dotConcentrate, and edge_normalize — all of which are testable without makeRegularEdge. | 656/656 tests pass. |
| 2026-06-01 | T38 | findVertical/findHorizontal needed ε=1e-5 termination per spec. Without it, beziers crossing exactly at a control point caused non-termination. Also: lh null guard added to makeCompoundEdge (lh!.info.bb crash when only ltail is set). | The T38 spec explicitly states the 1e-5 termination condition; the initial implementation missed it. | 665/665 tests pass after fix. |
| 2026-06-01 | T39 | dotSplines skipped in AC4 test via maxphase=3 (stop after position). dotSplines requires fully configured spline routing (routesplines, pathplan) not yet integrated. Test verifies coord ordering from dotPosition which is sufficient for rank/position validation. | dotLayout maxphase escape hatch (from C dotinit.c) enables partial pipeline for debugging and testing. | 674/674 tests pass. |
| 2026-06-01 | T40 | BFS unreachable sentinel follows C convention: closestDist+10 (not INT_MAX). BFS_UNREACHABLE=2147483647 exported for documentation but not used as the sentinel value. dijkstraSgd uses lazy heap re-insertion instead of decreaseKey; stale entries ignored when popped. | T40 spec says unreachable=INT_MAX but C bfs.c uses closestDist+10. Faithfully matched C. | 16 tests pass. |
| 2026-06-01 | T41 | Kahan summation required for Laplacian diagonal (DegType=long double in C). StressOpts.exp is injected by stressMajorizationKD before inner calls; default ?? 1 used by receiver. stressMajorizationKD wrapped in class to avoid Lizard false-merging of top-level functions. | Lizard TypeScript parser merges adjacent top-level functions. Class static methods force parser resets. | 6 stress tests pass. |
| 2026-06-01 | T42 | MT19937 rkInterval used exclusively — Math.random() forbidden. Initial positions use rkInterval(0xffff)/0x10000 to approximate drand48() from C. | AD-9: PRNG must be MT19937 for reproducibility. | 8 SGD tests pass. |
| 2026-06-02 | T45 | setSeed returns defaultMode always; seed parsing simplified to extract numeric value into g.info.seed; mode parsing lives in parseMode. g.info.seed propagated so sgdLayout reads it. | neatoinit.c split init/mode concerns; TS port merges for clarity. | 17 neato tests pass. |
| 2026-06-02 | T46 | WeakMap for per-node force scratch vectors avoids NodeInfo type cast (NodeInfo has no index signature). fdp/index.ts split into forces.ts (force primitives) + index.ts (pipeline) to stay under 500-line limit. clusteredges.ts is a stub that falls back to splineEdges. | No clean way to add dynamic fields to NodeInfo; WeakMap is GC-friendly alternative. | 44 fdp tests pass. |
| 2026-06-02 | T47 | Control struct save/restore uses const ctrl0={...ctrl} + Object.assign in finally block — guaranteed restore even on exceptions. randomSeed default=123 (not 0 like neato). maxlevels=0 is "no multilevel" sentinel; coarsening requires maxlevels>=2. smoother variants stubbed; SMOOTHING_NONE (0) is the active path. | Section 6.11 of interconnections.md: ctrl mutations during multilevel must not persist to caller. | 5 sfdp tests pass; 791 total. |
| 2026-06-03 | T48 | cval() uses Math.trunc (not Math.floor) for negative values to match C integer division semantics. C CVAL macro: ((v+1)/s)-1 truncates toward zero; Math.floor gives wrong result for (-3)/3 case. l_aspect is a no-op (not implemented in C source). l_node/l_clust return null for putRects (no node geometry). | C integer division truncates; JS Math.floor rounds toward -∞. Faithful port requires Math.trunc. | 73 tests; 864 total. |
| 2026-06-03 | T49 | CircoCData/CircoNData added as new union members to NodeAlgData. Single-free pattern: contiguous ndata array, only first node's alg nulled. ND_alg freed before splineEdges (stop condition). copyPositions reads from n.info.pos directly (not from DerivedNode.pos which is never updated from initial [0,0]). | AD-7 discriminated union. C frees single gv_calloc block via first node's pointer. | 10 circo tests; 941 total. |
| 2026-06-04 | T50 | PackMode.Graph used for multi-component packing fallback instead of PackMode.Node (which returns null in putRects because node geometry not ported). finaliseCoords() called per-component before packSubgraphs so computeSubgraphBB reads coord (points) not pos (inches). | putRects returns null for PackMode.Node — no node shape geometry in TS port yet. | 22 twopi tests. |
| 2026-06-04 | T52 | layoutTreeMargin(10,8,64)≈0.9377 (not 0.9029 as stated in plan doc — the plan had a calculation error). Test verifies (h-m)(w-m)≈childArea invariant instead of exact value to avoid brittle test. | Plan doc documented wrong expected value; C formula (h+w-sqrt((h-w)²+4A))/2 gives 0.9377. | 11 patchwork tests. |
| 2026-06-05 | Batch 11 | Node shape rendering missing from walkNodes pipeline. `renderGraph` calls `beginNode`/`endNode` but no shape draw code between them. C `emit_node` calls `ND_shape(n)->fns->codefn(job, n)` + `emit_node_label` — neither is ported. Result: every node group `<g id="nodeN">` has only `<title>` but no polygon/ellipse/text. Required before golden tests can pass for any engine. | Stop condition 1. See separate entry for root-cause chain of fixes. | golden tests still 50/50 fail (child count mismatch). |
| 2026-06-05 | Batch 11 | Fixed 8 root-cause bugs in the rendering pipeline: (1) dotInitNode lw/rw 54→27; (2) dotInitSubg nodesep 54→18, ranksep 54→36; (3) dotInitNode missing node_type=NORMAL (broke firstNormalNode bb scan); (4) SVG renderer missing 4pt padding+y-flip+translate group; (5) dot NS x-coords assigned 0 instead of lw (fixed by normalizeXcoords post-processing); (6) viewBox wrongly started at bb.ll.x instead of 0; (7) graph group id used graph name instead of "graph0"; (8) node/edge groups used names instead of "node1"/"edge1" sequential ids. | AD-13: never change test assertions; all fixes went to implementation. | dot-simple-box now produces correct 62×188 SVG; shape rendering gap remains. |
| 2026-06-04 | Batch 11 | **STOP CONDITION 1 HIT.** T55 golden-file suite reveals 50/50 test failures. Two root causes: (1) dot/osage/patchwork produce non-zero SVG but heights differ from C reference by 2–109pt — systematic font-metric difference between LutTextMeasurer (LUT approximation) and C FreeType-based measurement; (2) neato/fdp/sfdp/circo/twopi produce height=0 SVG — these engines never initialize n.info.rw / n.info.ht (node geometry), so computeSubgraphBB gives zero-area bb. Intermediate fix: class1 stub in rank.ts was wired to real classify.ts implementation (digraph { a -> b -> c } had all nodes at rank=0 without it). device.ts: fallback to computeSubgraphBB when g.info.bb is zero-area (helps iterative engines once node geometry is fixed, but that requires porting neato_init_node and equivalents). **Human input required before proceeding.** | Stop condition 1: SVG output does not match C binary within tolerance. | 941 pre-existing tests still pass; 50/50 golden tests fail. |
| 2026-06-04 | T49 | CircoCData/CircoNData defined in blocks.ts (not init.ts) to avoid circular import: cdata needs DerivedNode back-reference and DerivedNode needs cdata — both must live in the same module. CircoNData/CircoCData added to NodeAlgData union in nodeInfo.ts as concrete types (not via import from layout/) to keep model→layout dependency direction clean. copyPositions bug: sed partial-replacement left `dn.orig.info.pos[1] = dn.pos[1]` (dn.pos always [0,0]), zeroing all y-coordinates. Fixed to read from dn.orig.info.pos directly. Lizard TypeScript parser sweeps all functions between two `export function` boundaries into one; mitigation: every function must be exported (no private functions between exports). | C single-free pattern (all ndata in one block) cannot translate directly; using GC with alg=undefined cleanup instead. | 10 circo tests pass; 874 total. |
