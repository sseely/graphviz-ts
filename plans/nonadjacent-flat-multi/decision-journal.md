# Decision Journal — nonadjacent-flat-multi

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).
One writer per row.

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| scope | 2026-06-20 | Mission scoped from a COMPLETE oracle-validated diagnosis (findings-diagnosis.md), the latent follow-up banked at the close of `nonadjacent-flat-5ne8nw`. C `make_flat_edge`/`make_flat_bottom_edges` nest cnt≥2 non-adjacent flats via a `(i+1)·Multisep/(cnt+1)` loop; the port routes each independently at `nodesep/2` → identical overlapping splines. ZERO corpus triggers (all 74 corpus non-adjacent flats are cnt=1); validated synthetically; regression bar = cnt=1 conformant. Fix = group + cnt-loop in a new module (splines-flat.ts at line cap), reusing generalized box helpers; 4 sequential tasks (T1 helpers → T2 router → T3 dispatch → T4 regression). | Diagnosis-first + cnt=1-reduction proof bound the blast radius of a shared-router change; native-oracle synthetic conformant is the only available validation (no corpus trigger). | no |
| T1 | 2026-06-20 | Generalized `topBoxes`/`bottomBoxes` to `(endStepX, endStepY, midStepY)`, verified field-by-field against C `make_flat_edge`/`make_flat_bottom_edges` loop body (endStep→b0.ur + b2.ll offset; midStep→b1 height). Call site passes `(stepx,stepy,stepy)`. Exported topBoxes/bottomBoxes/flatSide/flatVspace/assembleFlatPath/FlatEndParts. Trimmed docstrings to hold splines-flat.ts at 499<500 (complexity hook fired twice at 502/501). GATES: tsc 0, lizard clean, wc 499, vitest 1995/1995 green (cnt=1 conformant). | Pure refactor; cnt=1 reduces to identical args so no behavior change. The 5-param signature is exactly at the lizard cap. | no |
| T4 | 2026-06-20 | Regression sweep + close. vitest 1999 (1995 unchanged + 4 new) — all goldens conformant. Corpus survey: ZERO new diverged/structural (diverged 356→356, structural 237→237). ONLY verdict change = `2222` conformant→timeout; INVESTIGATED + RULED OUT as a regression: 2222.dot is 2.3MB→22MB SVG rendering a steady ~13s (3 runs, exit 0) but the survey timeout is 20s at concurrency 8 → load flake; on `main` it also renders ~13s; decisive proof = 2222 SVG is BYTE-IDENTICAL main vs branch (`cmp` clean). Restored committed parity.json (no legitimate change). Synthetic top cnt=2/3 + bottom cnt=2 drawing elements conformant re-captured native dot; cnt=1 unchanged. Oracle clean (no instrumentation this mission; C source unmodified, 0 probe markers). Memory `flat-edge-241-is-y-only` + MEMORY.md updated (cnt≥2 CLOSED). | A conformant→timeout flip looks like a regression but the conformant output + identical timing on main prove it's a harness concurrency flake, not behavior; honest record = restore baseline parity. | no |
| T3 | 2026-06-20 | Wired `routeFaithfulSidePort` (edge-route.ts): the sameRank non-adjacent branch now collects the group + routes via `routeFlatEdgeGroupFaithful` (group router clip-installs siblings; routeDotEdges skips already-routed). Adjacent + regular branches untouched. JUDGMENT CALL: `routeFlatEdgeFaithful` is now only comment-referenced (no live caller) but I KEPT it — removing it touches splines-flat.ts (T3 write-set is edge-route.ts only + the conformant-critical cnt=1 anchor); it stays the documented cnt=1 reference. GATES: tsc 0, lizard clean, wc 386, vitest 1999 (all goldens conformant → cnt=1 unchanged). END-TO-END: render-one top cnt=2/cnt=3 + bottom cnt=2 DRAWING ELEMENTS conformant with native dot; viewBox grew 69→73 matching oracle (nested bulge raises bbox). | One faithful dispatch covering cnt>=1; cnt=1 group (length 1) reduces to the prior single route. Keeping the dead helper bounds blast radius per AD-1. | review (dead-code: routeFlatEdgeFaithful retained, removable in a follow-up) |
| T2 | 2026-06-20 | Created `splines-flat-multi.ts` (collectNonAdjacentFlatGroup + routeFlatEdgeGroupFaithful) + test. KEY DISCOVERY (push-forward, not in brief): `routeSplines` mutates its boxes IN PLACE (splines-routespl.ts:112), and the port's `addBox` stores box REFERENCES, whereas C `add_box` copies boxf BY VALUE. The cnt-loop reuses ONE shared tend/hend, so iteration 0 corrupted them → i>=1 produced an empty route (`ok=false`, only 1 spline emitted; iter-0 spline matched oracle exactly). Fixed faithfully with a per-iteration `copyPathEnd` deep-copy in the group router (NOT by touching shared addBox/assembleFlatPath — keeps blast radius minimal + splines-flat.ts at 499). After fix: top cnt=2/cnt=3 + bottom cnt=2 conformant with native dot 15.1.0~dev.20260610.0127 ALL splines; cnt=1 == oracle top1. lizard length attributed the trailing GroupRouteCtx interface to collect (per `lizard-length-inter-comment`) → moved interface up under imports. GATES: tsc 0, lizard clean, wc 172/101, vitest 1999 (1995 unchanged + 4 new). | C copies boxes by value; the port aliases them, a latent hazard the cnt=1 path never hit (routes once). copyPathEnd localizes the fix to the new module. | no |
