<!-- SPDX-License-Identifier: EPL-2.0 -->
# Decision journal — orientation=land

Appended during execution. One row per non-trivial judgment call.

| timestamp | batch/task | decision | rationale |
|---|---|---|---|
| 2026-06-24 | mission | Handle T1/T2 inline (Opus) rather than delegate to sonnet sub-agents as the brief suggests | Both tasks are <30-line focused edits tightly coupled to a C-source derivation I built in-context (init_job_viewport→init_job_pagination→setup_page). Delegating would force re-establishing that derivation in a blank-slate agent. Per global CLAUDE.md "handle directly for tasks under ~30 min". |
| 2026-06-24 | T1 | parseLandscape tests → new src/gvc/viewport.test.ts (outside declared write-set) | parseLandscape lives in viewport.ts, so its tests colocate there per naming-conventions. Lumping them into device.test.ts pushed it to 530 lines, tripping the 500-line complexity hook. New colocated test file is the correct home, conflicts with no other task. Brief PUSH-FORWARD allows test additions + complexity-hook refactors. device.test.ts keeps only AC16 (transformPoint guard). |
| 2026-06-24 | T1 | gate result: byte-stable | typecheck 0; npm test 2361 green; lizard clean on 4 changed files; survey parity.json conformant to baseline except generatedAt timestamp (reverted); b68 still rotate(0). |
| 2026-06-24 | T2 (pre) | Derive rotated translate from C trace, not empirical fit | Read setup_page rotation branch for SVG (GVRENDER_Y_GOES_DOWN set). For single-page/margin=0/not-centered: tx=-(focus.x+pageSize.x/2)=-(bb.ur.x+PAD); ty=focus.y+pageSize.y/2=bb.ur.y+PAD. pageSize=imageSize/zoom is Z-independent in graph units (outer scale carries Z), so formula holds for all Z. Matches b68 native translate(-634 208.5) exactly. |
| 2026-06-24 | T1 | parseLandscape self-contained in viewport.ts; do NOT import mapbool from layout/dot/rank.ts | rank.ts transitively imports the whole layout engine (ns/decomp/acyclic). Importing it into the leaf viewport.ts inverts layering (gvc←layout) and risks a cycle. mapbool is a lib/common/utils.c primitive; replicate the 3-line bool parse locally with @see. |
| 2026-06-24 | T2 (pre) | Dim swap = swap final w,h | C init_job_pagination width/height use page-orientation pageSize (exch_xyf'd). Equivalent to swapping the port's computed w,h since round() commutes with swap and scale is uniform. b68: 638×213 → 213×638 native. |
| 2026-06-24 | T2 | Corpus has 4 landscape graphs, not 3: NaN, b68, b69, proc3d | b69 sets `rotate = "90"` (quoted) inside its graph[] block — missed by the initial grep. All 4 are the only graphs whose bytes change. b69 maxDelta 2728→116 (stayed diverged, separate rankdir=LR blocker). No non-landscape graph changed → byte-stability holds. |
| 2026-06-24 | T2 | gate result: PASS — mission complete | typecheck 0; npm test 2367 green; lizard clean (svg-graph.ts/.test.ts); survey: only verdict change in 795 graphs is graphs-b68 structural→conformant (canary ✓), 0 non-landscape regressions. Landscape maxDeltas all improved, none entered a worse bucket (NaN 2007→1907, proc3d 2620→217, b69 2728→117). parity.json/PARITY.md regenerated. |
