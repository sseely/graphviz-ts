# Decision Journal — fix-concentrate-2559

Append one row per non-trivial judgment call during execution.

| When | Task | Decision | Rationale |
|------|------|----------|-----------|
| 2026-06-27 | setup | Branch `fix/concentrate-2559` off main; untracked `plans/` carried over | Per README; merge-commit on completion |
| 2026-06-27 | B1 | Start Batch 1: T1 (read-only investigation, run inline; subagent dispatch declined by user) | Single de-risking gate per batch-1/overview.md |
| 2026-06-27 | human | **Write-set expanded to include `src/layout/dot/splines.ts`** (user-approved). Resume at T2 with the faithful per-segment trunk fix. | User decision in chat; ADR-1/2/3 still apply; conc.ts/classify.ts stay untouched. |
| 2026-06-27 | T2 | Faithful fix landed. `routeMergedChain` (edge-route-chain.ts) splits a merged forward chain into merge-bounded runs and clip_and_installs one bezier per run on the representative; trunk runs owned via `getMainEdge`. splines.ts dispatch intercepts lone merged chains. TDD: golden+unit test (Red→Green). 2559 edge2 now BYTE-matches headless 15.1.0 (2 paths). tsc 0, vitest 2454 pass. | ADR-1 faithful per-segment port (no special-case); mirrors C dot_splines_ gather + clip_and_install. No gather/non-merge-routing change needed (driven from the original edge). Did not touch conc.ts/classify.ts/edge-route.ts. |
| 2026-06-27 | T2 | Kept change in expanded write-set; trimmed comments to hold edge-route-chain.ts at 500 lines (hook cap). splines.ts (540) was already 532 on main — pre-existing over-cap, not introduced here. | Avoided a 5th file; lizard fn-level clean (CCN/params/nloc). |
| 2026-06-27 | T2 | Spot-check watched cases (port vs headless 15.1.0 path counts): 167/2087/b71/b135/b62 OK; **b69 137→143** (native 144) = IMPROVEMENT (the trunk gap b69's x-coord noise masked, as predicted); **2825 9→9 UNCHANGED** by the edit (headless 0 = pre-existing oracle artifact). Full survey verdicts deferred to T3. | b69 verdict move (improvement, not regression) to be confirmed real in T3. |
| 2026-06-27 | T1 | **STOP — write-set assumption broken.** Fix locus is `splines.ts` (`dotSplines_` gather loop iterates `g.nodes.values()` = real nodes only; merge node lives in `g.info.nlist`. Probe: `g.nodes real=4 virt=0 merge=0 \| nlist real=4 virt=2 merge=1`). Trunk `vMERGE->b` never gathered → c->b gets 1 bezier not 2. | README stop condition #1 + T2 boundary "STOP if fix needs a file outside the 3 routing files". `splines.ts` is the load-bearing change; `conc.ts`/`classify.ts` confirmed correct. Findings: comparisons/T1-investigation.md. Awaiting human write-set decision. |
