# Decision Journal — compass-port-routing

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| T1 | 2026-06-19 | #2168 root cause = the **deferred ictxt path** in `compassPort`. | Port probe: `node1->node2:sw` returns `head_port.p=(-33.44,-18)=(-rw,-ry)` — the bbox corner. C `compassPort` (shapes.c) for a diagonal compass with `ictxt` computes `p=compassPoint(ictxt,-maxv,-maxv)` → `bezier_clip` against the node's `insidefn` → the point ON the ellipse boundary (≈(-15.85,-15.85) for rx33.44/ry18). The port deliberately deferred this (`compass-port.ts:296` "ictxt=null always — inside_t clipping path deferred") and always uses the `else` bbox-corner fallback (`dirS`/`dirSE`/etc. return `b.ll.x`/`b.ll.y`). divergentFn: `src/common/compass-port.ts` (dirN/dirS/dirE/dirW diagonal branches); cRef: `lib/common/shapes.c:compassPort` + `compassPoint`. | — |
| T1 | 2026-06-19 | Not a node-position bug; the 2.35 uniform y-shift is a bbox artifact. | Oracle and port both have node-center gap 72 (same ranksep); the 2.35 shift is uniform across both nodes = a translation/bbox-origin difference caused by the different `:sw` routing (oracle curls to the ellipse-boundary SW port at SVG `12.24,0`; port goes to the bbox-corner-derived `0,-8.89`). First divergence is the compassPort endpoint, everything downstream follows. | — |
| T2 | 2026-06-19 | #241_0 shares #2168's root cause — same divergent function. | Port probe: all four diagonal flat compass ports return bbox corners — `2:ne` head_port.p `(27,18)=(rw,ry)`, `3:sw` `(-27,-18)`, `1:se`/`5:ne` likewise. Identical mechanism to T1 (deferred ictxt → bbox corner, not ellipse boundary). Flat-edge routing (make_flat_edge) builds its box FROM the port point, so correcting `compassPort` flows through to the flat path. divergentFn: `src/common/compass-port.ts` (same as T1); cRef: `lib/common/shapes.c:compassPort`/`compassPoint`. | — |
| T2 | 2026-06-19 | **Collapse T3+T4 into one fix** (push-forward rule). | T1 and T2 named the SAME function and SAME root cause; one faithful port of the ictxt/compassPoint path fixes both regular (#2168) and flat (#241_0) diagonal-ellipse compass ports. Verify BOTH inputs + the variants against the oracle, 0 regressions. | ⚑ collapse |
| T1 | 2026-06-19 | Fix needs a `compassPoint` port (bezier_clip vs insidefn) — bounded, in-scope (NOT AD-4 stop). | The pieces exist: the port already has bezier subdivision + node inside-fns (`edge-route-clip.ts` deCasteljau/shape_clip0; `splines-clip.ts:getInsideFn`). T3 implements `compassPoint(ictxt,y,x)` (straight bezier [0,0]→[x,y] clipped to insidefn, return curve[0]) and replaces the bbox-corner fallbacks with the ictxt branch. Box/record nodes are unaffected (bbox corner == shape boundary), so risk is contained to diagonal-ellipse ports. Likely fixes #241_0 too → collapse T3+T4. | ⚑ likely-collapse |
