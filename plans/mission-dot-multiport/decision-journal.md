# Decision Journal — dot-multiport

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| Plan | 2026-06-19 | Handle T1+T2 inline (opus orchestrator), not via sub-agents. | Sequential single-batch faithful-port work; delegation would lose C-vs-TS context fidelity. Per CLAUDE.md "handle directly under ~30 min". | — |
| T1 | 2026-06-19 | Verdict **CONFIRMED**; AD-4 does not fire. `tail_port.p.x` = +27/−27/0 (populated) at mincross time. | Drove repro through `renderSvg` with env-gated probe in `accumCross` (reverted). p.x populated → comparator fix suffices. | — |
| T1 | 2026-06-19 | **Correction to README premise:** `port.order` is NOT 0 — it is 192/64/0 (C `compassPort` angular order, shapes.c:2865/2868). | README claimed "order=0, signal lost". Reality: order is the *wrong* signal (angular), not absent. C ties by `p.x`; TS ties by `port.order`. Same fix. Recorded so a maintainer is not confused by order≠0. | ⚑ premise correction |
| T2 | 2026-06-19 | Implemented `accumCross` tiebreak via `p.x` using 3 new local helpers (`endOrder`, `endPx`, `crossDir`) in the same file. | The faithful 2-level compare (ND_order, then p.x) pushed `accumCross` over CCN 10; split per the overview push-forward rule. Helpers cite `mincross.c:593,611`. `val()`/`crossContrib` left exported (val still used by mincross-order.ts; crossContrib pre-existing, untouched to stay in scope). | hook-split (logged) |
| T2 | 2026-06-19 | **Out-of-write-set edit:** completed the `inNode` mock Port in `mincross-cross.test.ts` (added required `p:{x,y}`). | The faithful change reads `port.p.x`; the mock built an invalid Port (missing the required `Port.p`, geom.ts:87) and crashed. Fix is non-behavioral — assertions unchanged (they pin the ND_order compare, p.x=0). Alternatives rejected: a defensive `?.p?.x` in production is unfaithful (AD-1 / no null-guard on type-guaranteed field); STOP would fail the mission's own "vitest 0 failures" gate over a malformed mock. Pushed forward per "self-explanatory, obvious fix". | ⚑ write-set deviation |
| T2 | 2026-06-19 | Verdict gates: tsc 0; vitest 1951/0; corpus `ports both dense` 56.10→0.25 **MATCH**, 25/25 0 DIVERGE; goldens byte-identical; lizard clean. 2 `near` (compass/record ports) remain — pre-existing sub-pixel, no regression (allowed by overview push-forward). | All AD-3 goldens unchanged → no porting-bug churn. | — |
