# Decision Journal

Append-only. One row per non-trivial judgment call made during
execution. "Non-trivial" means a reasonable developer might have chosen
differently.

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| — | 2026-06-13 | Baseline adapted: brief assumed 1466/0 + 82 goldens, but render-styling + multicolor-paint + 3 edge follow-ups merged to main since. Real baseline is **1720/0 + 115 goldens**. Gates use these; byte-stability vs the 115. Branch off **main** (post-parity already merged), not feature/post-parity. | Mission was planned before the intervening missions landed. | — |
| Batch 2 | 2026-06-13 | Serialized T3→T4 (brief said parallel): both set a portfn on the SAME file shapes.ts (POLY_FNS vs RECORD_FNS), and T4's record_port CALLS compassPort (T3's output). Serial avoids the shapes.ts write conflict + the stub dependency. T3 dispatched as a typescript-pro agent (compassPort ~181 LOC). | single-writer-per-file + real dependency. | — |
| T5,T6 | 2026-06-13 | T5 done (closestSide + real resolvePort; verified, byte-stable). T6 ARCHITECTURE FINDING (pre-implementation): the brief assumes splines-path-begin.ts (beginPath) is the render path and "already adds the port offset" — but the ACTIVE dot router is `routeOneEdge` (edge-route.ts → edge-route-boxes.ts/edge-route-routing.ts), which does NOT read tail_port/head_port. beginPath is exported (splines.ts) but unused by routeOneEdge. Probe: `A:s->B:n` and `[headport=n,tailport=s]` render IDENTICAL to no-port baseline (M27,-71.7…); C differs (M27,-72…). So T6 must wire port.p + port.side (+ resolvePort for dyna) into the ACTIVE router (edge-route.ts/edge-route-boxes.ts — both in the brief's write-set), not extend the unused beginPath. T6 is the keystone (port-resolution stack T1–T5 is complete + tested but invisible until T6) and is geometry-critical (0.5pt). Checkpointed here for fresh focus. | The brief's render-path assumption is stale (post-port the dot router was rewritten as edge-route.ts). | Surfaced to Scott before implementing T6. |
| T6a | 2026-06-13 | T6a IMPLEMENTED + validated vs dot 15.0.0. `PortRoute` threaded routeOneEdge → straightEdgeSplineWithRank → routeWithRank → buildRankCorridor (start/end = node.coord+port.p) + clipToNodes (skip clip when port.clip=false). `portRouteOf` gates on `resolvePort(...).defined`; no-port edges → undefined → 115 goldens BYTE-IDENTICAL. Oracle check: **compass ports pass within 0.5pt** — `A:s->B:n` start exact M27,-72 (vs no-port -71.7), end Δ0.06; `rankdir=LR A:e->B:w` start exact M54,-18, end Δ0.06. `record A:f0->B` start off ~1pt in BOTH x and y → traces to record-NODE text sizing (FreeType-96dpi metric divergence, out of mission blast-radius per stop-cond), NOT port math: the point is correctly at the field bottom-center. Record golden deferred to T8 (will compare-page-exclude if still >0.5pt after T6b). tsc 0; vitest 1749/0; new test src/layout/dot/edge-route-port.test.ts (4 cases). | Compass ports — the high-value T6a half — match C; record residual is a known sizing divergence, not a port bug. | Record divergence surfaced for T8 exclusion. |
| T6a | 2026-06-13 | WRITE-SET + HOOK notes: (1) expanded write-set to include `edge-route-helpers.ts` (one pass-through `port?` param on straightEdgeSplineWithRank) — routeOneEdge routes through it; thin, behavior-preserving. (2) Hook-forced (≤30 lines/fn): extracted `applyEndArrows` from routeEdgeNonForward and `routeForwardEdge` from routeOneEdge in edge-route.ts (push-forward cond: hook-forced split, @see preserved). (3) `clipToNodes` clip flags: named `ClipFlags` interface (inline `{tail;head}` literal tripped Lizard's PARAM counter to 6). (4) Test regex: `new RegExp('...')` not `/.../ ` literal (Lizard mis-spans regex-literal length). | Lizard/CCN-hook quirks per memory [[subagent-hook-loop-deaths]]; minimal faithful refactors. | — |
| T6 | 2026-06-13 | T6 DESIGN written ([batch-3/T6-DESIGN.md](batch-3/T6-DESIGN.md)). Decision: extend the ACTIVE router (`routeOneEdge`/`buildRankCorridor`/`clipToNodes`) with port handling via an optional `PortRoute` bundle — NOT switch to the unused faithful `beginPath`/`endPath` (too big, risks 115 no-port goldens; full routesplines path is itself deferred). Reuse the already-ported `BeginRegSide`/`EndRegSide` side-mask box arithmetic for T6b rather than re-port. Confirmed `computeSpline` and `beginPath` both use `Box[]` → compatible. Phase T6a (port point + clip-skip + dyna; fixes record fields + aligned compass) then T6b (side-mask boxes; steering/contradictory ports). No-port edges gated to `PortRoute===undefined` → byte-stable. Known risk journaled: active fitter ≠ C routesplines, so hard steering ports may exceed 0.5pt — fitting-algorithm divergence (wedged-class), T8 mints reproducible cases + comparison-page-excludes the rest. | Brief's render-path assumption was stale; chose the lower-risk surgical extension over a faithful-but-unused-path swap. | Design only — implementation pending Scott's go-ahead. |
| T1,T2 | 2026-06-13 | Batch 1 done inline (small wiring): T1 builder.ts portStringOf → tailport/headport attrs (explicit wins); T2 chkPort + initEdgePorts (exported) at top of initEdgeLabels, runs every edge. portfn type is `compass: string` (not `string\|null` per AD4) and types.ts is outside T2's write-set → pass `''` for C NULL (no-colon case); compassPort (T3) must treat ''/NULL alike. tsc 0; vitest 1730/0; 115 goldens byte-identical (no-port edges → default Center port, unchanged). | AD4 type already differs from brief; '' avoids touching types.ts out of write-set. | — |
