<!-- SPDX-License-Identifier: EPL-2.0 -->
# T3 — 2239 extra polygons

## Context
2239: port emits 210 polygons vs oracle 159 (+51); firstDiff
g[2][childCount]; maxΔ 5286 (positional smear downstream of extra
elements). Source: tests/2239.dot (inspect: arrowheads? peripheries?
multi-edges?).

## Task
Attribute the +51: per-group polygon counts (which groups, which
titles), then the emitting site (arrow ops? node peripheries? dup
installs). Instrument if needed. Mechanism per diagnosis.md.

## Acceptance criteria
- Given per-group counts, then the extra-polygon class is identified
  (object type + emit site + why the port emits more than C).

## Rollback/Observability: N/A. Reversible.
