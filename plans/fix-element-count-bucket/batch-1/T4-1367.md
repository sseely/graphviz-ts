<!-- SPDX-License-Identifier: EPL-2.0 -->
# T4 — 1367 raw byte + childCount

## Context
Node name contains raw 0x80. Oracle SVG emits the RAW byte (its output
is not valid UTF-8 but still parses for compareSvg); the port decodes
input as latin1 (render-one decodeDotInput) and emits U+0080 as UTF-8
(\xc2\x80). Additionally g[7][childCount] differs (maxΔ 0). Note
1367's clip-watch entry cleared 2026-07-02 (attr-or-tag mission).

## Task
1. Pin the g[7] childCount element (which element, why absent/extra).
2. Decide the byte-fidelity question: C's SVG output charset behavior
   for non-UTF8 input (utils.c latin1ToUTF8 path — does headless C
   convert or pass through? oracle emitted raw) vs the port's decode →
   is the port's re-encode faithful to C's charset=latin1 semantics or
   a divergence class? (parity-text-charset-cluster memory: utf8/latin1
   decode DONE — this may be an ORACLE quirk on invalid bytes.)
3. Verdict: port defect / harness classification / accept.

## Acceptance criteria
- Given the analysis, then the childCount element is attributed AND the
  byte question has a verdict with C refs (charset.c/utils.c).

## Rollback/Observability: N/A. Reversible.
