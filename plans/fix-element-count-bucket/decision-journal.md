<!-- SPDX-License-Identifier: EPL-2.0 -->
# Decision journal

| date | batch/task | decision / finding |
|---|---|---|
| 2026-07-02 | setup | Branch fix/element-count-bucket from main (002e5f9). Batch 1 order (quick wins first): T2 anchors, T1 decorate, T3 2239, T4 1367, T5 1581/2825. |
| 2026-07-02 | B1/T2 | Anchors pinned, 4 sub-defects: (a) HTML CELL anchors: C's trigger is `cp->data.href||target||title` (htmltable.c:630) with anchor URL/tooltip INHERITED via obj state (initMapData overlays only non-empty; emit iff obj->url||explicit_tooltip incl. inherited); port's initHtmlAnchor bails on target-only cells (no obj-state inheritance) — 2619's 4 missing per file (dat TDs, TARGET-only, table HREF inherited). (b) CLUSTER tooltip-only anchors: 1880's 2 truly-missing = a_node0/a_node4564915 around clusters with tooltip=" " — cluster anchor context skips tooltip-only. (c) anchor-group/a id values not dash-escaped (XML-equal, byte-level; gvputs_xml class). (d) 2619 I63->F23 style=tapered: C emits a filled POLYGON (taper.c), port a stroked path. I57/I63 ±1 polygon deltas likely nesting artifacts of (a) — re-measure post-fix. |
| 2026-07-02 | B1/T1 | decorate spec: emit_edge_label receives spl when mapbool(decorate)&&ED_spl (emit.c:3013, label + xlabel only, NOT head/tail labels); emit_attachment (emit.c:1870) draws a 3-pt polyline [label bottom-right, bottom-left, dotneato_closest(spl,pos)], style=defaultlinestyle (NOT edge style), pen=label fontcolor, skipped for all-whitespace text. Port needs: attachment emit inside the edge-label render + dotneato_closest (check exists). |
| 2026-07-02 | B1/T3 | 2239 pinned, layered: (a) THE +51 polygons = style="invis" clusters (73 declared, 51 rendered) — port draws their boxes, C skips invisible clusters entirely (C ref to confirm at fix: emit.c cluster invis gate). (b) NEW: port stacks all cluster-label lines at ONE y (-206.4 ×4) where oracle steps leading — cluster multi-line label y-offsets missing (text counts equal → numeric-only). (c) x-layout shift (1449 vs 1119) — re-measure after (a)+(b); may be label-size feedback. |
| 2026-07-02 | B1/T4b | (re-append after cwd trap) T4 journal entry above applied. |
| 2026-07-02 | B1/T5a | Upstream contracts: test_1581 = fuzz input, asserts ONLY no-ASan-crash (rc!=42); test_2825 = "should not crash when rebuild_vlists returns -1", only fails on crash. Both oracle outputs are crash-recovery artifacts; per ratified policy the port's clean renders are acceptance CANDIDATES pending inputs verification: (1581) does the port implement C's intended "X was already in a rankset, deleted from cluster" semantics? (2825) do the port's concentrate-entry inputs match C's, with the port's rebuild succeeding where C's aborts (our 2183-fixed machinery)? |
| 2026-07-02 | B1/T5 GATE | 1581: port IMPLEMENTS C's intended rankset-deletion (cluster.ts:284 = cluster.c:314-323) minus the warning print; C then errors in install_in_rank (fuzz-input recovery limitation, upstream asserts no-crash only) → right-for-right-reason acceptance candidate. 2825: C's OWN rebuild_vlists fails (the machinery the port line-verified byte-identical against C in fix-2183 and then fixed beyond); upstream asserts no-crash only → acceptance candidate. T10 plan: add C-format rankset warnings (fidelity + verification: warning parity on both ids), then accept with comparison pages + registry entries. 2825's 23pt clip-watch = port-side, out of bucket scope → follow-up note. |
| 2026-07-02 | B1 CLOSE | Batch 1 complete. Batch 2 shape: T6 decorate (graphs-decorate + 1367, incl. dotneato_closest check); T7 anchors (cell obj-state inheritance, cluster tooltip-only, id dash-escape) + tapered-edge weigh-in per D3; T8 2239 (invis-cluster skip + cluster label leading); T10 1581/2825 (warnings + dispositions). |
| 2026-07-02 | B2/T6 | decorate landed (faad09d): emitAttachment port + dotneatoClosest bz.size fix (over-allocated tail scan — standing Bezier-size hazard). graphs-decorate 26/26 polylines (24 byte-eq; 2 differ downstream of pre-existing spline deltas); 1367 23/23 byte-eq. Verbatim decorate.gv test (red/green by construction — port emitted 0 pre-fix). |
| 2026-07-02 | B2/T7 | anchors landed (dda8252 + test-sync): obj-state frame stack for HTML cell anchors (target-only inherits table HREF); resolveObjAnchor graph-attr dict walk (root graph[tooltip] reaches clusters); anchor-id escapeXmlTitle. 2619_1 13/13, 1880 42/42 + id sets byte-eq. PROCESS SLIP: T7 commit went in with 3 failing tests (grep chain didn't gate exit code) — caught immediately, synced in follow-up commit. Assertions were asserting the pre-C-faithful raw-hyphen ids. |
| 2026-07-02 | B2 tapered weigh-in | C taper.c = 340 lines (self-contained stroke-widening). 2619's remaining delta = 1 edge. D3 call: DEFER — port taper.c as its own small feature mission if 2619's post-T7 verdict still matters; 2619 likely improves to near-conformant minus this one edge. Tracked in the 2619 disposition note at T11. |
| 2026-07-02 | B2/T8 | 2239 element class FIXED: (1) invis clusters drew their box+label — pen-state gap; resolvePenType now returns PenType.None for invis, box pen-gated at the sole call site where PEN_NONE can arise (gvrender.c:497/543); textspan gate pre-existed. (2) renderClusterLabel was a duplicate emit_label missing per-span baseline advance + justification — deleted, delegated to renderOneLabel. Counts 505/159/93 byte-equal; label y-steps byte-match. RESIDUAL: width 12078(C) vs 6799(port) — NOT downstream of the extra elements (survived their removal); independent x-layout divergence, expect 2239 to re-bucket numeric on survey. Out of element-count scope per T3 acceptance. |
| 2026-07-02 | B2/T9 | 1367 accepted A5 (new class: invalid input bytes / encoding representation). Element counts identical post-T6 (23/103/44/24); compareSvg shows EXACTLY one structural diff — title raw 0x80 (C, invalid-UTF-8 output per utils.c:1200-1207 naked-trail-byte pass-through, no warning) vs U+0080 UTF-8 (port latin1 fallback, mirroring C's own 'treated as Latin-1' recovery). Irreducible: JS strings can't carry raw invalid bytes through renderSvg. Artifacts: comparisons/1367.{md,html,svg×2}, registry A5 entry, known-divergences §A5. Guards pass. |
| 2026-07-02 | B2/T10 | 1581/2825 disposed A4 (crash-recovery debris). Warning port: markClusterNode now takes the enclosing graph and emits C's exact message; parity verified per id (same node+graph names; 1581 emission ORDER differs — port Act_24 before Act_23, cluster-node iteration order — noted, not chased: stderr order is not layout-bearing and the input is fuzzer junk). Upstream contracts are no-crash only. C discards content after Error: install_in_rank (1581) / rebuild_vlists lead-null (2825); port never reaches the inconsistency, keeps 39-vs-22 / 22-vs-4 children. Artifacts: comparisons/{1581,2825}.{md,html,svg×2}, registry A4 entries, §A4 prose extension. 2825 clipOverflow 23pt = tracked port follow-up, out of bucket scope. |
| 2026-07-02 | B3/T11 | Survey run 1 (of ≤2): 600/163/14 vs baseline 599/162/16. Gate PASS 0 regressions. Movers all ⊆ bucket: 1880 diverged→conformant, graphs-decorate diverged→structural-match (residual maxΔ 104.56 = pre-existing spline deltas, T6 note). 2239 re-bucketed numeric (maxΔ 5286 = x-width, firstDiff path @d — element counts equal). 2619_1/2 reduced to single tapered-edge structural diff (g[8]/path — D3 deferral). 1367/1581/2825 at accepted verdicts (registry-matched). Canary 2475_2 = 17s (<180s). Baseline + PARITY.md committed. |
