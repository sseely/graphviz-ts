<!-- SPDX-License-Identifier: EPL-2.0 -->

# 1581 — crash-recovery divergence (2026-07-02)

Input: `~/git/graphviz/tests/1581.dot` (fuzzer-generated) · corpus id `1581` ·
oracle = headless native dot (`GVBINDIR=/tmp/ghl`, estimate metrics) · port =
graphviz-ts at `fix/element-count-bucket`.

> **Visual side-by-side:** [`1581.html`](1581.html) · golden:
> [`1581-golden.svg`](1581-golden.svg) · ours: [`1581-ours.svg`](1581-ours.svg)

## Verdict

**diverged, maxΔ 465 — top-level childCount 39 (port) vs 22 (oracle)**

| | native C oracle | graphviz-ts |
|---|---|---|
| exit | 0 | 0 |
| stderr | `Warning: Act_23 was already in a rankset, deleted from cluster G`, `Warning: Act_24 …`, then `Error: install_in_rank, line 1171: G %0 rank 9 i = 0 an = 0` | same two Warnings (order Act_24/Act_23), no Error — port state stays consistent |
| output | 22 top-level children (recovery discards content) | 39 top-level children |

## Upstream status

`test_1581` (test_regression.py:1176) asserts **only** that dot does not
trigger an Address-Sanitizer memory-safety violation ("this example found by
fuzzing should not cause an out-of-bounds write",
gitlab.com/graphviz/graphviz/-/issues/1581). The oracle's rendered output is
whatever falls out of its `install_in_rank` error-recovery path; upstream
attaches no meaning to it.

## Inputs verification (right-for-the-right-reason)

The port reaches the same rankset-deletion decisions as C: both sides delete
exactly `Act_23` and `Act_24` from cluster context `G` (warning parity added
in this mission, `mark_clusters` / cluster.c:317-320). C then hits an
internal inconsistency (`install_in_rank` out-of-range order values) and its
recovery drops ranks; the port's rank state does not reach that inconsistent
configuration and lays out the surviving graph normally.

## Disposition

Accepted A4-family (oracle output is crash-recovery debris on a fuzzer
input; upstream defines correctness as "no crash", which both sides
satisfy). Re-measure if upstream ever specifies real layout semantics for
this input.
