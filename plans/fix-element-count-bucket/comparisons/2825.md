<!-- SPDX-License-Identifier: EPL-2.0 -->

# 2825 — crash-recovery divergence (2026-07-02)

Input: `~/git/graphviz/tests/2825.dot` · corpus id `2825` · oracle = headless
native dot (`GVBINDIR=/tmp/ghl`, estimate metrics) · port = graphviz-ts at
`fix/element-count-bucket`.

> **Visual side-by-side:** [`2825.html`](2825.html) · golden:
> [`2825-golden.svg`](2825-golden.svg) · ours: [`2825-ours.svg`](2825-ours.svg)

## Verdict

**diverged, maxΔ 472 — top-level childCount 22 (port) vs 4 (oracle)**

| | native C oracle | graphviz-ts |
|---|---|---|
| exit | 0 | 0 |
| stderr | `Warning: B was already in a rankset, deleted from cluster cluster_outer`, `Warning: H …`, then `Error: rebuild_vlists: lead is null for rank 1` + `concentrate=true may not work correctly.` | same two Warnings, no Error |
| output | 4 top-level children (862 bytes — recovery discards nearly the whole graph) | 22 top-level children (full surviving layout) |

## Upstream status

`test_2825` (test_regression.py:6421) asserts **only** that dot does not
crash when `rebuild_vlists` returns -1 ("only fail if we crashed, not exited
with failure", gitlab.com/graphviz/graphviz/-/issues/2825). The oracle's
rendered output is concentrate-abort debris; upstream attaches no meaning
to it.

## Inputs verification (right-for-the-right-reason)

The port reaches the same rankset-deletion decisions as C: both sides delete
exactly `B` and `H` from `cluster_outer` (warning parity added in this
mission, `mark_clusters` / cluster.c:317-320). C's `rebuild_vlists` then
finds a rank with no leader and aborts `dot_concentrate`, discarding layout
content; the port's concentrate path survives the same input and lays out
the remaining graph.

Port-side follow-up (out of this bucket's scope, tracked): the port render
reports `clipOverflow: 23` — ~23pt of content outside the canvas — worth a
look independent of the oracle comparison.

## Disposition

Accepted A4-family (oracle output is crash-recovery debris; upstream defines
correctness as "no crash", which both sides satisfy). Re-measure if upstream
fixes #2825's underlying `rebuild_vlists` failure.
