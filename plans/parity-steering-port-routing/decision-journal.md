# Decision Journal — steering-port routing

Append-only. One row per non-trivial judgment call.

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| — | 2026-06-14 | Mission authored from the parity-edge-ports T6b post-mortem while context was warm. Core reframing: the faithful `routesplines` pipeline is ALREADY ported (`splines-routespl.ts`, `splines-path-begin/end.ts`, `splines-clip.ts`, `pathplan/route.ts`+`shortest.ts`) and proven via neato/pack/ortho — dot just bypasses it. So this is an INTEGRATION mission, not a from-scratch port. Batch 1 is a recon spike (SR1) because the dot orchestrator landscape (`routeOneEdge` vs `makeRegularEdge` vs `splines-flat.ts`) and the `beginPath` input contract need verification before task-level detail in batches 2–4 can be trusted. | Avoids over-specifying tasks against an unverified integration seam; de-risks the high-blast-radius core-router change. | Batches 2–4 task files are overview-level pending SR1; SR1 finalizes them. |
| — | 2026-06-14 | Absorbed parity-edge-ports T8 (port goldens). T8 found port goldens UN-minteable with the simplified fitter: (a) unclipped port edges' control points diverge ~11pt from C `routesplines` (endpoints match <0.06pt; e.g. `A:s->B:n` controls 27,-57.94 vs C 27,-55.32) — the node clip is what makes plain edges match, and ports skip it; (b) edge `<title>` drops ports and mis-escapes the hyphen (structural diff, no tolerance covers it). Both resolve here: (a) via routing ports through `routeSplines`, (b) via the `svgBeginEdge` fix folded into SR8. Recorded in SCOPE.md "Absorbed from T8". | Goldens need the faithful fitter + the title fix; neither exists pre-mission. T6a attachment behavior already pinned by edge-route-port.test.ts. | — |
| SR1 | 2026-06-14 | Baseline captured: **1749 passed / 0 failed, 115 goldens** (branch feature/parity-edge-ports). | First execution step. | — |
| — | 2026-06-14 | Branch set up: merged feature/parity-edge-ports → main (`--no-ff`, merge commit 0b9151e; T6a/T8 + this brief + SR1 now on main; main 1749/0 tsc-clean), then cut **feature/steering-port-routing** off main. SR2 production code starts here. | Scott chose "merge edge-ports to main first". | T7 (html_port) still open on edge-ports backlog. |
| SR2 | 2026-06-14 | Baseline re-captured on `feature/steering-port-routing` before SR2 src changes: **1749 passed / 0 failed (107 files)**, **115 goldens**. SR1 PoC re-run: complete 10-pt spline (apex 116.5, bulge 56.8, reaches B). Branch confirmed = feature/steering-port-routing off main. Gates anchored to these numbers. | Startup gate per brief — do not trust written numbers. | — |
| SR2 | 2026-06-14 | **DONE.** New `src/layout/dot/edge-route-faithful.ts` + `.test.ts`. Adapter `routeRegularEdgeFaithful(g,e)` first-assembles the C `make_regular_edge` adjacent-rank sequence: maximal_bbox → beginPath → makeregularend(BOTTOM) → rank_box → endPath → makeregularend(TOP) → completeregularpath (tail boxes fwd + rank box + head boxes REVERSED + adjustregularpath) → routeSplines. Returns internal y-up control points; NO clip/install (SR3). **Result: A:n->B routes a complete 10-pt loop spline** (27,109→121 apex→45.7,116.6→54,108→65.1 bulge→…→reaches B) matching C oracle's first 4 control pts within ~0.4pt (C: 27,109.32/27,121.33/45.67,116.97/54,108.32). A:s->B:n sane straight 72→36; A:e/A:w bulge laterally. 6 tests pass. | First exercise of the begin→route→end sequence (SR1's flagged risk) — pieces compose, premise confirmed end-to-end. | Exact per-pt oracle match is SR4. |
| SR2 | 2026-06-14 | DEVIATIONS journaled (per CLAUDE.md "C is sacred"): (1) `maximal_bbox` ports the no-cluster path only — `cl_bound` not ported (no batch-2/3 graph has clusters; clustered ports are later scope). FUDGE=4 (C's local redefine), LeftBound/RightBound via existing computeLeftBound/RightBound, Splinesep=nodesep/2. (2) `rank_box` Rank_box cache omitted (perf-only; fresh compute is numerically identical). (3) completeregularpath's top_bound/bot_bound guards ported but always pass — TS topBound/botBound already filter to spline-bearing edges (same as C). (4) `adjustregularpath` ported in full though it is a no-op for the single-rank-box adjacent case (its inter-rank widening only fires for SR7 multi-rank chains). | Faithful where it fires; documented where deferred. None affect SR2 cases. | cl_bound + virtual-label maximal_bbox branch exercised only by later batches. |
| SR2 | 2026-06-14 | Byte-stability gate clarified: `test/golden/refs/*` are the **dot 15.0.0 C-binary** SVGs (header `Generated by graphviz version 15.0.0`); `suite.test.ts` compares them by **structural/numeric XML tolerance**, NOT byte-identity — so `diff` vs refs is the wrong baseline. Verified instead by (a) suite.test.ts 115/115 green, (b) no live module imports edge-route-faithful, (c) rendered HEAD baseline (stashed the new file) and diffed render-all output pre/post: **byte-identical, 115 files**. Gates: tsc exit 0; vitest 1755 passed/0 failed (baseline 1749 + 6). | AD3 byte-stability satisfied; SR2 adds no live call site. | — |
| SR3 | 2026-06-14 | Baseline re-captured before SR3 src changes (HEAD a786041): **1755 passed / 0 failed (108 files)**, **115 goldens**. SR2 probe re-run: A:n->B returns the complete 10-pt loop spline (apex 121, bulge 65.1, reaching B 27,19); A:s->B:n straight 72→36; A:e/A:w bulge laterally. Verified `routeDotEdges` runs BEFORE `gvPostprocess` (rankdir rotation) in dotPhasePost — so the faithful path runs in the internal y-up TB frame for ALL rankdirs; rankdir is a final coordinate transform. Gates anchored to these numbers. | Startup gate per brief; confirmed the LR-frame assumption (SR4 canary risk). | — |
| SR3 | 2026-06-14 | **DONE.** Wired `routeRegularEdgeFaithful` into `routeOneEdge`/`routeForwardEdge` (edge-route.ts, +32 lines). New `hasSidePort(e)` gate resolves both ports (resolvePort, dyna-safe) and returns true iff `tail_port.side \|\| head_port.side` (≠0). When true, `routeFaithfulSidePort` calls `routeRegularEdgeFaithful(g,e)` → `clipAndInstall(e, e.head, pts, pts.length, buildDotSinfo())` and returns; on `null` (not adjacent-rank regular) it returns false and the edge falls through to the unchanged `straightEdgeSplineWithRank`+portRouteOf path. New test `edge-route-faithful-wired.test.ts` (3 tests). **Results: `A:n->B` renders M27,-109 (north port, unclipped) → apex -121.01 (above the port) → lateral bulge x→65.1 → descends to B with a black arrowhead at 38.37,-36.25 (B top boundary).** A:e/A:w bulge laterally. Gates: tsc exit 0; vitest **1758 passed / 0 failed** (1755 + 3); 115 goldens **byte-identical** (render-all pre/post diff + suite 118/118). | Live seam per AD1-revised; faithful only for side ports per AD2; byte-stability per AD3 held automatically (no golden has a side port). | Exact per-pt oracle match + ±1 nudge survival is SR4. |
| SR3 | 2026-06-14 | Judgment: added a SEPARATE `hasSidePort` predicate rather than mutating `portRouteOf`. portRouteOf still feeds the simplified fallback for declared NON-side ports (center/record-field-without-side); only `.side`≠0 edges take the faithful path. Consequence (flagged): the T6a edges (A:s->B:n, rankdir=LR A:e->B:w) now flow through the faithful path — their unit tests are the real canary. Verified green: M27,-72 and M54,-18 still hold (the unclipped port end is identical between the two fitters; only interior control points differ, which those tests do not assert). | Cleaner than overloading portRouteOf; avoids changing the fallback contract. | T6a green = the canary held. |
| SR3 | 2026-06-14 | Confirmed the SVG renderer emits arrows from the `clipAndInstall` path for dot edges (not just the simplified `_arrowPts` path): rendered `A:n->B` contains `<polygon fill="black"` at B's boundary. clipAndInstall→arrowClip→stashArrow writes both the spl sflag/eflag bezier AND `_arrowPts`; the renderer picks it up exactly as it does for neato. | Brief asked to verify dot arrows survive the neato install path. | — |
| SR3 | 2026-06-14 | Push-forward (hook): the wired test tripped lizard's `-L 30` function-length cap — regex literals / embedded `"` confused its tokenizer into a 58-line span (the [[active-fitter-no-loop-corridors]] / T6a miscount). Fixed per precedent: `String.fromCharCode(34)` for the path-marker quote, `new RegExp('-?[0-9.]+','g')` instead of a `/.../g` literal, and split helpers so each `it` callback spans <30 lines. | Documented lizard limitation, not a real complexity issue. | — |
| SR4 | 2026-06-14 | Baseline re-captured before SR4 (HEAD 6471827): **1758 passed / 0 failed (109 files)**, **115 goldens**, oracle `dot -V` = graphviz 15.0.0. SR4 is validation-only — no production-code change (edge-route.ts / edge-route-faithful.ts / pathplan / splines-* frozen); gates anchored to these numbers. | Startup gate per brief. | — |
| SR4 | 2026-06-14 | **DONE.** Oracle-validated side-port geometry vs the installed dot 15.0.0 binary (`.probes/sr4-oracle.ts`: `dot -Tsvg` vs `renderSvg('dot')`, first-path control points + arrowhead tip). New pinning test `src/layout/dot/edge-route-faithful-oracle.test.ts` (9 cases, AD6 0.5pt; exact starts at 0.06pt). **All six required cases PASS ≤0.5pt:** `A:n->B` (10-pt TOP loop, Δstart/Δinterior 0.32, tip exact), `A:e->B`/`A:w->B` (lateral bulge, Δ0.25, tip Δ0.06), `A->B:s` (contradictory-head 10-pt loop, Δstart 0.32/Δint 0.33, tip exact), record `A:f0:n->B` (field+side 10-pt loop, **EXACT** all 10 + tip), plus `A:s->B` straight (**exact**). LR confirmed: `A:e->B` Δmax 0.22 (tip 0.30), `A:e->B:w` **exact** — LR shares the internal y-up frame (routeDotEdges before gvPostprocess, per SR3). **`A:s->B:n` (T8's 11pt geometry blocker) now matches C EXACTLY** (controls 27,-60.62/27,-55.32 = C) — the faithful path resolves the T8 unclipped-port divergence, unblocking SR8 compass+steering goldens. Gates: tsc exit 0; vitest **1767 passed / 0 failed** (1758 + 9); 115 goldens byte-identical (no production line changed → automatic; suite 118/118 green). | Faithful router reproduces dot 15.0.0 for every named steering/contradictory/record-side case within tolerance; the simplified fitter's loop-truncation (T6b) and 11pt unclipped-port drift (T8) are both gone. | Title parity is SR8; compound `A:n->B:s` excluded (next row). |
| SR4 | 2026-06-14 | **±1 port-box nudge finding (sub-task d):** C's ±1 nudge does **NOT** survive `clipAndInstall` to the rendered output on the faithful path — every attachment point is ≤0.32pt off the oracle, never the ~1pt a surviving nudge would show (`A:s->B:n` start = M27,-72.00 **exact**, not -71; `A:n->B` start 27,-109.32 vs TS 27,-109.00 = 0.32 renormalization, not a box shift). Same conclusion as T6a found for the simplified path: the nudge is internal to pathplan and is absorbed before output. No pathplan change needed; no STOP. | The open SR3-flagged question, resolved by oracle measurement. | — |
| SR4 | 2026-06-14 | **EXCLUDED: `digraph{A:n->B:s}`** (compound double-steering — side mask on BOTH ends). >0.5pt: endpoints + both loop entries/exits match (≤0.65pt, arrow tip exact) but the mid-corridor lateral excursion joining the two loops diverges **~24pt** (C pushes to x=87.5 / width 87pt; TS holds x=70.65 / width 79pt). Diagnosed as a **box-assembly width difference for combined tail+head side masks**, NOT a Proutespline/Pshortestpath numeric divergence (shape faithful, controls renormalize correctly at both ends) — SR7-adjacent (spanning corridor). Out of AD4 batch-2 named scope; SR4 is validation-only and the fix is a corridor-assembly change (not the 1–3 line attachment/clip push-forward SR4 allows). Comparison page: [comparisons/An-Bs-double-steering.md](comparisons/An-Bs-double-steering.md) (side-by-side SVGs + per-point table). Surfaced for batch 3; not chased per boundary. | Honest classification of a discovered >0.5pt case; required cases all pass, so not a mission STOP — flagged for SR7. | **For Scott / batch 3: compound both-ends side ports need wider mid-corridor assembly.** |
| SR4 | 2026-06-14 | **Title-parity re-confirmed (SR8 scope, not asserted in SR4 geometry test):** dot 15.0.0 edge `<title>` includes ports and escapes the hyphen — `A:n->B`→`A:n&#45;&gt;B`, `A:f0->B`→`A:f0&#45;&gt;B`, and **`A:f0:n->B`→`A:n&#45;&gt;B` (compass replaces field)** — confirming SCOPE.md ~103. TS currently emits `A-&gt;B` for all. svgBeginEdge fix stays SR8. | Brief asked to confirm the field-vs-compass title rule before relying on it; confirmed via oracle. | Folded into SR8. |
| B3 | 2026-06-14 | **Batch 3 startup baseline** (HEAD e7fcfcc, branch feature/steering-port-routing confirmed). `npx vitest run`: **1767 passed / 0 failed (110 files)**; goldens **115**; oracle `dot -V` = graphviz 15.0.0 (20260523.1842). Read README/decisions/journal/batch-3 overview/SCOPE per brief. SR5/SR6/SR7 gates anchored to these numbers (each task adds its own tests above 1767). | Mandatory startup gate per brief; numbers match written expectations. | — |
| SR7 | 2026-06-14 | **DONE.** New `routeMultiRankEdgeFaithful` (edge-route-chain.ts) ports make_regular_edge's hackflag forward path: walk the virtual chain (chainSegments via to_virt), begin at the tail (REGULAREDGE → BeginRegSide steers the port), accumulate per-segment `rank_box` + virtual-node `maximal_bbox` (the chain `while` loop, non-straight-mode), end at the head, completeRegularPath (its adjustregularpath inter-rank widening finally fires — the SR2-noted no-op now active) → routeSplines. `rankBox`/`completeRegularPath`/`RegularPathParts` newly exported from edge-route-faithful.ts (additive; regular path + SR4 test byte-unchanged — make_regular_edge's helpers shared by adjacent + multi-rank). Wired into `routeOneEdge` ahead of the simplified `routeFwdMultiRankEdge`, behind the `hasSidePort` gate. New test `edge-route-chain-oracle.test.ts` (3 cases). **Results vs dot 15.0.0: `A:n->C` (brief demo, 2-rank) Δ0.32, `A:e->C` Δ0.26, `A:n->D` (3-rank) Δ0.32** — pinned. Gates: tsc 0; lizard 0; vitest **1775/0** (1772+3; SR4 oracle 9 + T6a port 4 green); 115 goldens **byte-identical** (render-all pre/post; no golden has a multi-rank side port). | Faithful chain path; gated per AD2; byte-stable per AD3; adjustregularpath now exercised. | Left-bulge + straight-mode excluded (next rows). |
| SR7 | 2026-06-14 | **Bug fixed: Splinesep = nodesep/4** (C `sd.Splinesep`, dotsplines.c:267), was nodesep/2 (SR2 deviation). It governs the gap to VIRTUAL neighbors in maximal_bbox — exercised only by the multi-rank chain's virtual nodes (the adjacent/flat paths see NORMAL neighbors → nodesep/2, so SR4/SR5 were unaffected; verified: SR4 oracle still 9/9 green). Applied in the chain's `chainBboxCtx` only. | More faithful to C; matters for parallel virtual chains though moot for single-column test graphs. | — |
| SR7 | 2026-06-14 | **EXCLUDED (comparison page [multirank-left-bulge.md](comparisons/multirank-left-bulge.md)):** (a) `A:w->C` (left-lateral) and (b) deep ≥4-rank chains (`A:n->E`) whose loop bulges left past x≈0 — TS clamps at the left boundary and the loop translates right (~21pt / ~11pt); Y and shape faithful. Corridor left-extent for the chain's virtual nodes (computeLeftBound/maximal_bbox left clamp), NOT a Proutespline/Pshortestpath numeric divergence — surfaced not chased. Also: the straight-mode run optimization (straight_len) is unported (did not fire for ≤3-rank cases). | Honest classification; named demo + RIGHT/TOP up-to-3-rank cases pass and are pinned. Corridor-extent change is beyond the chain-feed scope. | **For Scott / follow-up: chain left-extent for leftward/deep loops; straight-mode port.** |
| SR7 | 2026-06-14 | **SR4 compound `A:n->B:s` re-tested (per brief):** still ~17pt mid-corridor (TS x=70.7 vs dot 87.5), **unchanged** — it is an ADJACENT-rank edge (single rank box), not a multi-rank chain, so SR7's path does not apply. Remains excluded; the existing [An-Bs-double-steering.md](comparisons/An-Bs-double-steering.md) page stands (append-only; its combined tail+head side-mask corridor width is a separate adjacent-corridor question). The multirank-left-bulge page also records this re-test. | Brief asked to re-test and note; confirmed not resolved by multi-rank work. | Compound adjacent both-ends corridor still open. |
| SR7 | 2026-06-14 | **Judgment — write-set & lizard:** placed the multi-rank code in `edge-route-chain.ts` (the brief's SR7 seam) rather than edge-route-faithful.ts, exporting `rankBox`/`completeRegularPath`/`RegularPathParts` (additive). Reason: lizard's TS parser folds a trailing function's doc-comment into the preceding function's length; `routeRegularEdgeFaithful` (and `routeFwdMultiRankEdge`) sit at the 30-line cap, so appending after them tripped `-L 30` (length 42/33) — the [[active-fitter-no-loop-corridors]] miscount class. Putting the block ABOVE the existing chain functions and splitting `routeMultiRankEdgeFaithful` into `buildChainPath` + wrapper gives fold headroom; both protected functions stay byte-unchanged. | Honors the brief's seam, keeps protected functions untouched, works around the tooling cap. | edge-route-faithful.ts touched (additive exports only). |
| SR5 | 2026-06-14 | **DONE.** New `routeFlatEdgeFaithful` (splines-flat.ts) ports C `make_flat_edge`'s box-channel branch (top-routing + `make_flat_bottom_edges`): per-caller `sd` → `makeFlatEnd`/`makeBottomFlatEnd` (= freshEndp(maximalBbox) → set sidemask TOP/BOTTOM → beginPath/endPath(**FLATEDGE**, feeding the already-ported BeginFlatSide/EndFlatSide) → appendRegularEnd) → 3 connecting boxes (top or bottom) → assemble (tend fwd + 3 + hend rev, **no** adjustregularpath) → routeSplines. Wired into `routeForwardEdge` behind the existing `hasSidePort` gate: same-rank side-port edge → flat, else regular (edge-route.ts, +1 import +2 lines). New test `splines-flat-oracle.test.ts` (2 cases). **Results vs dot 15.0.0: `A:n->B:n` EXACT (Δ0.000, 4-pt loop over the middle node), `A:e->B:w` Δ0.25pt (16-pt lateral loop)** — pinned. Gates: tsc 0; vitest **1772/0** (1770+2); 115 goldens **byte-identical** (render-all pre/post diff — no golden has a flat edge); lizard clean. | Faithful box-branch flat routing; gated on side mask per AD2; byte-stable per AD3. | Bottom-tail + adjacent excluded (next rows). |
| SR5 | 2026-06-14 | **Adjacency finding (verify-before-assert):** C `make_flat_edge` routes ADJACENT same-rank endpoints (no NORMAL/labeled node between them in rank order, `flat.c:checkFlatAdjacent`) via `make_flat_adj_edges` (recursive rotated-aux pipeline), and NON-ADJACENT via the box branch. The brief's demo `digraph{rank=same; A->B [tailport=s headport=s]}` is **adjacent** → make_flat_adj_edges, NOT the box branch. `{rank=same;A;C;B}` does NOT place C between A,B (mincross puts C right of B). Test cases therefore force non-adjacency with an invisible same-rank ordering chain `A->C->B` (C provably between A,B). TS `makeFlatAdjEdges` spline copy-back is deferred (since SR2) → adjacent flat side-port edges return null and fall back to the simplified fitter. | The box branch the brief targets is the non-adjacent family; adjacency had to be resolved empirically before choosing test inputs. | make_flat_adj_edges completion = follow-up. |
| SR5 | 2026-06-14 | **EXCLUDED (comparison page [flat-bottom-port-offset.md](comparisons/flat-bottom-port-offset.md)):** (a) bottom-tail (`:s`) cases — `A:s->B:s` and mixed `A:s->B:n` match the loop SHAPE and X exactly but carry a **constant ~5–7pt vertical offset** (e.g. A:s->B:s every point Δ7.06 in y); traces to the frozen `BeginFlatSide`/`EndFlatSide` BOTTOM end-box geometry (AD5), not Proutespline/Pshortestpath numerics — surfaced not chased. (b) adjacent flat edges — need the deferred `make_flat_adj_edges` recursive pipeline; fall back to simplified. Both byte-irrelevant to the 115 goldens (none use flat edges). | Honest classification; the aligned top/lateral cases pass and are pinned. Constant offset into frozen side-box code → AD5 boundary. | **For Scott / follow-up: flat BOTTOM end-box offset; make_flat_adj_edges copy-back.** |
| SR5 | 2026-06-14 | **Judgment — write-set:** added purely-additive `export` to shared `maximal_bbox`/`makeregularend` helpers (`maximalBbox`, `appendRegularEnd`, `freshEndp`, types `BboxCtx`/`SplineBounds`) in edge-route-faithful.ts so splines-flat.ts can reuse them — these are single C functions in dotsplines.c shared by make_regular_edge AND make_flat_edge; duplicating them would split a C function (violates "C is sacred"). `routeRegularEdgeFaithful` and the SR4 test are byte-unchanged (the brief boundary protects "the regular path or its SR4 test", which additive exports do not touch); SplineBounds (`sd`) is rebuilt per-caller, matching C's per-orchestrator `sd`. Also: lizard folded the new `interface FlatEndParts` into the preceding `makeFlatEdge` (TS-parser miscount, length 40); moved the interface to just-after-imports to fix (the [[active-fitter-no-loop-corridors]]/T6a lizard-miscount class). | Sharing the C helper is faithful; additive export is the lowest-risk way (SR4 canary stayed green). | edge-route-faithful.ts touched (additive only) — expanded write-set, journaled. |
| SR6 | 2026-06-14 | **DONE — validation+pin, NO production change** (the brief's flagged trap, resolved). Verify-first confirmed: (1) the overview's write-set `splines-selfedge.ts` is in `src/common/` (the FROZEN black box), NOT `src/layout/dot/`; the dot-side seam is `self-loop.ts` (`routeSelfEdgeGroup`→`makeSelfEdge`). (2) Self-edges are dispatched by `dotSplines_`→`dispatchEdgeGroup` (tail===head → `routeSelfEdgeGroup`), and **skipped** by `routeDotEdges` (`e.tail===e.head` continue) — they NEVER pass through `routeOneEdge`/`hasSidePort`. So self-edges already route through the faithful `makeSelfEdge` common path; a side port reaches it via `e.info.tail_port`/`head_port`. (3) Oracle vs dot 15.0.0 (`.probes/sr6-self.ts`): plain `A->A` + parallel `A->A;A->A` **byte-identical** (tips exact); `A:n->A:s`, `A:n->A` side-port loops match within **0.32pt** (same clipAndInstall renormalization as regular edges). New pin test `src/layout/dot/self-loop-oracle.test.ts` (3 cases, 0.5pt; plain exact 0.06pt). **`self-loop.ts` unchanged** — SR6 collapses to validation+pin exactly as the brief anticipated. Gates: tsc exit 0; vitest **1770 passed / 0 failed** (1767+3); suite 118/118 (115 goldens byte-identical, automatic — no production line changed). | Brief said verify before writing; verified self-edges already faithful → manufacturing a port would be wrong. self-loop.ts confirmed correct as-is. | e/w lateral loop excluded (next row). |
| SR6 | 2026-06-14 | **EXCLUDED: `digraph{A:e->A:w}`** (lateral self-loop). >0.5pt: the lateral double-bulge **shape** is faithful (both go over the top, down the left past x=0, vertical extent to y=-144 exact) but node A's whole region is translated **~5.5pt** in x (self-loop start C 65.77 vs TS 71.01; AND the plain `A->B` edge incident to A shifts the same ~5.5pt: start x C 38.52 vs TS 44.01). Diagnosed as a **node-width-reservation** difference: the lateral loop's reserved width comes from `selfRightSpace` (`src/common/splines-selfedge.ts`) via `selfWidth` in `make_LR_constraints` (`position-aux.ts:194,216`). `selfRightSpace` is the **AD5 frozen black box** — per the mission boundary a >0.5pt divergence tracing into frozen common code is surfaced, not chased. Comparison page: [comparisons/self-ew-double-lateral.md](comparisons/self-ew-double-lateral.md) (side-by-side SVGs + per-point table). | Honest classification; required side-port self-edge cases (n/s) all pass, so not a STOP. Frozen-code divergence per AD5. | **For Scott / follow-up: lateral (e/w) self-loop width reservation parity in frozen `selfRightSpace`.** |
| SR1 | 2026-06-14 | Recon spike DONE — **GO** ([SR1-findings.md](batch-1/SR1-findings.md)). (1) Dispatch: live router is `dotSplines_→routeDotEdges→routeOneEdge` only. (2) **AD1 REVISED**: `makeRegularEdge` has ZERO callers (dead code) → integrate at `routeOneEdge`, not makeRegularEdge. (3) **Key risk found**: `beginPath`/`endPath`/`routeSplines` have ZERO callers anywhere — the begin→route→end→clip sequence is ported piecemeal but NEVER assembled/exercised (neato uses clipAndInstall+Proutespline directly, not the box channel); `routeSplines` has ~1 test. SR2 first-assembles it. (4) **PoC (.probes/sr1-poc.ts)**: hand-built the A:n->B loop corridor (T6b BeginRegSide.topRight boxes) and ran `routeSplines` → complete 10-pt spline (apex 116.5, bulge 56.8, reaches B) where `computeSpline` TRUNCATED. Premise confirmed. Exact C match awaits real beginPath boxes + ranksep/endpoints (SR2). (5) Golden-risk/full-switch quantification needs the full pipeline (clip) → deferred to SR2/SR9. | Recon resolved the seam + surfaced the unassembled-orchestration risk before committing batch-2 code. | AD1 superseded in decisions.md; batch-2 retargeted to routeOneEdge + a new edge-route-faithful.ts. |
