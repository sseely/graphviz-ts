# T10 — 1949 LR cluster-label order-axis
model: opus · branch: one per task, squash-merge after BATCH gate

## Context
Faithful port; C at ~/git/graphviz is the spec. Local validation before the
batch gate: target ids re-rendered + compareSvg 0-regression on the family's
control set, unit suite, typecheck. Diagnosis discipline applies to any
observed discrepancy (state mechanism before fixing).

## Task
1949 residual: a uniform y-shift class (~95pt worst path@d now) previously
localized to clusterAuto's label order-axis (LR) placement sitting low →
cluster top short (.agent-notes/1949-diagnosis.md, final Observation). D1/D2
(entity-decode, pen-color) are FIXED — do not re-chase (stop condition 7).
The prior session STOPPED because the cause was out of splines-flat scope; the
locus is the ORDER-AXIS (y under LR) cluster-label space reservation. Compare
C's dot position/cluster label space path (dotgen position.c / cluster.c
label handling under GD_flip) against the port's; state mechanism then fix.
Write-set (provisional): the order-axis label-space site + test; ASK to expand.
## Acceptance
- Given 1949.dot, when rendered, then canvas height matches oracle and the
  uniform y-shift is gone (target conformant)
- Given labelclust-* + fsm controls, when re-rendered, then 0 new diffs

## Quality bar
Target id(s) improved (ideally conformant); named controls 0-diff; vitest +
tsc clean; focused regression test added; one commit (conventional, ≤72-char
subject); write-set respected — need more files → ASK (decisions.md).
