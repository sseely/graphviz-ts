<!-- SPDX-License-Identifier: EPL-2.0 -->

# T1.1 — TDD goldens (red)

## Context
Pin the target behavior before changing the router. Goldens use the headless
oracle (ADR-4). The golden suite asserts a manifest entry count that must be
bumped when adding entries (see the concentrate mission: `suite.test.ts` has a
`manifest has N entries` test).

## Task
1. Add inputs to `test/golden/inputs/`: `graphs/ldbxtried.gv` (copy from
   `~/git/graphviz/tests/graphs/ldbxtried.gv`) as `parallel-cluster-ldbxtried.gv`,
   and the T0.1 minimal repro as `parallel-cluster-min.gv`.
2. Generate refs with the headless oracle (ADR-4):
   `GVBINDIR=/tmp/ghl ~/git/graphviz/build/cmd/dot/dot -Tsvg <input> > test/golden/refs/<id>.svg`
   (run `sh test/corpus/gen-headless-gvbindir.sh` first if `/tmp/ghl` is stale).
3. Add `manifest.json` entries (`id`, `engine:"dot"`,
   `toleranceClass:"deterministic"`, `input`, `reference`, `description`) following
   an existing entry as template.
4. Bump the `manifest has N entries` count in `test/golden/suite.test.ts` (+ the
   comment block) by the number of entries added.
5. Run `npm test` and confirm the two new goldens **FAIL** (red) — the under-
   segmented splines diverge from the headless refs. Record the failing `@d` diff.

## Write-set
- `test/golden/inputs/parallel-cluster-ldbxtried.gv`, `parallel-cluster-min.gv` (create)
- `test/golden/refs/parallel-cluster-ldbxtried.svg`, `parallel-cluster-min.svg` (create)
- `test/golden/manifest.json` (add entries)
- `test/golden/suite.test.ts` (count assertion + comment)

## Read-set
- `test/golden/manifest.json` (entry template), `test/golden/suite.test.ts:100-105`
- `comparisons/c-oracle-dump.md` (the minimal repro source from T0.1)

## Acceptance criteria
- Given the new manifest entries, when `npm test` runs, then the count test passes
  and the two new goldens FAIL with a spline `@d`/point-count mismatch.
- Given the refs, then they were generated by the **headless** oracle (`/tmp/ghl`),
  not pango.

## Observability / Rollback
N/A. Reversible — delete the entries/refs.

## Quality bar
Refs are headless-oracle output; goldens are red before T1.2. Return only the
structured result.

## Commit
`test(T1.1): add red goldens for parallel cluster-crossing edge routing`
