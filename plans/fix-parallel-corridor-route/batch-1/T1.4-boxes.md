<!-- SPDX-License-Identifier: EPL-2.0 -->

# T1.4 — Per-edge box/corridor construction (CONDITIONAL on T0.3)

## Context
If T0.3 finds the divergence is in the **box list** the corridor is built from
(not just the endpoint ports), the per-edge box construction in
`edge-route-boxes.ts` / `edge-route-routing.ts` must be made faithful to C's
`make_regular_edge` box loop. Otherwise mark `[x] N/A`.

## Task (only if T0.3 implicates the boxes)
1. Align the per-edge box list (count, widths, MINW additions per rank) with C's
   `make_regular_edge` (`dotsplines.c`), per the dump in `c-oracle-dump.md`.
   Note the known pattern: side-port spline bounds add MINW once PER RANK
   (see [[faithful-corridor-minw-per-rank]]).
2. Ensure boxes are built around the **offset** endpoints (consistent with T1.2),
   not the un-offset centers.
3. Keep the single-edge box path conformant (cnt==1).

## Write-set
- `src/layout/dot/edge-route-boxes.ts`
- `src/layout/dot/edge-route-routing.ts`

## Read-set
- `comparisons/root-cause.md`, `comparisons/c-oracle-dump.md` (C box list)
- `src/layout/dot/edge-route-boxes.ts`, `edge-route-routing.ts`
- `~/git/graphviz/lib/dotgen/dotsplines.c:make_regular_edge` (box loop)

## Acceptance criteria
- Given T0.3 names the boxes, when fixed, then the port box list matches the C
  dump (count + bounds) for `ldbxtried`'s `n0->n2` and the repro.
- Given the single-edge path, then box construction is conformant to before.
- Given the survey, then 0 regressions.
- If T0.3 does NOT implicate the boxes, then `[x] N/A` (journal it).

## Observability / Rollback
N/A. **Reversible** — revert the commit.

## Quality bar
typecheck/test/survey green; 0 regressions. Return only the structured result.

## Commit
`fix(T1.4): faithful per-edge box list for corridor routing`
(skip if N/A)
