<!-- SPDX-License-Identifier: EPL-2.0 -->

# T1.3 — Cross-rank straight-edge path (CONDITIONAL on T0.3)

## Context
`makeStraightEdges` (`straight-edges.ts`) routes some groups with straight `dumb`
points + perpendicular `spreadControlPoints` — a straight line, not the corridor
route (`splines.ts:414` even notes pathplan obstacle routing is "deferred" here).
If T0.3 identifies this path as a sub-bug for `ldbxtried`/the repro, fix it;
otherwise mark this task `[x] N/A`.

## Task (only if T0.3 implicates makeStraightEdges)
1. For cross-rank groups, route through the corridor/regular-edge router (as T1.2)
   instead of straight `dumb=[tail,tail,head,head]` points, then apply the
   perpendicular offset.
2. Preserve straight routing where C does route straight (adjacent stacked
   endpoints / no obstacles) — gate on the survey to find where it currently
   byte-matches.
3. Update the `dispatchEdgeGroup` branch in `splines.ts` if the dispatch (curved
   vs parallel vs straight) needs to change.

## Write-set
- `src/layout/dot/straight-edges.ts` (`makeStraightEdges`)
- `src/layout/dot/splines.ts` (`dispatchEdgeGroup` branch, if needed)

## Read-set
- `comparisons/root-cause.md` (whether straight-path is implicated)
- `src/layout/dot/straight-edges.ts:makeStraightEdges`,
  `src/layout/dot/splines.ts:357-420`
- `~/git/graphviz/lib/dotgen/dotsplines.c:make_regular_edge`

## Acceptance criteria
- Given T0.3 names the straight path, when fixed, then the implicated edges route
  through the corridor and their goldens pass.
- Given a graph that currently byte-matches through `makeStraightEdges`, then it
  still byte-matches (survey 0-regression).
- If T0.3 does NOT implicate this path, then this task is `[x] N/A` (journal it).

## Observability / Rollback
N/A. **Reversible** — revert the commit.

## Quality bar
typecheck/test/survey green; 0 regressions. Return only the structured result.

## Commit
`fix(T1.3): route cross-rank straight-edge groups through the corridor`
(skip if N/A)
