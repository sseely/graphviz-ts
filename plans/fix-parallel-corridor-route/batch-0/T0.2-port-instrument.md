<!-- SPDX-License-Identifier: EPL-2.0 -->

# T0.2 — Instrument the port dispatch; side-by-side divergence

## Context
The port routes a parallel cross-rank group via
`dispatchEdgeGroup` → `routeParallelEdgeGroup` (splines-route.ts) →
`baseSplineForGroup` → `routeRegularEdgeFaithful` (edge-route-faithful.ts), then
`shiftInteriorPts`/`installShiftedEdge` for the x-shifted copies. We need the
port's actual base spline, shift offsets, boxes, and per-edge installed points
for the SAME inputs T0.1 dumped, to localize the divergence.

## Task
1. Add temporary stderr dumps (behind an env flag, e.g. `DBG_ROUTE`) in
   `routeParallelEdgeGroup`, `baseSplineForGroup`/`routeRegularEdgeFaithful`, and
   the box builder (`edge-route-boxes.ts`/`edge-route-routing.ts`): print the base
   polyline/boxes, the `multisep` and per-edge shift, and each installed bezier
   pt-count for `ldbxtried` `n0->n2`(×3) + the T0.1 minimal repro.
2. Render both via the port; collect the dumps.
3. Produce `comparisons/port-vs-c-divergence.md`: a side-by-side table (C vs port)
   of boxes, endpoint ports, polyline, pt-count per edge, with the first point of
   divergence highlighted. Classify the sub-bug(s): (a) base routed from un-offset
   centers → corner-clip/under-segment; (b) boxes/corridor differ; (c) endpoint
   ports not offset before routing; (d) `makeStraightEdges` straight path taken.
4. **Remove the instrumentation** before finishing (the dumps live only in the
   doc). Confirm `git diff src/` is empty after this task.

## Write-set
- `plans/fix-parallel-corridor-route/comparisons/port-vs-c-divergence.md` (create)
- (temporary, reverted) instrumentation in `src/layout/dot/splines-route.ts`,
  `edge-route-faithful.ts`, `edge-route-boxes.ts` — MUST be reverted; no net
  `src/` change.

## Read-set
- `comparisons/c-oracle-dump.md` (T0.1 output)
- `src/layout/dot/splines.ts:dispatchEdgeGroup`,
  `splines-route.ts:routeParallelEdgeGroup`,
  `edge-route-faithful.ts:baseSplineForGroup`/`routeRegularEdgeFaithful`
- `src/layout/dot/edge-route-boxes.ts`, `edge-route-routing.ts`

## Interface contract (consumed by T0.3)
`port-vs-c-divergence.md` must state, per `n0->n2` edge: `{ cBoxes, portBoxes,
cPorts, portPorts, cPolyline, portPolyline, cPtCount, portPtCount,
firstDivergence }` and a `subBug` classification ∈ {centers-not-offset,
boxes-differ, ports-not-offset, straight-path}.

## Acceptance criteria
- Given both dumps, then the doc localizes the first divergence (box, port, or
  polyline) for each `n0->n2` edge and names the sub-bug(s).
- Given the task is complete, then `git diff --stat src/` is empty (instrumentation
  reverted).

## Observability / Rollback
N/A. Instrumentation reverted; nothing persisted in `src/`.

## Quality bar
Real port dumps vs T0.1's real C dumps. Return only the divergence doc.

## Commit
`docs(T0.2): port-vs-C parallel-route divergence for ldbxtried`
