<!-- SPDX-License-Identifier: EPL-2.0 -->

# T0.3 — Root cause + GO/STOP gate

## Context
Batch 1 (the router fix) is blocked until the root cause is pinned and the fix is
confirmed contained to the dispatch/route/box/straight-edges files (ADR-5). This
task converts T0.1/T0.2 evidence into a decision.

## Task
1. From `c-oracle-dump.md` + `port-vs-c-divergence.md`, write
   `comparisons/root-cause.md`: the precise mechanism (e.g. "base routed from
   un-offset node centers, so the shifted copies clip the cluster corner and the
   shortestPath bends/under-segments; C routes each edge from its own +offset
   ports"), the affected sub-cases, and the exact functions to change.
2. Map the fix to files and confirm **none require `src/pathplan/` changes**. If
   pathplan must change → record **STOP** and request re-plan (ADR-5).
3. Refine the Batch-1 task split (which of T1.2/T1.3/T1.4 are needed, and their
   write-sets) based on the sub-bug classification.
4. Append a `decision-journal.md` row: GO or STOP, with the one-line mechanism and
   the confirmed Batch-1 write-set.

## Write-set
- `plans/fix-parallel-corridor-route/comparisons/root-cause.md` (create)
- `plans/fix-parallel-corridor-route/decision-journal.md` (append GO/STOP row)

## Read-set
- `comparisons/c-oracle-dump.md`, `comparisons/port-vs-c-divergence.md`
- `decisions.md#adr-3`, `#adr-5`
- `batch-1/overview.md` (to refine the task split)

## Acceptance criteria
- Given the evidence, then `root-cause.md` states the mechanism, the sub-cases,
  and the exact functions/files to edit.
- Given the file map, then it explicitly confirms GO (fix contained to
  dispatch/route/box/straight-edges) or STOP (needs pathplan).
- Given a GO, then `decision-journal.md` records the confirmed Batch-1 write-set;
  Batch 1 inherits it.

## Observability / Rollback
N/A — analysis only.

## Quality bar
Decision is evidence-backed (cites the dumps), not assumed. Return only the
root-cause doc + journal row.

## Commit
`docs(T0.3): root-cause + GO/STOP for parallel corridor routing`
