<!-- SPDX-License-Identifier: EPL-2.0 -->

# T2.1 — Survey verification + perf

## Context
The fix touches the shared router, so the survey will churn. Confirm every change
is an improvement or a documented-faithful flip, with **0 regressions**, and that
corridor routing did not blow up perf.

## Task
1. Regenerate the rules survey with an ISOLATED fresh cache:
   `GVBINDIR=/tmp/ghl ORACLE_CACHE=$TMPDIR/oracle-ghl PARITY_OUT=parity-rules.json
   tsx test/corpus/survey.ts` then `npm run survey:gate`.
2. Diff per-id vs the pre-fix `parity-rules.json` (from `git show HEAD:...`).
   Confirm `ldbxtried` flips diverged → structural/byte and **no** input regresses
   match→diverged. For any flagged regression, re-verify vs FRESH C (render port vs
   `GVBINDIR=/tmp/ghl dot`) before treating it as real ([[make-edge-pairs-trunc-fix]]
   GOTCHA).
3. Perf: run `node test/corpus/bench.mjs` (or the `PERF.md` procedure); confirm no
   previously-passing input exceeds 2× its native time.
4. Write `comparisons/T2-survey-verification.md`: verdict deltas (improved /
   regressed / big-maxΔ-drops), perf summary, and the 0-regression proof.

## Write-set
- `plans/fix-parallel-corridor-route/comparisons/T2-survey-verification.md` (create)

## Read-set
- `test/corpus/survey.ts`, `rules-gate.ts`, `dashboard.ts`, `bench.mjs`
- README impact prediction; `comparisons/root-cause.md`

## Acceptance criteria
- Given the regenerated survey, then `ldbxtried` is no longer in the
  `path-structure` diverged bucket and **no** new id appears in diverged.
- Given any flagged regression, when re-checked vs fresh C, then it is confirmed
  real before any rollback decision.
- Given the perf bench, then no previously-passing input is > 2× native.

## Observability / Rollback
N/A. Reversible — artifacts regenerate from source.

## Quality bar
Survey completes; 0-regression holds; perf within budget. Return only the verdict
delta + perf summary.

## Commit
`test(T2.1): verify parallel corridor routing — survey + perf`
