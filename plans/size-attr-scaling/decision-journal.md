<!-- SPDX-License-Identifier: EPL-2.0 -->

# Decision Journal

Appended during execution. Every non-trivial judgment call gets a row.

| Date | Task | Decision | Rationale |
|------|------|----------|-----------|
| 2026-06-22 | plan | Selected `size=`/`ratio` drawing scaling as the mission (not just the empty-span fix) | Probing the `rankdir_dot*` cluster showed the dominant byte-match blocker is unported `size=` scaling (oracle `scale(0.123195)` vs port `scale(1)`); reaches ~137 `size=` corpus inputs vs 6 rows. User confirmed pivot + byte-match bar. |
| 2026-06-22 | plan | Excluded `ratio=compress\|expand\|auto\|value` layout reshaping (D3) | That code exists but inert; activating it moves nodes for 24 graphs (untested), a separate riskier mission. `init_job_viewport` needs only the `filled` boolean. |
| 2026-06-22 | setup | Committed `.gitignore` + `.claude/settings.autonomous.json` as an isolated `chore` (5a2e838) | Pre-existing working-tree edits enabling the autonomous run (ignore `plans/`, grant run perms). Isolated so task diffs vs main stay clean for the gate. |
| 2026-06-22 | T1 | Replicated the full C pen clause `(!job->obj \|\| job->obj->pen != PEN_NONE)`, not just the empty-string check | `job.obj.pen` (PenType, `None=0`) is already modeled, so the faithful port is free — no need to note an omission as the task allowed. |
| 2026-06-22 | T1 | Extracted `spanIsVisible(span, job)` helper | The added 2-clause guard pushed `renderOneLabel` to CCN 11 (cap 10); the complexity hook blocked. Extracting the predicate restores the cap and reads cleanly. |
| 2026-06-22 | T1 | Edited `test/golden/suite.test.ts` (not in declared write-set) to bump the hardcoded manifest-count assertion 157→158 | Necessary 1-line consequence of the new manifest entry (which IS in the write-set); self-explanatory. No other test references the count. |
| 2026-06-22 | T1 | Gate green: tsc clean, 2305 vitest pass (+1 golden), 0 empty `<text>` for the new input | Batch 1 complete. Stale LSP diagnostics flagging `emitRoundedBezier` as missing are false — tsc exit 0, export present at poly-shapes.ts:141. |
| 2026-06-22 | T2 | Created NEW module `src/gvc/viewport.ts` (parse + zoom) — outside the declared write-set | The file-length hook (500-line cap) blocked adding ~60 lines of helpers to device.ts (would be 527). Per CLAUDE.md "unless tooling forces a structural change", split the cohesive size/zoom logic into its own module. device.ts render() imports it. |
| 2026-06-22 | T2 | **Corrected D4's mechanism**: carry Z in `job.scale` (left `job.zoom=1`), do NOT set `job.zoom` | D4 assumed `transformPoint` short-circuits on `GVRENDER_DOES_TRANSFORM` for SVG. That flag is NEVER set in this port, so `transformPoint` (the shared raster ptf path — boundary: "never alter") applies `job.zoom*devscale` to every coord. Setting `job.zoom=Z` double-scaled SVG coords (verified: cx 27→5.86). C's `svg_begin_page` prints `job->scale.x/y` (=Z for dpi=72), NOT `job->zoom`, so carrying Z in the unused `job.scale` field is both C-faithful for the SVG group AND leaves the ptf path untouched. Verified body byte-match on a size= canary. |
| 2026-06-22 | T2 | **Corrected D3**: `init_job_viewport` filled = size-`!` only; do NOT OR-in `ratio=fill` | C: `GD_drawing->filled` is set solely by `getdoubles2ptf("size")` i.e. the trailing `!` (input.c:694); `ratio=fill` sets `ratio_kind=R_FILL` (setRatio) which reshapes the LAYOUT (out of scope, D3), not the viewport. Following D3's `filled \|\|= ratio==fill` made the port upscale ~31x where the oracle reshapes to ~1x (verified vs oracle). CLAUDE.md: C source is the spec. ratio=fill graphs now diverge on positions = the deferred ratio-aspect-layout mission. |
| 2026-06-22 | T2 | rankdir_dot canary lands at STRUCTURAL match, not byte | Scale (0.123195) + width (432) byte-match the oracle, but height (port 126 vs oracle 125) and translate.y (1017 vs 1009) differ: the port's native graph HEIGHT is 1021pt vs C's 1014pt — a ~7.5pt pre-existing layout/text-metric divergence (fontsize=36) independent of scaling. T3 accepts structural-match with a logged residual; this is a separate deferred mission. |
| 2026-06-22 | T2 | Gate green: tsc clean, 2306 vitest pass (+1 golden); filled/upscale cases match oracle (1, 31.0345); body byte-match on canary | Batch 2 complete. Verified D5 no-op (algebra: round(sz·1)=round(content)+8; scale(1 1) literal; all 165 goldens pass vs pre-mission refs). |
| 2026-06-22 | T3 | Survey regenerated: byte-match **278→280** (+2), structural 236→237, diverged 254→251, errored 5/timeout 7 unchanged | Regression floor (D5) met: byte ≥278, **0 rows regressed** (per-id rank diff: 5 improved [3 diverged→structural, 2 structural→byte], 0 worse, 0 ids added/removed). The 2 new byte-matches: graphs-b79, share-grammar (both `size=`). |
| 2026-06-22 | T3 | **rankdir_dot* canary: still `diverged`, NOT byte/structural** — maxDelta collapsed 3075→43–68 | The 6 rankdir rows do not meet the brief's optimistic "→byte-match" canary. Root cause is a pre-existing **layout** divergence, not scaling: scale (0.123195) + width (432) now byte-match the oracle, but the port's drawing is ~7.5pt taller (native 1021 vs C 1014) — a uniform +7.5pt y-offset (x-coords exact) from the 36pt 4-blank-line graph-label height computed in the layout pass. Out of scope (D3/D4: coordinates/layout). Comparison page: [comparisons/rankdir-dot-residual.md](./comparisons/rankdir-dot-residual.md). Deferred to a layout/text-metric mission. |
| 2026-06-22 | T3 | `ratio=fill` residuals recorded (deferred per D3) | ratio=fill graphs diverge on node POSITIONS (R_FILL layout reshaping, un-ported): graphs-jsort (maxDelta 737, `svg/g[1]/g[13]/path[1]/@d`), graphs-pgram (738, `g[32]/path/@d`), graphs-trapeziumlr (694), 1855 (1240), windows/share-pgram (1116). graphs-b22 byte-matches (ratio=fill is a no-op there). 0 regressions. The deferred ratio-aspect-layout mission (D3). |
| 2026-06-22 | T3 | Final full-branch gate green: tsc clean, 2306 vitest pass; parity.json + PARITY.md regenerated | Batch 3 complete. Mission: size= scaling faithfully ported (golden + filled/upscale cases match oracle), +2 byte-match / 0 regressions, all size= graphs' maxDelta reduced. Headline rankdir canary remains diverged on a pre-existing layout residual (comparison page written) — flag to user. |
