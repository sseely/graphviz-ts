# Decision Journal — flatedge-box-x

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| T1 | 2026-06-19 | **Mission premise INVALID — the +27 box-x is a compensating internal-frame offset, not a bug. STOP.** | Dumped C `ND_coord(n).x` directly: node1=72, node6=432, node5=360, node8=576. Port internal: node1=99, node6=459, node5=387, node8=603 — **uniformly +27** (the port's internal x-origin is one leftmost-node half-width higher than C's). Both compute box `LL.x = coord.x + rw` (C 72+27=99; port 99+27=126) — SAME formula; the +27 box delta is the frame offset, which the emit-time translate cancels. Proof: `1:se->6:sw` final X is conformant (`M114.02 ... 374.6 ... 432.62` in both); ONLY Y differs (0 vs -7.88). The earlier flat-edge-routing-241 diagnosis read internal box coords without catching the +27 node-frame offset and mis-called it an X bug. | ⚑ STOP — premise invalid |
| T1 | 2026-06-19 | The real #241_0 divergence is **Y-only** (bbox/curl extent), dominated by the adjacent path. | Flat-edge X matches C exactly; the 7.88 miss is purely the node-row Y, set by the graph bbox height, which is driven by how far the flat edges curl above/below — primarily the adjacent `make_flat_adj_edges` edges (`3:sw->2:se` curls below; `5:ne->8:nw` curls above by a different amount). Recommend a SINGLE follow-on targeting the flat-edge Y/curl (adjacent `make_flat_adj_edges` + the non-adjacent curl height), and DROP the X-box framing entirely. No `src/` change. | ⚑ refocus → curl/Y mission |
