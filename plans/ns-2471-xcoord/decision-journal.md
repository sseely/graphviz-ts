# Decision Journal — ns-2471-xcoord

| When | Task | Decision / Event | Rationale |
|------|------|------------------|-----------|
| B1 start | — | Execution plan: B1 reproduce+classify (inline) → B2 localize (inline) → B3 fix+verify. Probe `rank2Loop` via `globalThis.__ns`; C oracle via `dot -v` GVBINDIR=/tmp/gvmine. | Per ADR-3; harness state carries inline. |
| T1 | T1 | **CLASSIFIED: correctness/cycling, NOT faithful-but-slow.** C x-coord NS (8637n/20028e, balance=2) = **24543 iter / 2.20s, converges**. TS same-size graph = **≥200k pivots/72s, climbing** (8×+). `sI` rotates fine (maxSi=8635; my "sI pinned at 0" code-inspection guess DISPROVEN). 42.9% degenerate. → proceed to B2. | TS does 8×+ MORE pivots than C ⇒ genuine pivot-selection/cutval deviation, not constant-factor slowness (which would be ~24.5k pivots, slower wall-clock). Rules out ADR-5 STOP. |
| T2 r1 | T2 | NS internals (`ns.ts`/`ns-core.ts`/`ns-range.ts`) verified faithful line-by-line vs `ns.c`. Aux graph differs: TS **19695** edges vs C **20028** (−333). Per-stage: identical through `make_edge_pairs` (12674==12674); whole gap in `pos_clusters`. | Premise "same aux graph" falsified. Cause is aux construction, not NS. |
| T2 r2 | T2 | Localized: 333-edge gap is **entirely `keepout_othernodes` LEFT scan** (`keepoutLeft`, position-cluster.ts). C left=333/right=340; TS left=**0**/right=340. Reason: `nodeOrder(v0)===0` for all cluster ranks because `.v[0]` is read **ignoring `vStart`** (cluster `.v` is shared root array; `v0`=root's node 0, inRoot=0). Same `vStart`-window bug class as Layers 1 & 2, in position phase. Injection experiment added 0 edges (windowed v0 not found by identity) → confirmed model divergence, causation by elimination. | Exact function + C-vs-TS diff pinned. |
| T2 end | T2 | **STOP.** Fix is in `position-cluster.ts` (`keepoutLeft`/`keepoutRight` → honour `vStart`), **outside write-set `{ns.ts,ns-core.ts}`**. Batch 3 (NS fix) N/A. All instrumentation reverted (TS+C); gates green: typecheck 0, 1876 tests, build OK, `git -C ~/git/graphviz status --porcelain lib/` empty. Root-cause doc: batch-2/ns-root-cause.md. | Stop conditions: fix outside write-set + contradicts brief's NS-bug premise. Awaiting human write-set authorization for position-cluster.ts. |
