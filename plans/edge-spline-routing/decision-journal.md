<!-- SPDX-License-Identifier: EPL-2.0 -->

# Decision Journal

Appended during execution. Every non-trivial judgment call gets a row.

| Date | Task | Decision | Rationale |
|------|------|----------|-----------|
| 2026-06-23 | plan | Spike-first brief; fix file unknown until S1 localizes | Cause is upstream of the faithful fitter (box corridor / input chain / slopes) but not pinned; investigation in `src/.agent-notes/edge-spline-extra-segments.md`. Writing fix tasks now would guess the file. User opted for a speculative brief. |
| 2026-06-23 | S1 | Reproducer diverging edge is `n12->n14` (oracle 2 cubics / port 1) — opposite of the investigation's stale "port subdivides MORE" note (path renumbered post font-fix). | Rendered `/tmp/le_long.gv` through oracle (`GVBINDIR=/tmp/gvplugins`) + port (`render-one`); diffed `<path d>` — exactly one path differs (edge26 = n12->n14). |
| 2026-06-23 | S1 | First differing `Proutespline` field = the **box corridor** (port `pl` 2 pts straight / C 3 pts bent → port 1 cubic / C 2). Instrumented both `routesplines_` (C) + `routeSplinesInternal` (port) with guarded probes. | Pinned to instrumented C per D2. Node centres conformant; endpoint +41 x is a benign internal-frame translation. |
| 2026-06-23 | S1 | Box[2] (`maximal_bbox(v1)`) right wall differs (C 45 / port 106) because `v1`'s right neighbour (rank-13 order-1 vnode) is +12.7 too far right in the port at routing time. Dumped rank contents + pre-route vs at-time coords. | Neighbour's pre-route x MATCHES C (frame-aligned); it is **displaced by `recover_slack`** before n12->n14 routes. |
| 2026-06-23 | S1 | `recover_slack` is faithful — both C and port move the shared vnode to the **identical** box-centre (n12-rel 56.7). Root cause = **edge routing ORDER**: C routes rank-major + `edgecmp`-sorted (shorter span first), port routes `g.nodes.values()`. Order flip ⇒ n12->n14 sees the displaced neighbour. | Logged recover_slack moves in both trees; confirmed the displacing edge routes AFTER n12->n14 in C, BEFORE in the port. |
| 2026-06-23 | S1 | Prototype (rank-major collect + stable sort by rank-span asc) makes **all 26** reproducer paths conformant with the oracle, 25 unchanged. Filled `#d-fixsite`, rewrote T2 (port `edgecmp` + reorder `routeDotEdges`). All instrumentation reverted (C tree clean; `.probes/` removed); `tsc` clean. | Confirms the fix direction before T2. NOT the fitter (premise's stop condition) → NOT a re-scope; it IS hypothesis #1 (box corridor), realized via order. Global reorder ⇒ D4 risk: T3 survey is the gate, STOP if any of 280 regress. |
| 2026-06-23 | T2 | Implemented `src/layout/dot/edge-order.ts` (`edgeRouteCmp`: edge-type desc → \|rank-span\| asc → \|Δx\| asc → AGSEQ asc) + reordered `routeDotEdges` to collect rank-major and sort. Reproducer conforms to; 2320 vitest pass; golden `dot-long-edge-order` added. | Faithful to C `dot_splines_`+`edgecmp` (D2). Verified the port's routing order is **conformant to C's** on the reproducer and on `graphs/p3` (instrumented C `dot_splines_` route loop). |
| 2026-06-23 | T2 | Full corpus survey: conformant 280→**281**, structural 236→**274**, diverged 252→**213**; errored/timeout unchanged. **41 improvements, 1 regression** (`graphs-p3`: structural→diverged). | Net +39 better buckets + 1 conformant. The single regression flips on segment count only. |
| 2026-06-23 | T2 | `graphs-p3` regression diagnosed: edge `sleep--runmem` now 3 cubics vs oracle 4, **but maxDelta 72→0.48** (geometry near-perfect). Port routing order matches C **conformant** for p3 — so this is a **separate pre-existing under-segmentation residual** (D3), not an ordering bug; the old wrong order coincidentally produced 4 segments with bad geometry. | D4 strict 0-regression tripped. Per the brief, STOPPED and asked the user; user chose **Accept + finish mission** — commit T2, defer `graphs-p3` as a D3 residual with a comparison page in T3. |
| 2026-06-23 | T3 | Regenerated `parity.json` + `PARITY.md`. Final: conformant **281** (≥280 ✓), structural **274**, diverged **213**; errored 5 / timeout 7 unchanged. Per-id: **41 improvements, 1 accepted regression** (`graphs-p3`). Canary: `rankdir_dot1` + `nshare-rankdir_dot1` moved diverged→structural; the still-diverged `rankdir_dot`/`dot2` rows all improved maxDelta (e.g. 55.7→33.6) on the same residual. | D4 floor met (conformant ≥280). The lone regression is user-accepted and documented in [comparisons/graphs-p3-residual.md](./comparisons/graphs-p3-residual.md) (same under-segmentation class as the rankdir_dot residuals — deferred D3, order confound now removed for a future mission). |
