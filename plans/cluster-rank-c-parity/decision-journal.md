# Decision Journal — cluster-rank-c-parity

| When | Task | Decision / Event | Rationale |
|------|------|------------------|-----------|
| 2026-06-17 | — | Brief created. Root cause probe-confirmed: `dot1Rank(subg)` returns all-zero local ranks; `interclust1` offsets all `off=1`. Baseline tsc 0, vitest 1867. | Phase 1 deep-dive |
| 2026-06-17 | T1 | **Exact defect found:** `nodeInduce` omitted C `node_induce`'s second loop (`agsubedge`). When a cluster's internal edges are declared at ROOT scope (2471's idiom), they never enter `clust.edges`, so `outEdges(subg)`/`class1(subg)` see no edges → simplex ranks singletons → 0,0,0,0. Single-cluster repro worked only because its edges were declared IN the subgraph. | dot1Rank(subg) probe: outEdges=0 comps=4 (multi) vs 3/1 (single) |
| 2026-06-17 | T1 | Fixed: `induceClusterEdges` pulls root edges with both endpoints in the cluster into `clust.edges`. 6-cluster chain now **24 ranks / 54 vnodes == C** (was 6/24); root mincross starts cur_cross 4 in both. +2 unit tests (1869). **Zero golden churn** (corpus declares cluster edges in-subgraph). tsc 0, lizard clean. Committed b97052e. | TDD nodeInduce + STATS verify |
| 2026-06-17 | T1 | **New downstream divergence surfaced (out of scope, AD-3):** on the now-correct cluster graph, C reduces crossings 4→0 but TS holds at 4 (mincross_clust / cluster-aware mincross). Rank structure matches C; crossing minimization does not. Separate mincross mission. | C vs TS trajectory on ab_clusters_tb |
| 2026-06-17 | T2 | **Zero golden churn** — no oracle regeneration needed. The existing corpus declares cluster edges in-subgraph (already worked); the fix only adds edges for the root-scope-edge case. A full-render clustered golden CANNOT be C-pinned yet (TS≠C render due to the downstream mincross divergence), so none added; regression coverage is the 2 `nodeInduce` unit tests. | full suite 1869 pass, 0 churn |
| 2026-06-17 | T3 | **Rank-structure parity verified (AD-3 target met):** 6-cluster chain TS 24r/54n == C; 2471 root TS 23r/3213n == C (was 31/2197). Plain/HTML/RL/self-edge unaffected (suite green). 2471 full-render STILL blocked — now hangs in mincross (the separate crossing divergence), not from garbage rank input. All probes reverted; C source pristine. | STATS probe (reverted) |
| 2026-06-17 | — | **MISSION COMPLETE (scoped).** Cluster-ranking root cause fixed + verified to C parity. 2471 rank structure now matches C exactly. Out of scope per AD-3: 2471 full render (blocked by a newly-surfaced mincross_clust crossing divergence — recommend next mission). | — |
