# Decision Journal — fix-pack-dot-clusters

Append one row per non-trivial judgment call. Cascade fixes are each verified by
running 2592 (and the full gate after the cascade closes).

| When | Decision | Rationale |
|------|----------|-----------|
| 2026-06-27 | Branch `fix/pack-dot-clusters` off main; execution plan logged | Re-add the cluster-carry code (approach.md) + remove the `graphHasCluster` guard, then close the cluster-mincross-core cascade one crash at a time (run 2592 after each), full gate after. STOP on any confirmed regression or a site that contradicts C. |
| 2026-06-27 | Cascade was only 2 sites — cluster.ts/cluster-path.ts/mincross.ts NOT touched | (1) `rank.ts:expandRanksets` `g===g.root`→`g===(g.info.dotroot ?? g.root)` (C `dot_root`). (2) The mergeRanks/makeSlots crash was NOT a cluster.ts bug: `mergeRanks` calls `dotRoot(clusterClone)`, and the projected clones had no `info.dotroot` → fell back to the true root's empty rank array. Fix is dot-local: `projectOne` sets each clone's `info.dotroot = component` (C stores GD_dotroot on the shared agroot → resolves to the component for all its subgraphs). So the predicted cluster-core rework was unnecessary. |
| 2026-06-27 | Closed the `outputorder=nodesfirst` renderer gap (write-set expanded per user) | After cluster carry, 2592 stayed `diverged` on a STRUCTURAL diff (`@id`: node emit order). The verdict logic flags `diverged` only on structural diffs (element/id/childCount), not positional deltas — so the node-only-pack-bb position offset is harmless (structural-match). 2592 sets `outputorder=nodesfirst` which the port ignored (always breadth-first). Ported C's `emit_view` dispatch (chkOrder) into `device.ts`/new `emit-walk.ts`. 2592 → structural-match. User authorized the device.ts/emit-walk.ts write-set expansion. |
| 2026-06-27 | Extracted emit-walk to `src/gvc/emit-walk.ts` | device.ts hit 539 lines (>500 cap) after the outputorder helpers. Moved the node/edge walk (chkOrder + nodesfirst/edgesfirst/breadth-first + nodeShown/edgeShown) to a new file (idiomatic, cf. xlabels-place.ts). device.ts=461, all caps satisfied (lizard clean). |
| 2026-06-27 | DONE: 2592 diverged→structural-match, 0 regressions | Cluster carry verified correct on synthetic flat + nested fixtures (structural-match, matching cluster trees/ids). 2683 also improved (410→82, stays structural-match). Gate: stable=675, improvements=1 (2592), regressions=0. 2458/2682 stay byte-match. Baseline refreshed (structural-match 225→226, diverged 105→104). Residual: clustered-component pack POSITION offset (~8-57pt) from node-only `computeSubgraphBB` (pack module, ADR-3) — harmless to verdict (structural-match), a possible byte-match follow-up. |
