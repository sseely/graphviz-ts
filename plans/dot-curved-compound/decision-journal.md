# Decision Journal — dot-curved-compound

| When | Task | Decision / Event | Rationale |
|------|------|------------------|-----------|
| 2026-06-18 | T1 | Implement directly (no subagent dispatch) | Full C context (routespl.c:904-1042, dotsplines.c:241-465) + TS model already loaded in orchestrator; a fresh sonnet subagent would re-derive it. Task is a focused faithful port under ~30min. Per parallelism.md "handle directly under ~30 min". |
| 2026-06-18 | T1 | `get_cycle_centroid` returns `get_centroid` (bb center) for all 3 curved fixtures | single `a->b` has no cycle; parallel `a->b` uses the multi-edge perp branch (no `bend`); 2-cycle `a->b->a` has cycle length 2 < min_size 3 → NULL → `get_centroid`. Cycle≥3 DFS path ported faithfully but unexercised by fixtures (documented). |
| 2026-06-18 | T1 | Curved skips `routeDotEdges` (TS analog of make_regular_edge); loop calls `makeStraightEdges` per group | mirrors dotsplines.c:381-387 (curved replaces per-group routing) + :461-465 (skip routesplinesterm). `makeStraightEdges` installs `spl` on every edge so `routeDotEdges` would no-op anyway; skipping is explicit + faithful. |
| 2026-06-18 | T1 | `EDGETYPE_PLINE` sub-branch in `makeStraightEdges` throws (unported `make_polyline`) | Dispatch only passes `EDGETYPE_CURVED`; PLINE is structurally unreachable here. Per T1 boundary "stub — out of scope unless reachable". Loud throw beats silent wrong geometry if ever reached. |
| 2026-06-18 | T1 | `straight-edges.ts` defines `EDGETYPE_CURVED`/`EDGETYPE_PLINE` locally instead of importing from `splines.ts` | A `straight-edges ↔ splines` cycle (splines imports `makeStraightEdges`) triggered a latent TDZ in `ortho-adapter.ts:66` (top-level `buildDotSinfo()`). Local consts mirror the C enum values, breaking the direct edge. |
| 2026-06-18 | T1 | curved.test.ts uses top-level `it()` (no `describe`) and imports `splines.js` first | Complexity hook measures a `describe` callback as one function (>30-line cap). Import order: loading `self-loop.js` before `splines.js` re-triggers the ortho-adapter TDZ; `splines.js` first (as ortho-dispatch.test.ts does) avoids it. |
| 2026-06-18 | T1 | Batch-1 gate PASS | `npm run typecheck` exit 0; `npm test` 1944 pass / 141 files (baseline + 6 new curved, 0 regressions); `npm run build` 956kb OK; `git -C ~/git/graphviz status --porcelain lib/` no tracked .c/.h change. |
| 2026-06-19 | T2 | Ref-mint recipe = `GVBINDIR=/tmp/gvmine ~/git/graphviz/build/cmd/dot/dot -Tsvg` | Verified byte-identical to an existing ortho ref (15.1.0~dev.20260610). Homebrew `dot` (15.0.0) is the wrong version; plain build-dot without GVBINDIR emits empty. |
| 2026-06-19 | T2 | curved-parallel diverged (spline→edge mapping); fixed via `routeCurvedGroup` dedup+direction-partition+origSeq sort | TS collection over-groups parallels (3 entries, getMainEdge collapses to #0) and opposing edges (a->b/b->a share main). Dedup to distinct origs + partition by original direction matches C's getmainedge grouping; index→perp-offset order matches C (first→+perp). curved-single + curved-parallel now byte-exact. |
| 2026-06-19 | T2 | compound clipping was UNWIRED — fixed (faithful) | `g.info.compound` never set + `e.info.lhead/ltail` never populated → `dotCompoundEdges` never ran. Fix: gate `dot/index.ts` on `mapbool(g.attrs.get('compound'))` (dotinit.c:338); `dotCompoundEdges` drops its internal gate (compound.c:434 has none); `resolveClusterPair` reads `e.attrs` lhead/ltail (compound.c:274-275, agget) with e.info fallback for unit tests. Path now clips to cluster (start byte-exact). |
| 2026-06-19 | T2 | QUARANTINE dot-curved-cycle | Multiedge port offset (±5.92) lives in `ED_tail_port`, which C pre-sets but the TS pipeline leaves (0,0) (computes offsets lazily in the non-curved router; default-splines 2-cycle is byte-exact to C). Porting C's multiedge port-assignment is out of the curved scope. Comparison page: [quarantine/dot-curved-cycle.md](quarantine/dot-curved-cycle.md). |
| 2026-06-19 | T2 | QUARANTINE dot-compound-lhead | Path clips correctly post-wiring; arrowhead doesn't follow the clip — `clipHeadNormal` lacks `arrowEndClip` (arrows.c) + `ep`/`eflag` arrow handling that C uses (compound.c:345-348). Substantial T38-completion sub-task beyond ADR-2 verify-fix scope. Wiring fix kept as partial progress. Comparison page: [quarantine/dot-compound-lhead.md](quarantine/dot-compound-lhead.md). |
| 2026-06-19 | T2 | Full mission gate PASS | `npm run typecheck` exit 0; `npm test` 1947 pass / 141 files (3 new goldens: curved-single, curved-parallel, compound-splines; 0 regressions; every baseline ref byte-identical); `npm run build` OK; C tree clean; diff confined to src/layout/dot/**, test/golden/**, plans/**. 2 fixtures quarantined w/ comparison pages. |
