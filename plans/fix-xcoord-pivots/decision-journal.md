# Decision journal

Appended during execution. Log every non-trivial judgment call, the T3
root-cause pin, and the T5 before/after metrics.

| Date | Task | Decision / Finding | Rationale |
|------|------|--------------------|-----------|
| 2026-06-22 | exec | Execute Batch 1 directly (orchestrator), not via parallel debugger agents | T3 root-cause pin requires holding C + port in one mind; agent blank-slate re-derivation + PASS-claim re-verification (memory subagent-hook-loop-deaths) costs more than the T1/T2 parallelism saves. Env already verified working. |
| 2026-06-22 | T1 | Native x-coord NS confirmed: 26849 nodes / 391709 edges / 8748 iter (balance=2) via GVBINDIR=/tmp/gvplugins dot -v | Ground-truth target re-verified against memory hang-2475-2-xcoord-ns. |
| 2026-06-22 | T1/T3 | 2475_2 uses `cluster=true` on its 805 anon subgraphs → they ARE clusters. pos_clusters = 353989 of 391709 aux edges (90%). The pure-forest synthetic matched native only because it had no clusters. | Re-frames the memory's "createAuxEdges gap" — divergence is cluster-constraint-specific, not LR/edge-pairs. |
| 2026-06-22 | T3 | **firstDivergence (ADR-2): phase = pos_clusters → keepout_othernodes; file = position-cluster.ts; C ref = position.c:keepout_othernodes (lines ~398-419).** Per-phase aux-edge counts (native vs port): LR 12638=12638, pairs 25082=25082, containClust 14845=14845, **keepout 13924 vs 7019 (−6905)**, containSub 1610=1610, separate 323610=323610. The 6905 keepout deficit == the exact total edge gap (391709−384804). | Staged comparison stopped at first divergent phase; isolates root cause to one function. |
| 2026-06-22 | T3 | **Root cause:** `keepoutLeft`/`keepoutRight` (position-cluster.ts) read `g.info.rank[r].v[0]` to get the cluster's leftmost node v0, but `.v` is the shared ROOT array offset by `vStart`; v0 must be `rankGet(rk,0)`. With v0=root-leftmost (order≈0) the LEFT scan `for i=order-1..0` never runs → all left-keepout edges dropped. Same vStart-window bug class as memory contain-nodes-vstart-window / containNodesRank (already fixed there). | Faithful correction toward C (ADR-4); 1-symbol-per-function fix. |
| 2026-06-22 | T3 | Fixture: extracted first 8 `cluster=true` subgraphs from 2475_2 (26 nodes) → `__fixtures__/xcoord-pivot-divergence.gv`. Uses the exact real trigger. NOTE: it (and 2475_2) verdict `diverged` even post-fix due to a SEPARATE anon-subgraph naming issue (port `%0..` vs native `%3,%9,..`), so the regression guard is a unit test on keepout edge counts, not a parity-match. | ADR-3 fixture; parity-match polluted by unrelated naming divergence. |
| 2026-06-22 | T4 | Fix applied: `keepoutLeft`/`keepoutRight` v0 = `rankGet(rk,0)` (2 lines). Unit test `position-cluster.test.ts` (3 cases) verified RED pre-fix (3 failed) → GREEN post-fix. | TDD regression guard isolates the vStart-window fix. |
| 2026-06-22 | T5 | **Before→after (2475_2):** aux edges 384804→391709 (==native), keepout 7019→13924 (==native), x-coord NS pivots 34434→**8694** (native 8748, ≈ within 0.6% tie-break noise), render ~126s→~27.7s. tsc clean; vitest 2266 pass (+3). | Pivot-count parity = mission success bar (README) achieved via faithful fix. |
| 2026-06-22 | T5 | **Parity survey: 0 verdict regressions, 25 improvements** (byte-match 251→266, diverged 286→270). 15 cluster graphs improved diverged→byte/structural (graphs-clust*, share-clust*, windows-clust*, *-compound_dot, graphs-fdp, graphs-style). Confirms the missing left-keepout constraints caused real layout divergence corpus-wide. | The hard correctness gate; fix moves results TOWARD native (no mis-pin). |
| 2026-06-22 | T5 | **Render still 27.7s > 20s gate.** Phase split: mincross ~9.6s + position/NS ~16s + splines ~1.3s. Residual is broad constant-factor JS cost (mincross alone > native's 3.77s total) + per-pivot NS cost — NOT a faithfulness divergence; reducing it needs perf work on files beyond position-cluster.ts (violates write-set constraint + ADR-4 "no perf hacks"). README explicitly frames sub-20s as "a consequence, not the bar." | PUSH FORWARD: report honestly; do not chase <20s out of scope. |
