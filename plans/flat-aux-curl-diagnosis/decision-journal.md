# Decision Journal — flat-aux-curl-diagnosis

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).
One writer per row; parallel agents write their own findings file, the
orchestrator records the journal row after the batch.

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| plan | 2026-06-20 | Branch `fix/flat-aux-curl-diagnosis` off main; committed brief baseline. Batch 1 = T1 (debugger, runtime harness+canary) ‖ T2 (debugger, static input-parity), disjoint write-sets, parallel. Batch 2 = T3 after both. | Autonomous mode: log plan, skip user review (parallelism.md autonomous exception). | no |
| orient | 2026-06-20 | Pre-read C `make_flat_adj_edges` (dotsplines.c:1167-1201) + port `buildFlatAux` (splines-flat.ts:149-166). Confirmed by source: C clones `auxt` into a `rank=source` subgraph (`agsubg`+`agset(rank,source)`+`cloneNode(subg,tn)`); port clones both auxt/auxh into auxg with NO source pin. Prime suspect (AD-3 #1) present-in-C / absent-in-port confirmed statically before agents run — so I can verify T2's claim. | instrument-c-before-quarantine: read C ground truth first. | no |
| T1 | 2026-06-20 | debugger agent built `test/diagnostic/flat-aux-dump.ts` + synthetic repro `flat-back-port.dot` (3 nodes: anchor `1`→`2`,`1`→`3`, `{rank=same;2;3}`, fwd `2:ne->3:nw` + back `3:sw->2:se`). Canary GREEN (fwd 2->3 = size 7 both sides). Reversed 3->2: C size 7, port size 4 — bug reproduced outside #241_0. C instrumentation ephemeral, dotsplines.c restored clean, clean plugin back in /tmp/gvplugins (verified). | AD-2/AD-4 harness canary-first; AD-6 ephemeral C. | no |
| T2 | 2026-06-20 | debugger agent produced `findings-input-parity.md`. `rank=source` subgraph pinning auxt: ABSENT in port (`buildFlatAux` never calls `auxg.subgraphs.set`); the ranker machinery (`collapseSets`/`rankSetClass` in rank.ts) is PRESENT but unreachable because `auxg.subgraphs` is empty. Ranked suspect #1 high-impact. | AD-3 input-parity before layout instrumentation. | no |
| recon | 2026-06-20 | RECONCILED the two agents' conflicting root causes myself (they disagreed). T1 (empirical, instrumented native dot) is correct: C's `make_flat_adj_edges` is called ONCE with `cnt=2` (both fwd+back grouped in one aux graph) — verified by C dump `edge0=2->3` + a `3->2` back-edge spline size 7. Port's LIVE path `routeDotEdges`→`routeForwardEdge`→`routeFaithfulSidePort` calls `makeFlatAdjEdges(g,[e],1,...)` (edge-route.ts:297) — one isolated `cnt=1` aux graph PER edge (I verified this directly, not via T1's reconstruction). With cnt=1 the back edge `3->2` becomes auxt→auxh FORWARD (rank0→1, straight, size 4); in C's cnt=2 aux it is auxh→auxt BACK (rank1→0, curl, size 7). Verified my own earlier doubt ("cnt=1 ⇒ C also size 4") is moot because C does NOT use cnt=1. **First divergence = edge GROUPING (port per-edge vs C grouped), not the rank=source pin.** T2's rank=source gap is real but secondary. | instrument-c-before-quarantine; re-verify agent PASS claims (subagent-hook-loop-deaths). | no |
| gates-B1 | 2026-06-20 | Batch 1 gates: `tsc --noEmit` exit 0; `vitest run` 1991/1991 pass, 0 fail (goldens byte-identical — no src change); `git diff --name-only main` = only test/diagnostic + plans (no layout path); `lizard` 0 warnings. | Quality gates mandatory between batches. | no |
