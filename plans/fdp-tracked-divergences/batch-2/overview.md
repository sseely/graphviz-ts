# Batch 2 — B2 force-drift → A1 accept

Depends on Batch 0. **Provisional** — most of this bucket should EMPTY once T0.2
regenerates the attribution with working injection (the July-17 attribution was
unreliable). Read T0.3 `findings.md` first.

Candidates: the unix genealogy family (unix, lsunix1-3, size, unix2/2k, weight,
crazy) and other large-multi-kind residuals. Prediction: fdp is iterative
(spring-electrical, like sfdp) → chaotic force-drift that injection clears →
`drift-exonerated` → covered by the computed A1-drift class, NOT tracked.

| ID | Description | Agent | Writes | Depends On | Done |
|----|-------------|-------|--------|-----------|------|
| T2.1 | Confirm each B2 id is drift-exonerated (injection clears to 0) | debugger | batch-2/findings.md | Batch 0 | [ ] |
| T2.2 | Any residual that does NOT clear: diagnose (fix vs A9/other) | general-purpose | (src fix if any) + batch-2/findings.md | T2.1 | [ ] |

If T0.2's fresh attribution already marks these `drift-exonerated`, this batch is
just confirmation + a note that they join the A1-drift class (registry `A1-drift`
class entry, computed from `attribution-fdp.json`). No per-id registry entries
needed for A1 (membership is computed). Any id that survives injection here is a
genuine residual — escalate to B1 (frame) or B3 (FP-tie) with a stated mechanism.
Watch for no-op injection on space-named-node graphs ([[injection-parser-space-named-blindspot]]).
