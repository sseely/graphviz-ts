# Decision journal — fdp tracked divergences

Append one row per non-trivial judgment call or completed task. Include the
mechanism for every fix (per `~/.claude/rules/diagnosis.md`) and reference every
accepted divergence with its evidence pointer.

| Date | Task | Decision / Mechanism | Files | Gate result |
|------|------|----------------------|-------|-------------|
| 2026-07-21 | T0.1 | Added env-gated `GVTS_POS_DUMP` in `fdp_layout` after `fdpLayout()` returns, before `neato_set_aspect(g)` (mirrors port inject at `fdp/index.ts:91`). **Key: `cmake --build --target dot` did NOT rebuild the dump — fdp_layout compiles into `gvplugin_neato_layout`; had to build that target.** `/tmp/ghl` symlinks the build tree so the rebuilt plugin is picked up. Verified: 41 GVTS_POS lines == 41 nodes on unix.gv; space-named nodes ("5th Edition") dump correctly; xdot byte-identical without the flag. | ~/git/graphviz/lib/fdpgen/layout.c (native, gated) | new oracle bin sha1 `8fdd1294b3726c2a19fe384ca48f44a2954d1944`; render unchanged |
| 2026-07-21 | T1.1/T1.2 | **B1 FIXED (structural cluster-name collision).** Mechanism: fdp `processClusterEdges` (fdpinit.c:90, fdp-ONLY — verified dot/neato both draw the node) replaces an edge endpoint that names a cluster with an invisible box proxy sized to the cluster (setClustNodes) and deletes the original visible node; the port never ported it (init.ts assumed no such input — false: graphs-fdp `e -- clusterB`, graphs-b145 `cluster_foo -> …`). Structural residual survived injection ⇒ genuine emission bug (injection can't add/remove elements). Ported faithfully: new `cluster-edges.ts` + init.ts call + real setClustNodes + render/dot.ts clustnode suppression/name-strip. **Root-fix:** dot rank.ts:273 wrongly set `clustnode` on real cluster members (C node_induce sets ND_clust only) — benign until the serializer keyed on it; removed → clustnode fdp-exclusive → render/dot.ts inert for other engines. C `*idx++` pointer-bug replicated (one shared `__0:<cluster>` proxy). Verified: graphs-fdp 131→inj 0, graphs-b145 79→inj 0 (both A1); fresh fdp engine-walk 0 pass→diverged (2620 diverged→oracle-error = sweep TIMEOUT, oracle succeeds standalone, port renders it clean). | src/layout/fdp/{cluster-edges,init,layout,guards.test}.ts, src/model/graphInfo.ts, src/layout/dot/rank.ts, src/render/dot.ts, batch-1/findings.md | tsc clean; 3220 tests pass; 0 regressions |
| 2026-07-21 | T0.3 | Re-bucketed the 4 not-cleared via residual-diff diagnosis. **B1 (structural cluster-name collision, NEW):** graphs-fdp + graphs-b145 — port emits a visible node draw for a node whose name equals a cluster name (`e -- clusterB` w/ `subgraph clusterB`; `cluster_foo->…[lhead=cluster_foo]` w/ `subgraph cluster_foo`), oracle suppresses it (structural `node:clusterB`/`node:cluster_foo` present-in-port). Injection can't add/remove elements ⇒ genuine port emission bug. Predicted B1 (graphs-cairo, 2193) both drift-exonerated ⇒ **old B1 EMPTY**. **B2 (unix force-drift): EMPTY** — all drift-exonerated ⇒ Batch 2 SKIPPED. **B3 (A9):** 241_0 clean A9 (11 diffs, maxΔ 3.39pt); 2095 = **harness blindspot** — 45/52 residual are the 1 empty-named node + its 2 edges (port regex `(.+)` won't match empty name ⇒ node never injects, drags edges ~1000pt), only 7 small-Δ are genuine A9. | batch-0/findings.md | analysis only; Batch 2 empty |
| 2026-07-21 | T0.2 | Verified injection end-to-end on 2095: base 8256 → inj 52 (99.4% reduction), reproducing the July-17 data exactly — dump is complete/correct, NOT a no-op (275 GVTS_POS + 1 GVTS_BB). Ran `attribute-divergence.ts fdp --fresh` over 146 diverged ids: **142 drift-exonerated, 4 not-cleared, 0 harness-error** (jsonl integrity: 146 rows / 146 unique). Refined tracked count = **4** (stale attribution's "20 tracked" was against the broken `5caf7a36` oracle). Not-cleared: 241_0 (446→11, position/edge-bezier), 2095 (8256→52, position/edge-bezier) → B3 A9; graphs-fdp (162→45, **count**/node-structural), graphs-b145 (96→27, **count**/node-structural) → structural porting gap. B1 (graphs-cairo/2193) and B2 (unix family) EMPTY — all drift-exonerated. | test/corpus/attribution-fdp.{jsonl,json} | oracle sha1 `8fdd1294…`; 4 not-cleared |
| 2026-07-21 | T0.1 (amend) | **Deviation from literal spec (push-forward, obvious+self-contained):** T0.1's block mirrored neatosplines.c (GVTS_POS only), but the injection-recipe.md + port hook (`splines.ts:929`) + harness (`attribute-divergence.ts:330`) all document fdp as ALSO emitting `GVTS_BB` (root GD_bb, points) and per-cluster `GVTS_CLUST_BB <name> llx lly urx ury`, both LOAD-BEARING (GVTS_BB alone clears a 60-id phantom bb family; fdp's GD_bb+cluster bbs come from fdpLayout's setBB, upstream of inject, so are NOT re-derived from injected pos). Added a static recursive `gvts_dump_clust_bb` + the GVTS_BB line. Verified: unix 41 POS + GVTS_BB; KW91 2 CLUST_BB (nested outer→inner), b15 5 CLUST_BB, correct names. Running the sweep with the incomplete block would have poisoned the attribution (exactly what T0.2 step 3 guards against). Note: `b15.gv` render is fdp-non-deterministic across separate processes even without the flag (harness immune: 1 process yields xdot+dump together). | ~/git/graphviz/lib/fdpgen/layout.c (native, gated) | dump side-effect-free; render unchanged on deterministic graphs |
