# T0.2 — Regenerate fdp attribution + snapshot the parity baseline

## Context
With fdp injection restored (T0.1), the attribution harness can finally separate
force-drift (`drift-exonerated` → A1 accept) from genuine residuals
(`not-cleared` → tracked). The committed `attribution-fdp.jsonl` is stale (old
oracle `5caf7a36`, 2026-07-17) and was produced when the dump worked; overwrite it.

## Task
1. `pgrep` to confirm no sweep/render is running.
2. Snapshot the current committed `parity-fdp.json` as the mission baseline
   (copy to the scratchpad) for later 0-regression diffs.
3. Verify injection now takes effect end-to-end on one id: dump native positions
   for `2095`, inject into the port, and confirm the injected render's diff count
   is far below the base (the July-17 data showed base 8256 → inj 52). If
   injection is still a no-op (inj ≈ base for a graph that should clear), STOP —
   T0.1 did not actually restore the dump (recheck the site / port inject order).
4. Run the harness fresh over the current fdp diverged set:
   `npx tsx test/corpus/attribute-divergence.ts fdp --fresh`.
5. Record counts (drift-exonerated / not-cleared / harness-error) and the new
   oracle sha1 in decision-journal. This is the refined tracked count.

## Read-set
- `test/corpus/attribute-divergence.ts` (harness + CLI)
- `test/corpus/parity-fdp.json` (current diverged set + baseline to snapshot)
- `plans/iterative-parity-campaign/diagrams/injection-recipe.md` (POS_DUMP/INJECT recipe)
- memory [[injection-parser-space-named-blindspot]] (verify match count > 0 — a
  no-op injection is indistinguishable from "survives injection")

## Write-set
- `test/corpus/attribution-fdp.{jsonl,json}` (regenerated)

## Acceptance criteria
- Given the restored dump, when `2095` (or another large-base id) is injected,
  then the injected diff count is a large reduction from base (proves injection
  works) — NOT a no-op.
- Given the current fdp diverged set, when the harness runs, then
  `attribution-fdp.jsonl` has one verdict per id and `_meta.generatedAt` is today
  with the rebuilt oracle sha1.
- Given the output, when counted, then the new not-cleared set size is recorded
  in decision-journal (the refined "tracked" count).

## Observability / rollback
N/A. Reversible (revert the jsonl).

## Boundaries
- Never do: edit `src/` (analysis only). Trust a no-op injection (see step 3).
- Ask first: if the harness needs a code change to handle an fdp-specific shape.

## Commit
`chore(T0.2): regenerate fdp attribution over current diverged set`
