<!-- SPDX-License-Identifier: EPL-2.0 -->

# T2.1 — Survey verification + perf

## Context
The unify touches the shared router and changes routing ORDER, so the survey will
churn. Confirm every change is an improvement or a documented-faithful flip, with
**0 regressions**, and that reordering did not hurt perf.

## Task
1. Regenerate the rules survey: `GVBINDIR=/tmp/ghl PARITY_OUT=parity-rules.json
   tsx test/corpus/survey.ts` then `npm run survey:gate`. (Fast iteration during
   development: `npm run survey:fast`.)
2. Diff per-id vs the pre-fix `parity-rules.json` (`git show HEAD:...`). Confirm
   `ldbxtried` `n0->n1` flips to the corridor and **no** input regresses
   match→diverged. For any flagged regression, re-verify vs FRESH C (render port
   vs `GVBINDIR=/tmp/ghl dot`) before treating it as real
   (`[[make-edge-pairs-trunc-fix]]` GOTCHA).
3. Perf: `node test/corpus/bench.mjs` (or `PERF.md`); confirm no previously-passing
   input exceeds 2× its native time. Reordering should be perf-neutral (same work).
   Restore `perf.json` if a targeted `BENCH_IDS` run overwrote it.
4. Write `comparisons/T2-survey-verification.md`: verdict deltas (improved /
   regressed / order-flips), perf summary, and the 0-regression proof. Attribute
   honestly (separate the order fix from any measurement-path effects).

## Write-set
- `plans/fix-edge-route-order/comparisons/T2-survey-verification.md` (create)

## Read-set
- `test/corpus/survey.ts`, `rules-gate.ts`, `dashboard.ts`, `bench.mjs`
- `comparisons/root-cause.md`

## Acceptance criteria
- Given the regenerated survey, then `ldbxtried` `n0->n1` is the 7-pt corridor and
  no new id appears in diverged (0 regressions).
- Given any flagged regression, when re-checked vs fresh C, then it is confirmed
  real before any rollback decision.
- Given the perf bench, then no previously-passing input is > 2× native.

## Observability / Rollback
N/A. Reversible — artifacts regenerate from source.

## Quality bar
Survey completes; 0-regression holds; perf within budget. Return only the verdict
delta + perf summary.

## Commit
`test(T2.1): verify single-pass edge routing order — survey + perf`
