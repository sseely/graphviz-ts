<!-- SPDX-License-Identifier: EPL-2.0 -->

# T2.2 — Baseline refresh + close

## Context
After T2.1 confirms the fix is clean, refresh the committed survey artifacts so
the dashboard reflects current `main`, and close the mission. `PARITY.md` is built
from `parity.json` (the pango/LUT baseline).

## Task
1. Regenerate the pango baseline with an ISOLATED cache:
   `GV_TEXT_MEASURER=lut GVBINDIR=/tmp/gvplugins ORACLE_CACHE=$TMPDIR/oracle-pango
   PARITY_OUT=parity.json tsx test/corpus/survey.ts`.
2. Confirm the pango baseline delta vs `git show HEAD:test/corpus/parity.json` is
   improvements-only, 0 regressions (re-verify any flagged regression vs fresh C;
   an oracle-timeout/oracle-error on a pathological heavy graph is environmental,
   not a port regression).
3. `npm run survey:dashboard` → regenerate `PARITY.md`.
4. Commit `parity.json` + `parity-rules.json` + `PARITY.md`.
5. Write the mission summary in `README.md` (tasks done, verdict deltas, perf,
   residuals — e.g. ldbxtried `n0->n2` ~1px Proutespline residual if it persists).

## Write-set
- `test/corpus/parity.json`, `test/corpus/parity-rules.json`, `test/corpus/PARITY.md`
- `plans/fix-edge-route-order/README.md` (mission summary section)

## Read-set
- `comparisons/T2-survey-verification.md` (T2.1)
- `test/corpus/dashboard.ts`

## Acceptance criteria
- Given the regenerated pango baseline, then the per-id delta is improvements-only
  with 0 regressions, and `PARITY.md` shows `ldbxtried` with `n0->n1` fixed (and a
  smaller maxΔ, or out of the diverged worst-first table if fully resolved).
- Given the mission summary, then README records tasks completed, survey deltas,
  perf result, and any residual.

## Observability / Rollback
N/A. Reversible — artifacts regenerate; revert restores prior report.

## Quality bar
Baseline refresh clean; dashboard current. Return only the delta summary.

## Commit
`chore(T2.2): refresh parity baselines + dashboard after edge-order unify`
