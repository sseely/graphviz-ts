<!-- SPDX-License-Identifier: EPL-2.0 -->

# Decision journal

Appended during execution. One row per non-trivial judgment call. T0.3 must
record the GO/STOP decision and the confirmed Batch-1 write-set here before any
`src/` edit.

| Date | Task | Decision | Rationale |
|------|------|----------|-----------|
| 2026-06-25 | B0 | Execute Batch 0 (investigation/gate) **inline** in the orchestrator, not via dispatched agents | Sequential gating investigation whose GO/STOP (T0.3) depends on deeply understanding the T0.1/T0.2 evidence chain; keeping it in one context avoids re-verifying agent claims (`[[subagent-hook-loop-deaths]]`). C build is shared/finicky. |
| 2026-06-25 | T0.1 | Minimal synthetic repro (`repro1`) is an **order-signature** fixture, not a geometric-divergence golden; ldbxtried `n0->n1` is the Batch-1 red golden | repro1 reproduces the dispatch signature (lone `a->b`@seq1 before vnode-moving group `a->c`@seq4) but C routes `a->b` straight (4pt) regardless — verified port==C. A *large* recover_slack move that flips a lone edge's point count needs ldbxtried-scale lateral obstacles (tried repro2/repro3). Acceptance asks the repro reproduce the *signature*, which it does. |
| 2026-06-25 | T2.1 | **STOP — perf stop condition tripped. Paused for human decision.** | Survey gate PASS, 0 regressions, 17 byte improvements, n0->n1 fixed — correctness is clean. BUT large dot graphs slowed materially: b100 3.84×→7.76×, 2475_2 2.45×→5.1× (was under the 3× "ok" bar), share-b29 2.31×→3.06×. Backstop probe (ldbxtried reached=70 routedNow=0) rules out double-routing — it is the FAITHFUL cost of correct corridors (lone multi-rank edges now route full corridors, not the broken degenerate straights), exposing the port's pre-existing per-box routespline gap. No ≤2× graph crossed to >2×, but previously-"ok" inputs now exceed 2× native → brief stop condition. Awaiting user: (a) ship + document + follow-up routespline perf, or (b) optimize before close. |
| 2026-06-25 | T1.1 | **Scope deviation (logged):** pin the fix with a *focused* `n0->n1` path test, NOT a whole-SVG ldbxtried golden or the minimal repro | Discovered both brief fallbacks fail as red→green: (a) whole-SVG `parallel-cluster-ldbxtried` is broadly diverged port-vs-C (23 structural + 500+ numeric diffs across nodes/arrowheads — measured vs a *fresh* headless ref), so it can never go green → kept `knownResidual` (skipped), tightened description, ref regenerated from /tmp/ghl; (b) the minimal repro (`edge-order-min`) does NOT diverge (port==C, both 4pt — sub-pixel move) → added as a *green* guard fixture (manifest 164→165). The only clean red→green is a scoped assertion: render ldbxtried, assert port `n0->n1` `d` == C 7-pt corridor (14 nums) within 0.01. Added as `test/golden/suite.test.ts` focused test — RED now (port 8 nums vs 14). Stays within the T1.1 write-set (test/golden/*). Honors ADR-4 intent (TDD red→green pinned to the C oracle) better than the golden mechanism. |
| 2026-06-25 | T0.3 | **GO** — unify into one `edgecmp`-ordered pass (Option A). Contained to `splines.ts` + `edge-route.ts`. **T1.3 N/A.** | Instrumented C (T0.1) + instrumented port (T0.2): port `edgecmp` is **positional-exact** to C (ldbxtried 52 groups, `n0->n1`@pos34 / `n0->n2`grp@pos67; 13 GROUP + 39 LONE = C's 13 cnt>1 + 39 cnt=1). Divergence is the **two-pass deferral** of lone edges to `routeDotEdges` (after group `recover_slack`/`top_bound` mutations), NOT ordering. No comparator-semantics change; no reach into `routeRegularEdgeFaithful`/`recoverSlack`. **Confirmed Batch-1 write-set:** T1.1 = `test/golden/{inputs,refs}/*` + `manifest.json` + `suite.test.ts` count; T1.2 = `src/layout/dot/splines.ts` + `src/layout/dot/edge-route.ts`; T1.3 = N/A. |
