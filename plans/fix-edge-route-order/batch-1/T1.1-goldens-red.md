<!-- SPDX-License-Identifier: EPL-2.0 -->

# T1.1 — TDD goldens (red)

## Context
Pin the target behavior before changing the dispatch. Goldens use the headless
oracle (ADR-4). `ldbxtried` already exists as a `knownResidual` golden
(`parallel-cluster-ldbxtried`, from the parent mission) — its `n0->n1` is the
motivating regression. The suite asserts a manifest entry count that must be
bumped when adding entries.

## Task
1. **Flip `ldbxtried`**: regenerate `test/golden/refs/parallel-cluster-ldbxtried.svg`
   from the headless oracle (it is current), and remove/adjust the `knownResidual`
   field in `manifest.json` so the entry is ACTIVE again — OR, if the whole-SVG
   still diverges on the `n0->n2` ~1px residual, keep it `knownResidual` but tighten
   the description, and instead rely on the minimal repro below as the active pin.
   (Decide per the actual post-fix verdict in Batch 2; for RED, set it active so it
   fails now.)
2. Add a **minimal repro** input `test/golden/inputs/edge-order-min.gv`: a lone
   edge whose adjacent corridor reads a chain vnode that a parallel/opposing group
   (routed LATER in `edgecmp`) repositions (shape from `comparisons/c-order-oracle.md`
   / T0.1). Generate its headless ref.
3. Add `manifest.json` entries (`id`, `engine:"dot"`,
   `toleranceClass:"deterministic"`, `input`, `reference`, `description`).
4. Bump the `manifest has N entries` count + comment in `test/golden/suite.test.ts`.
5. Run `npm test`; confirm the new/flipped goldens **FAIL** (red) — the lone edge
   under-segments under the current two-pass order. Record the failing `@d` diff.

## Write-set
- `test/golden/inputs/edge-order-min.gv` (create)
- `test/golden/refs/edge-order-min.svg`, `test/golden/refs/parallel-cluster-ldbxtried.svg` (create/regen)
- `test/golden/manifest.json` (add/adjust entries)
- `test/golden/suite.test.ts` (count assertion + comment)

## Read-set
- `test/golden/manifest.json` (entry template; the existing `parallel-cluster-*`
  entries), `test/golden/suite.test.ts` (count test ~line 104)
- `comparisons/c-order-oracle.md` (the minimal repro shape from T0.1)

## Acceptance criteria
- Given the new manifest entries, when `npm test` runs, then the count test
  passes and the new/flipped goldens FAIL with a spline `@d`/point-count mismatch.
- Given the refs, then they were generated by the HEADLESS oracle (`/tmp/ghl`),
  not pango (run `sh test/corpus/gen-headless-gvbindir.sh` first if stale).

## Observability / Rollback
N/A. Reversible — delete the entries/refs.

## Quality bar
Refs are headless-oracle output; goldens are red before T1.2. Return only the
structured result.

## Commit
`test(T1.1): add red goldens for edge routing order (lone-before-group)`
