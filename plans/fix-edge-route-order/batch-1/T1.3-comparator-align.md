<!-- SPDX-License-Identifier: EPL-2.0 -->

# T1.3 — Align edgeRouteCmp to C's order (CONDITIONAL)

## Context
**Only execute if T0.3 found the port's `edgecmp`/`edgeRouteCmp` does NOT
reproduce C's dispatch order.** If T0.2/T0.3 confirmed the port `edgecmp` already
matches C, mark this task `[x] N/A` in `batch-1/overview.md` and the journal and
skip it.

## Task (only if implicated)
1. From `comparisons/root-cause.md`, identify the exact comparator divergence
   (tie-break, key, or direction) between `edge-order.ts:edgeRouteCmp` and C's
   edge ordering (`lib/dotgen/dotsplines.c` edge list build / `edgecmp`).
2. Apply the **minimal** alignment to `edgeRouteCmp` so the unified pass routes in
   C's order. A non-trivial semantic change is a STOP condition (ADR-5) — re-plan.
3. Re-run the survey; confirm 0 regressions.

## Write-set
- `src/layout/dot/edge-order.ts` (`edgeRouteCmp`)

## Read-set
- `comparisons/root-cause.md`, `comparisons/c-order-oracle.md`
- `~/git/graphviz/lib/dotgen/dotsplines.c` (edgecmp / edge list build)

## Acceptance criteria
- Given the aligned comparator, when the unified pass runs, then the port's edge
  order matches C's (re-verify with a throwaway `DBG_ORDER` dump vs T0.1).
- Given `npm run survey && npm run survey:gate`, then 0 regressions.

## Observability / Rollback
N/A. Reversible — revert the commit.

## Quality bar
Minimal alignment only; if it needs comparator-semantics changes → STOP. Return
only the structured result.

## Commit
`fix(T1.3): align edge route comparator to C dispatch order`
