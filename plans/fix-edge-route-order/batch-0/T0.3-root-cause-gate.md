<!-- SPDX-License-Identifier: EPL-2.0 -->

# T0.3 — Root cause + GO/STOP gate

## Context
Batch 1 (the unify) is blocked until the order divergence is pinned and the fix
is confirmed contained to the dispatch files AND the port's `edgecmp` reproduces
C's order (ADR-5). This task converts T0.1/T0.2 evidence into a decision.

## Task
1. From `c-order-oracle.md` + `port-vs-c-order.md`, write
   `comparisons/root-cause.md`: the precise mechanism (two-pass split exposes
   group `recover_slack`/spline state to earlier-`edgecmp` lone edges), the
   order-sensitive shared states involved (recover_slack vnodes; top/bot_bound),
   and the exact dispatch change.
2. Confirm the **containment**: the fix is Option A (fold lone dispatch into the
   `dotSplines_` `edgecmp` loop, drop the second pass) and touches only
   `splines.ts` + `edge-route.ts` — with at most a trivial `edge-order.ts`
   alignment (T1.3) IF the port `edgecmp` diverged from C. If it needs comparator
   *semantics* changes or reaches into `routeRegularEdgeFaithful`/`recoverSlack`
   → record **STOP** and request re-plan (ADR-5).
3. Refine the Batch-1 split: confirm T1.2 write-set, decide T1.3 needed vs N/A.
4. Append a `decision-journal.md` row: GO or STOP, the one-line mechanism, the
   confirmed Batch-1 write-set, and whether T1.3 is in scope.

## Write-set
- `plans/fix-edge-route-order/comparisons/root-cause.md` (create)
- `plans/fix-edge-route-order/decision-journal.md` (append GO/STOP row)

## Read-set
- `comparisons/c-order-oracle.md`, `comparisons/port-vs-c-order.md`
- `decisions.md#adr-3`, `#adr-5`
- `batch-1/overview.md` (to refine the split)

## Acceptance criteria
- Given the evidence, then `root-cause.md` states the mechanism, the
  order-sensitive states, and the exact dispatch change.
- Given the file map, then it explicitly confirms GO (contained to
  `splines.ts`/`edge-route.ts` [+ trivial `edge-order.ts`]) or STOP (needs more).
- Given the port `edgecmp` check, then GO requires `edgecmp` reproduces C's order
  (or T1.3 trivially aligns it); otherwise STOP.
- Given a GO, then `decision-journal.md` records the confirmed Batch-1 write-set.

## Observability / Rollback
N/A — analysis only.

## Quality bar
Decision is evidence-backed (cites the dumps), not assumed. Return only the
root-cause doc + journal row.

## Commit
`docs(T0.3): root-cause + GO/STOP for edge routing order`
