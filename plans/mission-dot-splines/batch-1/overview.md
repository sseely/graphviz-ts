# Batch 1 — Measure-first divergence inventory

Single task. Before migrating any category, measure exactly which goldens shift
and which corpus cases match dot when regular edges are forced through the
faithful path. The inventory defines the real scope of Batches 2–6.

| ID | Description | Agent | Writes | Depends On | Done |
|----|-------------|-------|--------|------------|------|
| T1 | Force-faithful measurement harness + divergence inventory (no production behavior change) | opus | `.probes/dot-splines-faithful-measure.ts`, `plans/mission-dot-splines/decision-journal.md` | — | [ ] |

Gate per [../README.md](../README.md). One commit.
Commit: `chore(T1): measure faithful-routing divergence inventory`.
