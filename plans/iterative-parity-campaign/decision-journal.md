# Decision Journal — iterative-parity-campaign

Appended during execution. Same columns as `plans/decision-journal.md`.

| Date | Item/Task | Decision / Mechanism | Files | Gate result |
|------|-----------|----------------------|-------|-------------|
| 2026-07-12 | B1 parallelization | T2/T3 launched concurrently with T1: their T1-dependency is only the attribution-json SHAPE, which is spec-locked in T1's interface contract (not discovery-dependent); write-sets disjoint. Push-forward per brief constraints. | plans/iterative-parity-campaign/batch-1/ | n/a |
| 2026-07-12 | T2 class-acceptance reports | Added A1-drift class entries (neato/fdp/sfdp) to accepted-divergences-engines.json (D2); parity-report.ts computes membership from attribution-<engine>.json at report time, renders a link-only "Accepted class" section (roster-brevity), and wires T3's already-landed `renderOracleErrorsSidecar` (oracle-error-classifier.ts) into engineMarkdown. Judgment calls: (1) task's step-3 guard-test text says the referenced attribution file "must exist"; T1 hasn't run yet, so a hard-existence guard would fail this commit's own `npx vitest run` gate and contradicts the report's explicit "tolerate missing file → attribution pending" requirement — resolved by making the guard test check well-formedness ONLY when the file exists, never bare existence; logged here per prompting-quality/parallelism decision-logging rule since a reasonable dev could've blocked instead. (2) No guard test previously existed for accepted-divergences-engines.json at all (only the dot-track accepted-divergences.json was guarded) — created test/corpus/accepted-divergences-engines.test.ts as the natural analog rather than extending accepted-divergences.test.ts (which is scoped to the unrelated dot-track file/type), closing a pre-existing gap for the per-id shape too. (3) Added `isMain` guard to parity-report.ts (previously ran its file-writing script unconditionally at module scope, unlike survey.ts/engine-walk.ts/xdot-walk.ts) so the new pure functions are unit-testable without side effects — behavior-preserving for the CLI entry point (`npx tsx test/corpus/parity-report.ts`), verified by diffing PARITY-*.md before/after (circo/twopi/osage/patchwork byte-identical; neato/fdp/sfdp additive-only). Regenerated all PARITY-*.md once. | test/corpus/accepted-divergences-engines.json, test/corpus/parity-report.ts, test/corpus/accepted-divergences-engines.test.ts, test/corpus/parity-report.test.ts, test/corpus/fixtures/attribution-fixture.json, docs/known-divergences.md, test/corpus/PARITY-{neato,fdp,sfdp}.md | tsc clean; vitest 227/227 files, 3047/3048 tests green (1 unrelated pre-existing flake in src/pathplan/pathplan.test.ts "does not overflow the stack" — passes standalone, not in T2 write-set) |
| 2026-07-12 | T3 oracle-error classifier | oracle-error-classifier.ts reruns every parity-<engine>.json oracle-error/timeout row up to 3x with escalating execFileSync timeouts (60s/120s/240s) per D6: 3/3 fail = native-crash, any success = timeout-flake. Reuses engine-walk.ts's oracle invocation shape (single native execFileSync child — NOT the detached-spawn port-render path implicated in the 20h-orphan bug; execFileSync's timeout kills its own child directly, verified pgrep=0 after every run). Ran on ALL 7 non-dot engines (task extended beyond the 3 iterative ones per coordinator): neato 7nc/0tf, fdp 13nc/3tf, sfdp 7nc/0tf, circo 10nc/5tf, twopi 8nc/0tf, osage 5nc/0tf, patchwork 5nc/0tf. Recoverable flakes (oracle succeeded on rerun): fdp graphs-badvoro(a3)/2620(a1)/2108(a2); circo windows-labelroot-ntr(a1)/graphs-sl_box(a1)/1718(a2)/2343(a2)/2095_1(a3). Judgment calls: (1) sidecar lastErr preserves the ORIGINAL sweep err when the classifying attempt succeeded (err=''), so flake rows keep their context; (2) circo's two port-side 'timeout' rows classify timeout-flake-attempts=1 by D6's letter (oracle fine first try — the timeout was the port's, correctly excluded-not-written-off); (3) hook kept as standalone export renderOracleErrorsSidecar per coordinator's no-touch on parity-report.ts — T2 wired the call-site same-day. Notable: 2222/2108 (2.4/3.1MB graphs) are native-crash on neato/sfdp/circo/twopi even at 240s but 2108 FLAKES to success under fdp — the pair is timeout-class, not crash-class; a >240s budget would likely reclassify them (logged, not changed — D6 budget is locked). | test/corpus/oracle-error-classifier.ts, test/corpus/oracle-error-classifier.test.ts, test/corpus/oracle-errors-{neato,fdp,sfdp,circo,twopi,osage,patchwork}.json | tsc clean; vitest 227 files/3048 tests green; pgrep dot = 0 post-run; commit 5c1e549 (feat(T3)) |
| 2026-07-12 | T1 injection harness | Landed ed337d9 (harness) + d165453 (lock removal). Native `%.17g` POS_DUMP patch at `spline_edges` entry (raw pre-shift `ND_pos`, inches — NOT `ND_coord`, which isn't populated there); committed `GVTS_POS_INJECT` hook in splines.ts guarded BOTH by `typeof process` and `process.getBuiltinModule('node:fs')` — a static `node:fs` import breaks the esbuild browser bundle at BUILD time regardless of any runtime guard. Native patch reverted (`git -C ~/git/graphviz checkout -- lib/neatogen/neatosplines.c`, then `make gvplugin_neato_layout -j4`); verified tracked tree clean and a `GVTS_POS_DUMP=1` run now emits 0 dump lines. **DEFECT FOUND AND FIXED**: the T1 agent's sweep and the coordinator's resume ran CONCURRENTLY against one append-only jsonl; each snapshots its resume skip-set once at startup, so each re-attributed ids the other hadn't flushed → 649 rows for 491 unique ids, copied verbatim into attribution-neato.json (origin: the summary read-back had no dedupe). Would have double-counted in parity-report.ts's A1-drift class membership. Fixed at origin via `dedupeRows` (last-write-wins collapse). Free determinism check: all 158 duplicated ids agreed EXACTLY on verdict+injectedDiffs across the two independent processes — the harness is reproducible. A pid lockfile was also added by the agent; **user decision: dropped it** (d165453) — the race is operator-controlled, dedupe is the guarantee consumers need. Judgment calls: (1) regenerated the summary from the existing jsonl rather than burning ~40min on a re-sweep that would be byte-identical (determinism verified first); (2) added attribute-divergence.test.ts (17 tests) — the agent shipped none, unlike T2/T3, and `classifyBucket` is explicitly exported as pure/testable. KNOWN LIMITATIONS (carry into B2): (a) the D4 hash guard sha1s the `dot` BINARY, but the POS_DUMP patch lives in `gvplugin_neato_layout.dylib` — it cannot detect a patched-vs-clean plugin swap; the "no GVTS_POS dump lines captured" check is the actual (loud) backstop. (b) the D5 uniform-translation/mirror detectors only parse diff paths ending in `[n]`, so `_draw_` op paths (`.../op[1].ellipse.y`) never reach them — uniformDelta fired on only 8 of 369 not-cleared ids, and the single largest bucket (`graph/_draw_/numeric`, 252 ids) is exactly the one they can't see into. | test/corpus/attribute-divergence.ts, test/corpus/attribute-divergence.test.ts, test/corpus/attribution-neato.{json,jsonl}, src/layout/neato/splines.ts, plans/iterative-parity-campaign/diagrams/injection-recipe.md | tsc clean; vitest 228 files/3065 tests green; neato smoke 491/491 attributed, 0 dupes: 119 drift-exonerated / 369 not-cleared / 3 harness-error (graphs-pgram, share-pgram, windows-pgram — port render timeout under injection) |
| 2026-07-13 | B2 prep / compute_bb edge arm | **MECHANISM**: C's `compute_bb` (lib/common/utils.c:633) expands the graph box over every out-edge's spline control points AND its centre/head/tail/x labels; the port's `computeSubgraphBB` (src/layout/pack/index.ts:98) walked node extents ONLY. Any spline leaving the node hull fell outside the bb, and the background `filled_polygon` (drawn from bb) inherited the truncation — `graphs-b76` (one 54pt node + self-loop) reported a 54pt-wide graph vs the oracle's 72pt, because the loop bulges 18pt right. NOT a neato bug: `computeSubgraphBB` is the compute_bb equivalent for neato/fdp/sfdp/circo/twopi/osage (src/gvc/device.ts:493) + pack. Injection (T1) had already proven node PLACEMENT correct in these ids — only the box containing them was wrong. Fixed in 29d4f0b; point loop bounded by `bz.size` not `bz.list.length` (over-allocated lists). Deliberate deviation logged in-code: C's compute_bb also expands over node xlabels/cluster bbs/graph label, but the port grows g.info.bb for those in later dedicated passes (xlabel placement, addClusters, placeGraphLabel) — folding them in here would double-expand. **HONEST SCORECARD**: I first claimed this was ONE mechanism behind the whole 252-id `graph/_draw_/numeric` bucket. WRONG — I sampled the 3 SMALLEST ids by diff count, which shared the bb cause; the bucket's median is 54 diffs and max 12036. Actual result: +7 passes corpus-wide, ~8 ids out of the bucket. The fix is correct and real but modest. **The durable finding is that the bucket KEY was lying**: D5's `shape` (first-diff only) collapsed >=2 unrelated mechanisms because a graph-level numeric diff sorts first in both. Implemented D5's specified-but-missing count-vs-position split + a `signature` (full sorted set of objectType/attr/kind). Re-attribution on the real key: **286 of 364 not-cleared are `count` kind (a structural/missing-element diff) — emission gaps, not arithmetic**. True families: 121 cluster-geometry, 53 pure edge-routing position, **43 missing EDGE label (edge/_ldraw_/structural ALONE — cleanest single mechanism in the corpus)**, 25 missing GRAPH label (+graph bb). B3 rounds should chase these, NOT `shape`. | src/layout/pack/index.ts, test/corpus/attribute-divergence.ts, test/corpus/attribute-divergence.test.ts, test/corpus/parity-*.json/jsonl, test/corpus/attribution-neato.json, .agent-notes/compute-bb-omits-edge-splines.md | tsc clean; vitest 228 files/3065 green; **0 pass->diverged regressions on ALL 7 engines + dot survey**; +7 passes (neato +3: graphs-b76/b146/NaN; sfdp +2; circo +2). 4 apparent regressions were port/oracle TIMEOUTS from my own over-parallel sweeping — all 4 re-run to `pass` serially, none was a divergence. Native POS_DUMP patch re-applied for the runs then reverted (`git -C ~/git/graphviz checkout -- lib/neatogen/neatosplines.c` + `make gvplugin_neato_layout -j4`); verified tracked tree clean and 0 dump lines emitted. |
| 2026-07-13 | PROCESS: I caused the duplicate-append race I had just diagnosed | Checked sweep liveness with `pgrep -fc`, which prints nothing AND exits non-zero when the count is 0 — a silent result read as "no processes" while 6 sweeps were in fact still running. Concluded they were dead, launched 3 more against the same append-only jsonls, and corrupted parity-{fdp,sfdp,circo}.jsonl with duplicates (786/833/784 rows for a 761-id corpus). Recovery: killed all, discarded the corrupt jsonls, restored the two half-written summaries (osage/patchwork) from HEAD, re-ran fresh 2-at-a-time serialized, verified every engine reached exactly 761 rows BEFORE reading any count. Note the summary JSONs still held STALE COMMITTED BASELINES during the bail — trusting them would have produced a confident "no change on any engine" report. RULES: (1) never infer process liveness from a silent pgrep; (2) verify jsonl row count == 761 before believing a sweep summary (the brief already says this); (3) cap sweep fan-out — 6-way parallel is what made them look hung. **This is direct evidence AGAINST the d165453 decision to drop the sweep lockfile** (rationale was "concurrency is operator-controlled"): within the hour, the operator raced two sweeps. Recommend restoring `runExclusive`; NOT re-added unilaterally since dropping it was an explicit user decision. | (process only) | n/a |
| 2026-07-13 | T5/T6 fdp+sfdp attribution | Ran in one patched window (trap-revert EXIT so the native tree is restored even if a sweep dies). **sfdp** attributed clean first pass: 489 diverged → 113 drift-exonerated / 373 not-cleared / 3 harness-error. **fdp came back 429/429 harness-error — and that was the harness working.** It refused loudly (`no GVTS_POS dump lines captured`) rather than emitting 429 verdicts from un-injected renders; this is the backstop that actually protects the campaign, since D4's sha1 hashes the `dot` BINARY and the patch lives in the plugin, so the hash guard cannot detect a missing patch. MECHANISM: fdp never calls `spline_edges` — it has its own `fdpSplines` → `spline_edges1` (lib/fdpgen/layout.c:1034), so the neatosplines.c patch site is unreachable under -Kfdp. TRAP AVOIDED: the naive mirror (dump/inject at `fdpSplines` entry) would have been a SILENT NO-OP — `neato_set_aspect` runs first and derives ND_coord (points) from ND_pos (inches), and fdp routing reads ND_coord, so injected `pos` would be read by nothing; every id would return not-cleared and the run would have looked like a giant fdp defect tail instead of a broken harness. Correct site (both sides) is between `fdpLayout()` and `neato_set_aspect()` — still D1's pre-routing ND_pos stage. Added port hook `injectOraclePositions(g)` in fdpLayoutEngine before neatoSetAspect (inert unless GVTS_POS_INJECT set); exported the injector from neato/splines.ts. Recipe doc now documents both dump sites + the smoke-check-before-sweep rule. fdp re-run: 429 → 4 drift-exonerated / 424 not-cleared / 1 harness-error. **B2 EXIT CRITERION — the `clusteredges.c` lead for fdp is REFUTED**: only 24 of fdp's 424 not-cleared ids involve cluster geometry at all; 193 are a missing GRAPH label. **CROSS-ENGINE FINDING: the tails are emission gaps, not FP drift.** `count`-kind (a structural/missing-element diff) dominates every engine — neato 286/364, fdp 305/424, sfdp 291/373 — and fdp is drift-exonerated on only 4 of 429 ids. Two mechanisms span all three engines: MISSING GRAPH LABEL (fdp 169, neato 25, sfdp 24) and MISSING EDGE LABEL (`edge/_ldraw_/structural` ALONE: sfdp 46, neato 43, fdp 37 = 126 ids, the cleanest single mechanism in the corpus). These, not FP attribution, are B3's targets. | test/corpus/attribution-{fdp,sfdp}.json/.jsonl, src/layout/fdp/index.ts, src/layout/neato/splines.ts, plans/iterative-parity-campaign/batch-2/{neato,fdp,sfdp}-buckets.md, plans/iterative-parity-campaign/diagrams/injection-recipe.md | tsc clean; vitest 228 files/3074 tests green; native tracked tree verified clean after revert (both windows). fdp/sfdp port hooks are inert with GVTS_POS_INJECT unset (early return; full golden suite green). |
| 2026-07-13 | B3 round 3a — MISSING EDGE LABEL (edge/_ldraw_/structural, 126 ids) | **MECHANISM**: the neato-family engines skipped BOTH halves of the edge-label chain. (1) `common_init_edge` never ran — C's `neato_init_node_edge` (neatoinit.c:142), fdp `init_edge` (fdpinit.c:73) and `sfdp_init_node_edge` (sfdpinit.c:44) each run a SECOND loop over out-edges calling `common_init_edge`, which creates `ED_label(e)` and ORs `GD_has_labels |= EDGE_LABEL`; the port's `commonInitNodeEdge` (nodeinit.ts:332) loops over NODES ONLY despite its name, so the label object never existed and `has_labels` stayed 0. (2) `gv_postprocess` never ran — it hosts `addXLabels`, which is the NORMAL edge-label placement pass for every engine except dot (dot sets `ED_label(e)->pos` itself during its spline pass and then sets `EdgeLabelsDone`, the very flag that makes `addXLabels` early-return). Port pipelines stopped at `placeGraphLabel`. Both gaps confirmed by instrumenting `addXLabels` on BOTH sides (obj table, label sizes, placed positions, post-`updateBB` bb all match the oracle). Splines/node positions were ALREADY byte-identical to native — this was purely placement+emission, which is why it presented as a pure structural bucket. `gv_postprocess` gains C's `allowTranslation` param (fdp passes 0, layout.c:1076). RULED OUT: layout/routing bug (geometry byte-identical); emitter gating on a set/pos flag (same emitter emits correct edge `_ldraw_` under dot); `addXLabels` port being wrong (values match C exactly); `EdgeLabelsDone` leaking true (probe showed 0 — the gate failed on `has_labels=0`). **SURPLUS**: 208 ids fixed vs the 126 targeted — `addXLabels`' `updateBB` write-back also repairs bounding boxes that omitted the edge-label extent. **NAMED-OPEN-MECHANISM (D3)**: the port emits `lp=` for NO engine (dot included, and dot is conformant) because the corpus comparator's `POSITIONAL_ATTRS` is only `['pos','bb','width','height']` — `lp`/`xlp`/`head_lp`/`tail_lp`/`rects`/`lwidth`/`lheight` are ALL structurally invisible to it. That blindness is why this 126-id bug and the 218-id graph-label bug read as "conformant" for so long. User-directed follow-up: emit them, then widen the comparator. | src/common/postproc.ts, src/layout/neato/index.ts, src/layout/fdp/{index,init}.ts, src/layout/sfdp/{index,init}.ts, .agent-notes/edge-label-iterative-engines.md | tsc clean; vitest 228 files/3074 tests green. FRESH sweeps (jsonl deleted first, 761 rows each), **0 pass->diverged on all three**: neato 266->324 (+58), fdp 311->403 (+92), sfdp 262->322 (+58) = **+208**. dot survey 788 ids: **0 conformant->other**, 761->762 conformant (1652 recovered from a timeout flake). Commits 6e1bae2 + merge 0271abb. |
| 2026-07-13 | B3 round 3b — MISSING ROOT GRAPH LABEL (graph/_ldraw_/structural + graph bb, 218 ids) + shared graph_init [USER-APPROVED] | **MECHANISM**: neato/fdp/sfdp never created the ROOT graph label object. C creates it in the engine-agnostic `graph_init` -> `do_graph_label` (input.c:719), which `gvLayoutJobs` (gvlayout.c:81) runs for EVERY engine before dispatching to the layout. The port had **no `graph_init` analogue at all** — that call was replicated into each engine's own init, and the neato family called `doGraphLabel` only for CLUSTER subgraphs, never the root. So `g.info.label` stayed `undefined` -> `expandBbForRootLabel` short-circuited on its `!label` gate (postproc.ts:314, mirroring C's `if (GD_label(g) && !GD_label(g)->set)`) -> GD_bb never grew by the padded label height -> Offset computed from the unexpanded bb, so translateDrawing never shifted the drawing up -> `placeRootLabel` also short-circuited -> no `_ldraw_`. Both symptoms (missing graph `_ldraw_`, bb short by exactly 24.8 = dimen.y 16.8 + YPAD 8) follow from ONE missing object. RULED OUT by probe at gvPostprocess entry: `label=undefined, has_labels=0` — there was no object to have `set` on it (candidate "something pre-sets label.set" refuted); bb ENTERING the pass already matched C bit-for-bit, so the expand/place/offset arithmetic was never at fault. **STRUCTURAL FIX (this was the 3rd defect of the same class after `common_init_edge` and `gv_postprocess`)**: ported `graph_init` (input.c:600) as `src/common/graph-init.ts`, called ONCE by all 7 engines. `useRankdir` is true for dot ONLY — grep-verified that `plugin/dot_layout/gvlayout_dot_layout.c` is the sole setter of `LAYOUT_USES_RANKDIR`; C's SET_RANKDIR then stores effective rankdir in bits 0-1 and real rankdir in bits 2-3, and the port's old dot-only `dotGraphInit` hardcoded the use_rankdir=true form. **INCIDENTAL DEFECTS FOUND**: (1) `setClType` had ZERO callers — `clusterrank` was silently ignored port-wide (now wired per input.c:706; no corpus file sets it, so output unchanged). (2) **sfdp had no rankdir handling whatsoever** — this is why sfdp gained +178 against a 24-id graph-label bucket. (3) An IMPORT CYCLE (graph-init -> nodeinit -> shapes -> compass-port -> dot/init -> graph-init) made a module-scope `const RANK_CODES = [LOCAL,...]` initialise to `undefined` — no TDZ throw, just a silently wrong value that disabled dot's cluster collapsing (8 test failures). Fixed at origin by building the code tables at CALL time. DELIBERATELY NOT CONSOLIDATED (would move dot output, left documented): C's unconditional `GD_drawing` alloc — `drawing === undefined` is a load-bearing guard at dot/position-bbox.ts:142, and allocating it would activate the unported ratio=expand|value|auto reshapes. **1652 IS NOT A REGRESSION**: it flipped conformant->timeout on the dot survey; A/B proves the cause is not this change — port @HEAD 1022.2s vs @8f6680a 1077.0s (HEAD marginally FASTER) with BYTE-IDENTICAL output (1851961 bytes both). Native itself takes 237.7s on this 8155-line graph; the port is ~4.3x native against a 3x budget, so 1652 sits permanently on the wrong side of the timeout line and oscillates with machine load. Recorded as a named-open-mechanism (D3) perf gap, excluded per D6; NOT a port defect introduced here. **HARNESS COUPLING FOUND**: engine-walk.ts:79-80 derives its id set from parity.json filtered to `verdict==='conformant'` — i.e. the ENGINE sweep universe is a function of the DOT survey. A single dot timeout flake silently changes the engine corpus size (761 -> 762 when 1652 went conformant in 3a). Engine baselines are not strictly comparable across runs where parity.json changed; the regression comparator must key by id, never by count. | src/common/graph-init.ts (new), src/layout/{dot/init,neato/index,fdp/index,sfdp/index,sfdp/init,circo/index,twopi/index,osage/index,patchwork/index}.ts, .agent-notes/{graph-label-iterative-engines,graph-init-consolidation}.md | tsc clean; vitest 228 files/3074 tests green. FRESH 7-engine sweeps (jsonl deleted first, 762 rows each), **0 pass->diverged on ALL SEVEN**: neato 324->403 (+79), fdp 403->579 (+176), sfdp 322->500 (+178); circo/twopi/osage/patchwork 0 changed, 0 regressions (graph_init is behaviour-neutral for them). dot survey: 0 real regressions (1652 timeout-boundary, A/B-exonerated above). Commits 0fa406a + 175a9bb + merge. **Cumulative 3a+3b: neato 266->403, fdp 311->579, sfdp 262->500 = +643 ids, zero regressions.** |
| 2026-07-13 | (b) EMIT + COMPARE the computed label attrs [USER-DIRECTED] + record-node xlabel drop | **THE COMPARATOR WAS BLIND.** The port emitted NONE of the computed label attributes for ANY engine — `lp`, `xlp`, `head_lp`, `tail_lp`, `rects`, `lwidth`, `lheight` — and `compare-xdot.ts`'s `POSITIONAL_ATTRS` was only `['pos','bb','width','height']`, so none of them were ever diffed. That blindness is precisely why the 126-id edge-label and 218-id graph-label bugs read as "conformant" for so long: the port could place a label anywhere, or nowhere, and the corpus would not notice. All seven now emit per C's `attach_attrs_and_arrows` (output.c) and the comparator diffs 11 attrs, not 4. Two conditions corrected against the oracle: `rects` is **record-only** (C strcmps the shape NAME — Mrecord and HTML tables get none), and all four edge label attrs live INSIDE the loop that writes `pos` (output.c:349-353), so an IGNORED/unrouted edge gets none of them even when it has a label. `lwidth`/`lheight` are the only computed attrs in INCHES at `%.2f`; the rest are points at `%.5g`. **RESULT: widening 4->11 attrs revealed ZERO new divergences on dot (788 ids, 761 conformant both sides) and ZERO on neato/fdp/sfdp (pass held at 403/579/500).** The port's label geometry was ALREADY correct everywhere — it was simply never written out and never checked. The blind spot was emission+verification, not computation. **PATCHWORK IS THE EXCEPTION — 173 ids, and it is a REVEAL, not a regression** (proven, not assumed): every diff is `<absent> vs <value>` (port missing), and at the pre-(b) commit 82efb7d the port emitted 0 `lp`/`rects` under patchwork vs native's 2 on share/record2.gv — i.e. the gap pre-dates the comparator change; a fix that ADDS xlabels cannot REMOVE lp/rects. patchwork 757->583 pass: its near-perfect score was partly an artifact of the blind comparator. Trigger is NOT universal (a simple `digraph{label="G";a->b[label="e"]}` emits lp correctly under all 8 engines, patchwork included) — it is conditional on CLUSTERS (cluster lp absent -> the labelclust-* ids) and RECORD nodes (field boxes never populated -> rects absent). Same mechanism family as 3a/3b, 4th engine. **=> ROUND 3C TARGET.** | **RECORD XLABEL (corpus-invisible)**: `nodeinit.ts` record branch early-returned past `initNodeXLabel`; C's `common_init_node` creates `ND_xlabel` UNCONDITIONALLY for every shape (utils.c:443) — `SH_RECORD` only sets `make_label`'s is_record flag and selects `initfn`. NOT merely a missing attr: the absent xlabel shortened `bb` and MOVED NODE POSITIONS (repro bb 0,0,73.448,109 vs oracle 0,0,74.221,125.8). C creates the xlabel BETWEEN `make_label` and `initfn`, so `recordNodeInit` was split into `recordMakeLabel` + `recordInit` to preserve C's side-effect ORDER rather than appending the call and relying on the two commuting. record INTERSECT xlabel is **empty across all 180 corpus files** — no sweep can ever catch this — so it ships with the hand-written regression test (24 tests, red-green validated: reverting the fix fails 13 of them while the `box` controls still pass; asserts oracle-literal values xlp="10.11,117.4", bb="0,0,74.221,125.8"). | src/render/dot.ts, test/golden/compare-xdot.ts, src/common/nodeinit.ts, src/common/record.ts, src/common/nodeinit.test.ts, .agent-notes/{xdot-missing-computed-attrs,record-node-xlabel-drop}.md | tsc clean; vitest 228 files/**3098** tests green (+24). FRESH 7-engine sweep + dot survey under the 11-attr comparator: dot 761 conformant (unchanged, 0 conformant->other); neato 403 / fdp 579 / sfdp 500 (all held, 0 regressions); circo/twopi/osage 0 regressions; **patchwork 757->583 (173 revealed, see above — NOT a regression)**. Commits b18e6b9 + f5c9de9 + merge. |
| 2026-07-13 | B3 round 3c — patchwork `[missing]` lp/rects (173 ids) — **MY LEAD WAS WRONG** | **THE HYPOTHESIS I HANDED THE AGENT WAS REFUTED, and that is the finding.** I scoped this as the 4th instance of the missing-call-site class (after `common_init_edge`, `gv_postprocess`, `graph_init`) — patchwork must be skipping some shared init. **It is not.** MECHANISM: C's `attach_attrs_and_arrows` (output.c) writes each computed attr BEHIND A GATE; when the gate fails C does not write — but the attribute slot STILL HOLDS THE VALUE THE INPUT FILE PARSED INTO IT, and `agwrite` serializes the WHOLE attribute table, not just computed fields. So **native ECHOES stale input values back out**. The graphviz corpus is largely re-fed dot output, so those slots are full: share/record2.gv ships `rects="23,108,..."`, labelclust-*.gv ship cluster `lp="43,168"`, longflat.gv ships edge `lp="160,54"`. Under -Kpatchwork C computes NONE of them (`patchwork_init_node` patchworkinit.c:74 agsets every node to shape=box; patchwork routes no edges; patchwork/circo/twopi/sfdp lay out no clusters) — yet native prints them verbatim, sitting next to a freshly computed `bb`. C's gates: output.c:314 (`rects` iff shape NAME is "record"), :351 (edge loop `continue`s on `ED_spl(e)==NULL`), :239 (`lp`/`lwidth`/`lheight` iff `GD_label(g)`), :249 (`bb` only for root + GD_clust). The port's xdot writer modelled **computed-or-nothing**; C models **print the whole table**. RULED OUT (by controlled experiment, not assertion): (1) a missing call-site — on a CLEAN HAND-WRITTEN input (root label + 2 labelled clusters + labelled edge) port and native ALREADY AGREE EXACTLY under -Kpatchwork, both emitting no cluster lp and no edge lp; nothing is missing from layout. (2) data genuinely absent in the port — stripping `lp=`/`rects=` from the corpus input makes NATIVE's values vanish too (rects count 2 -> 0; only the computed root lp survives). I re-verified this myself before merging. FIX: `echoAttr` in src/render/dot.ts — every attach_attrs-owned attr (`rects`, node/edge `xlp`, edge `lp`/`head_lp`/`tail_lp`, graph/cluster `lp`/`lwidth`/`lheight`, non-cluster subgraph `bb`) echoes the input value when C's gate fails, matching agwrite. **CROSS-ENGINE**: circo/twopi/**sfdp** SHARE the defect — they showed 0 regressions only because `collectGraphs` (compare-xdot.ts:189) collects a subgraph ONLY if it carries a **draw** attr, and those engines draw no cluster box; patchwork draws one, which is the SOLE reason its `cluster0/lp[missing]` surfaced. A second comparator blind spot, found only because the first one was closed. **osage does NOT share it** — it lays out clusters and computes a real cluster lp (58,77.2); port already matched. **DURABLE TRAP**: "attribute missing => call-site missing" was right 4x today and WRONG here. Distinguish with a clean hand-written input: if port and native agree there but differ on the corpus file, it is an ECHO, not a computation. | src/render/dot.ts, .agent-notes/patchwork-cluster-lp-record-rects.md | tsc clean; vitest 228 files/3098 tests green. FRESH 7-engine sweep + dot survey: **patchwork 583 -> 757 pass, 0 diverged (+173, bucket CLOSED)**; circo 744 / twopi 740 / osage 750 / neato 403 / fdp 579 / sfdp 500 — **0 pass->diverged on ALL SEVEN**; dot 762 conformant, 0 conformant->other. Commit 3b8612a + merge. |
| 2026-07-13 | B3 round 3d — fdp `graph/_draw_+graph/bb` (60 ids) — **A HARNESS ARTIFACT, NOT A PORT DEFECT. MY LEAD WAS WRONG (2nd round running).** | I scoped this as a real bb-expansion bug and told the agent injection had already exonerated node placement. **Both claims were wrong, and the agent refuted them with evidence rather than confirming me.** MECHANISM: fdp's `GD_bb` is a product of `fdpLayout` ITSELF (`finalCC` -> `setBB`, layout.c:1030), which has already run and RETURNED by the time the injection hook fires (`src/layout/fdp/index.ts:89`, after `fdpLayout(g)`). Injecting the oracle's `ND_pos` therefore overwrote the node coordinates while leaving the port's own drifted bb in place — emitting **native's nodes inside the port's bounding box**, a bb belonging to neither layout. `graph/_draw_` diverges only because the background `filled_polygon` is drawn FROM the bb; it is a consequence, not a second bug. DECISIVE EVIDENCE: **the port's bb is BIT-IDENTICAL with and without injection** (1909: 0,0,114.43,131.4 both ways) — injection changed the node positions to native's exactly and changed the bb by nothing, so it was never measuring what the not-cleared verdict claimed. RULED OUT: (a) the bb formula / missing compute_bb edge-spline expansion (the lead I gave) — on EACH side the bb equals THAT SIDE's own node extent to 0.01; same formula, different input; fdp never calls compute_bb on the root at all. (b) "port bb is systematically short" — refuted by POPULATION: of 42 family ids the port's bb is short on 24 and **TALLER on 18**; no bb-shortfall bug produces a taller bb. I had generalised from two hand-picked examples that both happened to be short — exactly the error this journal already warned about. (c) native echoing a stale input bb (the 3c trap) — refuted: both inputs are 3-line hand-written files with no `bb=`. FIX (harness, not port): the dump/inject hook now carries `GVTS_BB <llx> <lly> <urx> <ury>`; for fdp the bb IS layout-stage state exactly like `ND_pos`. **fdp-only and load-bearing**: neato/sfdp reach the hook at the top of `spline_edges`, ONE LINE ABOVE the `compute_bb` that derives GD_bb (neatosplines.c:841), so their bb is already a pure function of the injected positions; fdp is the exception. Omitting the line silently manufactures a phantom defect family. **HONEST SCORECARD**: this does NOT improve parity-fdp.json — those ids still diverge in the un-injected sweep. It RECLASSIFIES them: routing/emission are proven faithful given native positions, so the residue is fdp's force-layout converging differently from native (real, separate, still open). Progress toward D3's completion bar, not toward pass-rate. **CORRECTED ATTRIBUTION**: the broken injection was inflating fdp's real-defect count by **78 ids** (drift-exonerated 13 -> 91; not-cleared 153 -> 75). **CAMPAIGN TRUTH: of 772 diverged ids across neato/fdp/sfdp, 549 (71%) are layout FP drift (D2 class) and only 217 are genuine defects.** The 217 are a fragmented long tail — largest family is 26 ids, no dominant mechanism; 39 of them are `count`-kind (still-structural) and therefore the most tractable. | src/layout/neato/splines.ts, test/corpus/attribute-divergence.ts, plans/iterative-parity-campaign/diagrams/injection-recipe.md, test/corpus/attribution-{neato,fdp,sfdp}.json, .agent-notes/fdp-graph-bb-short.md | tsc clean; vitest 228 files/3098 green. Hook is inert without `GVTS_POS_INJECT`: **130/130 renders byte-identical** across fdp/dot/neato/sfdp (dot 45/45) — no corpus sweep required, and none run. Native C tree patched session-local twice, reverted+rebuilt clean both times (sha1 back to 5caf7a368dae, 0 dump lines). Commits 8d0db41 + merge + 09525d3. |
| 2026-07-14 | B3 round 3e — edge-spline PIECE COUNT (68 not-cleared ids, 31% of the real-defect surface) | **PROCESS FAILURE FIRST: the diagnosis agent branched from a STALE BASE (`0e8d27e`, which predates rounds 3a/3b/graph_init/3c/3d) and asserted it "contains 223ba48" — false.** Its vitest baseline came back 3011 vs the campaign's 3098, and it "discovered" that neato/fdp/sfdp never run `common_init_edge` — which round 3a had already fixed hours earlier. Its branch conflicted with 5 files and merging it would have risked reverting live work. **I discarded its patch entirely and re-derived both mechanisms against the C myself on the correct HEAD.** RULE: always check `git merge-base --is-ancestor <tip> <agent-branch>` before merging an agent's work; a wrong base makes a competent agent produce confidently wrong archaeology. (Memory already said "re-verify PASS claims" — extend it to "re-verify the BASE".) **MECHANISM A — setEdgeType macro-vs-function.** C's `setEdgeType` (lib/common/utils.c:1423) is a FUNCTION: it reads `agget(g,"splines")` and uses its argument only as the DEFAULT (`if (!s) et = defaultValue; ... GD_flags(g) |= et`). The port has BOTH forms — the macro `setEdgeType` (const.h, force-writes the nibble) and `setEdgeTypeFromAttr` (the function) — and circo/twopi/osage already called the right one. **neato/fdp/sfdp called the MACRO**, so `splines=true` was silently IGNORED, the edge type stayed EDGETYPE_LINE, and routing fell through to `makeStraightEdges` (ONE cubic) where C runs `makeMultiSpline` (several). Verified by my own repro on 2168.dot (`splines=true` + headports `node2:sw`/`node2:ne`): native fdp/sfdp `B 7`, port `B 4` → after fix `B 7`, dot unchanged at `B 10`. Five port sites mirror the five C sites: neatoinit.c:598,1398; sfdpinit.c:54,284; fdpgen/layout.c:1004. **SECOND HALF I had to find myself**: `setEdgeTypeFromAttr` must resolve the attr UP THE ANCESTOR CHAIN. C's `agget` does; the port's `g.attrs` Map holds only what was written on THAT graph — verified directly: a subgraph returns `undefined` for `splines` where the root holds `"true"`. Without the walk-to-root, the per-component call sites (neato's `gc` in layoutComponents, sfdp's `sg`) read undefined and silently take the default, so the fix would have been a no-op exactly where components are laid out. No-op on the root, so dot is untouched. **MECHANISM B — CDT cavity winding (independent).** GTS's `gts_delaunay_add_constraint` accumulates `left`/`right` with `g_slist_PREPEND` (reverse-walk order); the port accumulates with `push` (walk order). The port copied C's `reverse(right)` LITERALLY, which — given the opposite build order — inverts BOTH cavity rings' winding. `triangulatePolygon`'s `orient(v1,v2,v3) >= 0` ear test then rejects every candidate (instrumented determinants −400.6 / −52.3 where C's order gives +52.3 / +400.6), the `if (!found) return` bails silently, the cavity is left UNFILLED, and the constraint edge belongs to no triangle → `findMap` throws "no triangle for segment". **Latent whenever a constraint cuts exactly ONE triangle** (each list holds a single edge, so the reversal is a no-op) — which is why the multispline port survived until edge-type resolution started actually routing. Fixed at src/layout/neato/cdt-surface.ts:addConstraint. RULED OUT (agent, on its stale base but still valid evidence): pathplan fitter/`Proutespline` (never entered — the probe never fired); obstacle geometry/`Plegal_arrangement` (obstacle vertices reproduce C's `circumscribed_polygon_corner_about_ellipse` exactly, and C reports `legal`); robust predicates (rejected ears were far outside any ULP band); the parser (`headportAttr="sw"` was present — only the init consumer was missing). | src/layout/dot/index.ts, src/layout/neato/index.ts, src/layout/fdp/index.ts, src/layout/sfdp/index.ts, src/layout/sfdp/init.ts, src/layout/neato/cdt-surface.ts, .agent-notes/edge-spline-piece-count.md | tsc clean; vitest 228 files/3098 green. FRESH 7-engine sweep + dot survey: **0 pass->diverged on ALL SEVEN**; fdp 579->600 (+21), sfdp 500->508 (+9), neato 403->409 (+6) = **+36**; circo/twopi/osage/patchwork unchanged; dot 762 conformant, 0 conformant->other. Commit 44673ca. |
| 2026-07-14 | CORPUS BLIND-SPOT SCAN — "corpus silence is not coverage" | **THE PROBLEM**: the xusuxe crash survived every sweep we have ever run (thousands of ids x 7 engines) because **0 of 260 dot corpus graphs reach `abomination` with clusters present**. Line/branch coverage would call `abomination` covered — plenty of graphs reach it. What no graph reaches is the CONJUNCTION. **METHOD**: built `test/corpus/blind-spots.ts` — a feature CO-OCCURRENCE matrix over all 805 corpus inputs (22 syntactic feature detectors: cluster, pack, concentrate, minlen0, ranksame, edgelabel, headtaillabel, xlabel, record, htmllabel, compound, ports, samehead, splines, rankdir, ratio/size, newrank, constraint, ordering, selfloop, fixedsize, invis). It reports DARK CELLS (both features present in the corpus, but never in the same graph) and THIN ICE (a pair carried by only 1-2 graphs). **SELF-VALIDATING**: the matrix independently rediscovered the bugs we had just fixed — `record x xlabel` = 0 graphs (the record-xlabel drop, which the fixing agent had separately confirmed as "empty across all 180 corpus files"), and `pack` is where 3 of this audit's defects lived. **74 dark cells** in total. **HEADLINE FINDING — `pack` coverage is a single point of failure.** `pack` appears in only 8 of 805 graphs and co-occurs with NOTHING: 0 graphs pair it with concentrate, record, ports, samehead, headtaillabel, ordering, newrank, constraint or fixedsize. Worse, of the pairs that do exist (`pack x minlen0`, `pack x ranksame`, `pack x xlabel` = 2 graphs each), **2782 and 1494 are QUARANTINED (malformed) and never run** — so those cells are effectively n=1, and `pack x splines/ratio/nestedcluster/invis` rest on ONE graph (2592.dot). `pack` is precisely the switch that makes `g !== g.root`, the condition that HID all three defects this audit found (`has_labels` written to the component but read from the layout graph; `flat.ts` reading `e.tail.root` instead of `dot_root`; the rank sentinel). The entire subsystem was guarded by one graph. **PROBED the 8 highest-risk dark cells** with hand-authored minimal inputs (pack x {concentrate, record-ports, samehead, ordering, headtaillabel}, concentrate x {headtaillabel, samehead}, record x splines, cluster x samehead): **all 8 PASS** under the full 11-attribute comparator. No new bugs — the code is correct there, it had simply NEVER BEEN RUN. That is the honest result and still the point: the scan converts unknown into known. **ONE SURPRISE WORTH LOCKING IN**: `pack x headtaillabel` revealed that **native ITSELF drops `_hldraw_`/`_tldraw_` under pack** (without pack both sides emit them; with pack neither does). The port faithfully matches. This REFUTES the follow-up defect the flat/dot_root agent had predicted (`placePortLabels` gating on `has_labels` where C has no gate) — the observable behaviour is identical to C. It is exactly the kind of surprising C quirk a well-meaning future "fix" would break, so it ships as a golden fixture. **DELIVERABLE**: 9 new golden conformance fixtures closing the highest-risk dark cells, refs generated from the native oracle (dot-pack-{concentrate,record,samehead,ordering,headtail-label}, dot-conc-{headtail,samehead}, dot-record-splines, dot-cluster-samehead). Manifest 168 -> 177. Plus the scanner itself, committed and repeatable, so the next feature added to the port can be checked against the matrix before it ships. | test/corpus/blind-spots.ts (new), test/golden/inputs/*.dot (9), test/golden/refs/*.svg (9), test/golden/refs-xdot/*.xdot (9), test/golden/manifest.json, test/golden/suite.test.ts | tsc clean; vitest 230 files/**3136** tests green (+18 = 9 fixtures x SVG+xdot suites). All 9 new goldens PASS against native-generated refs. |
| 2026-07-14 | CLOSE the `pack` dark cells — and an UPSTREAM GRAPHVIZ SEGFAULT | `pack` was the campaign's worst blind spot: 8 of 805 upstream graphs, co-occurring with NOTHING, and it is the switch that makes `g !== g.root` — the condition that hid all three defects the 2026-07-14 audit found. Authored and PROBED the remaining dark cells before locking them in (the point of a fixture is to find bugs, not to decorate). **3 closed, all passing against native**: `dot-pack-constraint` (pack + constraint=false + cluster), `dot-pack-fixedsize` (pack + fixedsize nodes in a cluster), `dot-pack-selfloop` (pack + labeled/in-cluster/cross-component self-loops; was thin ice at 1 upstream graph). **`pack x newrank` CANNOT BE CLOSED — native graphviz SIGSEGVs.** Minimal trigger (both `pack` AND `newrank` required; drop either and it renders): `digraph G { pack=true; newrank=true; subgraph cluster0 { a; b; } a -> b; b -> c; { rank=same; c; d; } }` — `dot -Txdot` exits **139 with zero output** on BOTH our pinned oracle build (15.1.0~dev.20260610) and the RELEASED Homebrew build (15.1.0), so it is an upstream graphviz defect, not our build. **The port renders it correctly** (`bb="0,0,164,188"`). Per the A4 policy (no-replication != keep an unfaithful mechanism) we do NOT reproduce the segfault to match the oracle. `pack x newrank` is therefore a **permanently dark cell** — no conformance fixture can exist for it, which is a property of the ORACLE, not a gap in our coverage. What we can defend without a reference is locked in: `test/corpus/oracle-segfault-pack-newrank.test.ts` asserts the port completes and emits a non-degenerate, finite bb with all four nodes surviving component projection (not a vacuous "did not throw"). **SCANNER CORRECTIONS** (found by asking "how many cells are now covered?"): (1) the scanner walked only the upstream corpus, so it could not see coverage added by our own goldens and would report a cell dark after we closed it — it now scans both trees (`--upstream-only` preserves the old view); (2) the `ports` detector matched only HEAD ports (`a -> b:f1`) and missed TAIL ports (`a:f0 -> b`), so 2 of the originally-reported "74" were false positives and `pack x ports` was mislabelled dark when a pack fixture already covered it. **True upstream count is 72.** Also closed `record x xlabel` — the cell the record-xlabel drop ACTUALLY lived in; that bug shipped with a UNIT test but no conformance fixture, so the exact combination that broke was still never exercised against the oracle. Lesson: **fixing a bug and unit-testing it does not close its dark cell; only an oracle-referenced fixture does.** | test/golden/inputs/dot-pack-{constraint,fixedsize,selfloop}.dot + refs, test/golden/inputs/dot-record-xlabel.dot + refs, test/corpus/oracle-segfault-pack-newrank.test.ts, test/corpus/blind-spots.ts, test/golden/manifest.json, test/golden/suite.test.ts | tsc clean; vitest 231 files/**3145** tests green. Dark cells **72 upstream -> 57 with goldens (15 closed)**. `pack` dark cells: 4 -> **1**, and that one is unclosable (oracle segfault). |
| 2026-07-14 | ALL PAIRWISE DARK CELLS CLOSED (72 -> 1) — and the honest limit of that | Greedy set-cover over the 56 closeable dark pairs said **9 dense fixtures suffice**; authored all 9, PROBED each against native under the full 11-attribute comparator BEFORE locking any in (a fixture's job is to find bugs, not to decorate), and **all 9 PASS**. Dark cells **72 upstream -> 1**. The single survivor is `pack x newrank`, which is **permanently unclosable**: native graphviz SIGSEGVs (exit 139, zero output) on `pack + newrank + cluster + rank=same`, on BOTH our pinned oracle build (15.1.0~dev) and the RELEASED Homebrew 15.1.0 — an upstream defect, not our build. The port renders it fine. Per A4 we do not reproduce a segfault to match the oracle; `test/corpus/oracle-segfault-pack-newrank.test.ts` pins the port's robustness instead (finite, non-degenerate bb; all nodes survive component projection — NOT a vacuous "did not throw"). **THE HONEST LIMIT — pairwise coverage is a FLOOR, NOT A CEILING.** 22 features = 231 pairs / 1540 triples / 7315 quads. Exhaustive triple coverage is infeasible, and **the bugs we actually found this session were 3- and 4-way conjunctions**: the xusuxe crash needed `cluster x minlen0 x edgelabel x edgeless-cluster-member` (4-way); the pack/has_labels defect needed `pack x multi-component x flat-edge x edgelabel` (4-way). Only `record x xlabel` was a pair. **So closing all 231 pairs would NOT have caught 2 of the 3 defects.** What pairwise closure buys is a cheap, complete, re-runnable floor — and a gate for every NEW feature added to the port (any new detector immediately shows its dark cells against all existing features). What it does NOT buy is safety from conjunction bugs; those need risk-weighted triples chosen by SUBSYSTEM (rank x cluster x component is the danger zone, per every defect found), not exhaustive enumeration. Recorded as the next avenue rather than claimed as done. | test/golden/inputs/dot-gap-*.dot (9) + refs (SVG+xdot), test/golden/manifest.json (181 -> 190), test/golden/suite.test.ts | tsc clean; vitest 231 files/**3163** tests green (+18 = 9 fixtures x SVG+xdot suites). All 9 PASS against native-generated refs under the 11-attr comparator. |
| 2026-07-14 | `pack x newrank` UPGRADED: A4 oracle-bug where the PORT IS PROVABLY CORRECT | **USER CHALLENGE**: "I claim our output is correct — the grouping box disappears quickly under several scenarios in native; ours stays consistent." I had recorded this cell as unvalidatable ("we render it, but with no oracle we cannot claim our output is right"). **The user was right, and it is now proven.** REFINED TRIGGER (from the user): the last ingredient is NOT a `rank=same` set, as I first minimised — it is a single **bare, edge-less node** (`d;`). Under `pack`, an isolated node becomes **its own component**, and it is that extra component that tips native over. `digraph G { pack=true; newrank=true; subgraph cluster0 { a; b; } a -> b; b -> c; d; }` -> SIGSEGV. Delete `d` -> renders. (Same "degree-0 member" shape as the xusuxe crash, which needed an edge-less second cluster member.) **PROXY-ORACLE ARGUMENT (this is what makes correctness assertable)**: (1) `newrank` is **layout-neutral for this graph shape** — on the variant native CAN render (drop `d`), `dot -Txdot` emits BYTE-IDENTICAL output with and without `newrank` (`bb="0,0,86,196"`, cluster0 `bb="8,64,78,188"`). (2) Therefore native's own output for `pack + cluster + d` MINUS `newrank` is a legitimate oracle for the crashing graph: `bb="0,0,164,188"`, cluster0 `bb="0,64,70,188"`. (3) **The port reproduces that exactly.** So we are not asserting our answer is *plausible* — it is **the oracle's own answer, which the oracle cannot reach because it dies first**. I had also suspected our cluster box was wrong (LL.x=0, flush to the graph edge, no 8pt inset) — **that suspicion was WRONG**: native packs components flush the same way; its no-newrank output has the identical `0,64,70,188`. Test upgraded from a robustness assertion (finite, non-degenerate bb) to a **conformance assertion against the proxy oracle**, plus a guard test that `newrank` stays layout-neutral for this shape (if it ever diverges, the proxy argument needs re-deriving). Classification: A4 oracle-bug, port CORRECT, native BROKEN — not "unvalidatable". Reproduces on released Homebrew 15.1.0 (`-Txdot` and `-Tsvg`), so not an artefact of our build. Not reported upstream yet; the WASM build (graphvizonline) does NOT crash, which is consistent with a genuine OOB/uninit read — flat linear memory absorbs it and returns garbage, so it manifests there as the vanishing cluster box the user observed rather than as a fault. | test/corpus/oracle-segfault-pack-newrank.test.ts | tsc clean; vitest 231 files/3165 tests green. |
| 2026-07-14 | RISK-WEIGHTED TRIPLES — and a REAL BUG: `newrank` silently ignores `minlen=0` | **PROCESS FAILURE FIRST, and it invalidated two sweeps.** CONTINUE.md asserted the previous session's gating sweep was "dead, leaving half-written parity files"; I believed it, ran `git checkout -- test/corpus/`, and launched a fresh sweep. **It was not dead** — it was 2h04m into a legitimate fresh sweep (correct `rm -f` per-engine discipline), on its 6th engine (osage), and `parity-*.jsonl` are **git-TRACKED**, so my checkout reverted its outputs *underneath it* while my own survey wrote the same files concurrently. Both sweeps were thereby contaminated and both were discarded; killed everything, restored `test/corpus/` to HEAD, and ran ONE clean sweep on the final tree. **RULE: before acting on a handoff's claim that a process is dead, `pgrep` it.** A stale-process claim is a hypothesis, not a fact — the same "instrument before narrating" discipline applies to the environment, not just to the code. **THE HUNT.** All 231 pairwise dark cells were closed, but every real bug this campaign has found was a 3-4 way conjunction in one zone: rank x cluster x component-projection (`pack` is the switch that makes `g != g.root`). Authored 15 fixtures there and PROBED each against the native oracle before locking any in. 14 passed. **One diverged — and bisection collapsed it to a THREE-LINE graph with no cluster and no pack at all**: `digraph G { newrank=true; a -> b [minlen=0]; b -> c; }` — oracle 2 ranks (a,b flat, minlen=0 honored), port 3 ranks (bb 180 vs 108, exactly one rank of 72pt). The same graph without `newrank` passes, so **only the dot2/newrank path loses minlen=0**. (My first bisect wrongly concluded "two clusters required": the reduced graphs s3/s4 were *contradictory* constraint systems where cycle-breaking masked the difference — a vacuous agreement, not a passing test.) **MECHANISM (origin).** `rank-dot2.ts:102 xgAddEdge` builds aux-graph (Xg) edges with `new EdgeClass(...)`, leaving `info.minlen`/`info.weight` **undefined**. C builds the same edges with `agedge(Xg,...)` on a **calloc'd** struct: `ED_minlen == 0`, `ED_weight == 0` — NOT the user-edge default of 1. The accessors `eMinlen = info.minlen ?? 1` / `eWeight = info.weight ?? 1` hand back 1 where C has 0, so `xgMerge` (rank.c:761 merge) computes `MAX(1, 0) = 1` where C computes `MAX(0, 0) = 0`, and minlen=0 is silently clobbered. `?? 1` is *correct* for a user edge — `init.ts:189` sets `minlen = lateInt(attr, 1, 0)` on every one (dotinit.c:85) — so the fallback can **only** fire for Xg edges, i.e. exactly where it is wrong. This is the calloc-zero-vs-undefined hazard already in memory. THREE manifestations of the one cause: (1) `xgStrong`/`xgMerge` clobbering minlen=0 (the observed bug); (2) `xgWeakSetWeights`'s `Math.max(eMinlen(e), 0)` yielding 1 — C annotates that identical line `/* effectively a nop */` **because** ED_minlen is 0 there; (3) `compileClusters` top/bot edges, created and never merged, which C leaves at 0/0 and the port's NS solve read back as 1/1. **ALREADY BAND-AIDED ONCE**: `xgWeakSetWeights` carried a literal `- 1` on both weight lines whose only purpose was to cancel `eWeight`'s bogus 1-default — a compensation at the symptom that hid the origin. Fixing the origin REQUIRED removing them. FIX: `xgAddEdge` sets `minlen = 0, weight = 0` (mirror calloc); drop the two `- 1`s. 8 lines, one file. **THE FINDING THAT MATTERS MOST — a "closed" dark cell was closed VACUOUSLY.** `newrank x minlen0` is a *pairwise* cell, and the previous round recorded it CLOSED by golden `dot-gap-concentrate-html-ports.dot`, which does contain both features — and passes. It pairs `h:p0 -> t [minlen=0]` with `h:p1 -> t` (minlen 1) between the **same node pair**; `merge()` takes MAX over both, so the effective constraint is 1 either way and the buggy port agreed with C **for the wrong reason**. **Syntactic co-occurrence is not interaction coverage.** blind-spots.ts can see that two features appear in one graph; it cannot see whether the conjunction is semantically live. A cell is only truly closed by a fixture that would FAIL if the interaction were broken — which is precisely why every fixture must be probed against the oracle before it is locked in, and why a fixture that passes on the first try has proven nothing until you know it *could* have failed. DELIVERABLE: the fix + **15 new oracle-referenced goldens** (manifest 191 -> 206), incl. `dot-newrank-minlen0` (the minimal repro — the fixture that genuinely closes the cell) and `dot-newrank-cluster-ranksame` (the triple that found it). | src/layout/dot/rank-dot2.ts, test/corpus/probe-fixture.ts (new), test/golden/inputs/*.dot (15), test/golden/refs/*.svg (15), test/golden/refs-xdot/*.xdot (15), test/golden/manifest.json, test/golden/suite.test.ts, .agent-notes/newrank-minlen0-aux-edge-calloc.md | tsc clean; vitest 231 files/**3196** green (+30 = 15 fixtures x SVG+xdot). All 15 goldens PASS against native-generated refs; the 6 diverging repros now pass and all 8 controls hold. Corpus sweep: PENDING (gates this fix AND the previously-ungated conc.ts fix). |
