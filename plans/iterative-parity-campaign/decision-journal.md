# Decision Journal — iterative-parity-campaign

Appended during execution. Same columns as `plans/decision-journal.md`.

| Date | Item/Task | Decision / Mechanism | Files | Gate result |
|------|-----------|----------------------|-------|-------------|
| 2026-07-12 | B1 parallelization | T2/T3 launched concurrently with T1: their T1-dependency is only the attribution-json SHAPE, which is spec-locked in T1's interface contract (not discovery-dependent); write-sets disjoint. Push-forward per brief constraints. | plans/iterative-parity-campaign/batch-1/ | n/a |
| 2026-07-12 | T2 class-acceptance reports | Added A1-drift class entries (neato/fdp/sfdp) to accepted-divergences-engines.json (D2); parity-report.ts computes membership from attribution-<engine>.json at report time, renders a link-only "Accepted class" section (roster-brevity), and wires T3's already-landed `renderOracleErrorsSidecar` (oracle-error-classifier.ts) into engineMarkdown. Judgment calls: (1) task's step-3 guard-test text says the referenced attribution file "must exist"; T1 hasn't run yet, so a hard-existence guard would fail this commit's own `npx vitest run` gate and contradicts the report's explicit "tolerate missing file → attribution pending" requirement — resolved by making the guard test check well-formedness ONLY when the file exists, never bare existence; logged here per prompting-quality/parallelism decision-logging rule since a reasonable dev could've blocked instead. (2) No guard test previously existed for accepted-divergences-engines.json at all (only the dot-track accepted-divergences.json was guarded) — created test/corpus/accepted-divergences-engines.test.ts as the natural analog rather than extending accepted-divergences.test.ts (which is scoped to the unrelated dot-track file/type), closing a pre-existing gap for the per-id shape too. (3) Added `isMain` guard to parity-report.ts (previously ran its file-writing script unconditionally at module scope, unlike survey.ts/engine-walk.ts/xdot-walk.ts) so the new pure functions are unit-testable without side effects — behavior-preserving for the CLI entry point (`npx tsx test/corpus/parity-report.ts`), verified by diffing PARITY-*.md before/after (circo/twopi/osage/patchwork byte-identical; neato/fdp/sfdp additive-only). Regenerated all PARITY-*.md once. | test/corpus/accepted-divergences-engines.json, test/corpus/parity-report.ts, test/corpus/accepted-divergences-engines.test.ts, test/corpus/parity-report.test.ts, test/corpus/fixtures/attribution-fixture.json, docs/known-divergences.md, test/corpus/PARITY-{neato,fdp,sfdp}.md | tsc clean; vitest 227/227 files, 3047/3048 tests green (1 unrelated pre-existing flake in src/pathplan/pathplan.test.ts "does not overflow the stack" — passes standalone, not in T2 write-set) |
