<!-- SPDX-License-Identifier: EPL-2.0 -->
# Decision journal

| date | batch/task | decision / finding |
|---|---|---|
| 2026-07-02 | setup | Branch fix/b15-record-ports from main (597da1a). Batch 1: T1 provenance trace, T2 FlightToHover. Main-loop sequential. |
| 2026-07-02 | B1 GATE | T1 verdict: H3 (H1/H2 disproven by trace — zero sameport writes; all port objects distinct; the identical (-39.14,-18) was coincidental same field offsets). Mechanism 1: routeBackEdge routed the whole-edge makeFwdEdge view → beginpath dyna resolution aimed at the far endpoint (compass s) where C's segment view aims at the adjacent vnode (compass w). C refs: make_regular_edge segfirst walk; BOTH C branches (hackflag via aghead(le), plain via collected segment) target the first vnode. T2: FlightToHover shared the root. Artifact: .agent-notes/b15-port-provenance.md. |
| 2026-07-02 | B1 correction | The seed note's "4 residual groups" was stale (attr-or-tag-era snapshot); TRUE pre-fix baseline on today's main = 8 groups. Mid-mission false alarm ("fix regressed 4 edges") was an artifact of a ZERO-BYTE verification render (cwd-misdirected; zip() truncation made it read as 0 diffs) + comparing against the stale snapshot. LESSON journaled: always assert group-count equality before positional comparison; always verify render output is non-empty. |
| 2026-07-02 | B2/T3+T4 | Mechanism 2 (the other 4 groups): swapSpline swapped bezier flags (C swap_bezier) but the port's precomputed per-END arrow-op slots didn't swap → flag↔ops pairing broke on multi-bezier reversed edges → arrow fell to group end via the emit fallback. Fix: swapEdgeSpline (spline + ops slots) at all 7 swap sites. b15: 8 → 0 differing groups, fully byte-identical. Red/green: corpus-level (8 pre / 0 post); distilled repro did NOT discriminate (2183 rule — not shipped); unit test covers swapEdgeSpline pairing. |
