# Decision Journal — mincross-2471-order-parity

| When | Task | Decision / Event | Rationale |
|------|------|------------------|-----------|
| 2026-06-18 | B1 | **Harness rebuilt + divergence reproduced.** C dump: instrumented `dot_mincross` (mincross.c) with `cdump_ranks(g,label)` gated on `CDUMP`, at three checkpoints: `merge2` (post component-mincross loop + merge2, pre cluster), `clust` (post cluster loop, pre remincross), `final` (post remincross, pre cleanup2). Rebuilt `gvplugin_dot_layout`; **key gotcha**: homebrew `dot` ABI-mismatches a freshly-built plugin → segfault; must use the build's OWN `dot` (`~/git/graphviz/build/cmd/dot/dot`) + a self-consistent plugin dir (`/tmp/gvmine`, all built dylibs + `dot -c` config). Copy ALL THREE dylib name variants (.8.0.12/.8/unversioned) or a stale versioned copy loads. TS dump: `cdumpRanks(g,label)` gated on `globalThis.__CDUMP_TS`, same 3 checkpoints; driven by `/tmp/ts_dump_2471.mjs` over the esbuild bundle. 2471 final order diverges from C on all 11 even (real-node) ranks r2..r22; r0 + all odd (virtual) ranks match. Confirmed brief's finding. | Phase 1 harness |
| 2026-06-18 | B1 | ~~ROUTE — cluster mincross phase~~ **SUPERSEDED (was a dump-blindness artifact).** Initial per-checkpoint diff with virtuals printed as `_v` showed merge2=23/23, clust diverges. But `_v` is blind to virtual identity. Re-ran with virtuals fingerprinted by real up/down endpoints + owning-cluster name (`v[up>down\|clust]`): **merge2 now diverges too (only 2/23 ranks match).** At merge2 the clusters are still COLLAPSED — root ranks hold cluster skeleton-LEADER virtuals (one per cluster per rank), not the real nodes (those enter root only at expand_cluster). The blind dump showed every leader as `_v` so merge2 trivially "matched". | Corrected route |
| 2026-06-18 | B1 | **ROUTE (corrected) — divergence is in COMPONENT-LEVEL mincross ordering of cluster skeleton leaders.** merge2 = output of runComponents+merge2, purely component-level. Per-rank cluster-tag **multiset is identical** C↔TS (same leaders present per rank) but their **order differs** → a genuine within-rank ORDER divergence of collapsed-cluster representatives, not a membership/tag artifact. So the bug lives in the component mincross (`mincrossMain`/build/medians/transpose/flat) as it orders cluster-skeleton virtuals — NOT in expand_cluster/cluster recursion/remincross. Next: split initial-install order vs iterative refinement. | Route to component mincross (leaders) |
| 2026-06-18 | B1 | **Bisected within component mincross.** Fingerprinted per-component checkpoints: all 6 `build<c>` dumps (initial install after build_ranks+flat_reorder) **MATCH** → install order correct. 2471 has 6 components; comp2 is the big clustered one (cc starts 3.2M, 19 iters). comp2's FIRST `mincross_step` already diverges (it_p0_i0: C cc=3,222,920 vs TS cc=2,185,277) from a matched input. Split step → `medians`+`reorder` loop vs final `transpose`: **step1_preTr already differs** (before transpose) → it's medians/reorder, not transpose. mval dump for the first diverging rank (r1): TS mvals = mostly C+256 with some spurious `-1` → medians computes wrong values. | Bisect to medians/reorder |
| 2026-06-18 | B2 | **STOP — fix is correct but exposes a DEEPER layer (transpose non-convergence); reverted to not regress 2471.** Applied the faithful fix: `medians` uses `rankGet(rk,i)`; `reorder` iterates the absolute window `[vStart, vStart+n)`. Result: all 41 mincross unit tests pass (incl. RL+cluster order regression), tsc clean. BUT 2471 no longer completes — `transpose`'s `while(delta>=1)` loop oscillates (delta plateaus ~5221, never 0), the parent mission's exact non-convergence signature. Bisected with a hard step-cap: cap≤5 steps **completes clean, zero invariant violations**; the order-corruption (dup `ND_order`) seen uncapped is a *downstream symptom* of the hung/oscillating transpose, not a reorder bug. So the medians/reorder fix is correct for the early passes; once the big clustered component (comp2, 3.2M crossings) is *properly* reordered, TS transpose can't converge where C does (C: 3s). The original code "completes" only because medians/reorder read the WRONG (comp1) window → comp2 was effectively transpose-only → stable-but-divergent order. **Layer 2 = transpose convergence on the properly-reordered clustered component** — the brief's flagged latent gap (C `in_cross`/`out_cross` tiebreak on `port.p.x` vs TS `val()`/`port.order`; parent noted swapping it "changed without fixing"). Beyond this batch's scope. Decision: REVERT all changes (C instrumentation = sacred; TS fix regresses 2471 to a hang, violating parity-cardinal). Root cause + faithful fix preserved here + in memory for the follow-up mission. | STOP / revert |
| 2026-06-18 | B1 | **ROOT CAUSE (confirmed). `medians` and `reorder` ignore `vStart` (the window offset).** Probe: comp2 r1 has `vStart=2`, but `medians` loops `rk.v[0..n-1]` (absolute index 0) — processing comp1's nodes at 0,1 and missing comp2's at the tail; same in `reorder`. C reads `GD_rank(g)[r].v` which is the ALREADY-OFFSET window pointer (init_mccomp advances `.v`; merge_ranks aliases `.v+ipos`). TS keeps `.v` as the full root array with a separate `vStart`, so faithful ports must read `rk.v[vStart+i]` = `rankGet(rk,i)`. **`transpose` already uses `rankGet` (correct); `medians`/`reorder` were ported without it.** Affects every windowed graph: multi-component (comp≥2) AND clusters (vStart=ipos). The parent mission's "order==C" on cluster repros was a false-positive of the blind `_v` dump — real-node order survived; virtual/leader order diverged unseen. FIX: medians uses `rankGet(rk,i)`; reorder iterates absolute window range `[vStart, vStart+n)`. | ROOT CAUSE |
