# T4.2 — Fix or accept the B4 ratio=fill residuals

## Context
T4.1 verdict per rep: scale-math-fixable (site named) or amplified-drift
(scale faithful, accept class `A-fill`).

## Task
Scale-math-fixable: fix the aspect factor computation at the named site + a test
pinning the factor for the rep. Amplified-drift: append accept proposal (class
`A-fill`, evidence = scale-factor-matches-native experiment) to
`batch-4/findings.md`. Apply to all copies.

## Read-set
- `batch-4/findings.md` (T4.1) · `src/layout/neato/set-aspect.ts` (named lines)
- `diagnosis.md`, `testing.md`

## Write-set
- `src/layout/neato/set-aspect.ts` (or the named site) + `*.test.ts` (if fixable)
- `batch-4/findings.md`

## Interface input (from T4.1)
`{ input, verdict: "scale-fixable"|"amplified-drift", site?, scaleFactorEvidence, copies }`

## Acceptance criteria
- Given scale-fixable, when fixed, then a test pins the aspect factor, tsc
  clean, fresh sweep 0 regressions, targets pass.
- Given amplified-drift, when recorded, then the registry proposal cites the
  scale-factor-matches-native experiment.
- Given the scaling change (if any), when swept, then neato/fdp ratio graphs are
  re-checked (shared set-aspect path) — flag to Batch 6 cross-sweep.

## Observability / rollback
N/A. Reversible.

## Boundaries
- Stop if a set-aspect change regresses any neato/fdp/sfdp ratio graph.

## Commit
`fix(T4.2): aspect factor for sfdp ratio=fill <input>` (only if scale-fixable)
