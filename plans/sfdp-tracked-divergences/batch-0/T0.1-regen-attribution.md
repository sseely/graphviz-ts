# T0.1 — Regenerate sfdp attribution over the current diverged set

## Context
graphviz-ts is a faithful TS port of Graphviz; sfdp conformance is measured by
`test/corpus/engine-walk.ts` (port xdot vs native `dot -Ksfdp -Txdot`,
`GVBINDIR=/tmp/ghl`, ±0.5). The injection-attribution harness
(`test/corpus/attribute-divergence.ts`) injects native pre-routing positions
into the port and re-renders, classifying each diverged id as
`drift-exonerated` (injection clears it → chaotic-drift, accept) or
`not-cleared` (a residual survives exact positions → genuine). The current
`attribution-sfdp.jsonl` is dated 2026-07-14, pre-`bagInsert`, so it is stale.

## Task
Re-run the injection-attribution harness over the **current** 234 sfdp diverged
ids and overwrite `test/corpus/attribution-sfdp.jsonl`. The native
`GVTS_POS_DUMP` patch is already applied in `~/git/graphviz`
(neatosplines.c, env-gated) — no rebuild needed. Confirm the 3 pgram
`harness-error` entries now resolve to a real verdict (the space-named-node
parser bug is fixed).

## Read-set
- `test/corpus/attribute-divergence.ts` (the harness + its CLI entry)
- `plans/iterative-parity-campaign/diagrams/injection-recipe.md` (POS_DUMP/INJECT recipe)
- `test/corpus/parity-sfdp.jsonl` (current diverged set)
- memory [[injection-parser-space-named-blindspot]], [[neato-fixed-packing-pinned]]

## Write-set
- `test/corpus/attribution-sfdp.jsonl` (regenerated)

## Steps
1. `pgrep` to confirm no sweep/render is running.
2. Verify native POS_DUMP is live: `GVTS_POS_DUMP=1 GVBINDIR=/tmp/ghl
   ~/git/graphviz/build/cmd/dot/dot -Ksfdp -Txdot ~/git/graphviz/tests/2168.dot
   2>&1 >/dev/null | grep -c GVTS_POS` returns > 0.
3. Run the harness over the current diverged set (see attribute-divergence.ts
   for the exact invocation; it reads the diverged ids and writes the jsonl).
4. Confirm no `harness-error` verdicts remain for pgram (graphs-pgram,
   share-pgram, windows-pgram). If any persist, instrument the injector for
   that id and fix the harness (log in decision-journal) before proceeding.

## Acceptance criteria
- Given the current 234 diverged ids, when the harness runs, then
  `attribution-sfdp.jsonl` has one verdict per id and its `_meta.generatedAt`
  is today.
- Given the pgram family, when regenerated, then their verdict is
  `drift-exonerated` or `not-cleared` (NOT `harness-error`).
- Given the output, when counted, then the new not-cleared set size is recorded
  in decision-journal (this is the refined "tracked" count).

## Observability / rollback
N/A — no new observable operations. Reversible (revert the jsonl).

## Boundaries
- Never do: edit `src/` (analysis only). Modify the native repo beyond the
  already-applied POS_DUMP.
- Ask first: if the harness needs a code change to resolve a harness-error.

## Commit
`chore(T0.1): regenerate sfdp attribution over current diverged set`
