# T3.1 — Analyze the B3 rankdir_dot edge family

## Context
`rankdir_dot` inputs run under sfdp; both port and native ignore `rankdir` for
force-directed layout. Edge geometry diverges. This may be the same predicate
tie as B2, or a node-collection-order difference that only surfaces on these
inputs.

## Task
Inject exact native positions for the representative; confirm the edge still
diverges (isolates routing). Instrument the deciding routing predicate (as in
T2.1). Determine whether the mechanism is identical to a B2 finding (→ defer to
B2's fix) or distinct (→ its own verdict). Record in `batch-3/findings.md`.

## Read-set
- `batch-2/findings.md` (compare mechanisms) · same routing sources as T2.1
- `~/git/graphviz/tests/linux.x86/rankdir_dot.gv`
- memory: [[pgram-ordering-lr-overenforce]] (sfdp ignoring subgraph ordering),
  [[agseq-node-iteration-done]] (collection order)

## Write-set
- `batch-3/findings.md`

## Acceptance criteria
- Given the rep, when instrumented, then findings.md states the mechanism +
  `file:line` and whether it equals a B2 finding.
- Given "same as B2", when recorded, then T3.2 is a re-sweep-only confirmation
  (no new fix).
- Given "distinct", when recorded, then it names the change site or the
  irreducible evidence.

## Observability / rollback
N/A. Reversible (doc only).

## Commit
`docs(T3.1): root-cause sfdp rankdir_dot edge family (B3)`
