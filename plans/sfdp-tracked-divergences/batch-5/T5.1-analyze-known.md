# T5.1 — Analyze the B5 known ids (2470, 2475_2, nshare-arrows_dot)

## Context
Prior session work characterized 2470 (edge-label lossy-RTree) and 2475_2
(x-coord NS). Confirm they still stand post-`bagInsert` and push the
fix-aggressively angle before accepting.

## Task
- **2470**: reproduce the floor-boundary rect (memory recipe). Identify the
  object whose `objplpmks` y-low crosses the integer boundary; determine if its
  `pos`/`sz` is a computable node/label the port sizes 1 ULP off (fixable) or an
  edge-midpoint anchor on a drifted spline (drift). Test whether a faithful
  RTree structure would coincide the lossy gap with native.
- **2475_2**: inject exact positions; if a large residual survives, trace the
  x-coord NS keepout/rank read ([[hang-2475-2-xcoord-ns]]).
- **nshare-arrows_dot**: classify (drift / edge-tie / other).
Record verdicts in `batch-5/findings.md` per `diagnosis.md`.

## Read-set
- `src/label/*` (xlabels RTree), `src/common/xlabels-place.ts`,
  `src/label/xlabels-geom.ts` (objplpmks)
- `src/layout/neato/multispline.ts` / x-coord NS sources for 2475_2
- memory: [[sfdp-edge-label-rtree-lossy]], [[hang-2475-2-xcoord-ns]],
  [[2825-layout-yshift-bug]]

## Write-set
- `batch-5/findings.md`

## Acceptance criteria
- Given 2470, when reproduced, then findings.md states whether the
  floor-boundary object is fixable (computable-size ULP) or drift, with the
  controlled experiment.
- Given 2475_2, when injected, then the residual mechanism + `file:line` is
  stated or an irreducible verdict with evidence.
- Given arrows_dot, when classified, then it maps to an existing bucket verdict.

## Observability / rollback
N/A. Reversible (doc only).

## Commit
`docs(T5.1): root-cause sfdp known ids 2470/2475_2/arrows_dot (B5)`
