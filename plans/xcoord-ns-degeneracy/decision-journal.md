# Decision journal

Appended during execution. Every non-trivial judgment call, every write-set
expansion approval, and the T2 localization result go here.

| When | Batch/Task | Decision / Finding | Rationale |
|------|-----------|--------------------|-----------|
| 2026-06-26 | T1 | Inline orchestrator (not subagent) for T1–T4 | Oracle-harness context (instrument→rebuild→diff) must persist across batches; subagent would lose it (cf. [[Subagent hook-loop deaths]]). |
| 2026-06-26 | T1 | C instrument gated on `getenv("GV_XDUMP")` | Inert unless env set → shared oracle stays clean even pre-revert; positions in position.c create_aux_edges (Stage1) + ns.c rank2 balance==2 (Stage2 pivots, Stage3 pre, Stage4 post). |
| 2026-06-26 | T1 | C dump captured: 115 nodes, 165 aux edges, 18 pivots, all 4 stages | honda renders fine; named x at Stage3/4 fully comparable (n000..n023). LR_balance moves nodes (e.g. n011-15 102→90, n023 42→21). Exit criterion met. |
| 2026-06-26 | T2 | First divergence = Stage 1 aux-graph: port VIRTUAL/SLACK node ranks are FRACTIONAL (V@r108.4264..., V@r36.1421...); C ranks are INTEGER. Cascades to Stage 3 (pre-balance named x differ, many −6) and pivot seq (diverges from iter 0; C=18 port=20 pivots). | C `ND_rank` is `int`; make_LR_constraints `last = (ND_rank(v) = last + width)` truncates double→int each step and accumulates the *integer* back into `last`. Port `lrRankPair` (position-aux.ts:219) keeps float `last+width` and accumulates the fraction. Fractional seed ranks perturb slack/tight-edge (slack==0) detection → different feasible tree → different optimal-face vertex. Fix site: lrRankPair, Math.trunc. Reproducible/derivable (ADR-3 OK), no write-set expansion (position-aux.ts ∈ position*.ts). |
| 2026-06-26 | T3 | FIX APPLIED (lrRankPair Math.trunc). VERIFIED: Stage3+Stage4 named x byte-match C; all 18 pivots byte-match (count + sequence + virtual/slack refs) → entire x-coord NS solution matches C. honda node labels now byte-match native (were off 5–7px in cy; 28 labels differed → 0). numericDiffs 261→150. typecheck clean. | The mission-targeted x-coord weight=0 degeneracy is genuinely fixed and C-faithful. README's node-cy-shift diagnosis was correct. |
| 2026-06-26 | T3 | **STOP / SCOPE DECISION (stop cond. 3 & 4).** honda verdict STAYS "diverged" (maxΔ 27.9 unchanged; firstDiffPath `path[1]/@d`) due to a SEPARATE edge-spline divergence: only 2/40 edge paths differ in piece count (edge2 native2seg/port1seg; edge27 native2seg/port4seg) + coord deltas on labeled edges — the documented fitter piece-count / labeled-edge class (splines.c), NOT x-coord NS. Pivots+nodes byte-match C ⇒ NS fully correct ⇒ edge issue is downstream/independent. DISPROVED README premise: weight=1 (2 or all-4) still diverges (maxΔ 27.7/26.3) — weight=0 was NOT the edge driver. | DoD "honda byte/structural match" unachievable in x-coord scope. Keep the genuine x-coord fix (pending 0-regression survey) + open separate mission for the edge-spline divergence is the recommended path; presenting scope decision to user. |
| 2026-06-26 | T3 | USER DECISION: "Keep fix, split edge issue out." | Proceed: keep C-faithful x-coord fix, validate 0-regression, commit/merge; open separate edge-spline mission. |
| 2026-06-26 | T4 | C instrument reverted + clean oracle rebuilt (clean native == inert-instrumented native, confirming gate was truly inert). Port instrument removed; src diff = only lrRankPair. Full suite 2420 pass, typecheck clean. | — |
| 2026-06-26 | T4 | **VALIDATION: 0 verdict regressions on BOTH baselines.** Headless old-vs-new: 0 match→diverged. Pango old-vs-new: 0 match→diverged. survey:gate **PASS** (12 improvements diverged→match: 2193, graphs-NaN/b102/b143/ports/xx fam). honda stays diverged (edge-spline, out of scope) w/ node positions byte-matching C. | DoD "0 survey regressions both baselines" met (verdict-level, project std per [[Bucket-fix re-bucketing]]). |
| 2026-06-26 | T4 | 3 already-diverged graphs shifted maxΔ (artifact of misaligned/structurally-diverged trees, NOT verdict change). Direct node-position check: **2471 +2400px better** (19401→16995 headless; pango 9123→8898). **2796 (cluster, 43 clusters, port emits +1 edge) ~250px worse** — uniform whole-cluster translation on an already-broken graph; in gate pre-existing allowlist. | Fix is C-faithful (sacred principle); 2796 is downstream of a separate cluster emit/ranking bug ([[2471-blocker-is-cluster-ranking]]), surfaced not caused. Documented in .agent-notes/xcoord-ns-lrconstraints-int-truncation.md as cluster follow-up. |
