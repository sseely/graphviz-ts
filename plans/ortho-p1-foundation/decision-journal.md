# Decision Journal — ortho-p1-foundation

| When | Task | Decision / Event | Rationale |
|------|------|------------------|-----------|
| 2026-06-18 | startup | **STOP — brief is stale/redundant.** Pre-flight found `src/ortho/` already contains a faithful, committed, green port of all three P1 targets. | Brief premise (greenfield, all tasks `[ ]`, "P1 only creates files") is contradicted by reality; launching T1/T2/T3 would overwrite committed work. `[[backlog-catalog-stale]]` hazard. |
| 2026-06-18 | T1 | `rawgraph.ts` already present & faithful (make_graph/insert_edge/remove_redge/edge_exists/DFS_visit/top_sort) | Committed in `f1e615c` (T17, 2026-05-27), 3 wks before brief `b1f6ce5` (2026-06-18). |
| 2026-06-18 | T2 | `trapezoid.ts`+`trap-{types,segment,query,update}.ts` already present & faithful (Seidel: construct_trapezoids, init_query_structure, etc.) — split exactly as T2 spec anticipated | Same T17 commit. |
| 2026-06-18 | T3 | `sgraph.ts`+`fpq.ts` already present | Same T17 commit. |
| 2026-06-18 | gate | Baseline: `npm run typecheck` exit 0; `vitest run src/ortho` = 8 passed / 3 files. C tree (`lib/`) clean. | Existing port is healthy & green. |
| 2026-06-18 | scope | Genuine gap vs brief = missing oracle-pinned unit tests (`rawgraph.test.ts`,`trapezoid.test.ts`,`sgraph.test.ts`,`fPQ.test.ts`); `ortho-diag.test.ts` self-marked "temporary, delete after debugging". | Existing port was never C-oracle-pinned to the brief's standard. Awaiting user decision before any work. |
| 2026-06-18 | scope | **User approved "Re-scope to oracle tests"** (AskUserQuestion). | Keep T17 port; add the missing C-oracle tests, validate vs native C, fix parity bugs, drop temp diag. |
| 2026-06-18 | oracle | Recipe = tiny C harnesses linking **prebuilt** `libortho.a`+`libutil.a`(+`libcgraph.dylib` for fPQ/sgraph). `cc -DORTHO -Ibuild -Ilib -Ilib/{cdt,cgraph,common,gvc,pack,pathplan}`. | Zero C-tree modification (ADR-5 sanctions object-linking harness) → no "revert C" hazard, modules fully independent. Harnesses in `/tmp/ortho-oracle`. |
| 2026-06-18 | T1 | `rawgraph.test.ts` added; **green**, no parity fix. Pinned adj-lists + DFS reverse-finish topsort (6-node DAG v1=2,v2=1) to C. Commit `1689adb`. | Existing port byte-faithful. |
| 2026-06-18 | T3 | `fpq.test.ts`+`sgraph.test.ts` added; green. **2 faithfulness fixes** to `sgraph.ts`: `shortPath` guard `<=0`→`<0` (sgraph.c:164); extracted `relaxNeighbors` for CCN cap (order preserved). Commit `b8b0ebf`. | Pinned PQ pop+tie order, shortPath n_dad chain/cost, gsave/reset. **Brief's raw-PQ accept criteria [5,1,3,2,4]→ascending is INVALID**: fPQ guard sentinel n_val=0 acts as +inf, valid domain ≤0 (negated dists); positive values hang PQupheap. Tested in negated domain. |
| 2026-06-18 | T2 | `trapezoid.test.ts` added; **green, no parity fix**. Full trap table (all fields incl. u0/u1/d0/d1/sink links + sentinels) for 1-seg/triangle/rectangle byte-matches C under identity permute; determinism confirmed (2 C runs + 2 TS runs identical). Commit `5f4c6cb`. | Existing Seidel port byte-faithful. INF/MAX tokens erase C-printf vs JS-toString gap (DBL_MAX/SIZE_MAX). |
| 2026-06-18 | gate | Removed temp `ortho-diag.test.ts`. **All gates GREEN:** typecheck 0; `npm test` 1895/1895 (baseline 1876 + 25 new − 1 diag); build OK (943kb); C tree zero tracked-`.c/.h` mods; diff scope = `src/ortho/**`+`plans/**` only. No existing test/golden changed (ADR-4 satisfied). | Mission re-scope complete. |
