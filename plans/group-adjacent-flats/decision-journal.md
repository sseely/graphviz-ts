# Decision Journal — group-adjacent-flats

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).
One writer per row; parallel agents write their own findings file, the
orchestrator records the journal row after the batch.

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| scope | 2026-06-20 | Brief authored from the merged `#241_0` diagnosis (7490f43). Fix = caller-side grouping of adjacent port-bearing flats into one `makeFlatAdjEdges(cnt=N)` call; aux internals already faithful. Locked AD-1 (pin C order — `edges[0]` must be the forward edge), AD-4 (regression gate: goldens conformant out-of-family + survey net-improve). Batch 1 = T1 derisk (pin order + red test); Batch 2 = T2 implement + T3 regression. | The ordering detail (`edges[0]`) and the shared-aux golden-risk are the two things that sank/feared prior missions — both locked before src edits. | no |
| T1 | 2026-06-20 | debugger agent pinned the C order (ephemeral instrumentation, restored clean) + wrote red test. **AD-1 REFINED, not contradicted:** C's auxt is set by a SEPARATE forward-normalized `e0` param (`tn=agtail(e0)=node2`), while the raw `edges[]` stays edgecmp-sorted with `edges[0]=3->2` (back). The port's `buildFlatAux` uses `edges[0].tail` for `otn`, so the AD-2-compliant rule is: **place a forward edge (tail=node2, the lower-order node) at group index 0.** I verified this against C `make_flat_edge:1512-1528` (`e=*edges; if(BWDEDGE) makefwdedge; …make_flat_adj_edges(g,edges,cnt,e,et)`). Port AGSEQ analog = `Edge.seq`, but all three 2↔3 edges share it; the discriminator is direction (forward-first). Red test `splines-flat-group.test.ts` RED (back 4≠7) + GUARD green (fwd 7). | AD-1 pin-by-C; the e0-vs-edges[0] distinction is the load-bearing detail T2 must honor. | no |
| gates-B1 | 2026-06-20 | Batch 1 gates: tsc 0; red test fails as intended (TDD red), guard green; C `dotsplines.c` clean (AD-5); lizard 0; write-set = red test + findings only. | Quality gates between batches; the one failing test is the deliverable. | no |
| T2 | 2026-06-20 | Orchestrator implemented caller-side grouping in `routeFaithfulSidePort` (`collectAdjacentFlatGroup`+`isGroupableFlat`): all adjacent same-rank side-port flats between a pair → ONE `makeFlatAdjEdges(cnt=N)`, forward edge at group[0] (otn=node2). Verified grouping fires (`[2->3,2->3,3->2]`). tsc 0, lizard 0. BUT the back edge still routes size 4 — grouping NECESSARY but INSUFFICIENT. | AD-1/AD-2 caller-side grouping; the fix-hypothesis from the diagnosis. | no |
| T2-blocker | 2026-06-20 | **SECOND DIVERGENCE pinned (STOP).** Controlled experiment (otn fixed=node2, varied clone order incl. C's exact `[back,ne/nw,e/w]`) proves clone ORDER/otn are NOT the cause: the back edge stays size 4 in every order while the forward corner edge curls to 7. Root: in the aux, auxt(r0)/auxh(r1) are cross-rank, so the back-edge clone (auxh->auxt) is a regular adjacent-rank back edge routed by `routeOneEdge`→`routeFaithfulAdjacentBack` (guard tr===hr+1) STRAIGHT, ignoring its sw/se corner ports — where C's `make_regular_edge` curls it (size 7). The forward corner edge curls because it takes the side-port path; the back edge is intercepted by the straight back-edge path first. Fix is in CORE back-edge routing (all adjacent back edges), OUTSIDE the adjacent-flat dispatch. | Brief STOP: "restructuring routeDotEdges beyond the adjacent-flat dispatch ⇒ scope creep; bank and re-scope" + AD-2 contradiction. instrument-before-quarantine: proved by experiment, not guessed. | STOP |
| impact | 2026-06-20 | Grouping change characterized: golden-neutral. vitest 1992 pass + 1 xfail (red test converted to `it.fails` tripwire). Corpus survey 796: diverged 357→357 (ZERO new diverges); #241_0 unchanged (diverged/126); only 2 deltas (2743,2782) errored↔timeout flakiness. parity.json restored to baseline. C clean. | AD-4 regression data. Banked on branch, NOT merged (goal unmet). | no |
| DECISION | 2026-06-20 | STOP autonomous execution. Bank the grouping change + xfail tripwire + findings on `fix/group-adjacent-flats` (committed, **NOT merged** — main untouched, layout path output-neutral). Recommend a follow-up mission targeting the aux back-edge port-curl (`routeFaithfulAdjacentBack` should honor side ports / defer to the side-port path, matching C `make_regular_edge`), built on this branch. Surface to user. | Honest stop (matches prior #241_0 missions). The grouping half is proven; the back-edge-curl half needs its own scoped, golden-gated mission. | STOP |
