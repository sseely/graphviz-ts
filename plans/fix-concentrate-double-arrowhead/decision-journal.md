<!-- SPDX-License-Identifier: EPL-2.0 -->

# Decision journal

Appended during execution. One row per non-trivial judgment call.

| Date | Task | Decision | Rationale |
|------|------|----------|-----------|
| 2026-06-25 | batch-1 | Execute T1 then T2 directly (single-agent), not via dispatched sub-agents | Two sequential tasks, no parallel bottleneck; parallelism.md defaults to single-agent. Full C spec + context already loaded; the critical risk (headless-oracle golden generation) benefits from staying in the loop. |
| 2026-06-25 | T1 | Golden ids `concentrate-b135` / `concentrate-167`; refs from headless oracle (`GVBINDIR=/tmp/ghl`, present from today) | Matches ADR-4; descriptive ids independent of corpus survey ids. |
| 2026-06-25 | T1 | Bumped `suite.test.ts` count assertion 160→162 (file not in T1 write-set) — pushed forward, did not stop | The manifest-length assertion is mechanically coupled to adding manifest entries; the brief's write-set omitted it as an oversight. Change is trivial/obvious (push-forward: self-evident fix). Not in any other task's write-set. |
| 2026-06-25 | T1 | Skipped the optional `src/common/splines-clip.test.ts` unit test | `SplineClipHelper` is a non-exported class; a unit test would require exporting internals or going through render — the render path is exactly what the two byte-exact goldens already cover (both arrowhead emit AND spline clip). Brief says prefer golden / do not over-build. |
| 2026-06-25 | T2 | Targets `graphs-b135`/`167`/`2087` flip diverged→structural-match (childCount gone); goldens byte-match | Confirmed via regenerated `parity-rules.json`. All 3 are `concentrate=true`. See comparisons/T2-survey-verification.md. |
| 2026-06-25 | T2 | `1453`/`b69` improve in part; `2825`/`b15` unaffected (byte-identical w/ & w/o fix) — verdict unchanged for all 4 | README predicted these would improve. Reality: b69/1453 gain arrowheads but retain a separate dominant childCount/x-coord residual; 2825/b15 trigger no opposing-pair merge so the arrowhead bug never applied. Documented in docs/known-divergences.md (x-coord, not arrowhead; cross-links b69-concentrate-undermerge). |
| 2026-06-25 | T2 | 24 gate / 34 before-after "regressions" are PRE-EXISTING, not this fix — 0 regressions attributable | Proven 3 ways (see comparisons page): (1) arrowFlags only branches on conc_opp_flag = concentrate-only; (2) every flagged id is concentrate=false; (3) 8 sampled inputs render byte-identical with vs without the fix at HEAD~1. Root cause: committed parity baselines predate the text-measure-arch merge (last regen `7b796e1`/`274964e`); the drift is the EstimateTextMeasurer cutover, surfaced by re-running on current main. |
| 2026-06-25 | T2 | Did NOT regenerate/commit tracked `parity.json`/`parity-rules.json`/`PARITY.md`; preserved fresh survey under plans/.../comparisons/ instead | Refreshing them would fold an entire feature-merge's drift into a "concentrate arrowhead" commit, mislabeling git-blame and entangling rollback (commits.md: one commit = one task's change). Baseline refresh post-cutover is a separate maintenance task; flagged in the mission summary. The brief's literal "commit regenerated parity.json" assumed current baselines — push-forward deviation, logged. |
