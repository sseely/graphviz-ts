# Decision Journal — multicolor-paint

| Date | Task | Decision | Rationale | Alternatives considered |
|------|------|----------|-----------|------------------------|
| 2026-06-13 | Brief structure | Collapsed the separate "multicolor parser" task into G1 — C's findStopColor calls parseSegs, so the parser is foundational for gradients too, not just striped/wedged/edges. G1 builds src/common/multicolor.ts; S1 and M1 reuse it. Batch 2 became a single task (striped+wedged); Batch 3 single (edges). | One shared parser (AD4), no duplication; faithful to C's call graph. | Separate parser task in Batch 2 (would force a Batch 1↔2 back-dependency for gradients). |
| 2026-06-13 | M1 algorithm | multicolor() SPLITS each spline along its length (splitBSpline at cumulative fractions), one color per length-segment — NOT parallel/offset curves (corrected from the initial blast-radius wording). Tail arrow=first color, head=last. | Read emit.c:1991-2050 directly; the body uses splitBSpline, not offset geometry. | — (initial mental model corrected before writing M1). |
| 2026-06-13 | Batch 1 plan | Round 1: G1 ‖ G2 (disjoint: multicolor.ts+style-resolve.ts vs svg-gradient.ts+svg-helpers.ts+job.ts). Round 2: G3 ‖ G4. Branch feature/multicolor-paint off feature/parity-render-styling; baseline /tmp/mc-baseline (97 goldens). | parallelism.md single-writer-per-file. | — |
| 2026-06-13 | G1 done | Chose the additive `resolveNodeFillEx`/`resolveClusterFillEx` variants (kept old solid resolvers for back-compat callers) returning discriminated ResolvedFill. Orchestrator-verified: parseSegs distributes remainder (red;0.3→blue 0.7); findStopColor red:blue→{red,blue,0}, red→null; Ex linear/radial/solid discrimination correct. | Avoids breaking poly-gencode/device callers before G3 adapts. | Change return shape in place (would tsc-break G3/G4's files pre-adaptation). |
| 2026-06-13 | G2 done | emitGradientDefs(job, ptsYup, filled) triggers on obj.fill===Linear/Radial, emits <defs> inline (AD2), returns the url; shape emitters thread it to emitStyle's new optional gradFillUrl param. So G3/G4 only need to SET obj.fill + gradient fields — emission is automatic. Per-render linearGradId/radialGradId on RenderJob. | Clean separation: G3/G4 become pure obj-state population. | — |
| 2026-06-13 | Batch 1 R1 gates | tsc 0; vitest 1615/0; 97 goldens conformant. G1, G2 committed. | All Round 1 gates green. | — |
| 2026-06-13 | G3 done | poly-gencode applyFillState + device applyClusterObjState set obj.fill=Linear/Radial + gradient fields; obj.id set (node1/clust1) for the gradient id prefix. G3 extracted emitNodeBody/applyClusterPenState/clusterAttrsOf for the 30-line hook. | Gradient id must be prefixed by obj.id to match C. | — |
| 2026-06-13 | G4 done + fix | svgBeginGraph detects a 2-color bgcolor (findStopColor) and emits a gradient background (id graph0_l_0); resolveGraphBgcolor stays the solid-only fallback. G4's run truncated before writing tests — orchestrator added the end-to-end bgcolor-gradient test (and corrected the id assertion l_0→graph0_l_0 after oracle check). | G4 impl verified correct vs oracle; only the test was missing. | — |
| 2026-06-13 | Batch 1 gates | tsc 0; vitest 1626/0; 97 goldens conformant; orchestrator oracle-verified 8/8 gradient cases (node linear/radial/frac/gradangle/box, cluster, graph bgcolor, solid-unchanged) conformant dot 15.0.0. G3, G4 committed. Batch 1 complete. | All Batch 1 gates green; gradient parity confirmed. | — |
| 2026-06-13 | S1 striped done | stripedBox (THIN_LINE 0.5 pen bands) + poly-gencode striped branch conformant dot 15.0.0 (3-band, weighted, 2-color all PASS). Reuses G1 parseSegs. | — | — |
| 2026-06-13 | S1 WEDGED libm divergence (STOP-class, journaled) | wedgedEllipse + ellipticWedge render but do NOT conformant C: the arc subdivision count differs (estimateError ≈4.5e-6 in V8 vs >1e-5 in glibc, straddling the 1e-5 threshold → 8 vs 16 bezier cubics per wedge). First divergence: 3rd control point of the first wedge arc. This is the mission's FMA/libm stop-condition class (V8 trig vs glibc), NOT a port bug — formulas match C. Per stop rule + M8 precedent: no code fix; T-gold EXCLUDES the wedged golden (it would need a tolerance+portReference pin, but the divergence is structural —8 vs 16 segments— so a deterministic golden is not minted). The wedged FEATURE works (visually correct ~8-segment arcs); only byte-parity with glibc refs is unattainable. | Stop condition: libm divergence without disassembly → journal, don't chase. Structural (segment-count) divergence can't be tolerance-pinned. | (a) Force n=16 to match C — rejected (hardcoding to one libm's output is not faithful; would break on other platforms). (b) Pin via portReference — rejected (the ref is glibc's 16-seg path; the port's 8-seg path is a different structure, not within numeric tolerance). |
| 2026-06-13 | Batch 2 gates | tsc 0; vitest 1657/0; 97 goldens conformant; striped oracle-verified. S1 committed (residual CCN/length warnings in svg-multicolor.ts after 2 hook attempts — faithful C complexity, allowed by hook rule). Batch 2 complete. | All Batch 2 gates green; striped parity confirmed, wedged feature done w/ journaled libm byte-divergence. | — |
| 2026-06-13 | M1 algorithm CORRECTED | Oracle showed `color="red:blue"` (plain colon) emits PARALLEL offset curves (red x≈26, blue x≈28, SEP=2 apart, full length), NOT split-along-length. C dispatch: `numsemi && numc` → multicolor()/splitBSpline (semicolon syntax, split); `else if (numc)` → parallel offset Béziers (the `:` syntax, computeoffset_p/qr). The mission's "parallel-spline" target IS the offset branch. Rewrote M1 to port computeoffset_p (emit.c:1836), computeoffset_qr (:1849), SEP=2.0, numc2=(2+numc)/2, the parallel-bezier loop (:2442), head=1st color/tail=2nd. Semicolon split-along-length made optional/journal. Fixed data-flow.md. | Initial M1 spec mis-mapped which branch `:` hits; corrected before dispatch via direct oracle + C read. | — |
| 2026-06-13 | M1 agent died (API socket) — orchestrator recovery | The M1 agent hit an API socket error mid-run, leaving: edge-offset.ts (computeoffset_p/_qr), svg-parallel-edge.ts (emitParallelEdgePaths), svg-multicolor-edge.test.ts, svg.ts/svg-helpers.ts wiring — plus a broken mid-edit (an `import` inside an it() in device.test.ts) and an over-limit emitParallelEdgePaths (CCN 13/48 lines). Orchestrator finished it: removed the bad import, refactored emitParallelEdgePaths under the hook (buildSegData/emitColorPasses helpers, dropped dead `beziers`), and fixed the semicolon case to use parseSegs colors (was emitting invalid `stroke="red;0.5"`). | Agent transport failure, not a logic failure; the implementation was sound and recoverable inline. | Re-dispatch from scratch (would lose the agent's correct work). |
| 2026-06-13 | M1 dispatch location + scope | Multicolor detection landed in svg.ts endEdge (numc = count ':'), not device.ts renderEdge as the spec said — svg.ts is the SVG plugin's edge emission (calls svgEdgePath), the correct seam for SVG-specific parallel emission. Write-set adjusted: svg.ts + svg-parallel-edge.ts + edge-offset.ts instead of device.ts. | The port splits edge emission: device.ts sets obj-state, svg.ts emits the SVG path. Parallel paths are SVG-specific. | device.ts renderEdge (would mix common-layer + SVG-specific emission). |
| 2026-06-13 | M1 undirected divergence (pre-existing, out of scope) | `color="red:blue"` directed edges conformant C. Undirected `a--b` multicolor FAILs — but a PLAIN undirected `graph{a--b}` (zero M1) already diverges in the spline Y-coords (port −47.54 vs C −36.1). Pre-existing undirected-edge routing difference, outside M1/this mission's blast radius. T-gold mints DIRECTED multicolor goldens only. | Verified single-color & plain undirected edges pre-diverge → not M1's bug. | — |
| 2026-06-13 | M1 test split (hook-forced push-forward) | device.test.ts had grown to 584 lines at HEAD (agent edits in earlier batches bypass the orchestrator's Edit hook; git commit doesn't run it) and M1 pushed it to 646. Split: AC13/AC14/G3-gradient + M1 e2e tests → new device-styling.test.ts; device.test.ts trimmed to 469. | Hook-forced module split (allowed push-forward). | — |
| 2026-06-13 | Batch 3 gates | tsc 0; vitest 1685/0; 97 goldens conformant; directed 2/3-color parallel edges oracle-match dot 15.0.0; semicolon case emits valid colors (parallel, journaled divergence from C split). M1 committed. Batch 3 complete. | All Batch 3 gates green. | — |
| 2026-06-13 | T-gold done | 12 multicolor goldens minted (manifest 97→109), all PASS compareSvg vs dot 15.0.0 at deterministic 0.01pt: node gradient linear/radial/frac/angle/box, cluster gradient, graph bgcolor gradient, striped plain+weighted, directed 2/3-color edges, combined. Prior 97 refs byte-unchanged (shasum -c clean). suite count 97→109. Excluded per journal: wedged (libm), semicolon/undirected edges. tsc 0; vitest 1697/0. T-gold committed. | All Batch 4 gates green; first positive coverage of the multicolor behavior. | — |
| 2026-06-13 | MISSION COMPLETE | All 4 batches done. Final: tsc 0; vitest 1697/0; manifest 109 (prior 97 conformant, 12 new at deterministic). Gradient (node/cluster/bgcolor, linear+radial), striped, and directed multicolor parallel edges reach C parity. Branch feature/multicolor-paint ready for merge into feature/parity-render-styling on Scott's go-ahead. Out of scope (journaled): wedged libm divergence, semicolon split-along-length edges, undirected-edge routing divergence, box-node spline divergence. | Mission objective met (AD3 first-solid fallbacks converged for gradient/striped/directed-edge cases). | — |
| 2026-06-13 | WEDGED comparison page (CLAUDE.md rule compliance) | Per the CLAUDE.md rule "a mission/batch with any quarantined or excluded case is not complete until its comparison page exists and is referenced in the decision journal": the wedged exclusion's side-by-side comparison page is tracked at **plans/multicolor-paint/wedged-comparison.html** (port vs dot 15.0.0, 2/3/4-color wedges, with the V8-vs-glibc segment-count evidence). The port's wedge geometry is pinned by the drift guard src/render/wedged-divergence.test.ts. | Excluded case now has a durable, journal-referenced comparison artifact. | — |
| 2026-06-13 | Follow-ups resolved 3 of the 4 deferred items | Post-mission follow-up branches FIXED (real bugs, not exclusions, now at C parity): semicolon split-along-length edges (multicolor()/splitBSpline); undirected-edge phantom-arrow/short-spline (dir defaulted forward → none); node-penwidth edge clip (the "box-node 0.11pt" was actually node penwidth, shape-independent). Only WEDGED remains a genuine libm exclusion (comparison page above). | The journaled "out of scope" items 2–4 were tractable once root-caused. | — |
