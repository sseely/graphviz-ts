<!-- SPDX-License-Identifier: EPL-2.0 -->

# T2 — Oracle-pin the 5 grd* cases to byte-match + golden + survey

## Context

Depends on **T1** (rounded `<path>` fill + fill stroke-width implemented). This
task proves the 5 grd* cases now byte-match the native `dot` oracle, locks the
rounded behavior as a golden, and refreshes the parity survey. Native oracle =
the installed `dot` (15.0.0); never WASM (per `oracle-native-not-wasm`).

## Task

1. **Verify byte-match for all 5 targets.** For each of `grdfillcolor`,
   `grdlinear`, `grdlinear_angle`, `grdradial`, `grdradial_angle` (inputs under
   `~/git/graphviz/tests/graphs/`):
   - `npx tsx test/corpus/render-one.ts <input> dot > /tmp/port.svg`
   - `dot -Tsvg <input> > /tmp/oracle.svg`
   - Diff. Expect equality modulo the standard SVG preamble (the `Generated by`
     comment and the `<svg>` tag line-wrapping — the same normalization the
     golden `compareSvg` applies). The table fill must now be a `<path>` and
     bordered-cell fills must carry `stroke-width`.
   - If any case still diverges in the element tree, do NOT mark done — report
     which case and the first differing element back for a T1 follow-up.
2. **Add a golden** for a rounded gradient table (covers gap A + gap B +
   gradient bbox):
   - Create `test/golden/inputs/dot-htmltable-rounded-grad.dot` — a minimal
     `<TABLE style="rounded" bgcolor="c0:c1" gradientangle="…">` with at least
     one `border="3"` cell. Keep it small and feature-focused (avoid the
     unrelated header-only differences).
   - Generate the ref with the native oracle:
     `dot -Tsvg test/golden/inputs/dot-htmltable-rounded-grad.dot > test/golden/refs/dot-htmltable-rounded-grad.svg`
   - Add a `manifest.json` entry (`toleranceClass: "deterministic"`, engine
     `dot`, clear description). Optionally a 2nd golden for a rounded SOLID-fill
     table (push-forward; log the choice).
   - Update the suite count assertion in `test/golden/suite.test.ts`
     (156 → 157, or 158 with the 2nd golden).
   - Run `npx vitest run test/golden/suite.test.ts` — the new golden(s) must
     pass within `deterministic` tolerance.
3. **Regenerate the parity survey**:
   `npx tsx test/corpus/survey.ts && npx tsx test/corpus/dashboard.ts`.
   Confirm the 5 `graphs-grd*` ids move from `diverged` to **byte-match** and
   the byte-match total rises by exactly 5 with **zero regressions** elsewhere
   (compare before/after totals; the 6 grd* control cases stay byte-match).
4. **Append a decision-journal entry**: cases flipped, before/after parity
   totals (byte-match / structural / diverged), golden(s) added, any holdout.
   If any case did NOT reach byte-match, write a comparison page for it under
   `plans/htmltable-rounded-fill/comparisons/` and reference it (CLAUDE.md
   completeness rule).

## Write-set

- `test/golden/inputs/dot-htmltable-rounded-grad.dot` (create; + solid if added)
- `test/golden/refs/dot-htmltable-rounded-grad.svg` (create — native oracle)
- `test/golden/manifest.json` (modify — add entry/entries)
- `test/golden/suite.test.ts` (modify — bump count assertion)
- `test/corpus/parity.json` (regenerate)
- `test/corpus/PARITY.md` (regenerate)
- `plans/htmltable-rounded-fill/decision-journal.md` (append)
- `plans/htmltable-rounded-fill/comparisons/*` (only if a case does not flip)

Do NOT edit `src/**` — if a source change is needed, the fix is incomplete:
STOP and route back to T1.

## Read-set

- `test/golden/manifest.json` (entry shape), `test/golden/suite.test.ts`
  (count assertion + `compareSvg` normalization)
- `test/corpus/survey.ts`, `test/corpus/dashboard.ts`, `test/corpus/render-one.ts`
- `plans/htmltable-gradient-fill/comparisons/README.md` (prior gap docs — the
  baseline these cases are improving from)

## Interface inputs (from T1)

Port emits the table bgcolor fill as a rounded `<path fill="url(#l_N)">` (or
solid), bordered-cell fills carry `stroke-width="<border>"`, output byte-matches
the oracle for all 5 grd* graphs.

## Acceptance criteria

- Given each of the 5 grd* inputs, when rendered by the port, then it
  byte-matches the native oracle (modulo the standard SVG preamble).
- Given the new golden, when `vitest run test/golden/suite.test.ts` runs, then it
  passes and the manifest count increased.
- Given the regenerated `parity.json`, then the 5 `graphs-grd*` ids are
  `byte-match` (byte-match total +5) and no previously-passing id regressed.

## Observability / Rollback

N/A — test artifacts only. Reversible (revert commit).

## Quality bar

`npx vitest run` exits 0. Report only: cases flipped, parity delta
(byte-match / structural / diverged before→after), goldens added, any holdout.

## Commit

`test(htmltable): byte-match + golden for rounded table gradient fills`
