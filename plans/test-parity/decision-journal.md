# Decision Journal — test-parity project

| Date | Mission/Task | Decision | Rationale | Alternatives considered |
|------|--------------|----------|-----------|------------------------|
| 2026-06-10 | M1 start | Baseline re-run: 957 passed / 44 failed — identical to project baseline; no pruning needed | README requires re-baseline at mission start | — |
| 2026-06-10 | M1/T1 | Validated C ellipse formula numerically before coding: "center" @14pt Times (LUT: 33×16.5pt) → lw 33.438 (ref rx 33.44 ✓); "A" → 27/27/36 ✓. Spare-height branch (height > dimen.y·√2, valign=c) is the operative path | Confirms PAD(16,8) + sqrt(1/(1−(dy/h)²)) x-expansion is the exact C behavior to port | Plain √2 expansion (wrong: gives 69.3 not 66.88) |
| 2026-06-10 | M1/T1 | Skip C poly_desc size_gen hook (cylinder) and usershape/image sizing in poly-sizing port; document with TODO @see refs | TS shapeData has no poly_desc function tables yet and no image loader; no suite test uses cylinder/image shapes | Full port now (blocked: gvusershape has no TS counterpart) |
| 2026-06-10 | M1/T2 | Touched src/layout/osage/index.ts (clusterInitGraph) beyond the core write-set: route through commonInitNode when a measurer exists, keep the info.width fallback for measurer-less unit tests | C osage calls neato_init_node → common_init_node, so label sizing must reach osage; fallback preserves osage.test.ts expectations that drive sizing from n.info.width | Leaving osage unrouted (would contradict C and starve mission 2) |
| 2026-06-10 | M1/T2 | No patchwork change: C patchwork sizes nodes from tree-map area, common_init_node runs only in finishNode (label building), which TS does at render time | Verified patchworkinit.c + patchwork.c; label-childCount diffs are mission 3 scope | Routing patchwork through polySize (wrong — would fight area-driven sizes) |
| 2026-06-10 | M1/T2 | commonInitNode keeps a measurer-less fallback path (old 54x36 attr defaults) instead of requiring a TextMeasurer | Engine unit tests construct graphs without a GVC context; C has no such mode but render parity is unaffected (renderSvg always has a measurer) | Making measurer mandatory (breaks ~30 layout unit tests for no parity gain) |
| 2026-06-10 | M1/T2 gates | Suite after routing: 978 passed / 44 failed — identical failure set to baseline; all 11 dot goldens green; twopi-star first diff moved from ellipse@rx (27 vs 33.44, now fixed) to g[3] childCount (edge structure, mission 4) | T2 acceptance criteria all met | — |
| 2026-06-10 | M1/T3 | User pulled latest upstream into ~/git/graphviz mid-mission (now at 9d6e3abfd). Verified poly_init sizing block is byte-identical in the relevant formulas (PAD, SQRT2 expansion, spare-height branch, /cos inscribing) — port unaffected | Spec repo moved; revalidated rather than assumed | Re-reading the whole file (unnecessary; targeted grep of the ported formulas suffices) |
| 2026-06-10 | Project | Extended the spec review to 14.1.5..15.0.0 (port may have been written from 14.1.5 sources; refs are 15.0.0). NO tests added in that range either (only CI skips + diffimg tool removal). Ported-module behavior deltas: (a) circogen blockpath.c find_longest_path gained a null-guard returning an empty nodelist when no common ancestor exists (14.1.5 crashed on 0-node/overlapping graphs) — mission 5 must port the 15.0.0 form; (b) common/shapes.c: warning-string tweak + poly_gencode dead-code removal, no sizing/layout effect — mission 1 port unaffected; (c) sfdpgen SpringSmoother dead-store/leak cleanups, no numeric effect. Net: 14.1.5-vs-15.0.0 skew cannot explain any current golden failure | If existing TS code was ported from 14.1.5, the only divergence vs the 15.0.0 refs is a degenerate-graph crash path; nothing layout-numeric changed | — |
| 2026-06-10 | Project | Reviewed updated ~/git/graphviz (15.0.0-82-g9d6e3abfd, fresh clone). Tests added since 15.0.0: ONE — test_2835 (gvpr, marked xfail upstream = encodes a known unfixed bug, gvpr is outside port scope). No new layout tests. All mission 2–6 C surfaces (osage, patchwork, twopigen, circogen, dotgen, cdt, cgraph, pack, pathplan, label) have ZERO diffs since 15.0.0. fdpgen has post-15.0.0 behavior-adjacent churn: ULP-level float reorderings in tlayout/xlayout doRep (hypot vs sqrt(x²+y²)) and a NEW Mlimit force cutoff (-Lm flag; default HUGE_VAL = off). sfdpgen/sparse/neatogen churn is refactor-only. RULE going forward: missions 7–8 read fdpgen/sfdpgen specs at `git show 15.0.0:<path>` — refs are 15.0.0 output and D3 demands exact numerics; do NOT port the Mlimit branch | Refs are generated by 15.0.0; porting HEAD's fdp formulas risks ULP drift against a 0.5pt tolerance and adds a code path that didn't exist when refs were made | Porting HEAD and hoping 2-decimal SVG rounding hides ULP drift (violates D3) |
| 2026-06-10 | M2/T1 | Mission 2 baseline re-run: 978/44, unchanged. Recon found the dominant osage gap is OUTSIDE osage: GvcContext.layout() runs engine.cleanup before render (C runs gvFreeLayout after gvRenderJobs), destroying info.clust before emit. Also: osage DFLT_MARGIN=8 vs C 4; missing do_graph_label/place_graph_label; pack attr readers are stubs (info.packMode instead of packmode attr). osage-sortv ref proves sortv is IGNORED without packmode=array_u — the test fails on margins only | Full analysis in mission-2-osage/gap-analysis.md | — |
| 2026-06-10 | M2/T2 | PUSH-FORWARD outside mission write-set: fixing src/gvc/context.ts + src/index.ts pipeline order (cleanup after render) under the "obvious bug in already-ported shared code, with suite-green proof" condition; context.test.ts expectation of ['layout','cleanup'] encodes the buggy order → updated per D5 | C pipeline is gvLayoutJobs → gvRenderJobs → gvFreeLayout; every engine mission after this needs render to see layout state | Making osageCleanup non-destructive (deviates from C osage_cleanup; hides the real bug) |
| 2026-06-10 | M2/T5 | Touched src/layout/pack/* (allowed exception, journal required): ported chkFlags/parsePackModeInfo/getPackModeInfo/getPack to read the real pack/packmode graph attrs with full C syntax. osage.test.ts pack-mode tests set the old stub channel (g.info.packMode) and one regressed; updated all four to set the packmode ATTR per D5 | C reads agget(g,"packmode"); the stub channel was never populated by any caller, so PK_USER_VALS/COL_MAJOR/sz were unreachable | Keeping the stub and special-casing osage (leaves dead spec surface) |
| 2026-06-10 | M2/T6 | Mission 2 complete: 978/44 → 984/38; all 6 osage goldens pass; 11 dot goldens green. baseline-after-m2.md written. Surprise: the pipeline-order fix also un-blocked patchwork's missing element — its 6 tests now first-diff on coordinate translation only (mission 3 overview updated) | Counts and per-family tables recorded; merge follows | — |
| 2026-06-10 | M3/T2 | Touched src/layout/pack/* (allowed, journal): shiftOneGraph now also shifts g.info.bb, set label positions, and recurses cluster bbs (port of pack.c:shiftGraph). patchwork replaces node-union normalizeGraphBB with root-bb translate_drawing | C translate_bb/shiftGraph both move cluster bbs with the drawing; the node-union offset deviates from C whenever insets make cluster bbs exceed the node union | Patchwork-local cluster shift (leaves shiftGraphs/normalizeGraphBB diverging from C for every other engine) |
| 2026-06-10 | M3/T3 | Mission 3 complete: 984/38 → 990/32; all patchwork goldens pass; baseline-after-m3.md written; remaining families' first diffs unchanged | One-task mission; the M2 pipeline fix had pre-cleared the childCount gap | — |
| 2026-06-10 | M4/T2 | Edge routing was dead end-to-end for the neato family; fixes beyond src/layout/twopi/* (push-forward, shared-code bugs with suite-green proof): (a) ported C spline_edges wrapper (pos shift + cluster shift + coord sync) into neato/splines.ts; (b) ported poly_inside + same_side into src/common/poly-inside.ts and bound it in POLY_FNS (was null — clipping never ran anywhere); (c) clipSide passed a slice COPY to shape_clip0 so clip results were discarded — write-back added (src/common/splines-clip.ts); (d) makePort defaulted clip:false vs C Center port clip:true (src/model/edgeInfo.ts); (e) poly-sizing now also computes ND_outline_width/height (penwidth ring) used by poly_inside; commonInitNode stores them and installs shape_info per C poly_init; (f) undirected edge titles emit `--` (src/render/svg-helpers.ts, src/gvc/job.ts) | C pipeline parity; twopi-star edge paths now byte-identical to the ref; suite 990/32 → 993/29 with all dot goldens green | Twopi-local workarounds (would leave clipping broken for missions 5-7) |
| 2026-06-10 | M4/T2 | Documented deviation in poly-inside.ts: polygon segment walk uses the BASE vertex ring (C walks the outline ring, penwidth/2 farther out); the bb quick-reject does use outline extents. Storing multi-ring vertices would break renderPolygon, which maps the whole array | Affects polygon clip endpoints by ≤ penwidth/2 (0.5pt at default); ellipses (all current tests) are exact. Revisit in mission 6 if neato-polygon/diamond clip coords miss tolerance | Multi-ring vertices in shape_info (breaks node rendering); rings in a parallel field (defer until a test needs it) |
| 2026-06-10 | M4/T3-T4 | pack buildSubgraph (journal: pack/*) now parents component graphs to the original root (C ccomps uses agsubg, so g->root resolves root attrs — our detached components hid ranksep). twopi traversals switched to agfstedge order (out-edges then in-edges): the global-edge-list scan mirrored the child fan (twopi-chain flipped). hub-at-origin unit test asserted pre-shift coords; rewritten per D5 to assert hub-at-leaf-centroid | twopi-chain + twopi-ranksep goldens pass; suite 995/27 → 996/26 | — |
| 2026-06-10 | M4/T5 | Ported the polyomino packer (pack.c computeStep/fillLine/genBox/genPoly/fits/placeGraph/polyGraphs) to src/layout/pack/poly-place.ts; putGraphs now dispatches mode ≤ l_graph to it and reads sortv for array mode; shiftGraphs fixed to apply translation DELTAS (C semantics) instead of target-LL; twopi packs components in l_node mode (CL_OFFSET=8) per C. Not ported, journal-noted: l_clust top-cluster blocks in genPoly, doSplines spline-following in fillEdge, pinfo.fixed protocol (no callers yet) | twopi-disconnected passes; all 6 twopi goldens + both unit tests green; suite 996/26 → 997/25. neato/fdp/sfdp disconnected tests will reuse this packer in missions 6-8 | bb-mode approximation (cannot reproduce C component offsets) |
| 2026-06-10 | M4/T6 | Mission 4 complete: 990/32 → 997/25; all twopi goldens pass; baseline-after-m4.md written. dot+osage+patchwork+twopi green (29/53 goldens). Remaining families' first diffs unchanged (their core layouts still misplace nodes) | Missions 5-8 inherit working clipping, the spline_edges wrapper, and the polyomino packer | — |
| 2026-06-10 | M5 | Mission 5 highlights: (a) circo computes in INCHES per C (largest_nodesize from ND_width/height; rec verified against installed graphviz 15.0.0 binary, which byte-reproduces the refs — used as an oracle); (b) cgraph subgraph iteration is ID-ORDERED, not insertion-ordered — block/component node lists now sort by orig id (this decides the spanning-tree root and ring order); (c) blkParent was returning CHILD(b) instead of PARENT(CHILD(b)) so no child block was ever attached to its articulation node; sibling iteration walked a grandchild chain; (d) record_inside ported (record edge clipping); (e) root bb refreshed after splineEdgesShifted (stale pre-shift bb from packing); (f) equal-radius unit test measured from origin pre-translation — fixed per D5 (centroid). Shared-code touches (journal): src/common/poly-inside.ts (recordInside), src/common/shapes.ts (RECORD_FNS), src/common/record.ts (inch width/height per C record_init) | All 6 circo goldens + equal-radius unit test pass; suite 997/25 → 1004/18 | — |
| 2026-06-10 | M6 | Mission 6 complete: all 7 neato goldens pass (1004/18 → 1013/11). Key ports/fixes: exact drand48 (BigInt LCG, libc-verified); float32-faithful stress kernel (fround discipline; stress traces match the oracle digit-for-digit); C makeGraphData adjacency; overlap removal now defaults to NONE per C graphAdjustMode (our VPSC-by-default shifted every layout); effective (attr-resolved) polygon stored in shape_info with C extent-normalized vertices (shape=polygon sides=6, diamond non-box guard); poly_inside walks the outline ring (mission-4 deviation resolved); neato addCluster port (tight member bbs + labels, IPSEPCOLA branch active in the Homebrew oracle build). Shared-code touches (journal): poly-vertices/poly-inside/poly-sizing (outline ring), nodeinit (effective polygon), poly-init (keep layout shape_info), pack (spline-aware shifts, ID-ordered induced components) | Verified against the installed 15.0.0 oracle at each step (maxiter bisection isolated the overlap-removal divergence; -Gnotranslate isolated the shift) | — |
| 2026-06-10 | M1/T3 | Re-baseline written to baseline-after-m1.md: counts 957/44 → 978/44 (+21 = new unit tests; failure set identical). Surprise: NO golden flipped to pass — mission 1's "failure count strictly decreases" success criterion was met only via first-diff progress (twopi-star/root-attr rx fixed, circo-html-label 91→111). The suite-failure-count form of the criterion was optimistic: every failing family has deeper, engine-specific gaps behind the sizing gap | Documented honestly; mission overviews 2–8 updated so each recon starts from the new first diffs | Calling the mission failed (wrong: overview.md defines success as count decrease OR first-diffs shrinking, which happened) |
| 2026-06-10 | M7/T1 | Mission 7 baseline re-run: 1013/11, unchanged from baseline-after-m6 (fdp 6 + sfdp 5). T1 recon completed (prior session left it partial): prism overlap fallback NEVER runs for any fdp golden (oracle: `-Goverlap=9:` byte-identical to default) so no prism/delaunay port; x_layout force expansion IS load-bearing on all 6 (`-Goverlap=true` differs). fdpParms defaults verified at tag. Task plan restructured to T2 (flat+disconnected rewrite) + T3 (clusters) — old T4/T5 folded in because disconnected packing happens inside layout() on derived components, not in an outer pass | Avoids a ~2000-line dead-end port of prism/SparseMatrix/Delaunay; oracle experiments are the evidence | Porting prism anyway (wasted; unreachable for owned tests) |
| 2026-06-10 | M7/T2 | Rewrote src/layout/fdp/ to the 15.0.0 C architecture (10 files: model, grid, tlayout×3, xlayout, comp, derive, ports, layout, normalize, init, index). Three findings drove shared-code push-forwards (each suite-green): (a) **FMA contraction** — the Homebrew arm64 binary compiles `x*x+y*y` and `disp±delta*force` to fmadd/fmsub (otool-verified in _doRep); ported software fma (Dekker+two-sum) in src/common/fma.ts and applied at the contracted tlayout sites — 300 tlayout iterations then match the C binary BIT-FOR-BIT (full-precision C oracle probe linking installed libgvc, /tmp/fdp-oracle.c); (b) **cgraph out-edge order** — agfstout sorts by (head seq, edge seq) per edge.c:agedgeseqcmpf, not creation order; fixed Node.outEdges/inEdges (src/model) — this fixed fdp-large emit order; (c) **arrow_clip port** — clip_and_install's arrowhead pass was a stub returning ARR_NONE; ported arrow_flags (dir/arrowhead/arrowtail; normal+none types only) + arrowStartClip/arrowEndClip into src/common/splines-clip.ts reusing dot's validated normalArrowLen/arrowheadPolygon; splines.test.ts AC5 updated per D5 (it encoded the stub). Also: src/model/nodeInfo.ts FdpAlgData/FdpDndata union extension (AD-7 pattern); sepFactor/esepFactor/parseFactor ported to src/layout/neato/sep-factor.ts; prism overlap removal NOT ported (throwing guard; recon proved unreachable); C rand() coincident-node fallback NOT ported (throwing guard); processClusterEdges/setClustNodes compound path NOT ported (guard); fdp ndim fixed at 2 | All 6 fdp goldens pass (oracle parity ≤1.3e-15 inches end-to-end); suite 1013/11 → 1001/5 (only sfdp remains; count drop = old approximation-era fdp unit tests replaced by 14 C-derived ones incl. full-precision oracle-parity fixture) | Porting prism/Delaunay (~2000 lines, unreachable); emulating macOS rand() (unreachable); leaving outEdges creation-ordered with fdp-local sort (hides the C semantic from other engines) |
| 2026-06-10 | M7/T2 | T3 (clusters) and T4 (components/pipeline tail) were absorbed into the T2 rewrite: deriveGraph runs unconditionally in C, so the recursion/ports/packing machinery was inseparable from the flat path; fdp-cluster + fdp-nested-cluster + fdp-disconnected pass from the same commit. T3/T4 are verification-only and closed by the T2 gates | layout.c's structure does not decompose along test-family lines; splitting would have produced untestable intermediate states | Stub-first T2 with cluster STOPs (would fail the suite-green gate on cluster tests already passing mid-rewrite) |
| 2026-06-10 | M7/T-final | Mission 7 complete: 1013/11 → 1001/5; all 6 fdp goldens pass; only sfdp (mission 8) remains. Gates: tsc clean, suite green vs baseline (failure set strict subset), write-set = fdp/* + journaled shared code (common/fma, common/splines-clip arrow pass, neato/sep-factor, model/node outEdges order, model/nodeInfo union). baseline-after-m7.md written with mission-8 handoff notes (fma contraction, C oracle probe, adjacency ordering) | Per-task gates all ran during T2; this row records the mission close | — |
| 2026-06-10 | M8/T1 | Mission 8 baseline: 1001/5 (sfdp only). Recon: sfdp's default overlap mode is "prism0" = remove_overlap with ntry=0 → ONLY scale_to_edge_length runs, then `if (!ntry) return` — GTS/Delaunay/OverlapSmoother/rbtree are UNREACHABLE for all 5 goldens despite the binary linking GTS. tscheme resolves to QUAD_TREE_NORMAL (attr default overrides ctrl_new HYBRID) → spring_electrical_embedding only; QuadTree supernodes engage at n≥45 (sfdp-large). ALL randomness = one global libc rand() stream: macOS rand = Park–Miller minstd (verified srand(123) → 2067261, …); drand=rand/RAND_MAX; gv_permutation (coarsening matching order) draws from the SAME stream BEFORE srand(123), so stream state threads across components in sfdp-disconnected. Tasks: T2 substrate (minstd + SparseMatrix subset), T3 multilevel+embedding, T4 quadtree, T5 pipeline | Scope bounded to ~2200 TS lines, no triangulation port; bisection levers identified (-Glevels=1, -Gquadtree=none, C probe) | Porting prism/GTS (unreachable); porting fast/slow embedding variants as primary (unreachable at defaults — guard instead) |
| 2026-06-10 | M8/T2 | Substrate landed: src/common/crand.ts (Park–Miller minstd = macOS rand(), one global stream incl. gvRandom/gvPermutation rejection sampling) + src/layout/sfdp/sparse-matrix{,-multiply}.ts (CSR/COORD subset, REAL+PATTERN only — INTEGER unreachable from sfdp_layout; row order = C mask-append/counting-sort order, never sorted). 22 new unit tests incl. the empirically-captured srand(123) stream | crand in common/ (journal): it is the platform-libc model, not sfdp-specific (fdp's unreachable rand() fallback could reuse it) | Int32Array/Float64Array backing (plain arrays match C growth semantics more directly at our sizes) |
