# T8 — promote batch-2 goldens + create RL golden

## Context

T6 reported LR/BT PASS and T7 reported multi-edge PASS (if not, the
orchestrator stopped before launching this task). AD3 adds RL
coverage; AD5 governs promotion. Verify `dot -V` reports 15.0.0 before
generating any ref — STOP if not.

## Task

1. Promote dot-rankdir-lr, dot-rankdir-bt, dot-multi-edge per AD5
   (git mv from quarantine; append manifest entries with provenance
   description "ref: graphviz 15.0.0 dot -Tsvg; promoted from
   quarantine (mission 9)").
2. Create test/golden/inputs/dot-rankdir-rl.dot — same shape as the
   quarantined LR input but rankdir=RL (read the LR .dot as template).
   Generate ref: `dot -Tsvg test/golden/inputs/dot-rankdir-rl.dot >
   test/golden/refs/dot-rankdir-rl.svg`. Append manifest entry
   (description: "ref: graphviz 15.0.0 dot -Tsvg; AD3 mission 9").
   If it FAILS against the port: do NOT quarantine silently — this is
   a stop signal (T6 claimed RL smoke passed); report and stop.
3. Update suite.test.ts count assertion (comment + name + value) to
   66.
4. Full suite green.

## Write-set

test/golden/manifest.json (append only), test/golden/suite.test.ts
(count only), test/golden/inputs/*, test/golden/refs/*,
test/golden/quarantine/* (moves out)

## Read-set

T6/T7 final reports (in your prompt); test/golden/quarantine/
dot-rankdir-lr.dot (RL template); manifest entry shape; AD3/AD5

## Acceptance criteria

- Given promotion + the new RL golden, then manifest length = 66,
  quarantine contains only dot-head-tail-label.{dot,svg}, suite green
- Given the RL ref, then it was generated by the local 15.0.0 binary
  in this task (not copied)
- Given existing manifest entries, then byte-unchanged

## Observability: N/A. Rollback: Reversible.

## Quality bar

npx tsc --noEmit clean; npx vitest run green. Commit:
`test(T8): promote batch-2 goldens, add dot-rankdir-rl golden`
