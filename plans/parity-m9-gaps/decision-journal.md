# Decision Journal — parity-m9-gaps

| Date | Task | Decision | Rationale | Alternatives considered |
|------|------|----------|-----------|------------------------|
| 2026-06-11 | batch-1 | Execution plan: T1–T4 launched as 4 parallel sonnet agents (disjoint write-sets per overview.md conflict check); T5 sequenced after T1–T3 results. Agents do NOT commit — orchestrator commits each task separately after gates, to avoid concurrent git index-lock races between parallel agents. One commit per task preserved. | Parallel batch per brief; commit serialization is an execution detail, not a scope change | Agents self-commit (rejected: index.lock races with 4 concurrent writers) |
| 2026-06-11 | T3 | **Bug 1 fixed** (nodesep default): Changed `g.info.nodesep ?? 16` to `?? 18` in both `RoutingHelper.withVconfig` and `RoutingHelper.straight` in `src/layout/neato/splines.ts`. C spec: `lib/common/input.c:667` sets `GD_nodesep(g) = POINTS(DEFAULT_NODESEP) = POINTS(0.25) = 18`. This fixes circo-self-loop (PASS, maxDelta=0) and fixes the spline control-point coordinates for twopi-self-loop. Side effect: `fdp-tiny-self-loop` port-pin (`refs-port/`) becomes stale (was pinned against incorrect 16-pt stepx); T5 must regenerate the pin. C reference (`refs/fdp-tiny-self-loop.svg`) already shows x=72, so the C-reference comparison passes. | C is the spec; the C binary uses nodesep=18 for fdp too | Leave at 16 (rejected: wrong per C spec) |
| 2026-06-11 | T1 | **Write-set deviation accepted**: T1 modified src/layout/dot/dot.test.ts (outside its declared write-set) — one existing test pre-set `e.info.minlen` and asserted it was preserved; C's late_int always overwrites from the attr, so the expectation was wrong. The fix is forced by C-fidelity; leaving it would keep a test asserting non-C behavior. Accepted with this entry; flagged for human review in the batch close. | C is the spec; the old test encoded the port's bug | Revert + stop entire batch (rejected: disproportionate for a forced 1-test expectation fix; no write-conflict risk — no other task owns dot.test.ts) |
| 2026-06-11 | T1 | **dot-minlen NOT promotable**: comparison fails at maxDelta 4.32pt (deterministic tolerance 0.01) — minlen rank structure is correct, but node A sits at cx=63/cy=-198 vs ref 62/-199 and the A→C spline routes differently. T1 attributes this to a distinct port bug (position/emitter), not the attr-init change. Stays quarantined; needs its own investigation (new task — human to scope). | T5 promotes only reported PASS per contract | Promote anyway (rejected: AD5), widen tolerance (forbidden by README) |
| 2026-06-11 | T2 | **Write-set deviation accepted**: T2 created src/layout/dot/self-loop.ts (new module, not in declared write-set) holding the ported dotsplines.c self-edge branch (collectOtherEdges/computeSizey/routeSelfEdgeGroup/buildDotSinfo) instead of inlining into splines.ts. Also: edge-route.ts deliberately NOT modified — its self-loop `continue` guard is load-bearing (outEdges() includes self-loops; spl-set guard prevents re-routing). Accepted: factoring is content-neutral, golden passes exact (maxDelta 0). Flagged for review. | New-file factoring of declared work, no behavioral delta; C call order preserved in splines.ts dispatch | Force inline into splines.ts (rejected: churn with zero behavioral difference) |
| 2026-06-11 | T5 | **Pin regeneration (push-forward)**: fdp-tiny-self-loop port-pin (test/golden/refs-port/) re-baselined from current port output. T3's nodesep 16→18 fix moved fdp self-loop output toward C (x=72 now matches the C reference; pin delta 0.16pt from stale snapshot). Pin exists to catch unintended drift; this drift is intended and C-faithful. refs/ (C references), manifest entry, and tolerances untouched. | README push-forward: "Tolerance + portReference pinning for promoted iterative-engine goldens"; README NEVER-list covers refs/ + manifest + TOLERANCES, not refs-port snapshots | Leave pin stale (rejected: permanent red suite), revert nodesep fix (rejected: C is the spec) |
| 2026-06-11 | batch-1 | **Gate protocol note**: T1–T4 ran in one shared working tree, so per-task vitest runs overlapped each other's changes; the suite is gated once on the batch-final tree (after T5) instead of per intermediate commit snapshot. tsc + write-set checks run per commit. | Shared-tree parallel execution makes intermediate snapshots non-isolatable | Re-gate each snapshot via stash dance (rejected: high risk of losing agent work) |
| 2026-06-11 | T5 | T5 executed inline by the orchestrator (not a spawned agent): purely mechanical promotion with all T1–T3 reports already in context; per the ~30-min direct-handling rule. Promoted 3 PASS goldens only; dot-minlen + twopi-self-loop left quarantined per contract ("promote only on reported PASS"). | Spec contract anticipated partial promotion | Spawn sonnet agent per brief (rejected: pure overhead for a 9-file mechanical change) |
| 2026-06-11 | batch-1 | **BATCH CLOSE + MISSION STOP**: gates green on final tree (tsc clean; vitest 1090/0; manifest 57→60; quarantine 9→6). Five commits, one per task (77fc30c T4, 1ecb5af T1, 9fb1e2e T2, d5ecf99 T3, d2305d6 T5). Stopping before batch 2 per README stop condition "change needed outside the active task's write-set": (1) T3 Bug 2 twopi bb fix needs splines-clip.ts or twopi/index.ts — human chooses location; (2) dot-minlen residual 4.32pt offset needs an unscoped new debug task; (3) human to ratify T1/T2 write-set deviations. Full detail in README "Stop report". | Stop conditions are non-negotiable per autonomous-execution.md; batch 2 could technically proceed but write-set ratification is pending | Continue to batch 2 (rejected: triggered stop conditions require human input first) |
| 2026-06-11 | stop-resolution | **Human input (Scott, 2026-06-11) resolving the batch-1 stop:** (1) twopi bb fix = C-faithful option (a), landing in src/common/splines-clip.ts (thread bb through clipAndInstall per C splines.c update_bb_bz); (2) dot-minlen offset = new debug+fix task, wide write-set pre-approved ("figure out the bug and fix"), building on .agent-notes/dot-minlen-offset-2026-06.md; (3) T1/T2 write-set deviations ratified. Mission resumes: new batch 1b (T3b bb fix, T1b minlen debug, T5b promote) inserted before batch 2. | Direct instruction | — |
| 2026-06-11 | batch-1b | Execution plan: T1b + T3b parallel (sonnet). Conflict control: T3b owns src/common/splines-clip.ts + caller threading in src/layout/neato/splines.ts; T1b may fix anywhere its root cause dictates EXCEPT T3b's files and test/golden/ (stop+report if required). T5b promotion inline by orchestrator after both, same as T5. | Disjoint by construction; T1b's fix location unknown until diagnosed, so exclusion rule instead of enumeration | Serialize T1b after T3b (rejected: independent domains, dot vs neato family) |
| 2026-06-11 | T3 | **STOP CONDITION — Bug 2** (twopi viewBox/dimensions): twopi-self-loop still FAILS at maxDelta=18. SVG width is 62pt; expected 80pt. Root cause: after `splineEdgesShifted`, `normalizeGraphBB` (in `pack/index.ts`) calls `computeSubgraphBB` which is node-only, overwriting `g.info.bb` with bounds that don't include the self-loop's x=72 extent. C fix: `lib/common/splines.c:312` calls `update_bb_bz(&GD_bb(g), cp)` inside `clip_and_install` for every installed bezier segment. The TS port's `clipAndInstall` (`splines-clip.ts`) has `updateBbBz` available but always passes `bb=null`. The true fix location is either: (a) `src/common/splines-clip.ts` — pass `g.info.bb` through `clipAndInstall` (requires graph reference propagation), or (b) `src/layout/twopi/index.ts` — expand `g.info.bb` from spline extents after `normalizeGraphBB`. Both files are outside the T3 write-set. Declaring stop: write-set boundary violation. T3 partial result: circo PASS, twopi spline coords PASS but dimensions FAIL. | Write-set policy per task brief; fix requires file outside write-set | Expand BB in pipeline.ts before normalizeGraphBB (rejected: normalizeGraphBB overwrites g.info.bb after pipeline.ts returns) |
