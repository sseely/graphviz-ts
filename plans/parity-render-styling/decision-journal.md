# Decision Journal — parity-render-styling

| Date | Task | Decision | Rationale | Alternatives considered |
|------|------|----------|-----------|------------------------|
| 2026-06-13 | Batch 1 plan | Dispatch T1 + T2 as parallel agents (typescript-pro, model sonnet). Disjoint write-sets: T1 owns src/common/style-resolve.ts (+test); T2 owns src/gvc/device.ts + src/gvc/job.ts factory (+tests). | parallelism.md: no shared write target, no inter-dependency. Both must keep 82 goldens byte-identical. | Serial execution (slower, no benefit). |
| 2026-06-13 | T2 byte-stability | Verified all production node/cluster shape draws pass filled=false (grep: only htmltable passes true); edges/arrows use hardcoded fill/stroke, do not read job.obj. So default ObjState (penColor black, fillColor white, pen Solid, fill None, penWidth 1.0) reproduces today's emitStyle output exactly. | Pre-dispatch check to de-risk byte-stability gate for T2. | — |
| 2026-06-13 | T1 done | style-resolve.ts: parseStyleFlags/resolvePenType/resolvePenWidth/resolveNodeFill/resolvePenColor. Orchestrator re-verified C facts against ~/git/graphviz: penColor=color\|\|black (shapes.c:389), findFillDflt=fillcolor\|\|color\|\|lightgrey (shapes.c:401), DEFAULT_FILL=lightgrey/DEFAULT_COLOR=black (const.h:48,69). penwidth attr wins over bold flag; dashed before dotted. T1 agent's structured return was truncated but file is complete + tested. | T3/T4/T5 depend on T1 — verified independently, not on agent self-report. | — |
| 2026-06-13 | T2 done | createObjState(type=ObjType.Node) factory; push/pop in renderNode/renderEdge/renderOneCluster/renderGraph with try/finally. emit_clusters: sub-cluster recursion after parent pop (emit.c:3940). svg.test.ts makeObjState re-pointed to factory (pre-existing-violation cleanup). | — | Pooling ObjState (deferred — per-call alloc matches existing literal pattern; not a gate). |
| 2026-06-13 | Batch 1 gates | tsc 0; vitest 1514 passed/0 failed; 82 goldens byte-identical (diff -rq /tmp/t6-baseline clean). Branch feature/parity-render-styling created off feature/post-parity. T1, T2 committed (one commit each, write-sets clean). | All Batch 1 quality gates green. | — |
| 2026-06-13 | Batch 2 device.ts conflict (resolves T5 STOP flag) | C draws the cluster box in emit_clusters via gvrender_box(GD_bb,filled); the port draws it in device.ts renderOneCluster with hardcoded filled=false. Cluster fill therefore REQUIRES a device.ts change (filled flag + obj-state). T4 (renderEdge) also edits device.ts. Same file, different functions → cannot run T4 and T5 concurrently. DECISION: serialize device.ts. Round 1: T3 (poly-gencode.ts) ‖ T4 (device.ts renderEdge + svg-helpers.ts). Round 2: T5 (device.ts renderOneCluster + svg-cluster.ts + svg-graph.ts + cluster resolver). T5 write-set EXPANDED to include device.ts (renderOneCluster) + style-resolve.ts (resolveClusterFill) — the brief's flagged STOP-and-report, resolved by the orchestrator. | Single-writer-per-file (parallelism.md); faithful to C (box belongs in the common emit layer = device.ts, not the svg plugin). | (a) Collapse edge+cluster into one agent — rejected (mixes concerns, breaks one-commit-per-task). (b) Draw a 2nd filled polygon in svgBeginCluster — rejected (C emits ONE box; would fail T6 oracle). |
| 2026-06-13 | C-oracle ground truth (dot 15.0.0) | node fillcolor → ellipse fill=lightblue stroke=black; edge color=red → path stroke=red + arrow fill=red stroke=red; graph bgcolor → single bg polygon recolored (same coords); cluster: color=lightgrey→fill+stroke=lightgrey, style=filled(no color)→fill=lightgrey stroke=black, bgcolor→fill=bg stroke=black, unfilled→none/black. Only existing golden with styled edges: dot-edge-styles.svg (dasharray 5,2 / 1,5, pen black, width 1) — T4 byte-critical. bgcolor=transparent (SVG truecolor) → NO background polygon emitted. cluster penwidth=3 → stroke-width=3 on boundary. | Captured before dispatch so T3/T4/T5 + T6 have the spec. | — |
| 2026-06-13 | Batch 2 Round 1 (T3,T4) | T3 node styling + T4 edge styling dispatched in parallel (disjoint: poly-gencode.ts vs device.ts+svg-helpers.ts). Both agents' returns partly truncated; LSP diagnostics were stale mid-edit noise (tsc authoritative = 0). Orchestrator fixed a test-helper bug in poly-gencode.test.ts (polygons() picked the white background polygon for box nodes; added stroke="none" filter) — T3 impl verified correct vs C byte-for-byte. 11/11 styled node+edge cases match dot 15.0.0 oracle. | tsc 0; vitest 1540/0; 82 goldens byte-identical. T3, T4 committed. | — |
| 2026-06-13 | Batch 2 byte-risk survey | All 82 existing cluster goldens are unfilled (fill=none); all 82 backgrounds are white. So filled clusters + colored backgrounds are entirely NEW behavior — byte-stability needs only unfilled→none/black and no-bgcolor→white preserved. osage/patchwork clusters carry no fill attrs → stay none. | Confirms T5 byte-safety surface before dispatch. | — |
| 2026-06-13 | T5 done + Batch 2 gates | resolveClusterFill added to style-resolve.ts; renderOneCluster sets cluster obj-state + passes filled flag (applyClusterObjState helper for the 30-line hook); svg-graph resolveGraphBgcolor (white default / recolor / transparent-omit / gradient first-color). svg-cluster.ts untouched (fill flows through polygon→emitStyle). Orchestrator oracle-verified 10/10 cluster+graph+combined cases vs dot 15.0.0 (incl. bgcolor+filled-cluster+filled-node+colored-edge together). tsc 0; vitest 1569/0; 82 goldens byte-identical. T5 committed. Batch 2 complete. | All Batch 2 gates green; full styling parity confirmed against C oracle. | — |
| 2026-06-13 | T6 combined-case box divergence | The originally-specced dot-styled-combined (edge to a `shape=box` node `b` outside/inside a cluster) FAILED compareSvg at deterministic 0.01pt: edge spline @d coord -64.41 vs -64.52 (0.11pt). Isolated it to the BOX endpoint: identical graph with an ellipse `b` PASSES; box `b` FAILS the same coord. Pre-existing box-clipping/spline geometry divergence (likely the documented ARM optimized-routines pow vs Apple libm, M8/T3 precedent), OUTSIDE render-styling's blast radius. Per stop-condition "no silent fixes" + the brief's "keep style the variable", the combined golden was authored with ellipse endpoints (variant v4: bgcolor + filled cluster + node fill + node pencolor + blue dashed edge), which exercises MORE styling than the spec's four features and matches C at deterministic. No layout code touched. | Geometry divergence is not this mission's concern; swapping the confounding box endpoint keeps style the variable without a silent geometry fix or a loosened tolerance. | (a) Exclude dot-styled-combined entirely (journal) — viable but loses composition coverage. (b) Loosen tolerance + portReference pin (M8 precedent) — rejected: not authorized by T6, and unnecessary once the confound is removed. |
| 2026-06-13 | T6 done + Batch 3 gates | 15 styled goldens minted, all PASS compareSvg vs dot 15.0.0 at deterministic 0.01pt. Manifest 82→97 (provenance "ref: graphviz 15.0.0 dot -Tsvg; mission render-styling"); suite count test 82→97. Prior 82 refs byte-unchanged (shasum -c clean) AND prior-82 render output byte-identical to mission baseline. tsc 0; vitest 1584/0. T6 committed (manifest+suite+15 inputs+15 refs). | All gates green; first positive coverage of the new styling behavior. | — |
