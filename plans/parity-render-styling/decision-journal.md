# Decision Journal — parity-render-styling

| Date | Task | Decision | Rationale | Alternatives considered |
|------|------|----------|-----------|------------------------|
| 2026-06-13 | Batch 1 plan | Dispatch T1 + T2 as parallel agents (typescript-pro, model sonnet). Disjoint write-sets: T1 owns src/common/style-resolve.ts (+test); T2 owns src/gvc/device.ts + src/gvc/job.ts factory (+tests). | parallelism.md: no shared write target, no inter-dependency. Both must keep 82 goldens byte-identical. | Serial execution (slower, no benefit). |
| 2026-06-13 | T2 byte-stability | Verified all production node/cluster shape draws pass filled=false (grep: only htmltable passes true); edges/arrows use hardcoded fill/stroke, do not read job.obj. So default ObjState (penColor black, fillColor white, pen Solid, fill None, penWidth 1.0) reproduces today's emitStyle output exactly. | Pre-dispatch check to de-risk byte-stability gate for T2. | — |
| 2026-06-13 | T1 done | style-resolve.ts: parseStyleFlags/resolvePenType/resolvePenWidth/resolveNodeFill/resolvePenColor. Orchestrator re-verified C facts against ~/git/graphviz: penColor=color\|\|black (shapes.c:389), findFillDflt=fillcolor\|\|color\|\|lightgrey (shapes.c:401), DEFAULT_FILL=lightgrey/DEFAULT_COLOR=black (const.h:48,69). penwidth attr wins over bold flag; dashed before dotted. T1 agent's structured return was truncated but file is complete + tested. | T3/T4/T5 depend on T1 — verified independently, not on agent self-report. | — |
| 2026-06-13 | T2 done | createObjState(type=ObjType.Node) factory; push/pop in renderNode/renderEdge/renderOneCluster/renderGraph with try/finally. emit_clusters: sub-cluster recursion after parent pop (emit.c:3940). svg.test.ts makeObjState re-pointed to factory (pre-existing-violation cleanup). | — | Pooling ObjState (deferred — per-call alloc matches existing literal pattern; not a gate). |
| 2026-06-13 | Batch 1 gates | tsc 0; vitest 1514 passed/0 failed; 82 goldens byte-identical (diff -rq /tmp/t6-baseline clean). Branch feature/parity-render-styling created off feature/post-parity. T1, T2 committed (one commit each, write-sets clean). | All Batch 1 quality gates green. | — |
