# Decision journal

Appended during execution. Every non-trivial judgment call, every write-set
expansion approval, and the T2 localization result go here.

| When | Batch/Task | Decision / Finding | Rationale |
|------|-----------|--------------------|-----------|
| 2026-06-26 | B1/T1 Step0 | Precise divergent edges (ADR-4, keyed by tail->head): **edge2 `n002->n001`** native 2-bez / port 1-bez (under-seg); **edge25 `n023->n015`** native 2-bez / port 4-bez (over-seg). README's "edge index 27" was a stale emit-index; the real port-4-seg edge is id=edge25 (`n023->n015`). Controls (match): edge27 `n018->n015` (2/2, same head n015), edge12 `n022->n008` (2/2). | Per-edge bezier-pair count diff native vs port SVG, joined by `id=edgeN`. Piece-count signatures (2/1, 2/4) match the brief. |
| 2026-06-26 | B1/T1 Step0 | Both divergent edges carry a **headlabel** (`:r:`, `:s/r:`), NOT a mid-edge `label=`. No label-vnode rank is inserted → ADR-1 "intermediate label rank" premise does not apply. Divergence is pure regular-edge spline geometry in honda's tight LR corridor (ranksep/nodesep=0.2, node w=h=0). edge2 has `samehead="m002"` (shared head port w/ n005->n001); native/port head attachment points differ (477.46,-185.14 vs 479.86,-188.4). | Fixture inspection + endpoint compare. Follow the oracle (ADR-1 caveat) — true first-divergence TBD by 4-stage dump. |
| 2026-06-26 | B2/T2 localize | **First diverging stage = STAGE2 (box corridor).** edge2 (n002→n001): `maximal_bbox` boxes IDENTICAL to C; divergence is the path ENDPOINT — C ends at y=77.7 (above n001 center 59.7), port at center 60.7 → straight vs bent → 1 vs 2 bez. edge25 (n023→n015): box count matches (13) but neighbor-vnode walls shift +1..+5 (the *same* neighbor node resized differently). Control n018 shares edge2's end-Y offset yet stays 2-bez. Discriminator: edge2/n018 carry `samehead=`; n023 does not. | C ends same-`samehead` edges at a merged head port above center; port ends at node center. |
| 2026-06-26 | B2/T2 ROOT CAUSE | **`samehead`/`sametail` edge attrs are never parsed into `e.info`** — `dotInitEdge` reads weight/minlen/etc. but not samehead. So `graphHasSamehead()` is always false and the (correctly ported + wired at index.ts:127) `dotSameports` never fires. honda uses `samehead` heavily → unmerged head ports → endpoint/corridor divergence (edge2) and neighbor-wall shifts via other samehead edges' vnode resize (edge25). | Verified: temporarily wiring `e.info.samehead/sametail` in dotInitEdge → honda piece counts 0 divergent (was 2), maxΔ 27.9→17.4. |
| 2026-06-26 | B3/T3 fix applied | Faithful fix, 4 parts: (1) `init.ts` dotInitEdge parses samehead/sametail (root cause); (2) `sameport.ts` shapeClip replaced rect stub with ellipse/poly-faithful `bezierClipNode`+`nodeInsideFn` (C shape_clip) — fixed ~17px shared-port error on ellipse heads; (3) `splines-route.ts` routeParallelEdgeGroup base = first port-defined member (C `ea = port-defined ? e0 : le0`) — fixed parallel-sibling-of-samehead routing to node center; (4) `splines.ts` groupSize splits cross-rank parallels by portcmp (C dotsplines.c:373-376) so distinct samehead/sametail ports each get their own base. Honda: diverged→**structural-match**, maxΔ 27.9→1.06, 18→2 divergent edges, 0 piece-count diffs (target edges edge2/edge25 byte-match). Residual 1px = sub-pixel shared-port-Y round on n012→n011 (ellipse-clip tie). | typecheck+npm test (2424) green. Survey: 0 regressions BOTH baselines; +8 headless / +20 pango improvements (honda + arrows family + 2193/NaN/b102/b143/ports/xx). |
| 2026-06-26 | B3/T3 write-set expansion APPROVED | User approved expanding the write-set to `src/layout/dot/init.ts` + `src/layout/dot/sameport.ts` (both files). Proceeding with the full fix; T4 survey gate (both baselines, 0 regressions) is the guard. | Per decisions.md write-set-expansion rule. |
| 2026-06-26 | B2/T2 fix site (WRITE-SET EXPANSION) | T3 fix spans 2 files OUTSIDE provisional write-set: (1) `src/layout/dot/init.ts` `dotInitEdge` — parse samehead/sametail; (2) `src/layout/dot/sameport.ts` `shapeClip` — replace rectangle STUB with faithful `shape_clip` (honda ellipse nodes make the rect approx wrong → residual 17px shared-port position error after wiring). Requires write-set-expansion approval. | Oracle points at init.ts (attr) + sameport.ts (geometry). |
| 2026-06-26 | B1/T1 done | C oracle captured (`oracle/c-dump.txt`), 4 stages × 3 edges. **et=10, cnt=1** for all three → NO multi-edge offset; divergence is single-edge `routesplines` (boxes→polyline→spline-fit). **All edges span multiple ranks** (n002→n001 rankdiff=2, n018→n015=3, n023→n015=6) → routed via virtual-node chains (hackflag path), NOT adjacent edges. C `routesplines` returns **2 beziers (spl.pn=7) for all three**; even n023→n015 (polyline pl.pn=5) fits to 2 beziers. Port: edge2=1, edge25=4, control=2. Localization (T2): which stage first diverges — box corridor, Pshortestpath polyline, or Proutespline fit. | GV_XDUMP instrumentation of routespl.c routesplines_ (stages 2/3/4) + dotsplines.c make_regular_edge (stage 1), gated, reverted in T4. C-source edits live in ~/git/graphviz (separate repo), uncommitted. |
