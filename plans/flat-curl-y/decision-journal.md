# Decision Journal — flat-curl-y

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| T1 | 2026-06-19 | **Non-adjacent path is CORRECT — not a bug (AD-5).** | Instrumented C `make_flat_edge`: `5->8` routed spline start `(375.02,34.02)`, peak Y `83.44` (control), node5.y=18. Port internal spline (probe): start `(375.02,34.02)` [+27 x frame], peak `83.44`, node5.y=18 — IDENTICAL to C. The non-adjacent flat curve is installed via `clipAndInstall` (edge-route.ts:301) which grows `g.info.bb`. Its apparent Y divergence in the final SVG is purely the downstream 7.88 translate shift, not geometry. isolable=N/A (no fix needed here). | — |
| T2 | 2026-06-19 | The adjacent bug is **one edge**: `3:sw->2:se` (back edge, bottom ports) routes straight instead of curling below. | Port internal splines: `2:e->3:w` ys=[18,18,18,18] (cardinal straight, correct); `2:ne->3:nw` ys=[33,34,35.2,36,39.9,41,38.1] (curls UP, correct — matches the oracle up to the global 7.88 shift); `3:sw->2:se` ys=[3,3,3,3] (perfectly STRAIGHT at the port level). C curls it below to internal -1.79. So `makeFlatAdjEdges` produces the top-port curl (ne/nw) but NOT the bottom-port curl for a back edge (3->2, sw/se). That straight edge keeps `bb.ll.y=0`, suppressing C's +7.88 up-shift. divergentFn: `splines-flat.ts:makeFlatAdjEdges`/`buildFlatAux`/`copyFlatSplines` (aux-graph path); cRef: `lib/dotgen/dotsplines.c:make_flat_adj_edges`. | — |
| T2 | 2026-06-19 | **Exact aux mechanism NOT yet pinned — needs deeper aux-graph C instrumentation before fixing (AD-2).** | The bug is bounded to one sub-case (adjacent back-edge bottom-port curl) in the intricate aux-graph clone+rotate+route+copy-back path. Which step drops the curl (cloneFlatEdge orientation for a back edge / port transfer / aux routing / copyOneFlatSpline) is not yet isolated. Per AD-2 (no blind fix) and AD-4 (the aux path serves all adjacent flats — golden-risk), T3 should not start until the aux spline for `3:sw->2:se` is dumped from C and compared to the port's aux spline. `isolable`: likely yes (one sub-case), but unconfirmed. | ⚑ T3 blocked on aux dump |
| T3 | 2026-06-19 | Aux mechanism PINNED to the routing step: the port's aux graph routes the reversed back edge STRAIGHT. | Instrumented C `make_flat_adj_edges` copy-back + probed the port's aux splines (aux coords, before copy-back): C `3->2` aux X=[10.97,44.24] **auxsz=7** (curved, transforms to output Y [-9.67,2.98] = curl below); port `3->2` aux X=[-12.15,18.34] **auxsz=4** (STRAIGHT, wrong X). The forward `2->3` (ne/nw) matches C EXACTLY (aux [0,32.15], sz=7) in both. So copy-back/transform is fine; the divergence is the aux-graph LAYOUT of the reversed back edge — it routes direct (1 segment) where C bows it (2 segments) and at a different X (multi-edge spread / aux mincross-ordering for the reversed edge). cRef: dot_splines_ on the cloned aux graph. | — |
| T3 | 2026-06-19 | **STOP the fix attempt — disproportionate depth/risk for one input (AD-2/AD-4).** | The remaining work is correcting the cloned aux graph's layout of a reversed back edge (auxsz 4->7, X reposition) — a multi-edge-spread/mincross-ordering issue inside the intricate `make_flat_adj_edges` aux pipeline, which serves ALL adjacent flats (high golden-risk). The exact ordering line is still not isolated, and this is the 4th mission on a single corpus input (#241_0). Per AD-2 (no blind fix) and AD-4 (golden-risk, deep), bank the now-precise diagnosis (aux routes the back edge straight) and stop. Resumable: compare the aux graph's virtual-node/rank/edge-order for `3->2` between C and port. | ⚑ STOP — bank, resumable |
| T1 | 2026-06-19 | The 7.88 shift is a **bbox lower-bound** effect driven by the ADJACENT path. | C node.y=18 at routing time but emits at internal 25.88 (SVG -25.88): C shifts everything +7.88 because its `bb.ll.y ~= -7.88` (the bottom flat edges bow BELOW the baseline). Port `bb.ll.y = 0`. The difference: C's `3:sw->2:se` curls below (internal -1.79), the port routes it STRAIGHT (internal +2.98). So bb.ll.y stays 0 in the port → no up-shift → cardinal edges land 7.88 low. The fix is the adjacent curl, NOT the non-adjacent path or the bbox code. | — |
