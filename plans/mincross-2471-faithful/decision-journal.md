# Decision Journal — mincross-2471-faithful

| When | Task | Decision / Event | Rationale |
|------|------|------------------|-----------|
| 2026-06-18 | B1 | **Execution plan.** B1: T1 re-apply vStart fix (done inline), T2 small windowed repros vs C classify Layer 2 (inline). B2: T3 diagnose oscillation (inline, hard-gated 2 rounds). B3: T4 apply+verify. Branch `feature/mincross-2471-faithful` created from brief commit. Working tree has unrelated dirty files (.claude/settings, backlog plan) — excluded from all commits. | Autonomous plan log (no user review per parallelism.md autonomous exception) |
| 2026-06-18 | T2 | **Classification = path B (2471-scale/structure-only).** 2471 hangs with T1 (timeout 90s, rc124). Synthetic windowed graphs (multi-cluster + multi-component) up to ~1920 nodes with dense cross-edges ALL converge (≤17s) — the transpose non-convergence does not reproduce below 2471 scale; it is structural to 2471. Diagnose on 2471 with the convergence-point harness. | T2 classify |
| 2026-06-18 | T3 | **ROOT CAUSE FOUND + FIXED (Layer 2) — but exposes Layer 3 (out of scope). STOP.** Brief's "transpose non-convergence" is a symptom. True cause: `saveBest`/`restoreRank`/`restoreBest` ignore `vStart` (same class as Layer 1) — `restoreBest` at pass-2 scrambles the windowed component (probe: preRestoreBest clean → postRestoreBest dupRanks=9 orderNeIdx=548), desyncing order from slot; the next transpose inherits the dups and oscillates (delta plateaus 89/5221/11116, never 0). Fix (mincross-order.ts saveBest=rankGet, restoreRank=window [vStart,vStart+n)): **mincross now completes ~3.5s == C** (maxphase=2 render 3.6s); tsc 0, 1876 tests pass, build OK, C clean. Full root-cause + evidence in batch-2/layer2-root-cause.md. **NEW Layer 3:** correct order makes `dotPosition`'s x-coord network simplex (`rank(g,2,nsiter2(g))`, position.ts/ns-core.ts) non-converge → 2471 end-to-end hangs (maxphase=3 hangs; maxphase=2 fine). nsiter2=INT_MAX is faithful to C (C also INT_MAX); C's NS still terminates → genuine TS NS convergence gap, exposed not caused by the order. position.ts/ns-core.ts are OUTSIDE the AD-5 write-set → STOP for authorization + "never leave a hang." All temp probes reverted (tree clean). | T3 / STOP |
| 2026-06-18 | T1 | **vStart fix re-applied + committed (b4e6afb).** `medians` reads `rankGet(rk,i)`; `reorder` iterates absolute window `[vStart,vStart+n)` via new `{start,ep}` param on `reorderInner`. Extracted `reorderWindow` helper to keep `reorder` CCN≤10 (complexity hook). New `mincross-window.test.ts` (authorized under brief `+tests`; existing test file would exceed 500-line hook cap) with vStart>0 regression for medians (decoy sentinel guard — fails on old `rk.v[0..n)` loop) + reorderInner window contract. Gates: tsc 0, 1876 tests pass (132 files, +2), build OK. C repo clean. | T1 mechanical (push-forward per AD/decisions) |
