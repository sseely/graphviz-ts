<!-- SPDX-License-Identifier: EPL-2.0 -->
# Decision Journal

Appended during execution. Every non-trivial judgment call gets a row.

| Date | Batch/Task | Decision | Rationale |
|------|------------|----------|-----------|
| 2026-06-22 | planning | scope = 8 errored cases, 4 root causes (RC1 flatReorderRank ×3, RC2 mapPathLongSingle ×2, RC3 buildSkeletonEdgeCounts ×1, RC4 Stripper string-strip ×2) | pre-investigated via real inner stack traces |
| 2026-06-22 | planning | RC4 is the pre-parse `validateEdgeOperators` heuristic, NOT the peggy grammar (`dot.js` parses big.gv/biglabel.gv fine) → fix `Stripper.strip`, no dot.js regen | confirmed by parsing both files standalone |
| 2026-06-22 | planning | baseline parity (post arrowhead-geometry, dot 15.1.0): conformant 249, structural 222, diverged 288, errored 13, timeout 9, oracle-error 15 | pre-flight reference for the B3 regression diff |
| 2026-06-22 | B1/T1 | RC4 fix = change rQ regex `\\.`→`\\[\\s\\S]` in `Stripper.strip` so a `\<newline>` continuation (and any `\<char>` escape) cannot break the string match; interior `--`/`->` blanked | matches scan.l qstring rules (`\"`,`\\`,`\<newline>`); blankQ preserves length so findEdgeOp offsets stay aligned |
| 2026-06-22 | B1/T1 | colocated test uses synthetic big.gv-style inputs (not fs reads of ~/git/graphviz) for the unit test; verified real big.gv/biglabel.gv render SVG out-of-band via renderSvg | keeps the browser-targeted unit test self-contained; B3 survey regen is the real corpus proof |
| 2026-06-22 | B1/T1 | gates green: typecheck 0, vitest 2247 pass (+6), build 0. big.gv→44609B svg, biglabel.gv→17599B svg (were EDGE_OP_UNDIRECTED_IN_DIRECTED). committed 6d54892 | B1 complete |
| 2026-06-22 | B2/T2 | RC1 root cause (instrumented 121.dot): NOT a temprank-build bug — flat funcs read raw `rk.v[i]` not windowed `rankGet(rk,i)`. For cluster_G1 vStart=1, raw read [∅,B] instead of window [B,A]; A (B's constraining-edge root) sat beyond rk.n so postorder never reached B → temprank undercount → undefined.info crash | dump: g=cluster_G1 vStart=1 av=[B,A,NULL] v=[∅,B,A,NULL] rk.n=2 temprank.len=1 |
| 2026-06-22 | B2/T2 | fix = rankGet/rankSet in flatBreakcyclesRank, flatReorderRank, flatReorderBuildTemprank, flatReorderFixEdges (all in mincross-flat.ts, in write-set). ND_low(v)=i stays window index per C flat_breakcycles. NOT a guard (ADR-1) | matches C `GD_rank(g)[r].v[i]` = av+offset windowed pointer |
| 2026-06-22 | B2/T2 | 121/2239/258 stop crashing → render SVG (3859/153441/3560B). Residual: port cluster RANKS differ from native (rank0={C} vs {C,F}); pre-existing cluster-ranking axis (2471 saga), NOT from windowing — within cluster_G1 A→B order is faithful (A<B). Re-buckets to diverged per ADR-4 | gates: typecheck 0, vitest 2250 (+3), build 0; committed 36bf083 |
| 2026-06-22 | B2/T3 | MIS-SCOPE found: T2's windowing fix ALSO resolved RC2's mapPathLongSingle crash. Verified by file-swap: pre-T2 1332 crashes at cluster-path.ts:154 (`reading 'head'`, exactly RC2); post-T2 the chain walk completes (all hops out=size1) → no mapPath crash. RC1+RC2 shared one windowing root cause | instrument: walk completed, crash moved downstream |
| 2026-06-22 | B2/T3 | 1332 + b53 now crash DEEPER in containNodesRank (position-cluster.ts:82) — `g.info.rank` undefined for a subtree of clusters (clusterc4118+ in 1332; cluster_node_44 in b53). Root cause = cluster info.rank-table install gap, NOT mapPath; site is OUTSIDE all task write-sets (not cluster-path.ts, not cluster.ts). STOP+consult per autonomous protocol (mis-scope + out-of-write-set) | see [batch-2/deferred-1332-b53.md] |
| 2026-06-22 | B2/T3 | USER DECISION: proceed to T4, defer 1332/b53. T3's mapPath fix is moot (done by T2); 1332/b53 re-classified to a cluster-rank-install follow-on. No cluster-path.ts edits this mission. Deferral comparison page written | user-confirmed re-scope |
| 2026-06-22 | B2/T4 | Found a REAL faithful divergence in buildSkeletonEdgeCounts: port bumps `rankleader[r]` per r; C bumps a FIXED `rl=rankleader[ND_rank(v)]` (cluster.c:build_skeleton). But fixing it only moved the crash to the OUTER loop `rankleader[v.rank]` undefined | C read confirms fixed-rl |
| 2026-06-22 | B2/T4 | C-instrumented build_skeleton on 1767 (rebuilt gvplugin_dot_layout): C clusters hold ONLY owned nodes — cluster_1={f:2}, cluster_2={p1:1,p2:1,p3:1}. Port holds FOREIGN nodes — cluster_1={a:1,f:3,c:3}, cluster_2={p1:1,p2:0,p3:0,f:3} + wrong rank=same ranks. RC3's real cause is cluster node/edge MEMBERSHIP + ranking upstream of build_skeleton, same infra class as 1332/b53 — NOT the localized site the brief hypothesized | native oracle dump (DBG-C) |
| 2026-06-22 | B2/T4 | PIVOT (user: "ok to abandon + plan a derisk/investigation mission"): T2 is the only clean localized win. RC2+RC3 both bottom out in cluster membership+ranking infra (deep, cross-module, out of write-sets). Reverted T4 cluster.ts probe + C instrumentation (oracle restored clean). Authoring a derisk/investigation mission for the cluster infra; errored-cluster left at B1+T2 committed (5/8 original crashes fixed: big, biglabel, 121, 2239, 258) | mis-scope confirmed 3/4 RCs |
| 2026-06-22 | B3 | Parity survey regen (796 inputs, dot 15.1.0, clean oracle). Result: conformant 249, structural 222, diverged 293 (+5), errored 8 (−5), timeout 8, oracle-error 16. Per-id diff: IMPROVED 5 (121,2239,258,graphs-big,graphs-biglabel all errored→diverged), **REGRESSED 0** (ADR-5 gate MET). Lone noise: 2095_1 timeout→oracle-error (native-oracle flake, excluded per ADR-5) | parity.json + PARITY.md regenerated |
