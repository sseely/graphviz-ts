<!-- SPDX-License-Identifier: EPL-2.0 -->
# Decision journal

Appended during execution.

| date | batch/task | decision / finding |
|---|---|---|
| 2026-06-30 | prior mission | Diagnosed: port drops 6 concentrate edges (maxDelta 0, `svg/g[1][childCount]`). Root cause = `dotSplines_` collect iterates `g.nodes.values()` (NORMAL only), skipping the virtual `splineMerge` node `left` that owns the 5 secondary DOWN-sweep chains (+1 from Stand). Full artifact: `git show fix/graphs-b15:.agent-notes/graphs-b15-concentrate-drop.md`. |
| 2026-06-30 | prior mission | Fix attempt (iterate nlist + bespoke `routeConcentrateSecondaryChain`) emitted all 153 edges but DOUBLED ~40 beziers (maxDelta 0→432). Two boolean guards failed at the same 41 edges. Reverted (`a124fed`); deferred as needing "edgecmp-grouping/getmainedge dedup — each orig routes once". |
| 2026-07-01 | re-scope | Grounded the real fix: the port ALREADY has `edgecmp`/`getMainEdge`/`groupSize`/`routeEdgeGroup` (splines.ts). The gap is the COLLECT step (splines.ts:521) missing virtual `splineMerge` nodes. Faithful fix = mirror C's rank-array collect (`dotsplines.c:281-299`) and route new edges through the EXISTING group dispatch so each `getMainEdge` group routes once — NO side router, NO boolean guard (AD-2/AD-3). Bar = conformant AND maxDelta ≤ HEAD (AD-4). User approved this scope. |
| 2026-07-01 | T1 | Experiment: rank-array collect (null-guarded) + EXISTING dispatch → 147 edges, maxDelta 0. Collect-only is INSUFFICIENT (secondaries still absent). CAVEAT (corrected by T2): the maxDelta-0 here was MISLEADING — it holds only because collect-only does NOT route the secondaries; nothing new is installed, so nothing doubles. The "de-risked" claim was premature. |
| 2026-07-01 | T2 attempt-1 | **STOP — reproduced the prior regression.** Implemented: collectRoutingEdges (rank array) + routeConcentrateSecondaryChain folded into the lone dispatch, gated `splineMerge(member.tail)`. Result: 153 edges (all 6 restored) BUT **maxDelta 432.51**, 349 diffs. Cause: `left`'s out-edges include CONTINUATIONS of 8 long edges (Stand:Normal→JumpVertical:In, Walk→Strafe, WalkStepUp→Fall, …) already routed from their NORMAL tail; the secondary router installs an EXTRA bezier on them (3 paths vs 2). The `splineMerge(member.tail)` guard CANNOT separate a true orphaned secondary from a long-edge continuation through the merge node — **independently confirming the prior session's exact conclusion** ("a boolean guard cannot separate secondary-chain edges from routeMergedChain edges"). Source reverted to clean (147 edges, tsc 0). |
| 2026-07-01 | T2 | Root blocker (confirmed, both sessions): the faithful fix requires C's uniform group-loop routing where `left→vn_X` and a long edge's normal-tail edge (same `getmainedge`) coalesce into ONE make_regular_edge call, while true back-edge secondaries (no normal-tail edge) route standalone. The port's dedupByOrig/routeMergedChain/routeLoneEdge decomposition does not model this; a targeted secondary router double-installs. This is a larger dispatch rework than a bounded T2. Per mission stop condition, HALT and report. Not an AD-5 escape (real defect); genuinely needs deeper rework. |
