<!-- SPDX-License-Identifier: EPL-2.0 -->
# Component map

What the mission touches and how the pieces relate. Green = new, yellow =
modified, grey = read-only context.

```mermaid
graph TD
  subgraph src["src/ (library)"]
    IR["image-resolver.ts (new)<br/>setImageResolver / findImageBytes"]:::new
    SVG["render/svg.ts<br/>usershape() emit branch"]:::mod
    PUB["render/public.ts<br/>RenderOptions.inlineImages"]:::mod
    DEV["gvc/device.ts<br/>thread flag"]:::mod
    IDX["index.ts<br/>re-export setImageResolver"]:::mod
    USZ["gvc/usershape.ts<br/>(pattern to mirror)"]:::ctx
  end

  subgraph corpus["test/corpus/ (dashboards)"]
    CL["corpus-links.ts (new)<br/>gitlabTestUrl / scrubLocalPaths"]:::new
    PR["parity-report.ts"]:::mod
    DSH["dashboard.ts"]:::mod
    JDSH["json-dashboard.ts"]:::mod
    MDSH["map-dashboard.ts"]:::mod
    XDSH["xdot-dashboard.ts"]:::mod
  end

  subgraph docs["docs-site/ (VitePress)"]
    TYPES["guide/types.md (new)"]:::new
    RECIPES["guide/recipes.md (new)"]:::new
    IMAGES["guide/images.md (new)"]:::new
    OVER["guide/overview.md (new)"]:::new
    MIGC["guide/migrate-from-c-cli.md (new)"]:::new
    MIGJ["guide/migrate-from-js-libs.md (new)"]:::new
    GLOS["guide/glossary.md (new)"]:::new
    API["guide/api.md (mod)"]:::mod
    GS["guide/getting-started.md (mod)"]:::mod
    IDXMD["index.md (mod: landing)"]:::mod
    CFG[".vitepress/config.ts (mod: sidebar/nav)"]:::mod
    REF["reference/*.md (generated by TypeDoc)"]:::new
  end

  subgraph build["build/toolchain"]
    PKG["package.json (mod: devDeps + docs:api)"]:::mod
    TD["typedoc.json (new)"]:::new
    GI[".gitignore (mod: docs-site/reference/)"]:::mod
  end

  IR --> SVG
  USZ -. mirror .-> IR
  PUB --> DEV --> SVG
  IR --> IDX
  CL --> PR & DSH & JDSH & MDSH & XDSH
  TD --> REF
  PKG --> REF
  IR -. documented by .-> IMAGES
  PUB -. documented by .-> API & IMAGES
  REF -. linked from .-> CFG
  TYPES & RECIPES & IMAGES & OVER & MIGC & MIGJ & GLOS & API --> CFG

  classDef new fill:#d5f5d5,stroke:#2e7d32,color:#000;
  classDef mod fill:#fff3cd,stroke:#b8860b,color:#000;
  classDef ctx fill:#e0e0e0,stroke:#777,color:#000;
```

## Ownership (one writer per file)

| File(s) | Owner |
|---|---|
| `src/gvc/image-resolver.ts`(+test), `src/render/svg.ts`, `src/render/public.ts`, `src/gvc/device.ts`, `src/index.ts` | T1 |
| `test/corpus/corpus-links.ts`(+test), `parity-report.ts`, `dashboard.ts`, `json-dashboard.ts`, `map-dashboard.ts`, `xdot-dashboard.ts` | T2 |
| `package.json`, `typedoc.json`, `.gitignore` | T3 |
| `src/api/edge-ops.ts`, `src/render/xdot-public.ts`, `src/api/index.ts`, `src/render/index.ts` | T4 |
| `docs-site/guide/types.md` | T5 |
| `docs-site/guide/recipes.md` | T6 |
| `docs-site/guide/images.md` | T7 |
| `docs-site/guide/overview.md` | T8 |
| `docs-site/guide/migrate-from-c-cli.md`, `migrate-from-js-libs.md` | T9 |
| `docs-site/guide/glossary.md` | T10 |
| `docs-site/guide/api.md` | T11 |
| `docs-site/.vitepress/config.ts`, `docs-site/index.md`, `docs-site/guide/getting-started.md` | T12 |
