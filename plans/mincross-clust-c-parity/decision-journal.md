# Decision Journal — mincross-clust-c-parity

| When | Task | Decision / Event | Rationale |
|------|------|------------------|-----------|
| 2026-06-17 | — | Brief created. Minimal stuck repro `mc3`: C 1→0, TS stuck at 1, SAME initial order (not init-order; not a missing phase). Success bar = per-rank ORDER match vs C, not crossing count (AD-3). Baseline tsc 0, vitest 1869. | Phase 1 diagnosis |
| 2026-06-17 | T1 | **Localized via C↔TS probe (mc3).** Dumped collapsed-root per-rank order from both sides (C: temp `ts_dump_order` in mincross.c, reverted; TS: temp `dumpRankOrder`/`dumpMval` + `setL2rProbe`, reverted). Findings: (1) C INIT order == TS INIT order, byte-identical — NOT init-order. (2) C removes the crossing in `reorder` (POSTREORDER already shows the move), not transpose — my transpose `c1<c0` probe found ZERO blocked beneficial swaps, refuting the brief's transpose/left2right prime suspect as the *sole* cause. (3) The move is a reverse-tie reorder swap (`p1>=p2 && reverse`) of a cluster-skeleton node `vC[clust]` with an inter-cluster-edge virtual (`clust=null`), shifting the `b0→c3` chain left of clusters 1+2. **Two independent over-restrictions both gate it, both in the write-set:** (a) `mincross-order.ts:mediansProcessNode` had `if (v.info.clust) return true;` — NOT in C's `medians` (mincross.c:1646, which computes a median for every node; `hasfixed` comes only from `flat_mval`). This left the skeleton's `mval=undefined`→`-1`, so `reorderFindLp` skipped it as a candidate before any swap formed. (b) `mincross-cross.ts:left2right` was a stale port (`agContainsNode`-based, an OLD graphviz variant) that returned `block` for the `(cluster, non-cluster-virtual)` pair and lacked the current-C cluster-skeleton-VIRTUAL swap exception + the `ReMincross` branch. | T1 localize (AD-1) |
| 2026-06-17 | T2 | **Fix.** (a) Removed the `clust` skip in `mediansProcessNode` → all nodes get a median, matching C. (b) Ported current C `left2right` faithfully: module `reMincross` flag (mirrors C global, threaded from initMincross/runRemincross), guard blocks only BOTH-in-different-clusters pairs, releases on a cluster-skeleton VIRTUAL on either side. Result: mc3 1→0 at pass0 iter1; TS AFT0 order == C AFT0 byte-identical. Both fixes required (each alone leaves the swap gated). | T2 fix (AD-1) |
