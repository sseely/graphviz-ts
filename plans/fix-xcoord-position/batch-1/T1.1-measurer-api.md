# T1.1 — Public setTextMeasurer + auto-resolution chain

## Context
Consumers and tests need to choose the measurer; production needs a sensible
default that is host-faithful without pulling node-canvas into browser bundles.
(ADR-6, ADR-7; LUT demotion ADR-4; Node fallback ADR-5.)

## Task
1. **Public API** (`src/index.ts` or a dedicated `src/common/measurer.ts`,
   re-exported from the package root):
   `export function setTextMeasurer(m: TextMeasurer | undefined): void`
   storing a module-level override; `undefined` clears it.
2. **Auto-resolution** in `createMeasurer()` (`textmeasure-factory.ts`), used when
   no override is set, in order:
   1. explicit override (if set)
   2. browser: `typeof document !== 'undefined'` → `CanvasTextMeasurer(browser 2d)`
   3. Node + optional `canvas` present → node-canvas system measurer
      (**lazy/dynamic import** — never a static import; browser bundles must not
      include `canvas`)
   4. fallback → `EstimateTextMeasurer` + one-time warning advising
      `npm i canvas` for host-faithful metrics (ADR-5)
3. **Demote `LutTextMeasurer`** (ADR-4): no longer an auto-resolution result; keep
   the class exported as an internal/opt-in fallback. (Existing pre-cutover
   goldens still use it only via explicit injection in the OLD survey.)
4. **Browser-bundle guard:** ensure no static `import ... from 'canvas'` exists on
   any path reachable from the browser entry. Add a build/lint check or test that
   asserts the browser entry's dependency graph excludes `canvas`.
5. Wire `GvcContext`/`createDefaultContext` to honor the override + resolution.

## Read-set
- `src/common/textmeasure-factory.ts`, `src/gvc/context.ts`,
  `src/gvc/default-context.ts`, `src/index.ts`
- DESIGN.md §4.3, ADR-4/5/6/7
- (node-canvas lazy-load: dynamic `import('canvas')`, guarded)

## Acceptance criteria (Given/When/Then)
- Given `setTextMeasurer(m)`, when a graph is laid out, then `m.measure` is used.
- Given `setTextMeasurer(undefined)` in Node with `canvas` absent, when laid out,
  then `EstimateTextMeasurer` is used and a one-time warning advises installing
  `canvas`.
- Given a browser-like env (`document` defined), then the browser canvas measurer
  is selected and no `canvas` module is imported.
- Given the browser entry's module graph, then it contains no `canvas` import
  (asserted by test/build check).
- Given `npm test`, `npm run typecheck`, lizard → all pass/clean.
- Given the EXISTING survey, then still 0 regressions (side-by-side; the old survey
  injects `LutTextMeasurer` explicitly).

## Quality bar
Commit `feat(textmeasure): public setTextMeasurer + lazy host-canvas resolution`.
Lazy node-canvas load verified; browser bundle clean.
