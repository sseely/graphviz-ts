# Decision Journal — mincross-transpose-perf

| When | Task | Decision / Event | Rationale |
|------|------|------------------|-----------|
| 2026-06-17 | — | Brief created. Suspect carried in: per-swap scope already matches C (local `transposeCounts`, incremental `ncross`) → "global recompute" pre-refuted; the 90s-in-one-`transpose()` profile makes pass-count/non-convergence prime over constant-factor. Success = 2471 completes + order byte-identical to C + mid-size speedup (AD-1/AD-4). Write-set widened to the 4 hot-path files (AD-2). Measure-then-route (AD-3). Baseline tsc 0, vitest 1873. | Phase 1 diagnosis |
| 2026-06-17 | T1 | **ROUTE — cause (b) non-convergence, two root causes, BOTH outside the write-set → STOP/re-plan.** Measured (probes, reverted): C oracle 2471 = 3.06s; every C `transpose` converges (`finalDelta=0`), longest 198 passes. TS baseline: the root component `transpose` (11th call) plateaus at `delta=10109` for >50000 passes (cap) — never a true reduction (pass-2 delta 158577 > pass-1 148477 proves the per-rank delta is not a global reduction). The "98% in transposeStep" profile is the SYMPTOM of (b), not a constant-factor (c). Root cause = duplicate node orders within a rank (probe: rank0 window `[1,3)` had orders `[1,1]`), which the swap test can never resolve. Dup present at transpose ENTRY ⇒ created upstream; `exchange` is multiset-preserving ⇒ dup born in a non-`exchange` order writer. **Site #1: `mincross-build.ts:buildRanksFlip`** — reimplements C's flip block (`mincross.c:1293-1300`) as a manual 0-based array reverse assigning `order=j`/`last-j`, IGNORING `vStart`; C uses `exchange(vlist[j], vlist[n-1-j])` (absolute-order, multiset-preserving). Breaks only for windowed passes = RL/flip AND multi-component (`vStart>0`) = exactly 2471. Fix: `exchange(ctx, rankGet(rk,j), rankGet(rk,last-j))`. Confirmed: TS `transpose` #1 becomes BYTE-IDENTICAL to C (`510568,249996,418403,…`). **Site #2 (unmasked after #1): `cluster.ts:mergeRanksInstall:136`** — `subg.rank[r].v = root.rank[r].v.slice(ipos)` makes a DETACHED COPY; C's `merge_ranks` aliases via pointer `GD_rank(subg)[r].v = GD_rank(root)[r].v + ipos`. So cluster `transpose`'s `exchange` (writes root array) never updates the cluster's copied array that `transposeStep` reads → 2nd non-convergence (probe: cluster `isRoot=false` r8–16, `alias=false`, every order `outWin`, >300 passes, no dup). Fix: `subg.rank[r].v = root.rank[r].v; subg.rank[r].vStart = ipos`. **Both fixes together → 2471 completes in TS in 44.4s (was infinite); no dup, no runaway.** AD-4 = (b). Separate latent finding (OUT OF SCOPE, do not touch): `accumCross` uses `val()` (port.order) for the crossing tiebreak; C `in_cross/out_cross` use `port.p.x`. No-op on 2471 (px=order=0); flag only. | T1 diagnosis + route |
| 2026-06-17 | T1 | **STOP per brief** ("the fix needs a file outside the write-set"). Both confirmed fix sites — `mincross-build.ts` and `cluster.ts` — are outside the AD-2 write-set {mincross-cross, mincross-order, mincross-utils, fastgr}. `cluster.ts:mergeRanksInstall` has broad blast radius (every clustered graph). All probes (TS) + C source probe reverted; tree pristine, tsc 0. Awaiting human authorization to widen the write-set before Batch 2. | STOP |
