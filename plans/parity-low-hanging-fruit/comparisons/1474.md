# Deep case: 1474

- **Corpus path:** `1474.dot` · **engine:** `dot`
- **firstDiffPath:** `svg/g[1]/g[1]/text[1]/text()[1]`
- **Port:** oracle unavailable (native dot error) — parser rejects: `Expected … end of input but "L" found`
- **Oracle:** oracle unavailable (native dot error)
- **Root-cause group:** G5 — binary / corrupt input + charset
- **Why deep:** Binary/corrupt file (362 KB, 224 null bytes, `data` filetype); requires two fixes:
  (1) latin1 re-decode for high bytes, and (2) a tolerant lexer that ignores trailing binary
  garbage after the closing `}` (C's lex scanner recovers from trailing content; the PEG grammar
  requires consuming all input). Latin1 decode alone is insufficient.
- **Follow-on bucket:** `charset-encoding`

Deferred per ADR-3 (exceeds the localized ≤~30-line / single-module cutoff;
needs charset-aware reading AND tolerant lexer mode). Not fixed in this mission.
