# Deep case: 705

- **Corpus path:** `705.dot` · **engine:** `dot`
- **firstDiffPath:** `svg/g[1]/g[1]/@id`
- **Port:** `clust1`
- **Oracle:** `clust2`
- **Root-cause group:** G3 — cluster/object AGSEQ id numbering
- **Why deep:** Root graph takes AGSEQ 1 in C, so the first cluster gets AGSEQ 2 and the oracle
  emits `clust2`. Port's per-type counter starts at 1, giving `clust1`. DOT nodes in this graph
  also have explicit `id=` attributes (e.g. `id="bus01"`) that the AGSEQ fix would also need to
  respect via `getObjId`. Requires cgraph-compatible subgraph sequence tracking.
- **Follow-on bucket:** `agseq-id-numbering`

Deferred per ADR-3 (exceeds the localized ≤~30-line / single-module cutoff;
needs AGSEQ model infrastructure across Graph/Node/Edge/Cluster). Not fixed in this mission.
