# Deep case: 1408

- **Corpus path:** `1408.dot` · **engine:** `dot`
- **firstDiffPath:** `svg/g[1]/g[5]/polygon[1]`
- **Port:** `<polygon fill="red" stroke="red" points="94.04,-37.5 104.04,-34 94.04,-30.5 94.04,-37.5"/>`
- **Oracle:** `<ellipse fill="red" stroke="red" cx="101.06" cy="-34" rx="4" ry="4"/>`
- **Root-cause group:** G1 — arrowhead `dot`/`odot` emits polygon instead of `<ellipse>`
- **Why deep:** Port's `arrow_type_dot` emits a 3-point polygon (triangle); C's `arrow_type_dot`
  (arrows.c) always draws a filled circle as `<ellipse>`. Requires porting the dot/odot arm of
  `arrow_gencode` and teaching the SVG emitter to emit `<ellipse>` instead of `<polygon>`.
- **Follow-on bucket:** `arrowhead-geometry`

Deferred per ADR-3 (exceeds the localized ≤~30-line / single-module cutoff;
needs arrowhead geometry + ellipse emission infrastructure). Not fixed in this mission.


---

## Arrowhead-geometry mission outcome (2026-06-21)

**RESOLVED — `diverged` → `conformant`.** The arrowhead-geometry port (faithful `arrows.c` Arrowtypes dispatch + per-type clip length) makes this case conformant against native dot. Closed.
