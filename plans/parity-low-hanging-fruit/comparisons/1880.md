# Deep case: 1880

- **Corpus path:** `1880.dot` · **engine:** `dot`
- **firstDiffPath:** `svg/g[1]/polygon[1]`
- **Port:** `<polygon fill="white" stroke="none" points="-4,4 -4,-949 1123.2,-949 1123.2,4 -4,4"/>`
- **Oracle:** `<g id="a_graph0"><a xlink:title=" ">…<polygon … points="-4,4 -4,-946 1041.2,-946 1041.2,4 -4,4"/></a></g>`
- **Root-cause group:** G8 — graph/object tooltip anchor wrapper
- **Why deep:** Oracle wraps graph contents in a tooltip anchor `<g id="a_graph0"><a xlink:title=" ">…</a></g>` because the graph has `tooltip=" "`; port does not emit graph-level anchor wrappers. Background polygon coordinates also differ (separate layout size divergence). Porting tooltip-anchor wrapping for graph, cluster, node, and edge objects from C's `emit_begin_graph` / `svg_begin_anchor` requires changes across multiple emit modules.
- **Follow-on bucket:** `tooltip-anchor`

Deferred per ADR-3 (exceeds the localized ≤~30-line / single-module cutoff;
needs tooltip-anchor wrapping infrastructure across emit pipeline). Not fixed in this mission.
