# Deep case: 1622_0

- **Corpus path:** `1622_0.dot` · **engine:** `dot`
- **firstDiffPath:** `svg/g[1]/g[1]/text[1]`
- **Port:** `x="19.75" y="-91.25"`
- **Oracle:** `x="18" y="-109.5"`
- **Root-cause group:** G7 — HTML-table cell layout / rounded border
- **Why deep:** Text position differs because the port computes different `WIDTH`/`HEIGHT` for
  nested `<TABLE>` cells in a fixed-size HTML-label node. C's HTML table cell sizing pass
  (`htmltable.c`) correctly handles constrained nested tables; the port's equivalent diverges
  for this configuration. Diagnosing and fixing the cell dimension calculation is a layout
  subsystem task.
- **Follow-on bucket:** `html-table-layout`

Deferred per ADR-3 (exceeds the localized ≤~30-line / single-module cutoff;
needs HTML table cell dimension calculation fix in htmltable-layout). Not fixed in this mission.
