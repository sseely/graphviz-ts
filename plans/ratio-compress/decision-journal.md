<!-- SPDX-License-Identifier: EPL-2.0 -->
# Decision journal — ratio=compress

Appended during execution. One row per non-trivial judgment call.

| timestamp | batch/task | decision | rationale |
|---|---|---|---|
| 2026-06-24 | scoping | Root cause = `g.info.drawing` never populated; compress machinery already correct | Verified by probe: temporarily populating drawing for compress moved NaN width 307→396 (native 397), scale 0.443→0.572 (native 0.574). compressGraph/containNodes/makeLrvn all present. |
| 2026-06-24 | scoping | Scope to compress-only (ADR-1) | Faithful full setRatio activates setAspect for 5 fill graphs; b22 (fill) is byte-match and at risk. Compress-only leaves drawing unset for non-compress → byte-stable by construction. |
| 2026-06-24 | T1 | Deviate from ADR-2's "reuse parseDrawingSize": re-derive size parse locally in init.ts | parseDrawingSize lives in gvc/viewport.ts whose transitive deps reach render/ (svg-helpers). Importing it into layout/dot/init.ts would introduce the first layout→render dependency edge (0 exist today) — a real layering inversion. Re-derived getdoubles2ptf locally (~8 lines, reuses existing points() helper, cited to input.c:476); identical result. ADR-2 intent (size in points, no wholesale re-impl) preserved. |
| 2026-06-24 | T1 | Add makeDrawing factory (model/layoutParams.ts) | LayoutParams has 13 required fields (C calloc-zeroed layout_t). makeDrawing zero-inits all + overrides, so callers set only ratioKind/size/filled. Reusable by Batch 2. |
| 2026-06-24 | T1 | Handled inline (Opus), not delegated | Small focused wiring with full C-derivation context in-session; same call as the ranksep commit. |
| 2026-06-24 | T1 | Handle: NaN width 396 vs native 397, scale 0.5717 vs 0.5738, translate -2011 vs -2003 | Compression now correct; residual ~1pt width / ~7.5pt translate-x is the proc3d-class x-NS/font-metric delta (known-divergences A2), not a compress bug. Expect structural-match per ADR-3. |
| 2026-06-24 | T1 | **STOP** — canary NOT met; compress machinery produces a divergent arrangement | Survey: 0 regressions, but NaN diverged→diverged, maxDelta only 1907→1601 (not ≪). Node-level diff: overall dims + ranks (dy=0) match native, but within-rank x-positions diverge (median 535, max 1601, signs both ways). Identical without landscape → pure compress, not rotation. NaN is cluster-free → not the containNodes vStart bug. The compress x-NS (compressGraph + containNodes + ln→rn weight-1000 edge), dead until T1 activated it, does not reproduce C's compressed arrangement. This is the brief's explicit STOP condition ("machinery is correct, wiring only" — it is NOT). Premise contradicted → stop and get human input. T1 wiring itself is correct/faithful/0-regression. |
| 2026-06-24 | T6 | Investigation result: NOT compress; pre-existing base x-NS structural diff | Instrumented make_aux_edge C-vs-port for NaN ±compress. Compress is CORRECT (adds identical 21 edges, ln→rn matches, simple compress graphs byte-identical). NOT degeneracy (aux graphs differ structurally: 483 vs 471, and 462 vs 450 WITHOUT compress — same +12). NaN diverges with OR without compress (uncompressed max disp 1784) — a base x-NS constraint-graph difference: ≈6 extra make_edge_pairs saved-out-edges (+12 weight-1/2 aux edges) + weight-1↔2 redistribution, plus font-metric minlen±1. Reframed T6: compress done; the real lever is the base x-NS edge structure (separate, broader). Instrumentation reverted. |
