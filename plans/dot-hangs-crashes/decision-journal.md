<!-- SPDX-License-Identifier: EPL-2.0 -->

# Decision journal

Append one row per non-trivial judgment call, gate result, or won't-fix.

| date | task | decision / event | rationale |
|---|---|---|---|
| 2026-06-23 | plan | mission scoped: NS hot-path + recursion + mincross(cond.) + parser triage; bar = ≤3× native, floor <20s | investigation: dfsRange 40%/67% nonlib, 384M frames on 2471; 2108 stack overflow fixed by --stack-size; 5 errored = fuzzer garbage |
| 2026-06-23 | T4 | DONE — all 5 errored cases (1308_1, 1474, 1489, 1494, 1676) are input corruption (mojibake/null bytes/binary soup); won't-fix, NO code changed | each fails on bytes no sane grammar should accept; native dot's yacc limps through emitting garbage SVG (confirmed via `dot -Tsvg`). No clean per-construct grammar fix exists that wouldn't weaken valid-DOT parsing. AD-5 honored: grammar integrity preserved. Verdicts stay `errored`. |
| 2026-06-23 | T1 | DONE (representation) — flat SoA dfsRange/dfsRangeInit/dfsCutval stack, iterative rerank, de-guarded enter-edge lim reads. typecheck+2329 tests+build green; conformant on 2471/abstract/b7 (diff empty) | AD-1/AD-2/AD-3 implemented faithfully (mirrors C ns.c dfs_state_t list / iterative rerank). One success criterion met: **2108 renders at DEFAULT V8 stack** (was "Maximum call stack size exceeded") — browser-safe. |
| 2026-06-23 | T1 | **AD-1 PREMISE FALSIFIED** — allocation was NOT the dfsRange hotspot | `node --prof` of 2471 AFTER the SoA change: GC = **0.3%**, dfsRange still **47% total / 73.5% nonlib**. Instrumented step count = 59,861 calls / **386M** frame-steps — matches C exactly (no faithfulness/iteration bug). C does 386M in 0.5s (~770M/s); port ~30M/s → pure per-step JS-vs-C constant overhead, ~26×. Removing per-frame allocation (AD-1) gave only ~1.05× (2471 29s→27.5s). |
| 2026-06-23 | T1 | ROOT CAUSE of per-step cost = slow-mode `n.info` property access | `makeNodeInfo()` (src/model/nodeInfo.ts) pre-inits only 12 of 45 fields; NS-hot fields (par/low/lim/rank/tree_in/tree_out/mark/priority) are added lazily → NodeInfo objects degrade to dictionary/megamorphic shape → ~30M reads/s. EXPERIMENT: pre-declaring all 45 fields (value-preserving `undefined`) → **2471 27.5s→17.8s (~1.55×), BYTE-IDENTICAL**. No `in`/spread/Object.keys on node.info anywhere → shape-stab is value-safe. |
| 2026-06-23 | T1 | shape-stab alone does NOT meet the <20s bar for all cases | with shape-stab: 2471 17.8s, 2475_2 17.2s (rescued); but graphs-b100 30.8s, graphs-b104 33.4s, 1718 ~32s, 2108 ~60–96s (22MB SVG — emission likely dominates, not NS) still >20s. 2222 13–16s (emission-bound, ~unaffected). |
| 2026-06-23 | T1 | **STOP — escalate to human.** Two STOP conditions: (1) implementation contradicts AD-1 (allocation premise falsified); (2) the effective fix is in `src/model/nodeInfo.ts`, outside every task write-set (model-layer, corpus-wide blast radius). Authorized T1 committed as the foundation; awaiting scope decision before touching the model layer / pursuing deeper opts. | per autonomous-execution.md STOP rules. The README allows "representation/per-op cost" changes, but a model-layer shape change + accepting partial rescue (b100/b104/1718/2108 cannot hit <20s via conformant micro-opts) are scope calls for the human. T2/T3/T5 deferred pending that decision. |
| 2026-06-23 | human | DECISION: **"Apply shape-stab + document rest."** Land the conformant NodeInfo shape change; document cases that can't hit <20s; do NOT pursue deeper opts / raise the survey timeout. | user chose the most in-scope real improvement over stopping or chasing all cases. |
| 2026-06-23 | T1 | DONE — NodeInfo shape-stabilization landed (commit 27463a0): pre-declare all 57 fields in makeNodeInfo (value-preserving undefined; mirrors C calloc-zeroed struct); per-engine ND_alg types split to new algData.ts (re-exported) to keep nodeInfo.ts ≤500. 2471 27.5s→17.1s. | full survey: **0 changed verdicts**, byte 312 / struct 256 unchanged. typecheck+2329 tests+build green; conformant on 2471/abstract/b7. |
| 2026-06-23 | T2 | DONE — acyclic.ts dfs made iterative (commit 88ff118). Synthetic 50k-node path overflowed the default V8 stack even after rerank (AD-3); acyclic's recursive back-edge DFS was the remaining O(V) offender. Now renders (28.6MB, no overflow). treeAdjust / straight-edges.dfs did NOT overflow on the 50k path → left recursive. | full survey 0 changed verdicts; 2329 tests green. Browser-safe on deep chains. |
| 2026-06-23 | T3 | SKIPPED (journal-only). Conditional gate (2471 > ~3× native) is technically true (17.1s ≫ 1.5s), but mincross is only ~12% of 2471 and optimizing it cannot bridge 17s→<20s, let alone the borderline cases' tsx-survey overhead. Per the human's "document rest" decision, not pursuing deeper opts. | reorderInner/accumCross already benefit from shape-stab (they read n.info). No verdict would flip. |
| 2026-06-23 | T1/T5 | KEY: shape-stab is real (dist 2471 27.5→17.1s) but **the survey still shows timeout=7** — survey renders via `tsx` (on-the-fly transpile), ~1.4–1.6× slower than the esbuild `dist` bundle, and runs renders under CPU load. Even 2475_2 (16.0s isolated tsx) tips past 20s under survey load. So no verdict flips; the win benefits production/`dist` consumers (e.g. plantuml-js), not the tsx-survey metric. | the <20s budget is measured against the slower tsx harness, not production dist. |
| 2026-06-23 | T5 | DONE — final survey: byte 312 / struct 256 / diverged 200 / errored 5 / **timeout 7** / oracle-error 16, **0 changed verdicts vs baseline** (gate PASS). Final `dist` times: 2471 18.1s, 2475_2 15.7s, 2222 16.5s, b100 31.9s, b104 32.4s, 1718 25.7s, 2108 84.4s (was 96s). Comparison page: `comparisons/timeout-cases.md` (7 timeout + 5 errored documented). | mission gate met for correctness (0 regressions, output unchanged) + browser-safety (2108 + 50k chain render at default stack); the <20s tsx-survey budget is not met for the 7 (constant-factor JS-vs-C / emission-bound). |
