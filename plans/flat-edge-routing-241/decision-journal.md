# Decision Journal — flat-edge-routing-241

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| T1 | 2026-06-19 | Node positions + cardinal edges MATCH C exactly; divergence is purely flat-edge routing. | Instrumented C (`make_flat_edge` vspace/box dump) + port probe. node1 x=99, node6 x=459, node5 x=387, node8 x=603 — identical to the oracle. The 9 cardinal `:e->:w` flat edges match in x; they only miss in y via the global bbox shift. | — |
| T1 | 2026-06-19 | #241_0 flat edges split across **two code paths** → multi-cause. | `2:ne->3:nw` and `3:sw->2:se` are rank-ADJACENT (positions 2,3) → `make_flat_adj_edges` (aux-graph clone), which runs BEFORE the tside/hside test. `1:se->6:sw` and `5:ne->8:nw` are non-adjacent → `make_flat_edge` TOP branch. Side constants are identical (BOTTOM=1,RIGHT=2,TOP=4,LEFT=8), and `1:se->6:sw` (tside=BR=3, hside=BL=9) routes TOP in both C and port (`3==BOTTOM`/`9==BOTTOM` false). So the top/bottom decision is NOT the bug. | — |
| T1 | 2026-06-19 | Non-adjacent path: vspace/step MATCH; the FLATEDGE end-box is **x-shifted by ~rw (27)**. | C dump: `1->6` vspace=36 stepx=9 stepy=18; port probe identical. But C `tend.boxes[last]`=[99,109] / hlast=[395,403]; port=[126,136]/[422,430] — uniform +27 = rw. Same width (10) and y. So the flat-END box (after `makeFlatEnd`->`maximal_bbox`->begin/endpath FLATEDGE clip) is placed at the node EDGE in the port vs the node CENTRE in C. Isolated to FLATEDGE-gated code (`splines-path-end.ts` FLATEDGE side helpers + `maximalBbox`), but the exact line is not yet pinned. cRef: `lib/dotgen/dotsplines.c:make_flat_edge`/`makeFlatEnd`/`maximal_bbox`; `beginpath`/`endpath` FLATEDGE. | — |
| T1 | 2026-06-19 | **STOP (AD-4)** — deep multi-cause, two code paths; do not attempt a one-shot rewrite. | Fully fixing #241_0 needs (a) the non-adjacent FLATEDGE end-box x-shift AND (b) the adjacent `make_flat_adj_edges` curl (separate path, not yet pinned). The FLATEDGE box-clip lives in shared-ish begin/endpath (delicate; high regression surface). Per AD-4 ("not an isolated box/curl branch -> STOP"), recommend splitting into two focused follow-ons: (1) FLATEDGE end-box x-placement (maximal_bbox/begin-endpath); (2) make_flat_adj_edges curl geometry. No `src/` change. | ⚑ STOP — split into 2 follow-ons |
