<!-- SPDX-License-Identifier: EPL-2.0 -->

# 1435 — oracle-bug family comparison (2026-07-02, post-fixes)

Input: `~/git/graphviz/tests/1435.dot` · corpus id `1435` · oracle = headless
native dot (`GVBINDIR=/tmp/ghl`, estimate metrics) · port = graphviz-ts at
`chore/verify-oracle-bug-family`.

> **Visual side-by-side:** [`1435.html`](1435.html) · golden:
> [`1435-golden.svg`](1435-golden.svg) · ours: [`1435-ours.svg`](1435-ours.svg)

## Verdict

**structural-match, maxΔ 503 (spline debris only; same 11/11 edges post-F3)**

| | native C oracle | graphviz-ts |
|---|---|---|
| stderr / exit | exit 0; `triangulation failed` + `cannot find triangle path` warnings; routes all 11 edges (straight-line fallback) | exit 0; `triangulation failed` warning; routes all 11 edges after fix (was silently losing 8→10) |
| edges rendered | 11 / 11 | — |

## Upstream status

OPEN, `xfail(strict=True)` (tests/test_regression.py:899); triangulation test case added 2024 (commit 239629eaf)

## Inputs verification (right-for-the-right-reason)

All 16 rank2 calls line-identical (no NS-side defect). Port defect found+fixed in the pathplan layer: ear-clip dead end was fatal instead of C's warn-and-continue (commit for shortest.c:333). Residual: both sides degrade through pathplan fallbacks whose debris differs (C recovers via marktripath straight-line on different triangles).

## Disposition

Accepted A4-family (pathplan debris on an acknowledged-broken input). Re-measure when upstream fixes #1435.
