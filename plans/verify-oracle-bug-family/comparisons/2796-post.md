<!-- SPDX-License-Identifier: EPL-2.0 -->

# 2796 — post-family-fixes addendum (2026-07-02)

Supersedes the *measurement* (not the history) in
[`plans/fix-2796-cluster-ranking/comparisons/2796-cluster-ranking.md`](../../fix-2796-cluster-ranking/comparisons/2796-cluster-ranking.md),
which recorded the pre-fix baseline.

> **Visual side-by-side:** golden [`2796-golden-post.svg`](2796-golden-post.svg)
> · ours [`2796-ours-post.svg`](2796-ours-post.svg)

## What changed

The 2796 mission's open question — which side computes the faithful
wall-edge lengths — is answered: **C does**. The port's "acyclic variant"
was not a benign local difference but a missing C call
(`rec_reset_vlists`, flat.c:333) leaving cluster rank windows stale after
flat-label vnode insertion (fix `f0e28a6`; evidence
`.agent-notes/2471-stale-cluster-windows-missing-reset.md`).

## Measurement now

| | native C oracle | graphviz-ts |
|---|---|---|
| stderr / exit | exit 1; `trouble in init_rank`; `lost 3 16 edge` | exit 0; 1× `triangulation failed` + `Pshortestpath failed`; `lost 3 16 edge` |
| edges | 212 (loses `3->16`) | 212 (loses the same `3->16`) |
| verdict | — | **structural-match**, maxΔ 49 (was diverged, whole-layout) |

The port now follows C through its acknowledged-broken init_rank
recovery from **verified line-identical inputs** — the same 51
previously-divergent wall edges now match. The pre-fix port
coincidentally satisfied the upstream issue's expectations (0
overlapping clusters, 213/213 edges); it did so from unfaithful inputs
that demonstrably mislaid 2471 (9 lost edges vs C's 6). Faithfulness
won; re-measure when upstream lands a #2796 fix (draft !4849).
