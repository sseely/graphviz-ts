<!-- SPDX-License-Identifier: EPL-2.0 -->
# Batch 1 — Upstream review → disposition matrix

| task | description | status |
|---|---|---|
| T1 | Per-id upstream review: issues #2471/#1939/#1435 (status, discussion, expectations), draft MR !4849 (resolved? to satisfaction? perf objections as of now), graphs-structs upstream search (fixed-or-never-filed). Local checkout `~/git/graphviz` git-log/grep for closing MRs + xfail decorators. Output: disposition matrix in `decisions.md` — per id: upstream state, what the C failure is, what "accept" requires from B2. | [x] |

No production writes. Write-set: `plans/verify-oracle-bug-family/**`.
