# Decision Journal — dot-edge-multi

Appended during execution (per ~/.claude/rules/autonomous-execution.md).

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| setup | 2026-06-16 | Branch `feature/dot-edge-multi` off main (HEAD 02ef793). Baseline gate confirmed: 1789 passed / 0 failed / 115 goldens. | Per README; protects byte-identical gate. | — |
| T1 | 2026-06-16 | **BLOCKED — write-set insufficient.** Probe of `digraph{{rank=same a b} a->b[label="x"]}` after layout: node count 2, no label virtual node, `ED_label.set=false`, pos (0,0); SVG has 2 texts (dot: 3). The flat label vnode that `make_flat_labeled_edge` reads via the `ED_to_virt` chain (`ln`) is created in C by `flat.c:flat_node` (`make_vn_slot` on rank r-1) driven by the `flat_edges` preprocessing, and `abomination()` **inserts a whole rank** when a flat labeled edge sits at rank 0. That machinery is NOT in `splines-flat.ts` (T1's only write-set file) nor in T2/T3's sets. Faithful T1 needs flat-rank changes (flat.ts / mincross / position) — outside scope, and `abomination` is golden-blast-radius. STOP per autonomous-execution.md (out-of-write-set + mis-scope). | The brief's note "if the chain to ln is absent, port that creation too" assumes creation is reachable from splines-flat.ts; it is not — it is ranking-phase. | **YES — needs human decision on scope** |
| T1 | 2026-06-16 | **DEFERRED to its own mission** (human decision). G4 needs flat-rank machinery (flat_node + abomination) the brief didn't scope. Comparison page `comparisons/flat-labeled-edge.html` documents the gap (AD-4). README batch table updated. | Lowest risk to 115 goldens; G4 is one isolated case vs the G1 core. | Resolved (deferred) |
| T2/T3 | 2026-06-16 | Confirmed feasibility within write-set. Regular labeled-parallel ALREADY creates label vnodes + positions labels (4 texts, matches oracle) via T5's `placeRegularEdgeLabels` — gap is pure edge-path geometry. Opposing pair `a->b;b->a` DOES merge in TS class2 (b->a → `other_edge`, b.other=1; node a has 2 out-edges both `mainEdge=a->b`), so the cnt=2 group exists; bug is both copies install onto the SAME orig (a->b) so b->a renders malformed. Labeled-parallel currently crashes `getMainEdge` (to_virt → undefined) — a virtual-chain wiring gap to fix in-router. All fixable within splines.ts/splines-route.ts/edge-route*.ts. | These gaps need no ranking-phase vnode creation (unlike T1), so they are correctly scoped. | — |
