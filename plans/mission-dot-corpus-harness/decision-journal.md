# Decision Journal — dot-corpus-harness

Appended during execution (per `~/.claude/rules/autonomous-execution.md`).

| Task | Date | Decision | Rationale | Flagged |
|------|------|----------|-----------|---------|
| T1 | 2026-06-19 | Scope = full recursive walk of `~/git/graphviz/tests` (805 `.gv`/`.dot`); collect only those two extensions so `.svg`/`.xdot`/`.ps`/`.png` reference outputs are excluded by construction. | Brief lists all subdirs (graphs/share/windows/nshare/linux.*/macosx/shapes-reference). `windows/`+`share/` `.gv` carry stale `pos=`/`bb=` but both oracle+port recompute, so they are valid differential inputs (not quarantined). | no |
| T1 | 2026-06-19 | Quarantine taxonomy applied via cheap structural scan only: `engine-deferred` (6, `layout=` non-dot), `multi-graph` (3, >1 top-level graph), else applicable (796). `gvpr`/`include`/`non-graph`/`raster-only-ref`/`parse-unsupported` matched 0 (no `.gvpr`/`#include` collected; parse gaps deferred to T2 per brief). | T1 must not render; obvious-non-DOT only. | no |
| T1 | 2026-06-19 | Graph-keyword regex needed `(?:di)?graph` + case-insensitive `i` flag: plain `\bgraph\b` misses `digraph` (no word boundary inside) and `diGraph` (mixed case, DOT keywords are case-insensitive). Verified b993.gv (`diGraph G{`) reclassified non-graph→applicable. | Faithful DOT lexing; subgraph still excluded (no boundary precedes inner `graph`). | no |
| T1 | 2026-06-19 | Hoisted regexes containing `{`/`}`/`"`/`//`/`/*` out of function bodies into module-scope `new RegExp` built from `String.fromCharCode`. lizard's TS tokenizer mis-reads those chars in regex literals and false-merges adjacent functions (length warning); behavior identical. Logged per global lizard-comment rule. | Quality gate `lizard -L 30` must pass; this is a known tool false positive, not a real length violation. | no |
| T1 | 2026-06-19 | LSP emits spurious `Cannot find name 'node:fs'/'process'` for test/corpus/*.ts; `tsc --noEmit` (the real gate) passes. Trusting tsc. | Standalone LSP server lacks the project's @types/node resolution. | no |
| T2 | 2026-06-19 | ROOT-CAUSE FIX: SIGKILL the whole process GROUP (`spawn({detached:true})` + `process.kill(-pid)`), not just the child. The port hangs in a synchronous loop inside a `tsx` GRANDCHILD; killing only the tsx wrapper orphaned the grandchild, which held the stdout pipe open so `close` never fired and the survey stalled (observed: render-one on 1221.dot at 99% CPU for 4:46, survey wedged). This is the exact unkillable-in-process hazard AD-2 names. | Group-kill is the only reliable way to reap a subprocess tree; verified 0 orphans after, 1221.dot → `timeout`. | no |
| T2 | 2026-06-19 | Oracle validity is judged by OUTPUT COMPLETENESS (`</svg>` present), NOT exit code. Native `dot` exits 1 on warnings AND on recoverable errors (e.g. "trouble in init_rank", flat-edge-record warnings, invalid-UTF8) while still emitting a complete SVG. Exit-code gating produced ~58% false oracle-errors in the 12-input smoke; the fix dropped oracle-error to 16/796 (genuine syntax rejections with no SVG). | A differential survey must compare against every complete oracle render, including recoverable-error inputs — those are the interesting divergences. | no |
| T2 | 2026-06-19 | render-one emits a `__RENDER_ERROR__` sentinel line for the thrown error; survey extracts it. The port writes incidental warnings (e.g. "no hard-coded metrics for Times,serif") to stderr before throwing, so naive first-line capture mislabeled errors. Sentinel yields clean messages ("Cannot read properties of undefined (reading 'info')", ParseError text). | Accurate errMsg is what makes T3's error-bucket triage actionable. | no |
| T2 | 2026-06-19 | Touched `.gitignore` (outside declared write-set) to ignore `survey.log` + any in-repo `oracle-cache/`. AD-3 mandates the cache be gitignored; this is harness hygiene, not a divergence fix. Default cache lives under the OS tmpdir (outside the repo) anyway. | Honoring AD-3; minimal additive change, logged per write-set discipline. | no |
| T2 | 2026-06-19 | tsx resolved directly (cached npx binary / local bin / TSX_BIN) rather than via `npx tsx` per spawn — ~0.26s vs ~0.75s cold start, with an `npx tsx` fallback for portability. Full 796-input survey ran in 1m49s at concurrency 8, timeout 20s. | 796 subprocess spawns; per-spawn overhead matters; fallback keeps it runnable on a fresh checkout. | no |
| T2 | 2026-06-19 | HEADLINE (oracle dot 15.1.0, 796 applicable): conformant 112, structural-match 218, diverged 422, errored 20, timeout 8, oracle-error 16 (sum=796). | Measured parity verdict; feeds T3 dashboard + backlog. | no |
| T3 | 2026-06-19 | Capped the inline diverged table at 60 rows (worst-first by maxDelta) with a "… and N more" note; full 422-row set lives in parity.json and is fully accounted for in the triage buckets below it. | Stylistic per overview push-forward rule; a 422-row markdown table would bury the actionable backlog. Buckets carry complete counts. | no |
| T3 | 2026-06-19 | Triage buckets (the backlog). diverged by firstDiffPath shape: element-count 157, path-structure 109, color-stroke 56, font-metrics 49, attr-or-tag 36, text-content 7, polygon-points 7, compare-threw 1 (=422). errored by message: parser-gap 10, undefined-info 4, undefined-head 2, max-call-stack 2, undirected-edge-in-digraph 2 (=20). | Each bucket = one candidate oracle-pinned fix mission; parser-gap is dominated by Latin-1/UTF-8 inputs the port reads strictly while dot falls back. | no |
| T3 | 2026-06-19 | Linked PARITY.md from plans/port-catalog/README.md §"Verifying fidelity" with the headline table; reframed that section's dot-first slice as realized and the force-engine extension as the remaining follow-on. Satisfies the CLAUDE.md comparison-page requirement. | The catalog deferred this harness; T3 realizes + records it. | no |
| triage | 2026-06-23 | Quarantined `2782.dot` as `malformed` (new taxonomy reason; added to decisions.md + `MANUAL_QUARANTINE` in enumerate.ts). It had the survey's worst maxDelta (5572860) but is fuzzer garbage: native dot ITSELF emits HTML parse errors (`not well-formed`, `Unknown HTML element <TTD>`) and renders a degenerate 106302 × 10495018 pt canvas straight from `minlen=283647` (10.5M/283647 ≈ 19.6 pt/rank = rank height + ranksep). The 5.5M delta is one element offset at that scale, not a precision bug; the `g[6][childCount]` firstDiff is just the two engines recovering differently from invalid HTML. No spec signal — left poorly-formed per AD-5. | Worst-first triage of the diverged bucket: confirmed the headline outlier is degenerate reference geometry, not a port defect. Manual force-quarantine (not a structural heuristic) because the input is a structurally valid single-graph default-engine DOT file — only its geometry is meaningless. | no |
| T3 | 2026-06-19 | GATES (all green): tsc 0; vitest 1951 pass (128 goldens untouched); meta-gate survey exits 0 + parity.json; dashboard writes PARITY.md; lizard clean on all 4 new files; AD-5 verified — `git diff main -- src/` empty. | Batch-1 complete; mission deliverable (harness + dashboard + backlog) done. | no |
