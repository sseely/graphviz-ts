# Decision journal

| When | Task | Decision / observation | Rationale |
|------|------|------------------------|-----------|
| B0 start | T0 | Read README + decisions + all batch overviews + root-cause note + 2371 note. Env verified: C build present, /tmp/ghl present, ns.c clean, on new branch `feature/xns-absolute-anchor`. Started Batch 0 directly (single continuous investigation; subagent fragmentation would lose trace context). | Autonomous startup sequence. |
| B0 T0 | T0 | **MAJOR RE-SCOPE — instrumented finding contradicts locked AD-1.** Probed port x-coords PRE- and POST-`normalizeXcoords` for 2368_1, and dumped C `set_xcoords` (instrumented `lib/dotgen/position.c`, reverted+rebuilt clean). Port PRE-normalize x-frame is **conformant to C**, virtual nodes included: virtual −38/66, 376=−119, 196=−29, 256=43, 316=115, 76=205 — `xns-diff.mjs` reports MATCH on all 7 cells. The x-NS pivot order is ALREADY bit-exact. The entire internal-frame divergence (uniform +146 shift; 376→+27) is introduced by the **port-only** `normalizeXcoords(g)` at `position.ts:228`, a step C lacks. | "Instrument before hypothesizing." AD-1 ("anchor diverges because of NS pivot order") is empirically wrong; Batch-1 T1–T5 NS-pivot work is unnecessary. |
| B0 T0 | T0 | Decision: do NOT execute Batch-1 NS-pivot tasks (would be no-ops). Re-target the fix to `normalizeXcoords`. Running the decisive AD-3 test now: full survey with `XNS_NONORM=1` (normalize disabled) to measure whether the step is load-bearing for the 490 conformant corpus. Result gates the Batch-1 path. | The mission objective (internal x-frame == C) is still valid; only the means changes. Verify the invariant empirically before committing. |
| B0 T0 | T0 | **XNS_NONORM survey result: GATE PASS — stable=690, improvements=0, regressions=0, clip-regressions=0.** Removing `normalizeXcoords` preserves every graph's verdict (2368/2371 stay pre-existing-diverged; the 18 graphs `normalizeXcoords` was historically credited with fixing — p3/pm2way/process/b36/awilliams per `src/.agent-notes/edge-spline-extra-segments.md` — stay conformant). Committed baseline `parity.json`/`parity-rules.json` untouched (verified clean; survey wrote `parity-nonorm.json`; the "wrote parity.json" log is a hardcoded string). | `normalizeXcoords` is NOT load-bearing — it was a band-aid built on the false premise that the port's x-NS anchor differs from C's. The raw pre-normalize frame is already integer and == C; C has no normalize. AD-3 invariant holds under removal. |
| B2 | T6 | **Batch 2 done — degenerate labeled-flat wiring.** 3 changes + a 4th (fwd-normalize). svg `edgeHasDrawableContent` initially used a faithful spl.bb overlap test → regressed neato-circle/circo-star goldens + svg unit test (spl.bb computed differently there); reverted spl/xlabel/head/tail to HEAD triggers, applied overlap only to the (set) main label — the sole piece that legitimately falls off-canvas. makeFlatLabeledEdge: handled-on-degenerate (true) + forward-normalize backward flats (order-based makefwdedge, mirrors C make_flat_edge) so the non-degenerate `256->316` leg routes (8→9 paths). routeLoneEdge skip removed; corridor label-guard added. | Faithful edge_in_box; the spl overlap test was too aggressive (mission needs only the label test). |
| B2 | T6 | **Batch 2 verification.** 2368_1 + 1624 conformant (degenerate label suppressed off-canvas / preserved); 2368 childCount RESOLVED (11 edges, 9 paths, all 22 labels; was 6). Full survey GATE PASS — 0 regressions, 0 clip-regressions (fwd-normalize corpus-safe). vitest 2467 green. **Newly-revealed blocker**: 2368 retains a SEPARATE ~5pt flat-label-rank vspace + ~1pt x-NS-tiebreak residual (C label vnode for 256->316 = (20,40.4) conformant to port → not the label path; the top rank-group `{line7;136}` sits 5pt low; pre-existing, was masked by the childCount diff the brief targeted). 2368 stays diverged. Documented `.agent-notes/2368-residual-flat-label-ranksep.md`. | Mission's degenerate-label objective achieved + faithful; full 2368 conformant needs an out-of-scope flat-ranksep/x-NS-tiebreak fix (brief assumed only the label path blocked 2368). |
| B3 | T7 | **Batch-2 survey vs committed baseline: 0 verdict changes; 13 maxΔ detail shifts.** conformant 492 / structural 198 / diverged 89 / oracle-error 11 — IDENTICAL to baseline. Detail: 2368 maxΔ 5→65.25 (firstDiff childCount→`g[13]/path[1]/@d`); b29 family 1902→2558; b124 family 2538→1988 (better); arrowsize/biglabel better. The 65pt path is `376->76`: port routes a near-straight horizontal STUB (all y=−8.8) where C routes a curve. These are labeled-flat edges PREVIOUSLY SUPPRESSED by the removed skip — now drawn, but the port's non-degenerate labeled-flat channel geometry (makeSimpleFlatLabels straight-vs-curve) is imperfect, so maxΔ rises as newly-present edges become comparable. NOT a verdict regression. | The childCount divergence masked TWO deeper blockers (flat-ranksep vspace + adjacent-flat-label channel geometry). Mission core (degenerate-label emission) is done + faithful; full 2368 conformant needs separate, larger flat-channel-routing work. |
| B3 | T7 | **STOP — consult user before baseline refresh.** Mission's stated target (2368 conformant) NOT met due to the newly-revealed deeper blockers (brief assumed only the degenerate-label path blocked 2368). Refreshing the committed baseline would record the structurally-improved-but-still-diverged 2368 (maxΔ 65) + mixed maxΔ shifts. That is a significant reference-state change on a partial outcome → surface to the user (per autonomous mis-scope STOP rule) rather than auto-bless. Core work (B0–B2) is committed; 0 verdict regressions; vitest 2467 green. | Baseline refresh on a partial result is the user's call; protocol STOP on mis-scope. |
| B1 | B1 | **Batch 1 done.** Removed `normalizeXcoords` call + dead `minNormalLeftX`/`shiftAllXcoords` from `position.ts` (50 lines net removed; `NORMAL` import dropped, now unused). tsc clean, vitest 2467 green (run at T0 on identical-behavior code). Spot-check: 2368_1 + p3.gv geometry conformant with the C oracle after removal (comment/ws-normalized). Gate evidence = the XNS_NONORM survey (GATE PASS, 0 regr) — committed code path is identical to that env-gated run. Write confined to `position.ts` (within T1's declared write-set). | Faithful removal of a port-only deviation; AD-3 invariant preserved (0 regr); internal frame now == C for Batch 2. |
| B0 T0 | T0 | **Re-scope ratified.** Batch 1 = remove `normalizeXcoords` (+ dead `minNormalLeftX`/`shiftAllXcoords`); Batch 1 gate-evidence = this XNS_NONORM run (behaviorally identical to the call removal). Batch 2 (T6 label wiring) unchanged and now viable: with the frame == C, 2368's degenerate labels land at C's internal x (−119 off-canvas for 2368_1 → suppress; on-canvas for 2368 → draw), so a faithful `edge_in_box` yields the required opposite outcomes. T1–T5 marked no-op. | Push-forward (task simpler than scoped); evidence is decisive + instrumented. The objective is unchanged; only the means is corrected. |
